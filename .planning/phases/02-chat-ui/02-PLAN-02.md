---
phase: 02-chat-ui
plan: 02
type: execute
dependencies: [01]
waves: [8, 9, 10]
---

## Artifacts this phase produces

**New files:**
- `apps/desktop/src/components/WorkspaceContainer.tsx` — 主工作区画布占位组件
- `apps/desktop/src/components/SidebarResizeHandle.tsx` — 侧边栏拖拽手柄组件
- `apps/desktop/src/stores/layout.ts` — 布局 Zustand store（sidebarWidth 持久化）

**Modified files:**
- `apps/desktop/src/App.tsx` — 布局重构：移除左侧会话栏、ChatView 移入 CopilotSidebar、引入 WorkspaceContainer、侧边栏可拖拽
- `apps/desktop/src/components/ConversationList.tsx` — 适配为 CopilotSidebar header 内嵌模式
- `apps/desktop/src/components/ChatView.tsx` — 适配 CopilotSidebar 内部渲染

**New symbols:**
- `WorkspaceContainer` — React 组件（主工作区画布占位）
- `SidebarResizeHandle` — React 组件（拖拽手柄）
- `useLayoutStore` — Zustand hook for sidebar width persistence

## Requirements Addressed

- CHT-05: 主工作区与对话区隔离（聊天交互归拢至右侧，中间保留为工作区画布）
- CHT-06: 集成式历史会话入口（移除左侧独立对话栏，在聊天侧边栏内提供历史记录视图）
- CHT-07: 侧边栏可拖拽且宽度持久化（右侧聊天边栏支持拖拽调整宽度，且宽度状态重启后保持）

## must_haves

- [ ] `pnpm check` (biome ci) passes — 0 errors
- [ ] `cd apps/desktop && npx tsc --noEmit` passes
- [ ] App 布局为：Titlebar + WorkspaceContainer(中间) + CopilotSidebar(右侧) + BottomPanel + StatusBar
- [ ] 左侧独立 ConversationList 已移除，历史会话入口集成在 CopilotSidebar header 中
- [ ] ChatView（Virtuoso 消息列表）渲染在 CopilotSidebar 内
- [ ] CopilotSidebar 左侧边缘可拖拽调整宽度，拖拽时实时变化
- [ ] 侧边栏宽度持久化到 localStorage，重启后恢复

## Waves

### Wave 8: Layout Refactor — 聊天归拢右侧 (CHT-05)
**Objective:** 将 ChatView 从主区域移除，嵌入到 CopilotSidebar 中；中间区域改为 WorkspaceContainer。

### Wave 9: 会话列表内聚 (CHT-06)
**Objective:** 移除左侧独立 ConversationList 侧边栏，将历史会话切换功能集成到 CopilotSidebar header 内。

### Wave 10: 可拖拽侧边栏 + 宽度持久化 (CHT-07)
**Objective:** CopilotSidebar 支持拖拽调整宽度，宽度状态持久化到 Zustand + localStorage。

---

<tasks>

<!-- ═══════════════════════════════════════════════════════ -->
<!-- WAVE 8: Layout Refactor — 聊天归拢右侧 -->
<!-- ═══════════════════════════════════════════════════════ -->

<task id="8.1" wave="8" type="execute" depends_on="1.1">
  <description>创建 WorkspaceContainer 占位组件</description>
  <read_first>apps/desktop/src/App.tsx</read_first>
  <action>
    创建 `apps/desktop/src/components/WorkspaceContainer.tsx`:
    - 居中展示品牌标识 "Paladin" + tagline "AI 编程助手"
    - 使用 Tailwind flex 居中布局
    - 支持 dark mode（bg-white dark:bg-gray-900）
    - 暂时为纯展示占位，后续 Phase 可扩展为文件编辑器/Diff 全屏视图
  </action>
  <acceptance_criteria>
    Source: apps/desktop/src/components/WorkspaceContainer.tsx 存在
    Source: apps/desktop/src/components/WorkspaceContainer.tsx 包含 "Paladin"
    Source: apps/desktop/src/components/WorkspaceContainer.tsx 包含 "dark:bg-gray-900"
  </acceptance_criteria>
</task>

<task id="8.2" wave="8" type="execute" depends_on="8.1">
  <description>将 ChatView 嵌入 CopilotSidebar，主区域切换为 WorkspaceContainer</description>
  <read_first>apps/desktop/src/App.tsx</read_first>
  <read_first>apps/desktop/src/components/ChatView.tsx</read_first>
  <action>
    重构 App.tsx 布局：
    
    1. 将 CopilotSidebar 从 `<CopilotKitProvider>` 外移到内部的 flex 布局中，
       替换原来的右侧空白区，使 CopilotSidebar 作为右侧面板而非覆盖层。
    
    2. CopilotSidebar 的渲染方式：
       - 如果 CopilotSidebar 支持 `children` render prop，将 `<ChatView />` 传入 body 区域
       - 如果仅支持 slots，使用 CopilotChat 的 messageView slot 传入 ChatView
       - 最后手段：不通过 CopilotSidebar，直接使用 `<div>` 容器包裹 CopilotChat + ChatView
    
    3. 中间主区域替换：
       - 原来 `<ChatView />` 的位置替换为 `<WorkspaceContainer />`
       - WorkspaceContainer 占满 flex-1 空间
    
    4. 新的布局结构目标：
       ```
       Titlebar
       ├── WorkspaceContainer (flex-1, 中间主画布)
       └── CopilotSidebar (右侧聊天面板，嵌入 ChatView + 输入框)
       StatusBar
       BottomPanel (底部弹出)
       ```
  </action>
  <acceptance_criteria>
    Source: apps/desktop/src/App.tsx 包含 "<WorkspaceContainer"
    Source: apps/desktop/src/components/ChatView.tsx 存在且被 CopilotSidebar 引用
    Behavior: 中间区域显示 WorkspaceContainer（品牌标识），聊天交互在右侧 CopilotSidebar
    CLI: cd apps/desktop && npx tsc --noEmit exits 0
  </acceptance_criteria>
</task>

<task id="8.3" wave="8" type="execute" depends_on="8.2">
  <description>适配 ChatView 在 CopilotSidebar 内的样式</description>
  <read_first>apps/desktop/src/components/ChatView.tsx</read_first>
  <read_first>apps/desktop/src/index.css</read_first>
  <action>
    确保 ChatView 在 CopilotSidebar 内正常渲染：
    - Virtuoso 虚拟滚动容器高度自适应 sidebar 剩余空间
    - 消息气泡样式与 sidebar 宽度匹配（不溢出）
    - 输入框固定在 sidebar 底部
    - 暗色模式样式同步：消息背景、边框、文字颜色随主题变化
  </action>
  <acceptance_criteria>
    Behavior: ChatView 消息列表在 CopilotSidebar 内正常滚动，无溢出
    Behavior: 暗色/浅色切换时 ChatView 样式跟随变化
    CLI: cd apps/desktop && npx tsc --noEmit exits 0
  </acceptance_criteria>
</task>

<!-- ═══════════════════════════════════════════════════════ -->
<!-- WAVE 9: 会话列表内聚 -->
<!-- ═══════════════════════════════════════════════════════ -->

<task id="9.1" wave="9" type="execute" depends_on="8.2">
  <description>从 App.tsx 移除独立的左侧 ConversationList 侧边栏</description>
  <read_first>apps/desktop/src/App.tsx</read_first>
  <action>
    在 App.tsx 中：
    1. 删除 `<aside className="w-64 ...">` 及内部的 `<ConversationList />`
    2. 删除 ConversationList 的 import（如果只在 aside 中使用）
    3. 移除相关的 w-64、border-r、flex-shrink-0、bg-gray-50 dark:bg-gray-800 容器
    4. 确保主工作区 flex-1 自动填补移除后的空间
  </action>
  <acceptance_criteria>
    Source: apps/desktop/src/App.tsx 不包含 "w-64"
    Source: apps/desktop/src/App.tsx 不包含 "<aside"
    Behavior: 左侧不再有任何固定侧边栏，中间 WorkspaceContainer 占据全宽
    CLI: cd apps/desktop && npx tsc --noEmit exits 0
  </acceptance_criteria>
</task>

<task id="9.2" wave="9" type="execute" depends_on="9.1">
  <description>将 ConversationList 集成到 CopilotSidebar header</description>
  <read_first>apps/desktop/src/App.tsx</read_first>
  <read_first>apps/desktop/src/components/ConversationList.tsx</read_first>
  <action>
    将会话切换功能移入 CopilotSidebar：
    
    1. 调整 ConversationList 组件，使其适配紧凑的 header 模式：
       - 改为下拉选择器或横向 tab 式切换（而非竖排列表）
       - 显示当前对话标题 + 下拉箭头图标
       - 下拉菜单展示全部历史会话，点击切换
       - "+" 按钮在 header 右侧，创建新对话
       - 重命名/删除操作集成在下拉菜单每项的右侧
    
    2. 在 App.tsx 中将 ConversationList 传入 CopilotSidebar 的 header slot:
       `<CopilotSidebar header={<ConversationList />} ...>`
    
    3. 如果用 CopilotChat 代替 CopilotSidebar，在自定义 sidebar 顶部渲染 ConversationList
  </action>
  <acceptance_criteria>
    Source: CopilotSidebar 的 header 区域包含 ConversationList
    Behavior: 可以在 sidebar 顶部切换对话、创建新对话、重命名、删除
    Behavior: 对话切换后 ChatView 显示对应历史消息
    CLI: cd apps/desktop && npx tsc --noEmit exits 0
  </acceptance_criteria>
</task>

<task id="9.3" wave="9" type="execute" depends_on="9.2">
  <description>验证历史会话 CRUD 功能完整性</description>
  <read_first>apps/desktop/src/stores/chat.ts</read_first>
  <action>
    逐项验证 Wave 5 的所有功能在 header 集成模式下正常工作：
    - 创建新对话：点击 "+" → 新对话出现且自动切换
    - 切换对话：下拉选择另一对话 → ChatView 更新
    - 重命名：双击标题 → inline 编辑 → Enter 保存
    - 删除：右键/点击删除 → 确认弹窗 → 删除并切换
    - 空状态：无对话时显示 "暂无对话"
    - 暗色模式适配
  </action>
  <acceptance_criteria>
    Behavior: 创建、切换、重命名、删除对话均正常
    Behavior: 删除最后一个对话后自动新建空对话
    Behavior: 暗色/浅色模式下 UI 正常
  </acceptance_criteria>
</task>

<!-- ═══════════════════════════════════════════════════════ -->
<!-- WAVE 10: 可拖拽侧边栏 + 宽度持久化 -->
<!-- ═══════════════════════════════════════════════════════ -->

<task id="10.1" wave="10" type="execute" depends_on="8.2">
  <description>创建 layout Zustand store（sidebarWidth 持久化）</description>
  <read_first>apps/desktop/src/stores/theme.ts</read_first>
  <action>
    创建 `apps/desktop/src/stores/layout.ts`:
    
    参考 theme.ts 的 persist 模式：
    - Interface `LayoutStore { sidebarWidth: number; setSidebarWidth: (w: number) => void }`
    - 默认 sidebarWidth: 400（与 CopilotSidebar 默认一致）
    - 使用 Zustand `persist` 中间件，key: "paladin-layout"，version: 0
    - setSidebarWidth 校验：clamp 到 [300, 800] 范围
    - 导出 `useLayoutStore` hook
  </action>
  <acceptance_criteria>
    Source: apps/desktop/src/stores/layout.ts 存在
    Source: apps/desktop/src/stores/layout.ts 包含 "useLayoutStore"
    Source: apps/desktop/src/stores/layout.ts 包含 'name: "paladin-layout"'
    Source: apps/desktop/src/stores/layout.ts 包含 "persist("
    Source: apps/desktop/src/stores/layout.ts sidebarWidth 默认值为 400
  </acceptance_criteria>
</task>

<task id="10.2" wave="10" type="execute" depends_on="10.1">
  <description>创建 SidebarResizeHandle 拖拽组件</description>
  <read_first>apps/desktop/src/stores/layout.ts</read_first>
  <action>
    创建 `apps/desktop/src/components/SidebarResizeHandle.tsx`:
    
    Props: `{ onResize: (delta: number) => void }`
    
    实现：
    - 3px 宽的可拖拽竖条，位于 CopilotSidebar 左边缘
    - 默认透明，hover/focus 时显示蓝色（bg-blue-500）
    - cursor: col-resize
    - 监听 mousedown → mousemove → mouseup 事件
    - mousemove 计算水平位移 deltaX，回调 onResize(deltaX)
    - mouseup 清理事件监听
    - 使用 pointer-events 防止拖拽时误触下层内容
    - 添加 user-select: none 在拖拽期间防止文本选中
    - aria-label="拖拽调整侧边栏宽度"
  </action>
  <acceptance_criteria>
    Source: apps/desktop/src/components/SidebarResizeHandle.tsx 存在
    Source: apps/desktop/src/components/SidebarResizeHandle.tsx 包含 "col-resize"
    Source: apps/desktop/src/components/SidebarResizeHandle.tsx 包含 "aria-label"
    Behavior: 拖拽竖条时鼠标样式为 col-resize
    CLI: cd apps/desktop && npx tsc --noEmit exits 0
  </acceptance_criteria>
</task>

<task id="10.3" wave="10" type="execute" depends_on="10.2,8.2">
  <description>将 ResizeHandle 和持久化宽度集成到 CopilotSidebar</description>
  <read_first>apps/desktop/src/App.tsx</read_first>
  <read_first>apps/desktop/src/stores/layout.ts</read_first>
  <action>
    在 App.tsx 中集成侧边栏拖拽：
    
    1. 从 layout store 读取 sidebarWidth：
       `const sidebarWidth = useLayoutStore((s) => s.sidebarWidth);`
       `const setSidebarWidth = useLayoutStore((s) => s.setSidebarWidth);`
    
    2. 将 SidebarResizeHandle 放在 CopilotSidebar 包裹容器的左边缘：
       ```
       <div className="flex">
         <div style={{ width: sidebarWidth }}>
           <CopilotSidebar ... width={sidebarWidth} />
         </div>
         <SidebarResizeHandle
           onResize={(delta) => setSidebarWidth(sidebarWidth - delta)}
         />
       </div>
       ```
       注：拖拽向左为缩小（delta 为负），所以 newWidth = sidebarWidth - delta
    
    3. 将 width={sidebarWidth} 传给 CopilotSidebar
    
    4. 边界保护由 store 的 setSidebarWidth 中 clamp [300, 800] 实现
  </action>
  <acceptance_criteria>
    Source: apps/desktop/src/App.tsx 包含 "sidebarWidth" from useLayoutStore
    Source: apps/desktop/src/App.tsx 包含 "<SidebarResizeHandle"
    CopilotSidebar 的 width 由 sidebarWidth 驱动
    Behavior: 拖拽 ResizeHandle 可调整侧边栏宽度，300px ~ 800px
    Behavior: 刷新页面后侧边栏宽度保持上次调整值
    CLI: cd apps/desktop && npx tsc --noEmit exits 0
  </acceptance_criteria>
</task>

<task id="10.4" wave="10" type="execute" depends_on="10.3">
  <description>质量门：biome check + tsc + 手动验证</description>
  <read_first>apps/desktop/src/App.tsx</read_first>
  <action>
    1. `pnpm format` — 自动格式化
    2. `pnpm check` — biome ci 必须通过（0 errors）
    3. `cd apps/desktop && npx tsc --noEmit` — TypeScript 检查必须通过
    4. 修复所有 lint/type 错误
    5. 手动启动验证：
       - App 布局：Titlebar + WorkspaceContainer(中) + 聊天 sidebar(右) + StatusBar
       - 拖拽 sidebar 左边缘调整宽度
       - 关闭重启后宽度保持
       - 会话切换功能正常
       - 暗色/浅色模式切换正常
  </action>
  <acceptance_criteria>
    CLI: pnpm check exits 0
    CLI: cd apps/desktop && npx tsc --noEmit exits 0
    Behavior: 布局结构正确，sidebar 可拖拽，宽度持久化
  </acceptance_criteria>
</task>

</tasks>

---

*Phase: 02-Chat UI, Plan 02 — Layout Refactor + Sidebar Cohesion + Resizable Sidebar*
