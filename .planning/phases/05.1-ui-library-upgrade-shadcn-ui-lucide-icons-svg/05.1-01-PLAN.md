---
phase: "05.1"
plan: "05.1-01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - apps/desktop/package.json
  - apps/desktop/src/components/Titlebar.tsx
  - apps/desktop/src/components/ThemeToggle.tsx
  - apps/desktop/src/components/ChatToggle.tsx
  - apps/desktop/src/components/ConversationList.tsx
autonomous: true
requirements: []
---

# Plan 05.1-01: Wave 1 — lucide-react 安装与全局 SVG 替换

<objective>
安装 lucide-react，将所有手写 inline SVG 图标组件替换为 lucide-react 图标。
</objective>

<tasks>

<task id="05.1-01-1" type="execute">
<read_first>
- apps/desktop/package.json
</read_first>
<action>
安装 lucide-react 依赖：`pnpm --filter @paladin/desktop add lucide-react`
</action>
<verify>
package.json 中 dependencies 包含 "lucide-react"
</verify>
<acceptance_criteria>
- `pnpm --filter @paladin/desktop add lucide-react` 执行成功
- package.json dependencies 包含 lucide-react
</acceptance_criteria>
</task>

<task id="05.1-01-2" type="execute">
<read_first>
- apps/desktop/src/components/Titlebar.tsx
</read_first>
<action>
在 Titlebar.tsx 中：
1. 删除 MinimizeIcon、MaximizeIcon、CloseIcon 三个手写 SVG 函数组件
2. 导入 lucide-react 图标：`import { Minus, Maximize2, X } from 'lucide-react'`
3. 将 JSX 中的 `<MinimizeIcon />` → `<Minus className="size-4" />`
4. 将 `<MaximizeIcon />` → `<Maximize2 className="size-4" />`
5. 将 `<CloseIcon />` → `<X className="size-4" />`
6. 删除各 SVG 的 `<title>` 文本（lucide 内置 aria-label 支持）
</action>
<verify>
Titlebar.tsx 中不再包含手写 SVG 函数组件，使用 lucide 图标导入
</verify>
<acceptance_criteria>
- Titlebar.tsx 不再包含 MinimizeIcon/MaximizeIcon/CloseIcon 函数定义
- 导入并使用 Minus、Maximize2、X（lucide-react）
- `tsc --noEmit` 通过
</acceptance_criteria>
</task>

<task id="05.1-01-3" type="execute">
<read_first>
- apps/desktop/src/components/ThemeToggle.tsx
</read_first>
<action>
在 ThemeToggle.tsx 中：
1. 删除 SunIcon、MoonIcon、SystemIcon 三个手写 SVG 函数组件
2. 导入：`import { Sun, Moon, Monitor } from 'lucide-react'`
3. 更新 iconMap 对象：`{ light: Sun, dark: Moon, system: Monitor }`
4. 渲染时使用 `<Icon className="size-4" />`
</action>
<verify>
ThemeToggle.tsx 中 iconMap 使用 lucide 图标，无手写 SVG
</verify>
<acceptance_criteria>
- ThemeToggle.tsx 不再包含 SunIcon/MoonIcon/SystemIcon 函数定义
- iconMap 映射到 lucide 图标组件
- `tsc --noEmit` 通过
</acceptance_criteria>
</task>

<task id="05.1-01-4" type="execute">
<read_first>
- apps/desktop/src/components/ChatToggle.tsx
</read_first>
<action>
在 ChatToggle.tsx 中：
1. 删除 ChatIcon 手写 SVG 函数组件
2. 导入：`import { MessageSquare } from 'lucide-react'`
3. 将 `<ChatIcon />` → `<MessageSquare className="size-4" />`
</action>
<verify>
ChatToggle.tsx 使用 MessageSquare 图标，无手写 ChatIcon
</verify>
<acceptance_criteria>
- ChatToggle.tsx 不再包含 ChatIcon 函数定义
- 使用 lucide MessageSquare 图标
- `tsc --noEmit` 通过
</acceptance_criteria>
</task>

<task id="05.1-01-5" type="execute">
<read_first>
- apps/desktop/src/components/ConversationList.tsx
</read_first>
<action>
在 ConversationList.tsx 中：
1. 删除 PlusIcon、TrashIcon、EditIcon 三个手写 SVG 函数组件
2. 导入：`import { Plus, Trash2, Pencil } from 'lucide-react'`
3. `<PlusIcon />` → `<Plus className="size-4" />`
4. `<TrashIcon />` → `<Trash2 className="size-4" />`
5. `<EditIcon />` → `<Pencil className="size-3.5" />`
</action>
<verify>
ConversationList.tsx 使用 lucide 图标，无手写 SVG 函数
</verify>
<acceptance_criteria>
- ConversationList.tsx 不再包含 PlusIcon/TrashIcon/EditIcon 函数定义
- 使用 lucide Plus、Trash2、Pencil 图标
- `tsc --noEmit` 通过
</acceptance_criteria>
</task>

<task id="05.1-01-6" type="execute">
<read_first>
- apps/desktop/src/components/StatusBar.tsx
</read_first>
<action>
检查 StatusBar.tsx — 无手写 SVG 图标，仅有 CSS 圆点指示器。无需修改。
</action>
<verify>
StatusBar.tsx 无需变更
</verify>
<acceptance_criteria>
- StatusBar.tsx 保持原样
</acceptance_criteria>
</task>

</tasks>

<verification>
```bash
pnpm --filter @paladin/desktop exec tsc --noEmit
pnpm --filter @paladin/desktop exec biome ci src/
# 视觉验证：启动 dev server 检查图标显示
```
</verification>

<success_criteria>
- [ ] 所有手写 SVG 函数组件已删除（Titlebar: 3, ThemeToggle: 3, ChatToggle: 1, ConversationList: 3）
- [ ] 所有图标已替换为 lucide-react 对应组件
- [ ] tsc --noEmit 通过
- [ ] biome ci 通过
</success_criteria>

<artifacts_this_phase>
**新依赖：** lucide-react
**修改文件：** Titlebar.tsx, ThemeToggle.tsx, ChatToggle.tsx, ConversationList.tsx
</artifacts_this_phase>
