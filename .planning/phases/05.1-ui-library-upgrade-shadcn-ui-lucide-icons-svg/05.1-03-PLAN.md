---
phase: "05.1"
plan: "05.1-03"
type: "execute"
wave: 3
depends_on: ["05.1-02"]
files_modified:
  - apps/desktop/src/components/ChatToggle.tsx
  - apps/desktop/src/components/ThemeToggle.tsx
  - apps/desktop/src/components/Titlebar.tsx
  - apps/desktop/src/components/StatusBar.tsx
  - apps/desktop/src/components/ErrorToast.tsx
  - apps/desktop/src/App.tsx
  - apps/desktop/package.json
autonomous: false
requirements: ["D-02"]
---

# Plan 05.1-03: Wave 3 — Button 组件替换 + Sonner Toast 替换 ErrorToast

<objective>
添加 shadcn Button 和 Sonner 组件，将 ChatToggle/ThemeToggle/Titlebar/StatusBar 中的手写 button 替换为 Button 组件，将 ErrorToast 替换为 Sonner toast。
</objective>

<tasks>

<task id="05.1-03-1" type="execute">
<read_first>
- apps/desktop/package.json
</read_first>
<action>
添加 shadcn Button 和 Sonner 组件：
```bash
cd apps/desktop && npx shadcn@latest add button sonner
```
这会：
1. 创建 src/components/ui/button.tsx
2. 创建 src/components/ui/sonner.tsx
3. 安装 sonner 和 @radix-ui/react-slot 依赖
</action>
<verify>
src/components/ui/button.tsx 和 src/components/ui/sonner.tsx 已创建
</verify>
<acceptance_criteria>
- src/components/ui/button.tsx 存在
- src/components/ui/sonner.tsx 存在
- package.json 包含 sonner 依赖
</acceptance_criteria>
</task>

<task id="05.1-03-2" type="execute">
<read_first>
- apps/desktop/src/components/ChatToggle.tsx
</read_first>
<action>
重构 ChatToggle.tsx：
1. 导入 Button：`import { Button } from '@/components/ui/button'`
2. 将 `<button type="button" onClick={...} className="..." aria-label="...">` 替换为：
```tsx
<Button variant="ghost" size="icon" onClick={onClick} aria-label="Toggle Chat">
  <MessageSquare className="size-4" />
</Button>
```
3. 移除手写的 `p-1.5 rounded hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors` class（Button ghost 已包含）
4. 删除 ChatToggle 组件对 onClick 的手动处理（Button 原生支持）
</action>
<verify>
ChatToggle.tsx 使用 shadcn Button variant="ghost" size="icon"
</verify>
<acceptance_criteria>
- ChatToggle 使用 `<Button variant="ghost" size="icon">`
- 不再有手写 button 元素和 hover/transition Tailwind class
- `tsc --noEmit` 通过
</acceptance_criteria>
</task>

<task id="05.1-03-3" type="execute">
<read_first>
- apps/desktop/src/components/ThemeToggle.tsx
</read_first>
<action>
重构 ThemeToggle.tsx：
1. 导入 Button：`import { Button } from '@/components/ui/button'`
2. 将 `<button type="button" onClick={toggleTheme} className="p-1.5 rounded-md hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">` 替换为：
```tsx
<Button variant="ghost" size="icon" onClick={toggleTheme} title={`Theme: ${label}`}>
  <Icon className="size-4" />
</Button>
```
3. 移除手写 button class
</action>
<verify>
ThemeToggle.tsx 使用 shadcn Button
</verify>
<acceptance_criteria>
- ThemeToggle 使用 `<Button variant="ghost" size="icon">`
- 保留三态切换逻辑和 iconMap
- `tsc --noEmit` 通过
</acceptance_criteria>
</task>

<task id="05.1-03-4" type="execute">
<read_first>
- apps/desktop/src/components/Titlebar.tsx
</read_first>
<action>
重构 Titlebar.tsx 窗口控制按钮：
1. 导入 Button：`import { Button } from '@/components/ui/button'`
2. 将三个窗口控制按钮（最小化/最大化/关闭）从手写 `<button>` 替换为：
```tsx
<Button variant="ghost" size="icon" onClick={handleMinimize}>
  <Minus className="size-4" />
</Button>
```
3. 同理替换 Maximize 和 Close 按钮
4. 移除手写 hover/transition class
</action>
<verify>
Titlebar.tsx 中窗口按钮使用 shadcn Button
</verify>
<acceptance_criteria>
- Titlebar 窗口控制按钮使用 `<Button variant="ghost" size="icon">`
- `tsc --noEmit` 通过
</acceptance_criteria>
</task>

<task id="05.1-03-5" type="execute">
<read_first>
- apps/desktop/src/components/StatusBar.tsx
</read_first>
<action>
重构 StatusBar.tsx 终端状态按钮：
1. 导入 Button：`import { Button } from '@/components/ui/button'`
2. 将终端状态 `<button type="button" className="flex items-center gap-1 hover:text-gray-800 dark:hover:text-gray-200 transition-colors">` 替换为：
```tsx
<Button variant="ghost" size="sm" onClick={handleClick} title={...}>
  <span className={`inline-block w-2 h-2 rounded-full ${isTerminalRunning ? 'bg-green-500' : 'bg-gray-400'}`} />
  <span>{isTerminalRunning ? '终端运行中' : '终端未启动'}</span>
</Button>
```
3. 保留绿色/灰色状态圆点逻辑
</action>
<verify>
StatusBar.tsx 终端按钮使用 shadcn Button
</verify>
<acceptance_criteria>
- StatusBar 终端按钮使用 `<Button variant="ghost" size="sm">`
- 绿色/灰色圆点指示器保留
- `tsc --noEmit` 通过
</acceptance_criteria>
</task>

<task id="05.1-03-6" type="execute">
<read_first>
- apps/desktop/src/components/ErrorToast.tsx
- apps/desktop/src/App.tsx
- apps/desktop/src/hooks/useAgentHealth.ts
</read_first>
<action>
用 Sonner toast 替换 ErrorToast：

1. 在 App.tsx 中添加 Toaster：
```tsx
import { Toaster } from '@/components/ui/sonner';
// 在 JSX 中添加 <Toaster position="bottom-center" />
```

2. 找到 ErrorToast 的所有使用位置（`useAgentHealth.ts` 中渲染、App.tsx 中引用）

3. 将 ErrorToast 调用替换为 sonner toast：
```tsx
import { toast } from 'sonner';

// 错误 + 重试
toast.error('Agent 连接失败', {
  action: { label: '重试', onClick: () => retry() },
});

// 警告
toast.warning('消息内容');

// 信息
toast.info('消息内容');
```

4. 删除 ErrorToast.tsx 文件
5. 从 App.tsx 中移除 ErrorToast import 和 JSX 使用
</action>
<verify>
ErrorToast.tsx 已删除，App.tsx 使用 Sonner Toaster
</verify>
<acceptance_criteria>
- App.tsx 包含 `<Toaster position="bottom-center" />`
- ErrorToast.tsx 文件已删除
- 所有 ErrorToast 调用已替换为 sonner toast()
- `tsc --noEmit` 通过
</acceptance_criteria>
</task>

</tasks>

<verification>
```bash
pnpm --filter @paladin/desktop exec tsc --noEmit
pnpm --filter @paladin/desktop exec biome ci src/
pnpm --filter @paladin/desktop build
```
</verification>

<success_criteria>
- [ ] Button 和 Sonner 组件已添加到 src/components/ui/
- [ ] ChatToggle/ThemeToggle/Titlebar/StatusBar 使用 shadcn Button
- [ ] ErrorToast 已替换为 Sonner toast
- [ ] tsc + biome + vite build 全部通过
</success_criteria>

<artifacts_this_phase>
**新文件：** src/components/ui/button.tsx, src/components/ui/sonner.tsx
**新依赖：** sonner, @radix-ui/react-slot
**删除文件：** src/components/ErrorToast.tsx
**修改文件：** ChatToggle.tsx, ThemeToggle.tsx, Titlebar.tsx, StatusBar.tsx, App.tsx
</artifacts_this_phase>
