# Plan 05.1-03 SUMMARY: Button + Sonner Integration

**Status:** Complete
**Date:** 2026-06-17
**Commits:** 82d42f4

## Changes

- **Sonner component:** Added via `npx shadcn add sonner`, created `src/components/ui/sonner.tsx`
- **Button already existed** from Plan 02 (auto-created by shadcn init)
- **ChatToggle.tsx:** Replaced `<button>` with `<Button variant="ghost" size="icon">`
- **ThemeToggle.tsx:** Replaced `<button>` with `<Button variant="ghost" size="icon">`
- **Titlebar.tsx:** Replaced all 6 `<button>` elements (Terminal, Diff, Minimize, Maximize, Close) with `<Button variant="ghost" size="icon">`. Close button preserved special `hover:bg-red-500 hover:text-white` class.
- **StatusBar.tsx:** Replaced terminal indicator `<button>` with `<Button variant="ghost" size="sm">`
- **ErrorToast.tsx:** Deleted. Replaced by Sonner toast:
  - Offline state warning: `toast.warning(error, { action: { label: '重试', onClick: retry } })`
  - CopilotKit errors: `toast.error(friendly)` in handleCopilotError
- **App.tsx:** Added `<Toaster position="bottom-center" />` to both UI trees, added `useEffect` for offline warning toast

## Verification

- tsc --noEmit: 0 errors
- biome format: clean on all changed files
- vite build: success
