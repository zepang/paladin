---
phase: "05.1"
plan: "05.1-04"
type: "execute"
wave: 4
depends_on: ["05.1-03"]
files_modified:
  - apps/desktop/src/components/ConversationList.tsx
  - apps/desktop/package.json
autonomous: true
requirements: ["D-02"]
---

# Plan 05.1-04: Wave 4 — ScrollArea 替换 ConversationList 滚动容器

<objective>
添加 shadcn ScrollArea 组件，替换 ConversationList 中的原生 overflow 滚动容器。
</objective>

<tasks>

<task id="05.1-04-1" type="execute">
<read_first>
- apps/desktop/package.json
</read_first>
<action>
添加 shadcn ScrollArea 组件：
```bash
cd apps/desktop && npx shadcn@latest add scroll-area
```
这会创建 src/components/ui/scroll-area.tsx 并安装 @radix-ui/react-scroll-area。
</action>
<verify>
src/components/ui/scroll-area.tsx 已创建
</verify>
<acceptance_criteria>
- src/components/ui/scroll-area.tsx 存在
- package.json 包含 @radix-ui/react-scroll-area
</acceptance_criteria>
</task>

<task id="05.1-04-2" type="execute">
<read_first>
- apps/desktop/src/components/ConversationList.tsx
</read_first>
<action>
重构 ConversationList.tsx 滚动容器：
1. 导入：`import { ScrollArea } from '@/components/ui/scroll-area'`
2. 找到对话列表的外层滚动容器（目前使用原生 overflow 的 div）
3. 用 `<ScrollArea className="h-full">` 包裹列表内容
4. 移除手写 `overflow-y-auto` / `overflow` class
5. 确认 ScrollArea 高度从父容器继承（`h-full`）

注意：保留 ConversationList 所有业务逻辑（分组、重命名、删除、时间标签），仅替换外层滚动容器。
</action>
<verify>
ConversationList.tsx 使用 ScrollArea 组件包裹列表
</verify>
<acceptance_criteria>
- ConversationList 列表区域使用 `<ScrollArea>` 包裹
- 原有 overflow class 已移除
- 分组、重命名、删除、新建对话功能正常
- `tsc --noEmit` 通过
</acceptance_criteria>
</task>

</tasks>

<verification>
```bash
pnpm --filter @paladin/desktop exec tsc --noEmit
pnpm --filter @paladin/desktop exec biome ci src/
pnpm --filter @paladin/desktop build
```
</verification>

<success_criteria>
- [ ] ScrollArea 组件已添加到 src/components/ui/
- [ ] ConversationList 滚动容器已替换为 ScrollArea
- [ ] tsc + biome + vite build 全部通过
</success_criteria>

<artifacts_this_phase>
**新文件：** src/components/ui/scroll-area.tsx
**新依赖：** @radix-ui/react-scroll-area
**修改文件：** ConversationList.tsx
</artifacts_this_phase>
