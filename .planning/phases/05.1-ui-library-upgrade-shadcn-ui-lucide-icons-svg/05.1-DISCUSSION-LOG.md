# Phase 05.1: UI Library Upgrade - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-17
**Phase:** 05.1-ui-library-upgrade-shadcn-ui-lucide-icons-svg
**Areas discussed:** 替换策略与范围, 样式主题融合, 引入顺序与风险

---

## 替换策略与范围

| Option | Description | Selected |
|--------|-------------|----------|
| 全局替换所有 SVG | Titlebar 窗口按钮 + ThemeToggle + ChatToggle + StatusBar + 各处零散 icon，统一为 lucide-react 组件 | ✓ |
| 仅替换新组件 | 老组件不动，新引入的 shadcn 组件中使用 lucide | |
| 仅替换 Titlebar | 只替换 Minimize/Maximize/Close 图标 | |

| Option | Description | Selected |
|--------|-------------|----------|
| 全量替换 | Button + Sonner + ScrollArea + Sheet 一次性替换 | ✓ |
| 渐进替换核心组件 | Button + Sonner + ScrollArea，Sheet 暂缓 | |
| 最小替换 | 仅 Button + Sonner | |

| Option | Description | Selected |
|--------|-------------|----------|
| 保留但自由使用 shadcn | 业务组件保留不动，可自由使用 shadcn 原子组件优化内部 | ✓ |
| 完全不动 | 6 个业务组件完全不碰 | |

---

## 样式主题融合

| Option | Description | Selected |
|--------|-------------|----------|
| shadcn 色系为主 | OKLCH neutral/gray 色系，废弃 paladin 自定义蓝色阶 | ✓ |
| Paladin 色系为主 | shadcn --primary 映射为 Paladin 蓝紫色 | |
| 完全分裂 | 两边色系独立，不做统一 | |

| Option | Description | Selected |
|--------|-------------|----------|
| shadcn 变量完全覆盖 | @theme inline CSS 变量完整替换现有 index.css | ✓ |
| 合并保留 | shadcn + paladin 变量共存 | |

**User's choice:** 暗色模式直接兼容 — shadcn 和现有代码都使用 .dark class，Zustand theme store 无改动

---

## 引入顺序与风险

| Option | Description | Selected |
|--------|-------------|----------|
| lucide 先行 → shadcn 跟进 | Wave 1: lucide, Wave 2: shadcn init, Wave 3: 逐个替换 | ✓ |
| 同步引入 | 一次 PR 完成全部 | |
| shadcn 先行 | 先 init + CSS，最后换 lucide | |

| Option | Description | Selected |
|--------|-------------|----------|
| 先行尝试，失败回退 | 尝试 Sheet 替换 RightDrawer，保留原文件作为备份 | ✓ |
| 单独 PR | RightDrawer→Sheet 独立 Wave | |
| 先跳过 | Sheet 留到后续 Phase | |

| Option | Description | Selected |
|--------|-------------|----------|
| 构建+类型检查+Lint | tsc --noEmit + biome ci 0 errors + vite build 通过 | ✓ |
| 完整回归测试 | 手动验证 + 截图对比 | |
| 仅构建通过 | 只需 vite build | |

---

## Claude's Discretion

- shadcn CLI init 参数（style、baseColor）
- 各组件变体选择
- cn() utility 实现位置
- lucide icon 名称映射
- Sonner placement/duration
- ScrollArea 尺寸
- Sheet side/width 参数
