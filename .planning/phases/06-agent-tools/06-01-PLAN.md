---
phase: 06-agent-tools
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/agent/config/config.json
  - apps/agent/config/models.yaml
  - apps/agent/src/agent/paladin_agent.py
  - apps/agent/tests/test_agent.py
autonomous: true
requirements:
  - TLS-01

must_haves:
  truths:
    - "config.json 解析出的 ModelConfig 列表与 models.yaml 语义等价（字段名、类型、排序规则不变）"
    - "$ENV_VAR 语法在 JSON 的 api_key 字段中持续可用"
    - "JSON 格式错误时抛出包含 'JSON 解析失败' 的 ValueError"
    - "缺少 'models' 顶层字段时抛出明确 ValueError"
    - "models.yaml 被删除后，Agent 从 config.json 正常启动"
  artifacts:
    - apps/agent/config/config.json
  key_links:
    - "load_models() 导入从 yaml 切换为 json"
    - "create_paladin_agent() 默认参数 models_config_path 从 models.yaml 变为 config.json"
    - "src/server/main.py _config_path 指向 config/config.json"

prohibitions: []
---

<objective>
将模型配置从 models.yaml (YAML) 迁移到 config.json (JSON)，统一 models + mcp_servers 配置源。重写 load_models() 使用 json.load() 替代 yaml.safe_load()。

Purpose: 为 MCP 服务器配置（Plan 04）提供统一 JSON 配置基础，减少 PyYAML 依赖（D-05, D-07）。
Output: config.json 替代 models.yaml；load_models() 通过 json.load() 解析；test_agent.py 测试辅助函数适配 JSON。
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agent-tools/06-CONTEXT.md
@.planning/phases/06-agent-tools/06-RESEARCH.md
@.planning/phases/06-agent-tools/06-PATTERNS.md
</context>

<feature>
  <name>Config JSON Migration + load_models() JSON Rewrite</name>
  <files>apps/agent/src/agent/paladin_agent.py, apps/agent/tests/test_agent.py, apps/agent/config/config.json, apps/agent/config/models.yaml</files>
  <behavior>
    - Test 1 (load_models JSON): 创建临时 config.json 含 `{"models":[{...}]}` → load_models() 返回正确数量 (3 个) 的 ModelConfig 列表
    - Test 2 (sort): model priority 顺序 99,1,50 → load_models() 返回 priority [1,50,99]
    - Test 3 (missing field): config.json 的 model entry 缺少 "provider" 字段 → 抛出 ValueError 含 "缺少必需字段"
    - Test 4 (invalid JSON): 非 JSON 内容 → 抛出 ValueError 含 "JSON 解析失败"
    - Test 5 (missing models key): `{}` 空对象 → 抛出 ValueError 含 "缺少 'models' 顶层字段"
    - Test 6 (file not found): 不存在路径 → 抛出 FileNotFoundError
    - Test 7 (existing agent creation): 用 config.json + system.md → create_paladin_agent() 返回有效 Agent 实例
  </behavior>
  <implementation>
    1. 创建 apps/agent/config/config.json — 将 models.yaml 的内容用 JSON 格式输出，保持字段名不变（id/provider/model_id/api_base/api_key/priority/params），mcp_servers 字段初始化为空数组 `[]`，per D-05/D-06
    2. 重写 paladin_agent.py 中 load_models() 函数：`import yaml` → `import json`；`yaml.safe_load()` → `json.loads()`；错误信息 "YAML 解析失败" → "JSON 解析失败"；其余 ModelConfig 解析/校验/排序逻辑不变
    3. 删除 apps/agent/config/models.yaml
    4. 更新 tests/test_agent.py：`write_models_yaml()` → `write_config_json()`（用 json.dumps），所有 `import yaml` 移除
  </implementation>
</feature>

<verification>
- RED: `uv run pytest tests/test_agent.py::TestLoadModels -x` 首次运行失败（需要 JSON 实现）
- GREEN: `uv run pytest tests/test_agent.py::TestLoadModels -x` 全部通过
- REFACTOR: `uv run pytest tests/test_agent.py -x` 所有现有测试通过（含 TestAgentCreation）
</verification>

<success_criteria>
- RED commit: `test(06-01): add failing tests for JSON config migration`
- GREEN commit: `feat(06-01): migrate models config from YAML to JSON`
- 所有 7 个 behavior 测试通过
- models.yaml 已删除
- grep -c 'import yaml' apps/agent/src/agent/paladin_agent.py == 0
- grep -c 'models.yaml' apps/agent/src/agent/paladin_agent.py == 0
- grep -c 'models.yaml' apps/agent/src/server/main.py == 0 (not modified in this plan, verified for audit)
</success_criteria>

<output>
After completion, create `.planning/phases/06-agent-tools/06-01-SUMMARY.md`
</output>
