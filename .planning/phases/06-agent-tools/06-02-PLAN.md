---
phase: 06-agent-tools
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/agent/skills/coding/
  - apps/agent/skills/review/
  - apps/agent/skills/ops/
  - apps/agent/skills/review/code-review.md
  - apps/agent/prompts/system.md
autonomous: true
requirements:
  - TLS-04

must_haves:
  truths:
    - "skills/coding/、skills/review/、skills/ops/ 三个分类子目录存在"
    - "skills/review/code-review.md 包含合法 YAML frontmatter（name/description/license/compatibility）和 Markdown 主体"
    - "system.md 包含笼统工具描述：'我可以使用多种工具来协助你完成任务，包括文件系统操作、终端命令执行、MCP 外部工具集成、专业技能加载和子任务委派'"
    - "system.md 核心行为准则部分保持不变"
    - "system.md 不再逐项列举具体工具名称和参数"
  artifacts:
    - apps/agent/skills/review/code-review.md
    - apps/agent/skills/coding/ (directory)
    - apps/agent/skills/review/ (directory)
    - apps/agent/skills/ops/ (directory)
  key_links:
    - "skills/review/code-review.md YAML frontmatter 被 pydantic-deep SkillsToolset 正确解析"
    - "system.md 工具使用章节与 pydantic-deep 指令模板不重复，由运行时动态注入"

prohibitions:
  - statement: "MUST NOT 在 system.md 中逐项列举具体工具名称和参数"
    status: resolved
    verification: "grep: system.md 不含 'FilesystemToolset' 或 'ExecuteToolset' 或 'SkillsToolset' 或 'SubAgentToolset'"
---

<objective>
创建 Skills 分类目录结构、编写 1 个示例技能（code-review.md）、最小化更新系统提示（system.md）。

Purpose: 为 SkillsToolset（Plan 04 激活）提供可加载的技能目录和示例技能（D-08, D-09, D-10）；系统提示遵循最小化原则（D-11, D-12）避免与 pydantic-deep 运行时指令模板重复。
Output: skills/ 三分类目录 + 1 个 Markdown 技能文件 + 更新的 system.md。
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agent-tools/06-CONTEXT.md
@.planning/phases/06-agent-tools/06-RESEARCH.md
@.planning/phases/06-agent-tools/06-PATTERNS.md
@apps/agent/prompts/system.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 skills/ 三分类子目录</name>
  <files>apps/agent/skills/coding/, apps/agent/skills/review/, apps/agent/skills/ops/</files>
  <read_first>apps/agent/prompts/system.md, .planning/phases/06-agent-tools/06-RESEARCH.md</read_first>
  <action>
    Per D-08, 在 apps/agent/skills/ 下创建三个分类子目录：coding/、review/、ops/。
    每个目录用一个空的 .gitkeep 文件占位（子目录为空不影响 SkillsToolset，per RESEARCH.md Pitfall 3）。
    mkdir -p apps/agent/skills/{coding,review,ops} && touch apps/agent/skills/{coding,review,ops}/.gitkeep
  </action>
  <verify>
    <automated>test -d apps/agent/skills/coding && test -d apps/agent/skills/review && test -d apps/agent/skills/ops</automated>
  </verify>
  <acceptance_criteria>
    - apps/agent/skills/coding/ 目录存在
    - apps/agent/skills/review/ 目录存在
    - apps/agent/skills/ops/ 目录存在
    - 每个目录下有 .gitkeep 占位文件
  </acceptance_criteria>
  <done>三个分类子目录创建完毕，SkillsToolset 在空目录下可正常初始化</done>
</task>

<task type="auto">
  <name>Task 2: 编写 skills/review/code-review.md 示例技能</name>
  <files>apps/agent/skills/review/code-review.md</files>
  <read_first>.planning/phases/06-agent-tools/06-PATTERNS.md, .planning/phases/06-agent-tools/06-RESEARCH.md</read_first>
  <action>
    Per D-09/D-10, 创建 apps/agent/skills/review/code-review.md。
    文件为纯 Markdown + YAML frontmatter，不含可执行脚本（不包含 ```python 等代码块用于 run_skill_script）。

    YAML frontmatter 结构（per RESEARCH.md §Pattern 3）：
    - name: code-review
    - description: "Comprehensive code review guidance covering security, performance, maintainability, and best practices."
    - license: MIT
    - compatibility: universal

    Markdown 主体包含：
    - ## Review Checklist 章节
    - ### Security 子章节：检查硬编码密钥、输入验证、SQL 注入
    - ### Performance 子章节：N+1 查询、不必要的序列化、缓存策略
    - ### Maintainability 子章节：命名规范、函数长度、单一职责
    - ### Best Practices 子章节：错误处理、日志记录、类型安全
  </action>
  <verify>
    <automated>grep -c '^name: code-review$' apps/agent/skills/review/code-review.md && grep -c '^## Review Checklist' apps/agent/skills/review/code-review.md</automated>
  </verify>
  <acceptance_criteria>
    - apps/agent/skills/review/code-review.md 存在
    - 文件以 `---` 开头和 `---` 结尾的 YAML frontmatter 块
    - frontmatter 包含 name/description/license/compatibility 四个字段
    - 文件含 `## Review Checklist` 标题
    - 文件含 `### Security`、`### Performance`、`### Maintainability`、`### Best Practices` 子标题
    - 文件不包含 `run_skill_script` 或 `scripts` 配置
  </acceptance_criteria>
  <done>示例技能文件可被 SkillsToolset 的 SkillsDirectory 正确解析和加载</done>
</task>

<task type="auto">
  <name>Task 3: 最小化更新 prompts/system.md</name>
  <files>apps/agent/prompts/system.md</files>
  <read_first>apps/agent/prompts/system.md, .planning/phases/06-agent-tools/06-RESEARCH.md</read_first>
  <action>
    Per D-11/D-12/D-13, 对 system.md 做最小化修改：
    1. 保留 `## 核心行为准则` 全部内容（直接回答/主动澄清/逐步推理/诚实边界）
    2. `## 安全策略` 保留笼统安全原则（文件操作/网络请求/代码执行/敏感信息），移除具体工具引用
    3. `## 工具使用` 替换当前具体工具列表为 D-12 笼统描述：
       "我可以使用多种工具来协助你完成任务，包括文件系统操作、终端命令执行、MCP 外部工具集成、专业技能加载和子任务委派。"
    4. 不添加任意特定工具名称（FilesystemToolset/ExecuteToolset/SkillsToolset/SubAgentToolset）
    5. 保持 load_system_prompt() 文件加载方式不变（D-13）
  </action>
  <verify>
    <automated>grep -c '核心行为准则' apps/agent/prompts/system.md && grep -c '可以使用多种工具' apps/agent/prompts/system.md</automated>
  </verify>
  <acceptance_criteria>
    - system.md 包含 `## 核心行为准则` 且内容不变
    - system.md `## 工具使用` 包含 "可以使用多种工具来协助你完成任务"
    - system.md 不含 `FilesystemToolset`、`ExecuteToolset`、`SkillsToolset`、`SubAgentToolset` 字符串
  </acceptance_criteria>
  <done>系统提示最小化更新完成，工具说明由 pydantic-deep 运行时指令模板注入</done>
</task>

</tasks>

<verification>
- [ ] `grep -c 'core-review' apps/agent/skills/review/code-review.md` — 文件存在且格式正确
- [ ] `grep -r 'FilesystemToolset\|ExecuteToolset\|SkillsToolset\|SubAgentToolset' apps/agent/prompts/system.md` 返回空 — system.md 不含具体工具名称
- [ ] `grep -c '可以使用多种工具' apps/agent/prompts/system.md` > 0 — 笼统工具描述存在
</verification>

<success_criteria>
- skills/coding/、skills/review/、skills/ops/ 三个子目录存在（含 .gitkeep）
- skills/review/code-review.md 含合法 YAML frontmatter + Markdown 指导内容
- system.md 核心行为准则不变，工具使用改为笼统描述
- system.md 不含 pydantic-deep 工具集具体名称
</success_criteria>

<output>
After completion, create `.planning/phases/06-agent-tools/06-02-SUMMARY.md`
</output>
