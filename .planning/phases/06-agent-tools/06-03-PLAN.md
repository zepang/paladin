---
phase: 06-agent-tools
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/agent/pyproject.toml
autonomous: true
requirements:
  - TLS-03

must_haves:
  truths:
    - "pyproject.toml dependencies 包含 'pydantic-ai-slim[mcp]>=1.107.0'"
    - "uv sync 后 python -c 'from pydantic_deep import build_mcp_server' 成功"
    - "uv sync 后 python -c 'from mcp import types' 成功（mcp SDK 作为传递依赖安装）"
  artifacts:
    - apps/agent/pyproject.toml (modified)
  key_links:
    - "pydantic-ai-slim[mcp] extra 自动引入 fastmcp-slim 和 mcp SDK"

prohibitions: []
---

<objective>
安装 MCP SDK 依赖：在 pyproject.toml 中新增 `pydantic-ai-slim[mcp]` 并运行 uv sync 安装。

Purpose: 为 Plan 04 的 MCP 服务器配置加载提供必要的 Python 包（TLS-03 前置依赖）。MCP 集成按需加载（config.json 无 mcp_servers 时不触发 import），但依赖需提前安装。
Output: pyproject.toml 更新 + venv 中安装 mcp/fastmcp-slim 包。
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agent-tools/06-RESEARCH.md
@apps/agent/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 pyproject.toml 中添加 pydantic-ai-slim[mcp] 依赖</name>
  <files>apps/agent/pyproject.toml</files>
  <read_first>apps/agent/pyproject.toml</read_first>
  <action>
    在 pyproject.toml 的 dependencies 数组中新增一行：
    "pydantic-ai-slim[mcp]>=1.107.0",
    位置紧接在 "pydantic-ai-slim[ag-ui]>=1.107.0" 之后（同包不同 extra）。
    然后运行 uv lock --upgrade-package pydantic-ai-slim 和 uv sync 安装。
  </action>
  <verify>
    <automated>cd apps/agent && uv run python -c "from pydantic_deep import build_mcp_server; print('MCP import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml 的 dependencies 中包含 "pydantic-ai-slim[mcp]>=1.107.0"
    - uv sync 成功完成（exit code 0）
    - `from pydantic_deep import build_mcp_server` 可导入
    - `from mcp import types` 可导入（传递依赖）
  </acceptance_criteria>
  <done>MCP SDK 依赖已安装，Plan 04 可按需加载 MCP 工具集</done>
</task>

</tasks>

<verification>
- [ ] `grep -c 'pydantic-ai-slim\[mcp\]' apps/agent/pyproject.toml` 返回 1
- [ ] `cd apps/agent && uv run python -c "from pydantic_deep import build_mcp_server"` exit 0
- [ ] `cd apps/agent && uv run python -c "from mcp import types"` exit 0
</verification>

<success_criteria>
- pyproject.toml 依赖已更新
- uv sync 成功
- build_mcp_server 和 mcp.types 可导入
</success_criteria>

<output>
After completion, create `.planning/phases/06-agent-tools/06-03-SUMMARY.md`
</output>
