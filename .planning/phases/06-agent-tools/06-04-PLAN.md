---
phase: 06-agent-tools
plan: 04
type: tdd
wave: 2
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified:
  - apps/agent/src/agent/paladin_agent.py
  - apps/agent/src/server/main.py
autonomous: true
requirements:
  - TLS-01
  - TLS-02
  - TLS-03
  - TLS-04
  - TLS-05

must_haves:
  truths:
    - "create_paladin_agent() 调用 create_deep_agent 时 include_skills=True, include_subagents=True, include_execute=True, include_plan=True, web_search=True"
    - "include_builtin_subagents=True 且 max_nesting_depth=1, skill_directories 传入 skills/ 绝对路径"
    - "config.json 无 mcp_servers 字段或 mcp_servers 为空数组时，Agent 正常启动无 MCP 依赖错误"
    - "config.json 有 mcp_servers 但 mcp SDK 缺失时，抛出 ImportError 含 'pydantic-ai-slim[mcp] 未安装'"
    - "config.json 有 mcp_servers 且 mcp SDK 可用时，Agent 注册对应 MCPToolset"
    - "单个 MCP 服务器 enabled=false 时跳过，连接失败时记录 warning 日志但不影响 Agent 启动"
    - "main.py 中 _config_path 指向 config/config.json（非 models.yaml）"
    - "skills/ 目录不存在时自动创建（mkdir parents=True exist_ok=True）"
    - "Agent 创建日志包含 '含 Todo + Filesystem + Skills + SubAgents + Execute + Plan'"
  artifacts:
    - apps/agent/src/agent/paladin_agent.py (modified: _load_mcp_servers helper + create_paladin_agent update)
  key_links:
    - "create_deep_agent() 参数: include_skills/include_subagents/include_execute/include_plan/web_search 全部 True"
    - "_load_mcp_servers() 按需 import build_mcp_server → MCPServerConfig → MCPToolset"
    - "main.py _config_path 从 models.yaml 变为 config.json"

prohibitions:
  - statement: "MUST NOT 在 config.json 中存储明文 API 密钥（使用 $ENV_VAR 语法）"
    status: resolved
    verification: "config.json 的 api_key 字段均为 $ENV_VAR 格式"
  - statement: "MUST NOT 在 MCP SDK 未安装时静默失败（必须给出明确错误提示）"
    status: resolved
    verification: "MCP servers 已配置但 import 失败时抛出 ImportError 含明确安装指引"
---

<objective>
激活 pydantic-deep 全部内置工具集：翻转 include_skills/include_subagents/include_execute/include_plan/web_search 布尔参数；新增 _load_mcp_servers() 按需加载 MCP 工具集；同步 main.py 配置路径到 config.json。

Purpose: 一次性激活 Phase 6 所有工具类别（D-01），满足 TLS-01~05 全部 requirements。这是 Phase 6 的核心交付。
Output: 更新后的 create_paladin_agent() 工厂函数（含 _load_mcp_servers helper）+ main.py 路径修正。
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agent-tools/06-CONTEXT.md
@.planning/phases/06-agent-tools/06-RESEARCH.md
@.planning/phases/06-agent-tools/06-PATTERNS.md
@apps/agent/src/agent/paladin_agent.py
@apps/agent/src/server/main.py
</context>

<feature>
  <name>Agent Tool Activation — 5 个工具集一键启用 + MCP 按需加载</name>
  <files>apps/agent/src/agent/paladin_agent.py, apps/agent/src/server/main.py</files>
  <behavior>
    - Test 1 (all toolsets active): 用 config.json + system.md + skills/ 创建 Agent → Agent 实例非 None；verify 通过 inspect Agent 的工具列表确认 FilesystemToolset/ExecuteToolset/SkillsToolset/SubAgentToolset 已注册（从 agent._agent._tools 或类似内部属性检查工具名称包含 'list_skills'/'execute'/'delegate_to_subagent'）
    - Test 2 (no mcp servers — normal startup): config.json 无 mcp_servers 字段 → Agent 正常创建，无 import 错误
    - Test 3 (mcp servers configured, SDK missing): config.json 含 mcp_servers，但 mock `build_mcp_server` import 抛出 ImportError → 抛出 ImportError 含明确错误消息 "pydantic-ai-slim[mcp] 未安装"
    - Test 4 (mcp servers enabled): config.json 含 1 个 enabled=true 的 stdio MCP server，mock build_mcp_server 返回 mock toolset → Agent 的 mcp_servers 参数包含该 mock toolset
    - Test 5 (mcp server disabled): config.json 含 enabled=false 的 server → 跳过，mcp_toolsets 为空
    - Test 6 (skills dir auto-create): skills/ 目录不存在 → create_paladin_agent() 自动创建 skills/ 目录
    - Test 7 (main.py config path): main.py 模块级 agent 创建使用 config/config.json（通过 monkeypatch _config_path 或读取源码验证）
    - Test 8 (include_plan + web_search): create_deep_agent 调用参数 include_plan=True, web_search=True
  </behavior>
  <implementation>
    1. 在 paladin_agent.py 中新增 _load_mcp_servers(raw_config: dict) -> list 函数（per RESEARCH.md §Pattern 2）：
       - 检查 "mcp_servers" key，空则返回 []
       - try/except ImportError 包裹 `from pydantic_deep import MCPServerConfig, build_mcp_server`
       - 遍历 entries，跳过 enabled=false 的项
       - 对每个 enabled 项创建 MCPServerConfig(name/transport/command/args/env/url/headers/enabled/description)
       - 调用 build_mcp_server(server_config) 获取 toolset
       - Exception 时 logger.warning("mcp_server_unavailable", name=...)
       - 返回 toolsets 列表
    2. 更新 create_paladin_agent()：
       - models_config_path 默认值改为 "config/config.json"
       - 在 json.load 后保存 raw_config 变量（用于传给 _load_mcp_servers）
       - 新增：skills_dir 路径构建 + mkdir(parents=True, exist_ok=True)
       - 新增：mcp_toolsets = _load_mcp_servers(raw_config)
       - create_deep_agent() 调用更新（per D-01/D-02/D-04）：
         include_skills=True, include_subagents=True, include_builtin_subagents=True,
         include_execute=True, include_plan=True, web_search=True,
         skill_directories=[str(skills_dir)], max_nesting_depth=1,
         mcp_servers=mcp_toolsets
       - 日志更新："Agent 已创建（含 Todo + Filesystem + Skills + SubAgents + Execute + Plan）"
       - 移除 import yaml（如果 load_models 已改为 json 但 yaml import 仍在文件顶部）
    3. 更新 main.py：
       - _config_path 从 project_root / "config" / "models.yaml" 改为 project_root / "config" / "config.json"
    4. verify: `uv run pytest tests/test_agent.py -x` 所有现有测试仍通过（使用 config.json 格式的 helpers）
  </implementation>
</feature>

<verification>
- RED: 新增 tests/test_agent.py 中的 TestToolsetActivation 类测试（8 个测试），首次运行全部失败
- GREEN: 实现 _load_mcp_servers() + 更新 create_paladin_agent() + 更新 main.py → 全部 8 个测试通过
- REFACTOR: 确认 yaml import 已从 paladin_agent.py 移除；确认 main.py 无 models.yaml 引用
</verification>

<success_criteria>
- RED/GREEN/REFACTOR 各 1 个 atomic commit
- 8 个 behavior 测试全部通过
- grep -c 'models.yaml' apps/agent/src/server/main.py == 0
- grep -c 'models.yaml' apps/agent/src/agent/paladin_agent.py == 0
- grep -c 'include_skills=True' apps/agent/src/agent/paladin_agent.py == 1
- grep -c 'include_execute=True' apps/agent/src/agent/paladin_agent.py == 1
- grep -c 'include_subagents=True' apps/agent/src/agent/paladin_agent.py == 1
- grep -c 'include_plan=True' apps/agent/src/agent/paladin_agent.py == 1
- grep -c 'web_search=True' apps/agent/src/agent/paladin_agent.py == 1
</success_criteria>

<output>
After completion, create `.planning/phases/06-agent-tools/06-04-SUMMARY.md`
</output>
