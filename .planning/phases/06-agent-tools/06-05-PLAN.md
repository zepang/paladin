---
phase: 06-agent-tools
plan: 05
type: execute
wave: 3
depends_on:
  - 06-04
files_modified:
  - apps/agent/tests/test_tools.py
  - apps/agent/tests/test_agent.py
  - apps/agent/tests/test_server.py
autonomous: true
requirements:
  - TLS-01
  - TLS-02
  - TLS-03
  - TLS-04
  - TLS-05

must_haves:
  truths:
    - "test_tools.py 覆盖 FilesystemToolset 的读/写/编辑/删除操作（TLS-01）"
    - "test_tools.py 覆盖 include_execute 的 ls/cat/python 命令执行及日志含时间戳/命令内容/退出码（TLS-02）"
    - "test_tools.py 覆盖 MCP Stdio 服务器配置后工具可调用、依赖缺失时报错（TLS-03）"
    - "test_tools.py 覆盖 list_skills 返回列表、load_skill 加载内容、空目录正常启动（TLS-04）"
    - "test_tools.py 覆盖子 Agent 委派、嵌套深度限制、内置默认子 Agent（TLS-05）"
    - "test_tools.py 覆盖 5 个工具类别独立失败互不影响"
    - "test_tools.py 覆盖端到端流程：读取文件 → 修改内容 → 写回"
    - "test_agent.py 新增 TestToolsetActivation 类从 Plan 04 RED 阶段迁移（8 个测试）"
    - "test_agent.py 新增 test_agent_no_mcp_server_normal_startup、test_agent_empty_skills_directory_normal_startup、test_agent_default_subagents"
    - "test_server.py 的 /health 端点验证 agent.models 列表正确返回"
  artifacts:
    - apps/agent/tests/test_tools.py
  key_links:
    - "test_tools.py 的 12 个测试用例映射到 RESEARCH.md §Phase Requirements → Test Map 表格"

prohibitions: []
---

<objective>
创建综合测试套件：test_tools.py（工具类别专项测试 12 个用例）+ test_agent.py 新增工具激活验证 + test_server.py 确认端点兼容。

Purpose: 满足 SPEC 15 条 acceptance criteria 中所有可通过自动化测试验证的项，覆盖 TLS-01~05 每个工具类别的核心行为。
Output: test_tools.py（新建 12 个测试）+ test_agent.py（新增 TestToolsetActivation 类 8 测试）+ test_server.py（适配 config.json 路径）。
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agent-tools/06-CONTEXT.md
@.planning/phases/06-agent-tools/06-RESEARCH.md
@.planning/phases/06-agent-tools/06-PATTERNS.md
@.planning/phases/06-agent-tools/06-SPEC.md
@apps/agent/tests/test_agent.py
@apps/agent/tests/test_server.py
@apps/agent/src/agent/paladin_agent.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 tests/test_tools.py — 12 个工具类别测试用例</name>
  <files>apps/agent/tests/test_tools.py</files>
  <read_first>apps/agent/tests/test_agent.py, apps/agent/src/agent/paladin_agent.py, .planning/phases/06-agent-tools/06-RESEARCH.md</read_first>
  <behavior>
    - TLS-01: test_filesystem_read_write — Agent 可读取/写入 workspace 内文件；test_filesystem_sandbox_block — 写入 workspace 外路径被拒绝
    - TLS-02: test_execute_basic_command — ls/cat 命令执行返回输出；test_execute_logging — 命令执行日志含 command/exit_code/timestamp
    - TLS-03: test_mcp_stdio_server — 配置 Stdio MCP server 后工具可调用；test_mcp_missing_dependency — mcp SDK 缺失时 ImportError 含安装指引；test_mcp_disabled_server — enabled=false 时跳过
    - TLS-04: test_list_skills — list_skills 返回技能列表含 code-review；test_load_skill — load_skill('code-review') 返回 Markdown 内容；test_empty_skills_dir — 空 skills/ 目录 Agent 正常启动
    - TLS-05: test_subagent_delegation — 子 Agent 委派并获取结果；test_subagent_nesting_limit — max_nesting_depth=1 生效
    - test_independent_failure — 一个工具类别配置错误不影响其他（如 mock execute tool 失败时 filesystem 仍可用）
    - test_e2e_read_modify_write — 创建临时文件 → Agent 读取 → Agent 修改 → Agent 写回 → 验证内容
  </behavior>
  <action>
    创建 apps/agent/tests/test_tools.py。
    导入结构遵循 PATTERNS.md §test_tools.py 模式：pytest/Path/json/tempfile/patch。
    复用 write_config_json() 和 make_model_config() 辅助函数（从 test_agent.py 复制或提取到 conftest.py）。
    每个测试方法遵循同一模式：
    1. 创建临时 config.json + system.md + skills 目录（含 code-review.md）
    2. patch 环境变量（DEEPSEEK_API_KEY="fake-key"）
    3. 调用 create_paladin_agent() 创建 Agent
    4. 通过 Agent 内部 API 或直接调用工具函数验证行为
    对 MCP 测试：mock build_mcp_server 和 MCPServerConfig。
    对 execute 测试：mock subprocess.run 或在允许的 commands 内运行真实命令。
    对文件系统测试：使用 tmp_path 内的真实文件操作。
    使用 @pytest.mark.asyncio 标记异步测试。
  </action>
  <verify>
    <automated>cd apps/agent && uv run pytest tests/test_tools.py -x --timeout=60</automated>
  </verify>
  <acceptance_criteria>
    - apps/agent/tests/test_tools.py 文件存在
    - 文件包含 12 个 test_* 方法（对应上述 12 个行为）
    - 文件顶部 docstring 说明 "Agent Tools 测试套件 — 覆盖 TLS-01~05 工具类别"
    - `uv run pytest tests/test_tools.py -x` 全部通过或明确标记 skip（仅 MCP integration 测试可 skip）
  </acceptance_criteria>
  <done>12 个工具类别测试用例全部通过，覆盖 TLS-01~05 核心行为</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 tests/test_agent.py — TestToolsetActivation 类 + 独立失败/空目录测试</name>
  <files>apps/agent/tests/test_agent.py</files>
  <read_first>apps/agent/tests/test_agent.py, apps/agent/src/agent/paladin_agent.py</read_first>
  <action>
    在 test_agent.py 中新增 TestToolsetActivation 类，包含 8 个测试方法（这些测试在 Plan 04 的 RED 阶段已编写，此处确保它们存在并通过）：
    1. test_all_toolsets_active — Agent 创建后包含所有 5 个工具集
    2. test_no_mcp_servers_normal_startup — config.json 无 mcp_servers → 正常启动
    3. test_mcp_missing_dependency_error — MCP SDK 缺失时明确错误
    4. test_mcp_server_enabled — enabled=true 时注册 MCPToolset
    5. test_mcp_server_disabled — enabled=false 时跳过
    6. test_skills_dir_auto_create — skills/ 不存在时自动创建
    7. test_main_py_config_path — 验证 main.py 使用 config.json
    8. test_plan_and_web_search — 验证 include_plan=True, web_search=True

    另外新增 3 个独立测试：
    - test_agent_no_mcp_server_normal_startup（如已在上面的类中覆盖则跳过）
    - test_agent_empty_skills_directory_normal_startup — 空 skills/ 目录 Agent 正常启动
    - test_agent_default_subagents — 未配置自定义子 Agent 时使用内置默认（include_builtin_subagents=True）
  </action>
  <verify>
    <automated>cd apps/agent && uv run pytest tests/test_agent.py -x --timeout=60</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_agent.py 包含 TestToolsetActivation 类
    - TestToolsetActivation 类含 8 个 test_* 方法
    - tests/test_agent.py 含 test_agent_empty_skills_directory_normal_startup
    - tests/test_agent.py 含 test_agent_default_subagents
    - `uv run pytest tests/test_agent.py -x` 全部通过
  </acceptance_criteria>
  <done>test_agent.py 覆盖工具激活验证、空目录/默认子 Agent 边界情况</done>
</task>

<task type="auto">
  <name>Task 3: 更新 tests/test_server.py — 适配 config.json 路径</name>
  <files>apps/agent/tests/test_server.py</files>
  <read_first>apps/agent/tests/test_server.py, apps/agent/src/server/main.py</read_first>
  <action>
    验证 test_server.py 现有测试（/health 和 /copilotkit 端点）在 config.json 迁移后仍通过。
    如果现有测试 patch 了 DEEPSEEK_API_KEY 并依赖 models.yaml 路径，确认在 Plan 01/04 更新 main.py _config_path 后测试仍兼容。
    如需要，添加 test_health_returns_config_json_models 测试：验证 /health 端点返回的 models 列表来源自 config.json。
  </action>
  <verify>
    <automated>cd apps/agent && uv run pytest tests/test_server.py -x --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_server.py -x` 全部通过
    - /health 端点返回 status: "ok" + agent: "paladin-agent" + models 列表非空
  </acceptance_criteria>
  <done>test_server.py 确认与 config.json 兼容</done>
</task>

</tasks>

<verification>
- [ ] `cd apps/agent && uv run pytest tests/test_tools.py -x --timeout=60` — 12 个工具测试通过
- [ ] `cd apps/agent && uv run pytest tests/test_agent.py -x --timeout=60` — 包含 TestToolsetActivation 全部通过
- [ ] `cd apps/agent && uv run pytest tests/test_server.py -x --timeout=30` — 服务器测试兼容 config.json
- [ ] `cd apps/agent && uv run pytest tests/ -x` — 全量测试通过
</verification>

<success_criteria>
- test_tools.py 新建，12 个测试用例覆盖 TLS-01~05
- test_agent.py 新增 TestToolsetActivation 类 (8 tests) + 空目录/默认子 Agent 测试
- test_server.py 与 config.json 兼容
- 全量测试套件 `uv run pytest tests/ -x` 通过
</success_criteria>

<output>
After completion, create `.planning/phases/06-agent-tools/06-05-SUMMARY.md`
</output>
