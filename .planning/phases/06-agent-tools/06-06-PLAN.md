---
phase: 06-agent-tools
plan: 06
type: execute
wave: 3
depends_on:
  - 06-04
files_modified:
  - apps/agent/tests/test_prohibitions.py
  - apps/agent/tests/fixtures/skill-escape.md
autonomous: true
requirements:
  - TLS-04

must_haves:
  truths:
    - "test_prohibitions.py 包含 test_skill_sandbox_escape 测试"
    - "skill-escape.md 恶意技能文件尝试读取 /etc/passwd"
    - "test_skill_sandbox_escape 验证技能脚本无法访问 workspace 外路径"
    - "test_prohibitions.py 顶部 docstring 说明 'Prohibition 验证测试套件'"
  artifacts:
    - apps/agent/tests/test_prohibitions.py
    - apps/agent/tests/fixtures/skill-escape.md
  key_links:
    - "SPEC §Prohibitions 第 1 项 MUST NOT 对应 test_prohibitions.py::test_skill_sandbox_escape"

prohibitions:
  - statement: "MUST NOT allow skill scripts unrestricted system access outside the workspace sandbox"
    status: resolved
    verification: "test — check_kind: pytest, check_target: apps/agent/tests/test_prohibitions.py::test_skill_sandbox_escape, check_violation_fixture: apps/agent/tests/fixtures/skill-escape.md"
---

<objective>
验证 SPEC §Prohibitions 中 resolved 的 must-NOT 约束：技能脚本沙箱越权禁止。（已 dismissed 的第二项 prohibition 由 Phase 7 HITL 覆盖，不在本计划范围。）

Purpose: 确保 pydantic-deep SkillsToolset 的 LocalBackend 沙箱正确限制技能脚本的文件访问范围，无法越权访问系统路径。
Output: test_prohibitions.py + fixtures/skill-escape.md 恶意技能文件。
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agent-tools/06-CONTEXT.md
@.planning/phases/06-agent-tools/06-RESEARCH.md
@.planning/phases/06-agent-tools/06-PATTERNS.md
@.planning/phases/06-agent-tools/06-SPEC.md
@apps/agent/tests/test_agent.py
@apps/agent/src/agent/paladin_agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 tests/fixtures/skill-escape.md — 恶意沙箱越权技能文件</name>
  <files>apps/agent/tests/fixtures/skill-escape.md</files>
  <read_first>.planning/phases/06-agent-tools/06-PATTERNS.md, apps/agent/skills/review/code-review.md</read_first>
  <action>
    创建 apps/agent/tests/fixtures/ 目录（如不存在）。
    创建 skill-escape.md，包含 YAML frontmatter（name: escape-test, description: 沙箱越权测试）和 Python 代码块尝试读取 /etc/passwd：
    ```python
    with open('/etc/passwd') as f:
        print(f.read())
    ```
    此文件用于测试 SkillsToolset 的 sandbox 隔离——技能脚本应被 LocalBackend 阻止访问 workspace 外路径。
  </action>
  <verify>
    <automated>grep -c 'name: escape-test' apps/agent/tests/fixtures/skill-escape.md && grep -c '/etc/passwd' apps/agent/tests/fixtures/skill-escape.md</automated>
  </verify>
  <acceptance_criteria>
    - apps/agent/tests/fixtures/skill-escape.md 存在
    - 包含 `name: escape-test` YAML frontmatter
    - 包含尝试读取 `/etc/passwd` 的 Python 代码
  </acceptance_criteria>
  <done>恶意技能 fixture 文件就绪，可用于越权测试</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建 tests/test_prohibitions.py — 沙箱越权禁止测试</name>
  <files>apps/agent/tests/test_prohibitions.py</files>
  <read_first>apps/agent/tests/test_agent.py, apps/agent/src/agent/paladin_agent.py, .planning/phases/06-agent-tools/06-PATTERNS.md</read_first>
  <behavior>
    - test_skill_sandbox_escape: 创建包含恶意技能（skill-escape.md）的临时 skills/escape/ 目录 → 创建 Agent → 验证技能脚本执行时无法访问 /etc/passwd（预期：被 LocalBackend 拒绝或执行结果不含 /etc/passwd 内容）
  </behavior>
  <action>
    创建 apps/agent/tests/test_prohibitions.py。
    遵循 PATTERNS.md §test_prohibitions.py 模式：
    - Imports: pytest, os, Path, patch
    - Fixture: skill_escape_tmpdir(tmp_path) — 创建临时 skills/escape/ 目录，复制 skill-escape.md
    - Test: test_skill_sandbox_escape — 使用 create_paladin_agent() 创建 Agent，传入临时 skills 目录作为 skill_directories，验证技能脚本被沙箱限制
    - 使用 patch.dict(os.environ, {"DEEPSEEK_API_KEY": "fake-key"}) 绕开 API key 检查
    
    关键验证逻辑：pydantic-deep 的 LocalBackend(root_dir=workspace) 限制技能脚本只能访问 workspace 内文件。尝试访问 /etc/passwd 应该被操作系统权限（macOS SIP/TCC）或 pydantic-deep DEFAULT_BLOCKED_READ_PATHS 阻止。测试验证点是：技能脚本执行不会返回 /etc/passwd 的内容。
  </action>
  <verify>
    <automated>cd apps/agent && uv run pytest tests/test_prohibitions.py::test_skill_sandbox_escape -x --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - apps/agent/tests/test_prohibitions.py 存在
    - 文件顶部 docstring: "Prohibition 验证测试套件"
    - 包含 test_skill_sandbox_escape 测试方法
    - 测试验证技能脚本无法访问 /etc/passwd
    - `uv run pytest tests/test_prohibitions.py -x` 通过
  </acceptance_criteria>
  <done>沙箱越权测试通过，验证 SPEC §Prohibitions MUST NOT 约束</done>
</task>

</tasks>

<verification>
- [ ] `grep -c '/etc/passwd' apps/agent/tests/fixtures/skill-escape.md` > 0
- [ ] `cd apps/agent && uv run pytest tests/test_prohibitions.py -x --timeout=30` 通过
- [ ] `cd apps/agent && uv run pytest tests/ -x` 全量通过（含 test_prohibitions.py）
</verification>

<success_criteria>
- tests/fixtures/skill-escape.md 恶意技能 fixture 创建
- test_prohibitions.py 新建，含 test_skill_sandbox_escape
- 沙箱越权测试通过 — 技能脚本无法访问 workspace 外系统路径
- 全量测试套件通过
</success_criteria>

<output>
After completion, create `.planning/phases/06-agent-tools/06-06-SUMMARY.md`
</output>
