---
phase: 07-hitl-sidecar
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/agent/src/agent/hitl.py
  - apps/agent/tests/test_hitl.py
autonomous: true
requirements:
  - HIT-02

must_haves:
  truths:
    - "approval_callback 工厂函数创建 asyncio.Event、生成 UUID4 request_id、推入 _pending_approvals、写入 _sse_queue、await Event.wait(timeout)、返回 bool 决策"
    - "_pending_approvals 和 _pending_decisions 为模块级 dict[str, asyncio.Event] 和 dict[str, bool]"
    - "超时（默认 30s）返回 False（拒绝），且从 _pending_approvals 清理 request_id 防止内存泄漏"
    - "_sse_queue 为 asyncio.Queue | None，支持注入和 None 时优雅跳过推送"
    - "validate_hitl_config() 校验 hitl 配置段：不存在的工具名记录 structured warning（logger.warning('hitl_config_unknown_tool', tool_name=...)），不崩溃"
    - "验收：并发 3 个 approval_callback 调用 → 各自独立 Event + request_id，互不干扰（排队逻辑在 SPEC R4，由 Agent 工具调用路径保证串行，hitl.py 不负责排队）"
  artifacts:
    - apps/agent/src/agent/hitl.py
    - apps/agent/tests/test_hitl.py
  key_links:
    - "approval_callback 返回 False 时 ToolGuard.before_tool_execute 抛出 ToolBlocked → 模型收到工具调用错误（Plan 02 接入）"
    - "SSE 推送失败（_sse_queue=None）不阻塞审批 → 等待超时后自动拒绝"
  prohibitions:
    - statement: "MUST NOT 在审批卡片未决策时继续执行后续工具调用"
      status: resolved / test
      verification: "test_hitl.py: test_approval_callback_blocks_until_decision — 调用 approval_callback() 启动后，在未注入决策前断言 callback 未返回；注入 decision=True 后断言返回 True"

---

<objective>
实现 HITL 审批回调核心模块 —— `hitl.py` 提供 `create_approval_callback()` 工厂函数、模块级待审批状态管理（asyncio.Event 字典）、配置校验函数。

Purpose: 审批回调是 HITL 架构的核心异步等待机制——Agent 工具调用被 ToolGuard 拦截后，通过 approval_callback 创建 Event、推送 SSE、阻塞等待前端决策。
Output: 经过 TDD 验证的 `hitl.py` 模块 + `test_hitl.py` 测试文件。
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-hitl-sidecar/07-CONTEXT.md
@.planning/phases/07-hitl-sidecar/07-RESEARCH.md
@.planning/phases/07-hitl-sidecar/07-SPEC.md
@.planning/phases/07-hitl-sidecar/07-PATTERNS.md
@apps/agent/src/agent/paladin_agent.py
</context>

<feature>
  <name>HITL 审批回调与状态管理</name>
  <files>apps/agent/src/agent/hitl.py, apps/agent/tests/test_hitl.py</files>
  <behavior>
    - Test 1 (create_approval_callback): 创建 callback = create_approval_callback(timeout=5.0)，调用 callback("write_file", {"path": "/tmp/test"})，在事件循环中验证：
      - request_id 为合法 UUID4 格式
      - _pending_approvals[request_id] 为 asyncio.Event 实例
      - 向 _sse_queue 放入 mock 事件后，验证 queue 中有符合 D-03 格式的事件：type="approval_request", request_id=UUID4, tool_name="write_file", args=dict, reason 包含 "write_file"
      - 注入 decision=True → _pending_decisions[request_id]=True, event.set() → callback 返回 True
      - 注入 decision=False → callback 返回 False
    - Test 2 (timeout): callback("execute", {"command": "rm -rf /"})，不注入任何决策，等待 >3s 超时 → callback 返回 False，request_id 从 _pending_approvals 清理
    - Test 3 (sse_queue_none): _sse_queue=None 时 callback 不抛异常，等待超时后返回 False（优雅降级）
    - Test 4 (validate_hitl_config 空配置): 传入 {"require_approval": [], "blocked": []} + 已知工具名列表 → 无 warning
    - Test 5 (validate_hitl_config 未知工具): 传入 {"require_approval": ["nonexistent_tool"]} + 已知工具名列表 → 调用 logger.warning，不抛异常
    - Test 6 (validate_hitl_config blocked 未知): 传入 {"blocked": ["nonexistent_tool"]} + 已知工具名列表 → 调用 logger.warning，不抛异常
    - Test 7 (concurrent_events): 同时启动 3 个 callback("t1", {}), callback("t2", {}), callback("t3", {}) → 3 个独立 request_id、3 个独立 Event，各自注入决策后各自返回正确 bool
  </behavior>
  <implementation>
    参照 RESEARCH.md §1.3 的 approval_callback 实现模板和 §5.2 的配置校验模板。

    RED phase: 创建 test_hitl.py，编写上述 7 个测试用例，运行 pytest 确认全部 FAIL。
    GREEN phase: 实现 hitl.py：
    - 模块级变量：`_pending_approvals: dict[str, asyncio.Event]`, `_pending_decisions: dict[str, bool]`, `_sse_queue: asyncio.Queue | None = None`
    - `create_approval_callback(timeout: float = 30.0) -> Callable` 返回 async 函数 `approval_callback(tool_name, args) -> bool`
    - `validate_hitl_config(raw_config: dict, known_tool_names: list[str]) -> dict` 读取 hitl 段，校验工具名，返回 `{"require_approval": [...], "blocked": [...], "timeout_seconds": 30}`
    - 日志使用 `logging.getLogger(__name__)` 的 structlog 模式（与 paladin_agent.py 一致）
    - SSE 事件格式严格按 D-03: `{"type": "approval_request", "request_id": str, "tool_name": str, "args": dict, "reason": str}`
    - `get_pending_approval(request_id)` 和 `resolve_approval(request_id, decision)` 辅助函数供 server 层 HTTP 回调使用
    REFACTOR phase: 确保 try/finally 清理逻辑正确，异常安全。
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → hitl.py | 工具调用通过 ToolGuard 进入 approval_callback |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Spoofing | `resolve_approval(request_id, decision)` | medium | mitigate | request_id 为 UUID4 且由 _pending_approvals.get() 验证存在性——未通过 SSE 获得 request_id 的攻击者无法猜测有效 ID |
| T-07-02 | Denial of Service | `_pending_approvals` 字典 | medium | mitigate | 超时后 finally 块自动清理 request_id；SSE 队列为空时优雅降级不抛异常 |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for [SUS] pyautogui (in Plan 04) |
</threat_model>

<verification>
  <automated>cd apps/agent && uv run pytest tests/test_hitl.py -v</automated>
</verification>

<success_criteria>
- 7 个测试用例全部 GREEN
- hitl.py 导出的 4 个符号：create_approval_callback, validate_hitl_config, get_pending_approval, resolve_approval
- approval_callback 在 event.set() 后 100ms 内返回决策
- 超时场景返回 False 且清理 _pending_approvals
</success_criteria>

<output>
Create `.planning/phases/07-hitl-sidecar/07-01-SUMMARY.md` when done
</output>
