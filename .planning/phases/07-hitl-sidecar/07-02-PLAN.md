---
phase: 07-hitl-sidecar
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - apps/agent/config/config.json
  - apps/agent/src/agent/paladin_agent.py
autonomous: true
requirements:
  - HIT-02

must_haves:
  truths:
    - "config.json 包含 `hitl` 顶级段：`require_approval: [\"execute\", \"write_file\", \"edit_file\"]`, `blocked: []`, `timeout_seconds: 30`"
    - "`create_paladin_agent()` 解析 hitl 配置 → 调用 `validate_hitl_config(raw_config, all_known_tool_names)` → 创建 `ToolGuard(blocked=..., require_approval=..., approval_callback=...)` → 传入 `create_deep_agent(capabilities=[guard])`"
    - "`require_approval` 中不存在的工具名仅记录 warning 日志，不崩溃（SPEC 边缘「无效配置」）"
    - "`blocked` 列表中的工具被 ToolGuard.prepare_tools 从模型可见工具列表中移除（RESEARCH.md §1.1）"
    - "`agent._hitl_sse_queue = asyncio.Queue()` 挂载到 Agent 实例上，供 server 层注入（遵循 `agent._default_deps` 的 attr 补丁模式）"
    - "验证：修改 config.json 中 require_approval 添加/移除工具后重启 Agent → 仅新配置中的工具触发审批（SPEC AC）"
  artifacts:
    - apps/agent/config/config.json（新增 hitl 段）
    - apps/agent/src/agent/paladin_agent.py（新增 ToolGuard 集成 + _hitl_sse_queue）
  key_links:
    - "ToolGuard.before_tool_execute 拦截 require_approval 工具 → 调用 approval_callback（hitl.py Plan 01）→ 阻塞等待决策"
    - "agent._hitl_sse_queue 被 server/main.py 注入到 hitl._sse_queue（Plan 03 完成桥接）"
  prohibitions:
    - statement: "MUST NOT 绕过 HITL 审批执行 require_approval 列表中的工具"
      status: resolved / test
      verification: "test_hitl.py（Plan 01 已覆盖）— ToolGuard 在工具执行前拦截，approval_callback 返回 False 时抛出 ToolBlocked → 工具不执行"
    - statement: "MUST NOT 允许 Computer Use 操作不经 HITL 直接执行"
      status: resolved / judgment
      verification: "Plan 04 将 computer_* 工具名硬编码加入 require_approval——与 config.json 配置无关，permanently enforced"
---

<objective>
将 HITL 配置和 ToolGuard 集成到 Agent 工厂函数中。config.json 新增 `hitl` 顶级段驱动审批策略，`create_paladin_agent()` 解析配置、创建 ToolGuard、注入到 `capabilities` 参数。

Purpose: Phase 6 激活了危险工具能力后 Agent 无审批闸门——此计划在工具执行路径中插入可配置的 ToolGuard 守卫。
Output: config.json 扩展 + paladin_agent.py 的 ToolGuard 集成代码。
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-hitl-sidecar/07-CONTEXT.md
@.planning/phases/07-hitl-sidecar/07-RESEARCH.md
@.planning/phases/07-hitl-sidecar/07-SPEC.md
@.planning/phases/07-hitl-sidecar/07-PATTERNS.md
@apps/agent/config/config.json
@apps/agent/src/agent/paladin_agent.py
@apps/agent/src/agent/hitl.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: config.json 新增 hitl 顶级段</name>
  <files>apps/agent/config/config.json</files>
  <read_first>
    - apps/agent/config/config.json — 现有结构（models + mcp_servers）
    - .planning/phases/07-hitl-sidecar/07-PATTERNS.md — config.json 两空格缩进约定
  </read_first>
  <action>
    在 config.json 的 "mcp_servers": [] 之后（同缩进层级）新增 "hitl" 顶级段：
    - "require_approval": ["execute", "write_file", "edit_file"] — 默认需审批的工具
    - "blocked": [] — 默认无 blocked 工具
    - "timeout_seconds": 30 — 审批超时秒数
    遵循现有 2 空格缩进约定。确保 JSON 合法（逗号位置正确，mcp_servers 闭合后加逗号）。
  </action>
  <verify>
    <automated>python3 -c "import json; c=json.load(open('apps/agent/config/config.json')); assert 'hitl' in c; assert c['hitl']['require_approval']==['execute','write_file','edit_file']; assert c['hitl']['timeout_seconds']==30"</automated>
  </verify>
  <done>
    - config.json 包含 hitl 顶级段，require_approval 含 execute/write_file/edit_file
    - blocked 为空数组
    - timeout_seconds 为 30
    - JSON 合法（python3 -m json.tool 通过）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: create_paladin_agent() 集成 ToolGuard</name>
  <files>apps/agent/src/agent/paladin_agent.py</files>
  <read_first>
    - apps/agent/src/agent/paladin_agent.py — create_paladin_agent() 完整函数（行 239-340），尤其 raw_config 解析和 create_deep_agent() 调用
    - apps/agent/src/agent/hitl.py — Plan 01 的 create_approval_callback / validate_hitl_config 导出
    - .planning/phases/07-hitl-sidecar/07-RESEARCH.md §1.2 — ToolGuard 接入 create_deep_agent 的代码模板
    - .planning/phases/07-hitl-sidecar/07-PATTERNS.md — paladin_agent.py 的配置解析模式和 attr 补丁模式
  </read_first>
  <behavior>
    - Test 1: 模拟 raw_config 包含 hitl 段 → create_paladin_agent() 返回的 agent 上 _hitl_sse_queue 为 asyncio.Queue 实例
    - Test 2: 模拟 raw_config 无 hitl 段 → ToolGuard 使用空默认值创建，不崩溃
    - Test 3: hitl.require_approval 含 "execute" → ToolGuard.require_approval 含 "execute"
    - Test 4: hitl.blocked 含 "some_tool" → ToolGuard.blocked 含 "some_tool"
  </behavior>
  <action>
    在 create_paladin_agent() 中，紧跟 raw_config = json.loads(...) 之后、create_deep_agent() 调用之前插入 HITL 初始化代码：

    1. 从 hitl.py 导入：create_approval_callback, validate_hitl_config
    2. 调用 validate_hitl_config(raw_config) 获取 require_approval/blocked/timeout_seconds
    3. 调用 create_approval_callback(timeout=timeout_seconds) 创建 approval_callback
    4. 创建 ToolGuard(blocked=blocked, require_approval=require_approval, approval_callback=approval_callback)
    5. 在 create_deep_agent() 调用中添加 capabilities=[guard]（若已有 capabilities 关键字，追加 guard）
    6. 在 agent._default_deps 赋值之后，新增 agent._hitl_sse_queue = asyncio.Queue() 挂载（遵循 agent._default_deps / agent._model_configs 的 attr 补丁模式）

    导入：文件顶部新增 `from pydantic_ai_shields import ToolGuard` 和 `import asyncio` 以及 `from src.agent.hitl import create_approval_callback, validate_hitl_config`。
  </action>
  <verify>
    <automated>cd apps/agent && uv run python -c "from src.agent.hitl import create_approval_callback, validate_hitl_config; print('imports OK')"</automated>
  </verify>
  <done>
    - create_paladin_agent() 解析 config.json hitl 段
    - ToolGuard 实例传入 create_deep_agent(capabilities=[guard])
    - agent._hitl_sse_queue 为 asyncio.Queue 实例
    - hitl 段缺失时 Agent 正常创建（使用默认空配置）
    - uv run python -c "from src.agent.paladin_agent import create_paladin_agent" 无 ImportError
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 端到端验证 ToolGuard 拦截 + Agent 集成</name>
  <files>apps/agent/src/agent/paladin_agent.py</files>
  <read_first>
    - apps/agent/src/agent/paladin_agent.py — 当前的 create_paladin_agent() 和 create_deep_agent() 调用
  </read_first>
  <action>
    验证 Task 2 的集成完整性：
    1. 启动 Agent 时日志包含 HITL 初始化信息（参照 PATTERNS.md 的 structured logging 模式记录 hitl_initialized require_approval=[...] blocked=[...] timeout=30）
    2. 确保 create_deep_agent() 的 kwargs 中 capabilities=[guard] 不与其他现有参数冲突
    3. 手动验证：调用 create_paladin_agent() 后检查 agent._hitl_sse_queue 存在且类型为 asyncio.Queue
  </action>
  <verify>
    <automated>cd apps/agent && uv run python -c "
import asyncio
from src.agent.paladin_agent import create_paladin_agent
agent = create_paladin_agent(
    models_config_path='config/config.json',
    system_prompt_path='prompts/system.md',
)
assert hasattr(agent, '_hitl_sse_queue')
assert isinstance(agent._hitl_sse_queue, asyncio.Queue)
print('Integration OK: agent._hitl_sse_queue exists')
"</automated>
  </verify>
  <done>
    - create_paladin_agent() 成功创建带 ToolGuard 的 Agent
    - agent._hitl_sse_queue 为 asyncio.Queue
    - Agent 可正常启动（uv run paladin-agent serve --dev 无崩溃）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.json → Agent | 配置文件驱动安全策略——恶意修改 config.json 可绕过审批 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Tampering | config.json hitl 段 | medium | accept | config.json 为本地文件，由开发者管理——Phase 10 打包时固化 |
| T-07-04 | Information Disclosure | blocked 工具列表 | low | accept | blocked 工具从模型可见列表中移除，但 config.json 本身可读 |
| T-07-05 | Elevation of Privilege | ToolGuard bypass via config | high | mitigate | validate_hitl_config 校验工具名有效性；不存在的工具名仅 warning，不会导致 ToolGuard 失效 |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for pyautogui (Plan 04) |
</threat_model>

<verification>
  <automated>cd apps/agent && uv run python -c "from src.agent.paladin_agent import create_paladin_agent; a=create_paladin_agent(models_config_path='config/config.json', system_prompt_path='prompts/system.md'); assert hasattr(a,'_hitl_sse_queue')"</automated>
</verification>

<success_criteria>
- config.json 含 hitl 段，格式合法
- create_paladin_agent() 创建 ToolGuard 并传入 capabilities
- agent._hitl_sse_queue 存在
- Agent 启动无崩溃
</success_criteria>

<output>
Create `.planning/phases/07-hitl-sidecar/07-02-SUMMARY.md` when done
</output>
