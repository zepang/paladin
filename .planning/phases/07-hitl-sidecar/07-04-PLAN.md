---
phase: 07-hitl-sidecar
plan: 04
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - apps/agent/src/agent/computer_use.py
  - apps/agent/tests/test_computer_use.py
  - apps/agent/pyproject.toml
  - apps/agent/src/agent/paladin_agent.py
autonomous: false
requirements:
  - HIT-03

must_haves:
  truths:
    - "computer_screenshot() 调用 pyautogui.screenshot() → toBytesIO → base64.b64encode → 返回 str（base64 PNG）"
    - "computer_click(x: int, y: int) 调用 pyautogui.click(x, y) → 返回 f'已在屏幕坐标 ({x}, {y}) 处点击。'"
    - "computer_type(text: str, interval: float = 0.0) 调用 pyautogui.typewrite(text, interval=interval) → 返回 f'已输入文本（{len} 字符）。'"
    - "pyautogui 未安装时 ImportError → 3 个工具均返回 'Error: pyautogui 未安装。' 字符串（优雅降级，不抛异常）"
    - "macOS 辅助功能权限未授予时 pyautogui.FailSafeException → 工具返回 'Error: 触发 FailSafe。请授予 macOS 辅助功能权限。'"
    - "`_create_computer_use_tools()` 返回 list[@tool 函数]，import 失败返回 []"
    - "create_paladin_agent() 中 computer_* 工具名硬编码追加到 require_approval（永久标记，与 config.json 无关）"
  artifacts:
    - apps/agent/src/agent/computer_use.py
    - apps/agent/tests/test_computer_use.py
    - apps/agent/pyproject.toml（新增 pyautogui 依赖）
    - apps/agent/src/agent/paladin_agent.py（注入 Computer Use 工具）
  key_links:
    - "computer_* 工具函数被 ToolGuard 永久标记 require_approval → 每次调用强制走 HITL 审批（Plan 01/02 的 ToolGuard 机制）"
    - "工具通过 create_deep_agent(tools=[...existing..., *computer_tools]) 注入——与现有工具集并列（D-12）"
  prohibitions:
    - statement: "MUST NOT 在非 macOS 环境因 pyautogui 导入失败导致 Agent 崩溃"
      status: resolved / test
      verification: "test_computer_use.py: test_pyautogui_import_failure_graceful — mock ImportError → _create_computer_use_tools() 返回 []; Agent 正常启动"
    - statement: "MUST NOT 允许 Computer Use 操作不经 HITL 直接执行"
      status: resolved / judgment
      verification: "plan 02 中 computer_* 工具名硬编码加入 require_approval——paladin_agent.py 验证：computer_use_names 恒在 require_approval set 中"
---

<objective>
实现基于 pyautogui 的 Computer Use 3 个 @tool 函数（screenshot/click/typewrite），TDD 驱动的测试覆盖正常路径和降级路径，注入到 create_paladin_agent() 并永久标记 require_approval。

Purpose: 让 Agent 具备桌面操作能力——截图分析屏幕、鼠标点击、键盘输入。每次操作强制走 HITL 审批（与 config.json 无关），防止 Agent 未经授权操控桌面。
Output: computer_use.py + test_computer_use.py + pyproject.toml 更新 + paladin_agent.py Computer Use 注入。
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-hitl-sidecar/07-CONTEXT.md
@.planning/phases/07-hitl-sidecar/07-RESEARCH.md
@.planning/phases/07-hitl-sidecar/07-SPEC.md
@.planning/phases/07-hitl-sidecar/07-PATTERNS.md
@apps/agent/pyproject.toml
@apps/agent/src/agent/paladin_agent.py
@apps/agent/src/agent/hitl.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: pyautogui 包合法性验证 [SUS]</name>
  <files>apps/agent/pyproject.toml</files>
  <read_first>
    - .planning/phases/07-hitl-sidecar/07-RESEARCH.md — Package Legitimacy Audit 表格
  </read_first>
  <action>
    RESEARCH.md §Package Legitimacy Audit 标记 pyautogui 为 [SUS]（PyPI 无下载数据——API 限制，非真实风险）。
    人工确认 pyautogui 为 asweigart 维护的合法包（github.com/asweigart/pyautogui, 12k+ stars, 10+ 年历史），然后批准安装。
  </action>
  <verify>
    <human-check>手动访问 https://pypi.org/project/pyautogui/ 确认后回复 "approved"</human-check>
  </verify>
  <done>
    用户确认 pyautogui 为合法包 → 批准安装
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Computer Use @tool 函数 TDD — computer_use.py</name>
  <files>apps/agent/src/agent/computer_use.py, apps/agent/tests/test_computer_use.py, apps/agent/pyproject.toml</files>
  <read_first>
    - .planning/phases/07-hitl-sidecar/07-RESEARCH.md §4.2 — @tool 装饰器模板和 §4.3 优雅降级模式
    - .planning/phases/07-hitl-sidecar/07-PATTERNS.md — pyproject.toml 依赖添加模式
    - apps/agent/pyproject.toml — 现有 dependencies 列表
  </read_first>
  <behavior>
    - Test 1 (computer_screenshot success): mock pyautogui.screenshot() 返回 PIL Image → computer_screenshot() 返回 base64 字符串 → 解码验证
    - Test 2 (computer_click success): mock pyautogui.click → computer_click(100, 200) → 返回 "已在屏幕坐标 (100, 200) 处点击。"
    - Test 3 (computer_type success): mock pyautogui.typewrite → computer_type("hello") → 返回 "已输入文本（5 字符）。"
    - Test 4 (computer_screenshot ImportError): mock pyautogui 导入失败 → computer_screenshot() 返回 "Error: pyautogui 未安装。"
    - Test 5 (computer_click FailSafeException): mock pyautogui.click 抛出 pyautogui.FailSafeException → computer_click(1, 1) 返回 "Error: 触发 FailSafe。请授予 macOS 辅助功能权限。"
    - Test 6 (_create_computer_use_tools with pyautogui): pyautogui 可导入 → 返回 [computer_screenshot, computer_click, computer_type] 长度为 3
    - Test 7 (_create_computer_use_tools without pyautogui): mock ImportError → 返回 [] 且 logger.warning 被调用
  </behavior>
  <action>
    RED phase: 创建 test_computer_use.py，编写 7 个测试用例，运行 pytest 确认全部 FAIL。
    GREEN phase: 实现 computer_use.py — 3 个 @tool async 函数 + _create_computer_use_tools() 工厂，参照 RESEARCH.md §4.2/§4.3。
    同时更新 pyproject.toml：在 dependencies 中 python-dotenv 和 pyyaml 之间插入 "pyautogui>=0.9.54"，运行 uv sync。
    REFACTOR phase: 确保所有 ImportError / FailSafeException 路径测试覆盖。
  </action>
  <verify>
    <automated>cd apps/agent && uv run pytest tests/test_computer_use.py -v</automated>
  </verify>
  <done>
    - 7 个测试用例全部 GREEN
    - computer_use.py 导出 computer_screenshot, computer_click, computer_type, _create_computer_use_tools
    - pyproject.toml 含 pyautogui>=0.9.54 且 uv run python -c "import pyautogui" 成功
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: paladin_agent.py 注入 Computer Use 工具</name>
  <files>apps/agent/src/agent/paladin_agent.py</files>
  <read_first>
    - apps/agent/src/agent/paladin_agent.py — create_paladin_agent() 函数，create_deep_agent() 的 tools= 和 capabilities= 参数
    - apps/agent/src/agent/computer_use.py — _create_computer_use_tools() 返回的工具列表
    - .planning/phases/07-hitl-sidecar/07-RESEARCH.md §4.4 — 注入模板
  </read_first>
  <action>
    在 create_paladin_agent() 中，HITL 初始化之后、create_deep_agent() 调用之前：
    1. 导入 _create_computer_use_tools，调用获取 computer_tools
    2. 如果 computer_tools 非空：computer_use_names = ["computer_screenshot", "computer_click", "computer_type"]；require_approval = list(set(require_approval + computer_use_names))；更新 guard.require_approval 属性
    3. create_deep_agent() 调用中添加 tools=list(existing_tools) + computer_tools（合并到现有工具集）
    4. 日志：logger.info("computer_use_tools_loaded", count=len(computer_tools)) 或 logger.warning("computer_use_tools_unavailable")
    注意：ToolGuard 应在 computer_tools 确定后创建（或在创建后更新 require_approval），确保 computer_* 工具名恒在审批列表中。
  </action>
  <verify>
    <automated>cd apps/agent && uv run python -c "
from src.agent.computer_use import _create_computer_use_tools
tools = _create_computer_use_tools()
assert len(tools) in (0, 3), f'Expected 0 or 3 tools, got {len(tools)}'
print(f'Computer Use tools: {len(tools)} available')
"</automated>
  </verify>
  <done>
    - create_paladin_agent() 调用 _create_computer_use_tools()
    - 有 pyautogui 时 3 个 Computer Use 工具注入到 create_deep_agent()
    - computer_* 工具名在 ToolGuard.require_approval 中（永久标记）
    - 无 pyautogui 时工具不注入，Agent 正常创建
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → macOS Desktop | pyautogui 直接操作鼠标/键盘/截图——跨越沙箱边界 |
| Agent → pyautogui | Computer Use 工具通过 pyautogui 操作系统 UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-09 | Elevation of Privilege | computer_click / computer_type | critical | mitigate | 每次 Computer Use 操作强制走 HITL 审批（permanent require_approval）；pyautogui FAILSAFE 机制可紧急中止 |
| T-07-10 | Information Disclosure | computer_screenshot | high | mitigate | 截图操作走 HITL 审批；base64 编码仅通过 AG-UI SSE 返回前端，不在日志中记录 |
| T-07-11 | Denial of Service | pyautogui fail at import | low | mitigate | ImportError 时优雅降级——工具不可用但 Agent 正常运行（_create_computer_use_tools 返回 []） |
| T-07-12 | Spoofing | macOS 辅助功能权限 | medium | accept | 由 macOS 系统级别权限控制——未授权时 FailSafeException 被捕获，返回友好错误 |
| T-07-SC | Tampering | pyautogui package install | high | mitigate | slopcheck + blocking human checkpoint (Task 1) 验证 pypi.org/project/pyautogui |
</threat_model>

<verification>
  <automated>cd apps/agent && uv run pytest tests/test_computer_use.py -v</automated>
</verification>

<success_criteria>
- 7 个测试用例全部 GREEN
- computer_use.py 导出 4 个符号：computer_screenshot, computer_click, computer_type, _create_computer_use_tools
- pyautogui 已安装至 .venv
- create_paladin_agent() 成功注入 Computer Use 工具
- Computer Use 工具名硬编码在 ToolGuard.require_approval 中
</success_criteria>

<output>
Create `.planning/phases/07-hitl-sidecar/07-04-SUMMARY.md` when done
</output>
