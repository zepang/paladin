---
phase: 07-hitl-sidecar
plan: 06
type: execute
wave: 5
depends_on:
  - "05"
files_modified:
  - apps/desktop/src/components/ChatToolbar.tsx
  - apps/desktop/src/App.tsx
autonomous: true
requirements:
  - HIT-01

must_haves:
  truths:
    - "ChatToolbar 新增「审批状态」区域——当 pendingApprovals.length > 0 时显示「等待审批中…」状态行 + 当前待审批工具名称"
    - "ChatToolbar 审批状态行使用警告色调：bg-yellow-500/10 border border-yellow-500/30 text-yellow-600"
    - "ChatToolbar 审批状态行显示当前工具名称（font-mono truncate）和队列计数（'第 1/3 个'）"
    - "App.tsx 在 CopilotKitProvider 内部挂载 ApprovalBridge 组件"
    - "App.tsx 中 useApprovalBridge() hook 的 pendingApprovals 状态通过 props 传递给 ChatToolbar"
    - "ApprovalCard 和 ApprovalDialog 在 App.tsx 聊天区域中渲染——pendingApprovals.current 非 null 时展示"
    - "验收：Agent 触发 write_file → ChatToolbar 显示「等待审批中…write_file」+ 审批卡片 + AlertDialog 弹窗"
  artifacts:
    - apps/desktop/src/components/ChatToolbar.tsx（新增审批状态段）
    - apps/desktop/src/App.tsx（集成 ApprovalBridge/ApprovalCard/ApprovalDialog）
  key_links:
    - "ChatToolbar 审批状态 ← useApprovalBridge() hook（Plan 05）→ pendingApprovals 队列状态"
    - "App.tsx ApprovalBridge 挂载 → SSE 连接生命周期管理"
  prohibitions:
    - statement: "MUST NOT 在审批卡片未决策时继续执行后续工具调用"
      status: resolved / test
      verification: "后端保证（Plan 01）— 前端仅展示状态，审批决策通过 HTTP POST 回传触发 Event.set()"
---

<objective>
将 Phase 07 的 HITL 审批 UI 组件集成到应用主框架中：ChatToolbar 升级为审批状态栏，App.tsx 挂载 ApprovalBridge 并渲染审批卡片和弹窗。

Purpose: ChatToolbar 当前仅被动展示工具调用列表——升级后提供审批状态的实时可见性。App.tsx 集成确保审批 UI 在整个应用中可用。
Output: ChatToolbar.tsx 新增审批状态段 + App.tsx 集成审批组件。
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-hitl-sidecar/07-CONTEXT.md
@.planning/phases/07-hitl-sidecar/07-PATTERNS.md
@apps/desktop/src/components/ChatToolbar.tsx
@apps/desktop/src/App.tsx
@apps/desktop/src/components/approval/ApprovalBridge.tsx
@apps/desktop/src/components/approval/ApprovalCard.tsx
@apps/desktop/src/components/approval/ApprovalDialog.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ChatToolbar 新增审批状态栏</name>
  <files>apps/desktop/src/components/ChatToolbar.tsx</files>
  <read_first>
    - apps/desktop/src/components/ChatToolbar.tsx — 现有结构：上下文信息 → 工具调用列表 → 快捷操作，3 个 section div，border-b 分隔
    - apps/desktop/src/components/approval/ApprovalBridge.tsx — ApprovalRequest 类型 + useApprovalBridge hook
    - .planning/phases/07-hitl-sidecar/07-CONTEXT.md — D-09 决策：ChatToolbar 升级为审批状态栏
  </read_first>
  <action>
    修改 ChatToolbar.tsx，新增审批状态 section：

    **Props 变更：**
    - 新增 prop: `pendingApprovals: ApprovalRequest[]`（从 App.tsx 传入）
    - 导入 ApprovalRequest 类型和 lucide icons: `AlertTriangle`, `Clock`

    **新增审批状态段（插入到「上下文信息」div 和「工具调用」div 之间）：**
    ```tsx
    {/* 审批状态 — D-09: pendingApprovals.length > 0 时显示 */}
    {pendingApprovals.length > 0 && (
      <div className="p-3 border-b border-yellow-500/30 bg-yellow-500/5">
        <div className="flex items-center gap-1.5 text-xs font-medium text-yellow-600 mb-2">
          <AlertTriangle className="size-3.5" />
          <span>等待审批中…</span>
        </div>
        <div className="space-y-1 text-xs">
          <div className="font-mono text-yellow-700 truncate">
            {pendingApprovals[0].tool_name}
          </div>
          {pendingApprovals.length > 1 && (
            <div className="text-yellow-600/70">
              第 1/{pendingApprovals.length} 个
            </div>
          )}
        </div>
      </div>
    )}
    ```

    **组件签名更新：**
    ```typescript
    export function ChatToolbar({ pendingApprovals = [] }: { pendingApprovals?: ApprovalRequest[] }) {
    ```

    **导入新增：**
    - `import { AlertTriangle } from 'lucide-react'` — 在现有 icons 导入行追加
    - `import type { ApprovalRequest } from '@/components/approval/ApprovalBridge'` — 类型导入

    遵循现有组件的 Tailwind 风格：xs 字号、muted-foreground 文本、border-border 边框。
  </action>
  <verify>
    <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | grep -E '(ChatToolbar|approval)' | head -5; echo "TypeCheck done"</automated>
  </verify>
  <done>
    - ChatToolbar 接受 pendingApprovals prop（类型 ApprovalRequest[]）
    - pendingApprovals.length > 0 时显示黄/棕色审批状态段
    - 状态段显示当前工具名（pendingApprovals[0].tool_name）
    - 队列 > 1 时显示「第 1/N 个」计数
    - pendingApprovals.length === 0 时不显示审批段
    - tsc --noEmit 无新增错误
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: App.tsx 集成审批 UI 组件</name>
  <files>apps/desktop/src/App.tsx</files>
  <read_first>
    - apps/desktop/src/App.tsx — 现有组件结构：CopilotKitProvider 包裹 Titlebar + ChatArea + StatusBar + Toaster
    - apps/desktop/src/components/approval/ApprovalBridge.tsx — useApprovalBridge hook 导出
    - apps/desktop/src/components/approval/ApprovalCard.tsx — ApprovalCard 组件
    - apps/desktop/src/components/approval/ApprovalDialog.tsx — ApprovalDialog 组件
    - apps/desktop/src/components/ChatToolbar.tsx — ChatToolbar 新 prop 签名
    - .planning/phases/07-hitl-sidecar/07-CONTEXT.md — D-05: 聊天流内嵌卡片 + AlertDialog 弹窗
  </read_first>
  <action>
    修改 App.tsx，集成审批 UI 组件：

    **导入新增：**
    ```typescript
    import { useApprovalBridge, ApprovalBridge, type ApprovalRequest } from '@/components/approval/ApprovalBridge';
    import { ApprovalCard } from '@/components/approval/ApprovalCard';
    import { ApprovalDialog } from '@/components/approval/ApprovalDialog';
    ```

    **Hook 挂载（在 Component 函数体内，现有 hooks 之后）：**
    ```typescript
    const { pendingApprovals, current, approve, deny, isConnected } = useApprovalBridge();
    ```

    **ChatToolbar 传参更新（找到 `<ChatToolbar />` 无 prop 调用处）：**
    ```tsx
    <ChatToolbar pendingApprovals={pendingApprovals} />
    ```

    **审批 UI 渲染（在 CopilotKitProvider 内、主布局 div 中，ChatArea 的同级位置）：**
    ```tsx
    {/* HITL 审批 UI — D-05: 聊天流内嵌卡片 + AlertDialog 弹窗 */}
    {current && (
      <div className="fixed bottom-20 left-1/2 -translate-x-1/2 z-50 w-full max-w-lg">
        <ApprovalCard
          request={current}
          onApprove={approve}
          onDeny={deny}
          queueLength={pendingApprovals.length}
        />
      </div>
    )}
    <ApprovalDialog
      request={current}
      onApprove={approve}
      onDeny={deny}
    />
    {/* ApprovalBridge 无 UI，仅生命周期管理 */}
    <ApprovalBridge />
    ```
    - ApprovalCard 定位为 fixed bottom-20 centered（在聊天输入框上方，不遮挡输入）
    - ApprovalDialog 无定位——AlertDialog 自带 portal overlay
    - ApprovalBridge 组件（无 UI）作为生命周期挂载点

    **ChatArea 中 ChatToolbar 传参（找到 ChatArea 组件中的 ChatToolbar 使用位置）：**
    - 如果 ChatToolbar 在 ChatArea 内部渲染，需要将 pendingApprovals prop 透传
    - 如果 ChatToolbar 在 App.tsx 直接渲染，直接传 prop
    - 优先在 App.tsx 中找到 ChatToolbar 的 `<ChatToolbar />` 调用并添加 `pendingApprovals={pendingApprovals}`

    **注意：** ChatToolbar 当前可能在 ChatArea.tsx 中渲染。需要检查 ChatArea.tsx 中 ChatToolbar 的使用处，添加 pendingApprovals prop 透传。
  </action>
  <verify>
    <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - App.tsx 中使用 useApprovalBridge() hook
    - ApprovalBridge 组件挂载在 CopilotKitProvider 内
    - current 非 null 时 ApprovalCard 渲染在聊天区域上方
    - ApprovalDialog 在任何审批请求时自动弹出
    - ChatToolbar 接收 pendingApprovals prop
    - tsc --noEmit 0 错误
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 端到端验证审批流集成</name>
  <files>apps/desktop/src/App.tsx</files>
  <read_first>
    - apps/desktop/src/App.tsx — Task 2 修改后的完整文件
    - apps/desktop/src/components/ChatArea.tsx — ChatToolbar 使用位置
  </read_first>
  <action>
    端到端集成验证：

    1. 检查 ChatArea.tsx 中 ChatToolbar 组件是否需要 pendingApprovals prop 透传——如果是，修改 ChatArea.tsx：
       - ChatArea 的 props 添加 `pendingApprovals?: ApprovalRequest[]`
       - 传递给 `<ChatToolbar pendingApprovals={pendingApprovals} />`

    2. 确保所有导入路径正确：
       - `@/components/approval/ApprovalBridge` 路径匹配实际文件
       - `@/components/approval/ApprovalCard` 路径匹配
       - `@/components/approval/ApprovalDialog` 路径匹配

    3. 生物群落检查（biome ci）：
       - `cd apps/desktop && npx biome ci src/App.tsx src/components/ChatToolbar.tsx src/components/ChatArea.tsx`

    4. 类型检查：
       - `cd apps/desktop && npx tsc --noEmit`
  </action>
  <verify>
    <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | grep -c 'error' && echo 'errors found' || echo '0 errors'</automated>
  </verify>
  <done>
    - ChatArea.tsx 透传 pendingApprovals 到 ChatToolbar（如需要）
    - 所有导入路径解析正确
    - tsc --noEmit 0 错误
    - biome ci 通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView → Agent Server | 审批 UI 组件的 fetch 请求 |
| ApprovalCard → User | 用户交互边界——误点击审批按钮 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-16 | Spoofing | ApprovalCard fixed overlay | low | accept | CORS + localhost-only 限制（Plan 03）；审批卡片 UI 不会出现在不可信上下文中 |
| T-07-17 | Repudiation | 审批决策 | low | accept | 审计日志属 Phase 9——v1 无持久化审批记录 |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for pyautogui (Plan 04) |
</threat_model>

<verification>
  <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | tail -3</automated>
</verification>

<success_criteria>
- ChatToolbar 显示审批状态段（有待审批时）
- App.tsx 挂载 ApprovalBridge 并渲染 ApprovalCard/ApprovalDialog
- ChatArea.tsx 透传 pendingApprovals（如需要）
- tsc --noEmit 0 错误
- 端到端：Agent 触发 write_file → 前端展示审批 UI
</success_criteria>

<output>
Create `.planning/phases/07-hitl-sidecar/07-06-SUMMARY.md` when done
</output>
