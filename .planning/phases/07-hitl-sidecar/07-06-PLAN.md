---
phase: 07-hitl-sidecar
plan: 06
type: execute
wave: 5
depends_on:
  - "05"
files_modified:
  - apps/desktop/src/components/ChatToolbar.tsx
  - apps/desktop/src/App.tsx
  - apps/desktop/src/components/ChatArea.tsx
autonomous: true
requirements:
  - HIT-01

must_haves:
  truths:
    - "ChatToolbar 新增「审批状态」区域——当 pendingApprovals.length > 0 时显示「等待审批中…」状态行 + 当前待审批工具名称（per D-09）"
    - "ChatToolbar 审批状态行使用警告色调：bg-yellow-500/10 border border-yellow-500/30 text-yellow-600"
    - "ChatToolbar 审批状态行显示当前工具名称（font-mono truncate）和队列计数（'第 1/3 个'）"
    - "App.tsx 在 CopilotKitProvider 内部用 `<ApprovalProvider>` 包裹聊天区域组件（per D-07 修订：自定义 useApprovalBridge + React Context）"
    - "ApprovalCard 在 ChatArea.tsx 的 CopilotChat 消息容器内渲染——作为 normal-flow 元素嵌入消息流，不使用 fixed/absolute 定位（per D-07 修订）"
    - "ApprovalDialog 在任何审批请求到达时自动弹出（shadcn AlertDialog portal overlay）——per D-05"
    - "ChatToolbar 通过 useApprovalContext() 消费 pendingApprovals 状态——非 props drilling"
    - "验收：Agent 触发 write_file → CopilotChat 消息流中出现审批卡片（随消息滚动）+ AlertDialog 弹窗 + ChatToolbar 显示「等待审批中…write_file」"
  artifacts:
    - apps/desktop/src/components/ChatToolbar.tsx（利用 useApprovalContext 新增审批状态段）
    - apps/desktop/src/App.tsx（挂载 ApprovalProvider + ApprovalDialog）
    - apps/desktop/src/components/ChatArea.tsx（在 CopilotChat 消息容器内渲染 ApprovalCard）
  key_links:
    - "ChatToolbar 审批状态 ← useApprovalContext()（Plan 05 ApprovalContext）→ pendingApprovals 队列状态"
    - "ApprovalCard 嵌入 ChatArea CopilotChat 消息流 ← useApprovalContext() → current + approve/deny"
    - "App.tsx ApprovalProvider 挂载 → EventSource SSE 连接生命周期（Plan 05 useApprovalBridge）"
  prohibitions:
    - statement: "MUST NOT 在审批卡片未决策时继续执行后续工具调用"
      status: resolved / test
      verification: "后端保证（Plan 01）— 前端仅展示状态，审批决策通过 HTTP POST 回传触发 Event.set()"
---

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `ChatToolbar` (pendingApprovals from context) | modified React component | `apps/desktop/src/components/ChatToolbar.tsx` |
| `App` (ApprovalProvider + ApprovalDialog integration) | modified React component | `apps/desktop/src/App.tsx` |
| `ChatArea` (ApprovalCard in message flow) | modified React component | `apps/desktop/src/components/ChatArea.tsx` |

<objective>
将 Phase 07 的 HITL 审批 UI 组件集成到应用主框架中。D-07 修订后：ApprovalCard 嵌入 CopilotChat 消息流（normal flow，非 fixed 覆盖层），AlertDialog 使用 shadcn，ChatToolbar 消费 React Context 展示审批状态。

Purpose: ChatToolbar 当前仅被动展示工具调用列表——升级后通过 ApprovalContext 实时展示审批状态。App.tsx 挂载 ApprovalProvider 提供 SSE 生命周期，ChatArea 在 CopilotChat 消息容器内内联渲染 ApprovalCard。
Output: ChatToolbar.tsx（审批状态段）+ App.tsx（Provider 集成 + ApprovalDialog）+ ChatArea.tsx（ApprovalCard 内嵌）。
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-hitl-sidecar/07-CONTEXT.md
@.planning/phases/07-hitl-sidecar/07-PATTERNS.md
@apps/desktop/src/components/ChatToolbar.tsx
@apps/desktop/src/App.tsx
@apps/desktop/src/components/ChatArea.tsx
@apps/desktop/src/components/approval/ApprovalBridge.tsx
@apps/desktop/src/components/approval/ApprovalCard.tsx
@apps/desktop/src/components/approval/ApprovalDialog.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ChatToolbar 通过 useApprovalContext 新增审批状态栏</name>
  <files>apps/desktop/src/components/ChatToolbar.tsx</files>
  <read_first>
    - apps/desktop/src/components/ChatToolbar.tsx — 现有结构：上下文信息 → 工具调用列表 → 快捷操作，3 个 section div，border-b 分隔
    - apps/desktop/src/components/approval/ApprovalBridge.tsx — useApprovalContext hook + ApprovalRequest 类型
    - .planning/phases/07-hitl-sidecar/07-CONTEXT.md — D-09 决策：ChatToolbar 升级为审批状态栏
  </read_first>
  <action>
    修改 ChatToolbar.tsx，通过 useApprovalContext() 消费审批状态（非 props drilling）：

    1. 新增导入：
       import { useApprovalContext } from '@/components/approval/ApprovalBridge'
       import { AlertTriangle } from 'lucide-react'（追加到现有 lucide-react 导入行）

    2. 在组件函数体顶部调用：const { pendingApprovals, isConnected } = useApprovalContext();

    3. 在「上下文信息」div 和「工具调用」div 之间插入审批状态段：
       {pendingApprovals.length > 0 && (
         <div className="p-3 border-b border-yellow-500/30 bg-yellow-500/5">
           <div className="flex items-center gap-1.5 text-xs font-medium text-yellow-600 mb-2">
             <AlertTriangle className="size-3.5" />
             <span>等待审批中…</span>
           </div>
           <div className="space-y-1 text-xs">
             <div className="font-mono text-yellow-700 truncate">
               {pendingApprovals[0].tool_name}
             </div>
             {pendingApprovals.length > 1 && (
               <div className="text-yellow-600/70">
                 第 1/{pendingApprovals.length} 个
               </div>
             )}
           </div>
         </div>
       )}

    4. 组件签名不变——无需新增 props，状态来自 context。

    遵循现有组件的 Tailwind 风格：xs 字号、muted-foreground 文本、border-border 边框。
  </action>
  <verify>
    <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | grep -E '(ChatToolbar|approval)' | head -5; echo "TypeCheck done"</automated>
  </verify>
  <done>
    - ChatToolbar 通过 useApprovalContext() 消费 pendingApprovals（无需 props）
    - pendingApprovals.length > 0 时显示黄/棕色审批状态段
    - 状态段显示当前工具名（pendingApprovals[0].tool_name）
    - 队列 > 1 时显示「第 1/N 个」计数
    - pendingApprovals.length === 0 时不显示审批段
    - tsc --noEmit 无新增错误
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: App.tsx 挂载 ApprovalProvider + ApprovalDialog（per D-07 修订）</name>
  <files>apps/desktop/src/App.tsx</files>
  <read_first>
    - apps/desktop/src/App.tsx — 现有组件结构：CopilotKitProvider 包裹 Titlebar + ChatArea + StatusBar + Toaster
    - apps/desktop/src/components/approval/ApprovalBridge.tsx — ApprovalProvider 导出
    - apps/desktop/src/components/approval/ApprovalDialog.tsx — ApprovalDialog 组件
    - .planning/phases/07-hitl-sidecar/07-CONTEXT.md — D-05（聊天流内嵌卡片 + AlertDialog 弹窗）、D-07 修订（自定义 useApprovalBridge + CopilotChat 消息嵌入）
  </read_first>
  <action>
    修改 App.tsx，集成审批 UI 组件（per D-07 修订）：

    **导入新增：**
    import { ApprovalProvider, useApprovalContext } from '@/components/approval/ApprovalBridge'
    import { ApprovalDialog } from '@/components/approval/ApprovalDialog'

    **ApprovalProvider 包裹聊天区域：**
    在 CopilotKitProvider 内部，用 <ApprovalProvider> 包裹 ChatArea（或整个聊天区域的组件树）：
    ```tsx
    <div className="flex-1 flex overflow-hidden">
      {/* ... sidebar ... */}
      <ApprovalProvider>
        <ChatArea />
        <ApprovalDialogConsumer />  {/* 见下方说明 */}
      </ApprovalProvider>
    </div>
    ```

    **ApprovalDialogConsumer 组件（在同一文件内定义或作为独立内联组件）：**
    创建一个小型包装组件 ApprovalDialogConsumer，从 context 读取 current/approve/deny 并渲染 ApprovalDialog：
    ```typescript
    function ApprovalDialogConsumer() {
      const { current, approve, deny } = useApprovalContext();
      return <ApprovalDialog request={current} onApprove={approve} onDeny={deny} />;
    }
    ```

    关键变更（per D-07 修订）：
    - ❌ 不再使用 `<ApprovalCard>` 以 fixed overlay 方式渲染
    - ✅ ApprovalCard 嵌入在 ChatArea.tsx 的 CopilotChat 消息容器内（Task 3）
    - ✅ ApprovalDialog 通过 shadcn AlertDialog portal overlay 弹出（D-05）
    - ✅ ApprovalProvider 包裹 ChatArea + ApprovalDialogConsumer，Context 向下传递

    **注意：** 如 ChatArea 当前不在独立组件中而直接在 App.tsx 内联，则 ApprovalProvider 包裹相应的 JSX 片段。
  </action>
  <verify>
    <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - App.tsx 中用 ApprovalProvider 包裹聊天区域组件
    - ApprovalDialogConsumer 从 context 读取并渲染 ApprovalDialog
    - ApprovalCard 不在 App.tsx 中以 fixed overlay 渲染（per D-07 修订）
    - tsc --noEmit 0 错误
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: ChatArea.tsx — 在 CopilotChat 消息容器内内联渲染 ApprovalCard</name>
  <files>apps/desktop/src/components/ChatArea.tsx</files>
  <read_first>
    - apps/desktop/src/components/ChatArea.tsx — 现有结构：CopilotChat 组件 + ChatToolbar 放置位置
    - apps/desktop/src/components/approval/ApprovalCard.tsx — ApprovalCard 组件（normal flow 布局）
    - apps/desktop/src/components/approval/ApprovalBridge.tsx — useApprovalContext hook
    - .planning/phases/07-hitl-sidecar/07-CONTEXT.md — D-07 修订：审批卡片通过 CopilotChat 消息容器嵌入（非 fixed 覆盖层）
  </read_first>
  <action>
    修改 ChatArea.tsx，在 CopilotChat 的消息容器区域内内联渲染 ApprovalCard。

    CRITICAL（D-07 修订）：ApprovalCard 不使用 fixed/absolute 定位。它作为 normal-flow 元素嵌入 CopilotChat 的消息容器中，随消息流自然滚动。

    1. 新增导入：
       import { useApprovalContext } from '@/components/approval/ApprovalBridge'
       import { ApprovalCard } from '@/components/approval/ApprovalCard'

    2. 在组件函数体内调用：const { current, pendingApprovals, approve, deny } = useApprovalContext();

    3. ApprovalCard 放置位置：在 CopilotChat 组件之前或紧邻之后，位于消息容器的 DOM 层级内：
       - 方案 A（推荐 — 如果 CopilotChat 是独立组件）：在 CopilotChat 组件的 children 或内部渲染区域放置 ApprovalCard
       - 方案 B：在 CopilotChat 的 <div className="copilotkit-chat"> 容器内部，消息列表之后、输入框之前添加：
         ```tsx
         {current && (
           <ApprovalCard
             request={current}
             onApprove={approve}
             onDeny={deny}
             queueLength={pendingApprovals.length}
           />
         )}
         ```

    4. 定位策略：
       - 不设置任何 fixed/absolute/sticky 定位
       - 使用 className="w-full max-w-2xl mx-auto my-2" 配合 CopilotChat 消息容器已有的居中布局
       - ApprovalCard 作为消息流中的一条「特殊消息卡片」渲染，紧随 CopilotChat 的 AssistantMessage/UserMessage 之后

    5. 如果 ChatArea.tsx 中 ChatToolbar 需要审批信息：ChatToolbar 已通过 useApprovalContext() 自行消费（Task 1），无需 props 传递
  </action>
  <verify>
    <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ChatArea.tsx 通过 useApprovalContext() 消费审批状态
    - ApprovalCard 在 CopilotChat 消息容器内以 normal flow 渲染（per D-07 修订）
    - 不使用 fixed/absolute 定位——随消息流自然滚动
    - ChatToolbar 无需额外 props（通过 context 消费）
    - tsc --noEmit 0 错误
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: 端到端验证审批流集成</name>
  <files>apps/desktop/src/App.tsx, apps/desktop/src/components/ChatArea.tsx</files>
  <read_first>
    - apps/desktop/src/App.tsx — Task 2 修改后的文件
    - apps/desktop/src/components/ChatArea.tsx — Task 3 修改后的文件
    - apps/desktop/src/components/ChatToolbar.tsx — Task 1 修改后的文件
  </read_first>
  <action>
    端到端集成验证：

    1. 验证所有导入路径正确：
       - ApprovalProvider/useApprovalContext from @/components/approval/ApprovalBridge
       - ApprovalCard from @/components/approval/ApprovalCard
       - ApprovalDialog from @/components/approval/ApprovalDialog

    2. 验证 Context 使用正确：
       - ChatToolbar.tsx 使用 useApprovalContext()（非 props）
       - ChatArea.tsx 使用 useApprovalContext()（非 props）
       - App.tsx 中 ApprovalDialogConsumer 使用 useApprovalContext()

    3. 生物群落检查（biome ci）：
       cd apps/desktop && npx biome ci src/App.tsx src/components/ChatToolbar.tsx src/components/ChatArea.tsx

    4. 类型检查：
       cd apps/desktop && npx tsc --noEmit

    5. 验证 D-07 修订合规：
       - grep -n 'fixed' apps/desktop/src/components/ChatArea.tsx → 不应出现在 ApprovalCard 相关代码中
       - grep -n 'fixed' apps/desktop/src/App.tsx → 不应出现在 ApprovalCard 相关代码中
       - grep -n 'useHumanInTheLoop' apps/desktop/src/ → 不应出现在审批相关组件中
  </action>
  <verify>
    <automated>cd apps/desktop && npx biome ci src/App.tsx src/components/ChatToolbar.tsx src/components/ChatArea.tsx 2>&1 | tail -3; npx tsc --noEmit 2>&1 | grep -c 'error' && echo 'errors found' || echo '0 errors'</automated>
  </verify>
  <done>
    - 所有导入路径解析正确
    - 审批状态通过 React Context 传递（无 props drilling）
    - ApprovalCard 以 normal flow 嵌入 CopilotChat 消息流（per D-07 修订）
    - 无 fixed overlay 定位在审批相关代码中
    - tsc --noEmit 0 错误
    - biome ci 通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView → Agent Server | 审批 UI 组件的 fetch 请求 |
| ApprovalCard → User | 用户交互边界——误点击审批按钮 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-16 | Spoofing | ApprovalCard in message flow | low | accept | CORS + localhost-only 限制（Plan 03）；审批卡片 UI 不会出现在不可信上下文中 |
| T-07-17 | Repudiation | 审批决策 | low | accept | 审计日志属 Phase 9——v1 无持久化审批记录 |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for pyautogui (Plan 04) |
</threat_model>

<verification>
  <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | tail -3</automated>
</verification>

<success_criteria>
- ChatToolbar 通过 useApprovalContext 显示审批状态段（有待审批时）
- App.tsx 挂载 ApprovalProvider + ApprovalDialog
- ChatArea.tsx 在 CopilotChat 消息容器内 normal flow 内联渲染 ApprovalCard（per D-07 修订）
- 无 fixed overlay 定位在审批 UI 代码中
- tsc --noEmit 0 错误
- 端到端：Agent 触发 write_file → CopilotChat 消息流中出现审批卡片 + AlertDialog + ChatToolbar 审批状态
</success_criteria>

<output>
Create `.planning/phases/07-hitl-sidecar/07-06-SUMMARY.md` when done
</output>
