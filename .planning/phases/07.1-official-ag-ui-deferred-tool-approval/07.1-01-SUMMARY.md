---
phase: 07.1-official-ag-ui-deferred-tool-approval
plan: 07.1-01
subsystem: agent-ui
tags: [ag-ui, pydantic-ai, copilotkit, hitl, approval]

requires:
  - phase: 07-hitl-sidecar
    provides: legacy SSE HITL fallback and Computer Use approval boundary
provides:
  - Official Pydantic AI AG-UI deferred tool approval path as the default approval flow.
  - Backend AGUIAdapter delegation with unchanged RunAgentInput resume forwarding.
  - Frontend inline CopilotKit tool_call interrupt rendering with official resume payloads.
  - Phase 07.2 handoff for retained legacy SSE cleanup.
affects: [07.2-legacy-sse-cleanup, hitl, copilotkit, agent-tools]

tech-stack:
  added: []
  patterns:
    - "Official AGUIAdapter owns AG-UI resume to deferred tool result conversion."
    - "CopilotKit useInterrupt(renderInChat: true) renders official approval cards inline."

key-files:
  created:
    - .planning/phases/07.1-official-ag-ui-deferred-tool-approval/07.1-07.2-HANDOFF.md
  modified:
    - apps/agent/pyproject.toml
    - apps/agent/src/server/main.py
    - apps/agent/src/agent/paladin_agent.py
    - apps/agent/tests/test_agui_dependency_probe.py
    - apps/agent/tests/test_server.py
    - apps/agent/tests/test_agent.py
    - apps/desktop/src/components/approval/AguiApprovalInterrupt.tsx
    - apps/desktop/src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx
    - apps/agent/src/agent/agui_approval_bridge.py
    - apps/agent/tests/test_agui_approval_bridge.py

key-decisions:
  - "Keep agui_interrupt as the default approval mode and retain legacy_sse as a selectable fallback."
  - "Do not convert AG-UI resume entries to DeferredToolResults in Paladin; delegate that to AGUIAdapter."
  - "Classify full desktop build failures as non-blocking only when they are unrelated Titlebar unused-variable errors."
  - "Defer legacy SSE route, store, bridge, dialog, and documentation cleanup to Phase 07.2."

patterns-established:
  - "Backend approval path: replay complete /copilotkit JSON body into a Starlette Request and pass it to AGUIAdapter.dispatch_request(request=..., agent=..., deps=...)."
  - "Agent approval wiring: agui_interrupt maps configured approval-required tools to interrupt_on, clears ToolGuard.require_approval, and avoids installing approval_callback."
  - "Frontend approval wiring: only reason === \"tool_call\" renders the inline approval card, and decisions resolve { approved: true } or { approved: false, reason }."

requirements-completed: [R1, R2, R3, R4, R5, R6, R7]

coverage:
  - id: D1
    description: "Official AG-UI dependency baseline and protocol imports are present."
    requirement: R1
    verification:
      - kind: unit
        ref: "apps/agent/tests/test_agui_dependency_probe.py"
        status: pass
      - kind: other
        ref: "rg dependency constraints in apps/agent/pyproject.toml and apps/agent/uv.lock"
        status: pass
    human_judgment: false
  - id: D2
    description: "Custom Paladin AG-UI approval bridge is removed from the official path."
    requirement: R2
    verification:
      - kind: integration
        ref: "apps/agent/tests/test_server.py#TestAguiDispatchEntrypoint"
        status: pass
      - kind: other
        ref: "test ! -e apps/agent/src/agent/agui_approval_bridge.py && test ! -e apps/agent/tests/test_agui_approval_bridge.py"
        status: pass
    human_judgment: false
  - id: D3
    description: "/copilotkit preserves resume in the forwarded AGUIAdapter request body."
    requirement: R3
    verification:
      - kind: integration
        ref: "apps/agent/tests/test_server.py#test_copilotkit_preserves_resume_for_official_agui_adapter"
        status: pass
    human_judgment: false
  - id: D4
    description: "agui_interrupt uses interrupt_on while legacy_sse remains selectable."
    requirement: R4
    verification:
      - kind: unit
        ref: "apps/agent/tests/test_agent.py -k hitl_mode or agui_interrupt"
        status: pass
    human_judgment: false
  - id: D5
    description: "Computer Use tools are registered as native approval-gated Pydantic AI Tool instances."
    requirement: R5
    verification:
      - kind: unit
        ref: "apps/agent/tests/test_agent.py#test_computer_use_tools_are_registered_with_native_approval"
        status: pass
    human_judgment: false
  - id: D6
    description: "Frontend renders official tool_call interrupts inline through CopilotKit useInterrupt."
    requirement: R6
    verification:
      - kind: unit
        ref: "apps/desktop/src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx"
        status: pass
    human_judgment: false
  - id: D7
    description: "Frontend approve and deny actions use official AG-UI resume payloads."
    requirement: R7
    verification:
      - kind: unit
        ref: "apps/desktop/src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx"
        status: pass
    human_judgment: false
  - id: D8
    description: "Phase 07.2 cleanup handoff records retained legacy SSE cleanup candidates without implementing cleanup."
    verification:
      - kind: other
        ref: "rg 07.2 legacy cleanup candidates in ROADMAP.md and 07.1-07.2-HANDOFF.md"
        status: pass
    human_judgment: false

duration: 27min
completed: 2026-07-03
status: complete
---

# Phase 07.1 Plan 07.1-01: Official AG-UI Deferred Tool Approval Summary

**Official AG-UI deferred approval is now the default path, with legacy SSE retained only as a documented fallback for Phase 07.2 cleanup.**

## Performance

- **Duration:** 27 min
- **Started:** 2026-07-03T02:40:00Z
- **Completed:** 2026-07-03T03:06:52Z
- **Tasks:** 3 completed
- **Files modified:** 11

## Accomplishments

- Backend dependency, server, and agent wiring now use official Pydantic AI AG-UI deferred approval contracts for R1-R5.
- Frontend inline approval rendering now gates on official `reason: "tool_call"` interrupts and resolves official `{ approved: boolean }` payloads for R6-R7.
- Phase 07.2 handoff captures the retained legacy SSE cleanup boundary without removing fallback behavior in 07.1.

## Task Commits

Each task was committed atomically:

1. **Task T1: Backend dependency/server/agent official approval consolidation and tests** - `0fc52ca` (feat)
2. **Task T2: Frontend inline interrupt rendering and tests** - `825cde5` (test)
3. **Task T3: Phase 07.2 handoff plus consolidated verification/build classification** - `1ecc66b` (docs)

## Files Created/Modified

- `apps/agent/pyproject.toml` - Uses `pydantic-ai[ag-ui]>=2.3.0` and `pydantic-deep>=0.3.34`.
- `apps/agent/src/server/main.py` - Delegates `/copilotkit` directly to `AGUIAdapter.dispatch_request()` with the replayed request body.
- `apps/agent/src/agent/paladin_agent.py` - Routes official approval through `interrupt_on` and native `Tool(..., requires_approval=True)` Computer Use tools.
- `apps/agent/src/agent/agui_approval_bridge.py` - Deleted; local bridge no longer exists in the official path.
- `apps/agent/tests/test_agui_approval_bridge.py` - Deleted with the removed bridge.
- `apps/agent/tests/test_agui_dependency_probe.py` - Probes official AG-UI protocol and deferred approval types.
- `apps/agent/tests/test_server.py` - Verifies official dispatch and unchanged resume forwarding.
- `apps/agent/tests/test_agent.py` - Verifies default/fallback mode parsing, `interrupt_on`, and native Computer Use approval.
- `apps/desktop/src/components/approval/AguiApprovalInterrupt.tsx` - Uses official `tool_call` predicate, official resume payloads, and synchronous duplicate decision protection.
- `apps/desktop/src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx` - Covers negative interrupt reasons, inline `useInterrupt`, payloads, and repeated-click behavior.
- `.planning/phases/07.1-official-ag-ui-deferred-tool-approval/07.1-07.2-HANDOFF.md` - Documents Phase 07.2 cleanup candidates.

## Requirement Coverage

| Requirement | Evidence |
|---|---|
| R1 | Dependency constraints and lockfile entries verified; dependency probe passed. |
| R2 | Bridge source/test deleted; server imports `AGUIAdapter` and not local bridge helpers. |
| R3 | `/copilotkit` resume body preservation test passed; no Paladin `DeferredToolResults` conversion remains in server. |
| R4 | Agent tests passed for default `agui_interrupt`, selectable `legacy_sse`, and `interrupt_on` wiring. |
| R5 | Agent test passed for Computer Use tools registered as native approval-gated `Tool` instances. |
| R6 | Frontend tests passed for `reason === "tool_call"` positive and negative cases plus inline `useInterrupt` registration. |
| R7 | Frontend tests passed for exact approve and deny official resume payloads. |

## Decision Coverage

D-01 through D-03 are satisfied by executing the single consolidation plan. D-04 and D-05 are satisfied by targeted backend and frontend tests. D-06 is satisfied by build classification: after fixing one 07.1 test compatibility issue, `pnpm run build` fails only on unrelated Titlebar unused-variable errors. D-07 is satisfied by retaining `legacy_sse`. D-08 and D-09 are satisfied by the Phase 07.2 handoff and existing ROADMAP follow-up note.

## Verification Results

- `cd apps/agent && uv run pytest tests/test_agui_dependency_probe.py` — PASS, 3 passed.
- `cd apps/agent && uv run pytest tests/test_server.py -k "AguiDispatchEntrypoint or copilotkit"` — PASS, 7 passed, 2 deselected; FastAPI `on_event` deprecation warnings only.
- `cd apps/agent && uv run pytest tests/test_agent.py -k "hitl_mode or agui_interrupt or computer_use_tools_are_registered_with_native_approval"` — PASS, 5 passed, 22 deselected.
- `cd apps/desktop && pnpm test src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx --run` — PASS, 8 passed.
- `cd apps/desktop && pnpm run build` — NON-BLOCKING FAIL: only `Titlebar.test.tsx` and `Titlebar.tsx` unused-variable errors remain, matching D-06 out-of-scope classification.
- `test ! -e apps/agent/src/agent/agui_approval_bridge.py && test ! -e apps/agent/tests/test_agui_approval_bridge.py` — PASS.
- `git diff -- apps/agent/src/server/main.py apps/agent/src/agent/hitl.py apps/agent/src/agent/approval_store.py apps/desktop/src/components/approval/ApprovalBridge.tsx apps/desktop/src/components/approval/ApprovalDialog.tsx` — PASS, no uncommitted legacy fallback behavior changes.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- Frontend build initially reported one 07.1 test compatibility issue because `Array.prototype.at()` is unavailable under the current TypeScript lib target. Fixed by using indexed access in the T2 test and amended the T2 task commit.
- Full desktop build still reports unrelated Titlebar unused-variable errors. These are outside 07.1 approval files and remain non-blocking under D-06.

## User Setup Required

None - no external service configuration required.

## Security Findings

No high severity threat remains open. The high-risk approval bypass paths are covered by tests for `interrupt_on`, empty `ToolGuard.require_approval`, absent `approval_callback` in official mode, native Computer Use approval, exact official resume payloads, and no local server-side resume conversion.

## Next Phase Readiness

Phase 07.1 is ready for verification and Phase 07.2 planning. The legacy SSE fallback remains available, and its cleanup candidates are captured in `.planning/phases/07.1-official-ag-ui-deferred-tool-approval/07.1-07.2-HANDOFF.md`.

## Self-Check: PASSED

---
*Phase: 07.1-official-ag-ui-deferred-tool-approval*
*Completed: 2026-07-03*
