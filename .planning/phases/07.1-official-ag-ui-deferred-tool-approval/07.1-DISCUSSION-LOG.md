# Phase 07.1: Official AG-UI Deferred Tool Approval - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-02
**Phase:** 07.1-official-ag-ui-deferred-tool-approval
**Areas discussed:** Plan 切分方式, 验证口径, Phase 07.2 cleanup 交接

---

## Plan 切分方式

| Option | Description | Selected |
|--------|-------------|----------|
| 单一收束 plan | 一个 `07.1-01-PLAN.md` 覆盖依赖升级、后端 adapter 切换、Agent approval wiring、前端 interrupt payload、测试验证。 | ✓ |
| 三段式 plans | Backend official adapter / Frontend useInterrupt / Verification docs 三个 plan。 | |
| 两段式 plans | Implementation consolidation + Verification handoff。 | |

**User's choice:** 单一收束 plan
**Notes:** Phase 07.1 是把已经完成的 official adapter spike 纳入 GSD，不是从零拆分开发。

---

## 验证口径

| Option | Description | Selected |
|--------|-------------|----------|
| 相关测试 + build 例外记录 | 后端跑 AG-UI/Agent/Server 相关 pytest，前端跑 approval interrupt vitest；full build 若失败，只允许既有 `Titlebar` unused 错误。 | ✓ |
| 只跑相关测试 | 不跑/不记录 full frontend build。 | |
| 修到 full build 全绿 | 把 `Titlebar` unused 错误也纳入 07.1。 | |

**User's choice:** 相关测试 + build 例外记录
**Notes:** This aligns with SPEC: approval-specific tests must pass; unrelated `Titlebar` build blocker stays out of scope.

---

## Phase 07.2 cleanup 交接

| Option | Description | Selected |
|--------|-------------|----------|
| 明确列 cleanup 候选清单 | 后续 07.2 可删除 legacy SSE 端点、store/callback、旧前端桥接组件、旧测试和文档引用；07.1 不执行。 | ✓ |
| 只写一句后续清理 legacy_sse | Lightweight handoff, but later planning would need to rediscover targets. | |
| 不写 07.2 handoff | CONTEXT only serves 07.1. | |

**User's choice:** 明确列 cleanup 候选清单
**Notes:** 07.1 keeps `legacy_sse` as fallback; cleanup should be a later dedicated phase.

---

## the agent's Discretion

- The planner may choose exact command grouping for related pytest/vitest verification.
- The executor may keep legacy fallback files untouched unless compatibility requires a small adjustment.

## Deferred Ideas

- Phase 07.2 Legacy HITL SSE Cleanup — remove the legacy SSE approval fallback after official AG-UI approval is verified stable.
