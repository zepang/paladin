---
phase: 07.1-official-ag-ui-deferred-tool-approval
verified: 2026-07-03T03:23:55Z
status: passed
score: "30/30 must-haves verified"
behavior_unverified: 0
overrides_applied: 0
---

# Phase 07.1 Verification

## Goal Achievement / Observable Truths

Result: PASSED. Phase 07.1's operative traceability contract is SPEC R1-R7 plus `07.1-01-PLAN.md` must_haves. `.planning/ROADMAP.md` still lists Phase 07.1 requirements as TBD, and `.planning/REQUIREMENTS.md` only contains older AGT/HITL IDs, so they were not used as the authoritative R1-R7 source.

| Truth | Status | Evidence |
|---|---|---|
| Official AG-UI approval is the default path. | VERIFIED | `_approval_mode()` defaults to `agui_interrupt` and rejects unsupported modes in `apps/agent/src/agent/paladin_agent.py:234`; tests cover default/legacy/reject in `apps/agent/tests/test_agent.py:60`. |
| `legacy_sse` remains selectable and not deprecated/removed. | VERIFIED | `_approval_mode()` accepts `legacy_sse`; legacy routes remain present at `apps/agent/src/server/main.py:146` and `apps/agent/src/server/main.py:190`; handoff explicitly says retained in Phase 07.1. |
| `/copilotkit` delegates official interrupt/resume handling to `AGUIAdapter`. | VERIFIED | Server imports `AGUIAdapter` from `pydantic_ai.ui.ag_ui` at `apps/agent/src/server/main.py:17`, replays body at `apps/agent/src/server/main.py:270`, and calls `AGUIAdapter.dispatch_request(request=..., agent=..., deps=...)` at `apps/agent/src/server/main.py:277`. |
| Paladin-side resume-to-`DeferredToolResults` conversion is absent from server path. | VERIFIED | `rg` found no `DeferredToolResults` or `deferred_tool_results` in `apps/agent/src/server/main.py`; those symbols appear only in dependency probe tests. |
| Frontend renders official approval interrupts inline in chat. | VERIFIED | `AguiApprovalInterrupt` uses `useInterrupt({ enabled: isApprovalRequired, renderInChat: true })` at `apps/desktop/src/components/approval/AguiApprovalInterrupt.tsx:94`; `ChatArea` mounts it next to `CopilotChat` at `apps/desktop/src/components/ChatArea.tsx:39`. |

## Required Artifacts

| Artifact | Status | Evidence |
|---|---|---|
| Dependency baseline | VERIFIED | `apps/agent/pyproject.toml:11` has `pydantic-ai[ag-ui]>=2.3.0`; `apps/agent/pyproject.toml:12` has `pydantic-deep>=0.3.34`. `apps/agent/uv.lock:12` resolves `ag-ui-protocol`; `apps/agent/uv.lock:1299` resolves `pydantic-ai==2.3.0`; `apps/agent/uv.lock:1498` resolves `pydantic-deep==0.3.34`. |
| Official protocol/deferred approval probe | VERIFIED | `apps/agent/tests/test_agui_dependency_probe.py:4` imports AG-UI protocol types; `apps/agent/tests/test_agui_dependency_probe.py:6` imports `DeferredToolRequests`, `DeferredToolResults`, `ToolApproved`, and `ToolDenied`; `RunAgentInput.resume` is asserted at line 17. |
| Deleted bridge files absent | VERIFIED | `test ! -e apps/agent/src/agent/agui_approval_bridge.py` and `test ! -e apps/agent/tests/test_agui_approval_bridge.py` both passed. |
| Phase 07.2 handoff | VERIFIED | `.planning/phases/07.1-official-ag-ui-deferred-tool-approval/07.1-07.2-HANDOFF.md:18` lists `/approval/stream`, `/approval/{request_id}`, `approval_store.py`, `hitl.py`, `ApprovalBridge.tsx`, `ApprovalDialog.tsx`, legacy tests, and old Phase 07 references. ROADMAP follow-up exists at `.planning/ROADMAP.md:187`. |
| Code review evidence | VERIFIED | `07.1-REVIEW.md` reports status `clean`, total findings `0`, after remediation review. |

## Key Link Verification

| Link | Status | Evidence |
|---|---|---|
| R3 body preservation into official adapter | VERIFIED | Test patches `AGUIAdapter.dispatch_request`, posts a body with `resume`, and asserts captured body equals original at `apps/agent/tests/test_server.py:142`. |
| R4 configured approvals to `interrupt_on` | VERIFIED | `paladin_agent.py:335` builds `{tool_name: True}` in `agui_interrupt`; `create_deep_agent(... interrupt_on=interrupt_on)` is wired at `apps/agent/src/agent/paladin_agent.py:378`; test asserts `{"write_file": True, "execute": True}` at `apps/agent/tests/test_agent.py:484`. |
| R4 no ToolGuard approval callback in official mode | VERIFIED | Official branch sets `guarded_require_approval = []` and `approval_callback = None` at `apps/agent/src/agent/paladin_agent.py:337`; test asserts both at `apps/agent/tests/test_agent.py:514`. |
| R5 Computer Use native approval | VERIFIED | Computer Use names are added to approval set at `apps/agent/src/agent/paladin_agent.py:330`; official mode wraps tools with `Tool(..., requires_approval=True)` at `apps/agent/src/agent/paladin_agent.py:339`; test asserts `Tool` instance and `requires_approval is True` at `apps/agent/tests/test_agent.py:518`. |
| R6 official `tool_call` predicate | VERIFIED | `isApprovalRequired()` returns true only for object values with `reason === "tool_call"` at `apps/desktop/src/components/approval/AguiApprovalInterrupt.tsx:43`; tests cover positive and negative reasons at `apps/desktop/src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx:101`. |
| R7 official resume payloads | VERIFIED | `buildApprovalResumePayload()` returns `{ approved: true }` or `{ approved: false, reason: "User denied this tool call." }` at `apps/desktop/src/components/approval/AguiApprovalInterrupt.tsx:47`; tests assert exact objects at `apps/desktop/src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx:113`. |

## Behavioral Spot-Checks

Commands run during this verification:

| Command | Result |
|---|---|
| `cd apps/agent && uv run pytest tests/test_agui_dependency_probe.py` | PASS: 3 passed. |
| `cd apps/agent && uv run pytest tests/test_server.py -k 'AguiDispatchEntrypoint or copilotkit'` | PASS: 7 passed, 3 deselected, 4 FastAPI deprecation warnings. |
| `cd apps/agent && uv run pytest tests/test_agent.py -k 'hitl_mode or agui_interrupt or computer_use_tools_are_registered_with_native_approval'` | PASS: 5 passed, 23 deselected. |
| `cd apps/desktop && pnpm test src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx --run` | PASS: 1 file passed, 8 tests passed. |
| `cd apps/desktop && pnpm run build` | NON-BLOCKING FAIL under D-06: errors are only unused variables in `src/components/__tests__/Titlebar.test.tsx` and `src/components/Titlebar.tsx`, not 07.1 approval files or behavior. |

Additional accepted evidence from orchestrator context:

| Evidence | Result |
|---|---|
| `cd apps/agent && uv run pytest tests/test_agent.py tests/test_tools.py tests/test_server.py tests/test_prohibitions.py -q` | PASS: 54 passed, 4 FastAPI deprecation warnings. |

Disconfirmation pass:

| Check | Result |
|---|---|
| Partial requirement risk | D-06 full frontend build does not pass globally, but the only errors are the explicitly allowed unrelated Titlebar unused-variable failures; targeted approval tests pass. |
| Misleading test risk | Dependency probe is import/schema availability only, but behavior-dependent truths are covered by server, agent, and frontend tests that patch real call boundaries and exact payloads. |
| Untested error path risk | Unsupported `hitl.mode`, non-tool interrupt reasons, duplicate approve clicks, invalid `/copilotkit` JSON, and non-object JSON forwarding are covered by targeted tests. |

## Requirements Coverage

| Requirement | Status | Evidence |
|---|---|---|
| R1 official dependency baseline | VERIFIED | `pyproject.toml`, `uv.lock`, and dependency probe test verify official AG-UI, `pydantic-deep`, `ag-ui-protocol`, `RunAgentInput.resume`, and deferred approval types. |
| R2 no custom approval bridge in AG-UI path | VERIFIED | Bridge source/test files absent; server imports official `AGUIAdapter`; no `agui_approval_bridge` references in current server path. |
| R3 preserve official RunAgentInput resume | VERIFIED | `/copilotkit` replays the complete body and test asserts a `resume` body is forwarded unchanged. No local `DeferredToolResults` conversion is present. |
| R4 agui_interrupt official deferred approval | VERIFIED | Default mode, selectable `legacy_sse`, unsupported rejection, `interrupt_on`, empty guard approvals, and absent callback are all code-backed and test-backed. |
| R5 Computer Use native approval | VERIFIED | Computer Use functions are wrapped as native `Tool(..., requires_approval=True)` in official mode, with test coverage. |
| R6 frontend official tool-call interrupts | VERIFIED | `AguiApprovalInterrupt` uses `useInterrupt(renderInChat: true)`, is mounted in `ChatArea`, and only accepts `reason === "tool_call"`. |
| R7 frontend official resume payload schema | VERIFIED | Approve/deny payload helpers and rendered click path use exact official payloads; tests assert exact objects and duplicate-click protection. |

Decision coverage:

| Decision / must-have | Status | Evidence |
|---|---|---|
| D-01/D-03 single consolidation plan only | VERIFIED | Phase directory contains one completed 07.1 plan, `07.1-01-PLAN.md`; no backend/frontend split plans were found. |
| D-02 end-to-end consolidation scope | VERIFIED | Dependency, backend delegation, agent wiring, frontend interrupt payloads, bridge deletion, and verification artifacts are all present. |
| D-04 backend verification coverage | VERIFIED | Dependency probe, server dispatch/resume, and agent wiring tests all passed. |
| D-05 frontend verification coverage | VERIFIED | `AguiApprovalInterrupt` tests passed for `tool_call` predicate and official payloads. |
| D-06 Titlebar build exception | VERIFIED | Full build fails only on unrelated Titlebar unused-variable errors; approval-specific tests pass. |
| D-07 keep `legacy_sse` selectable | VERIFIED | `_approval_mode()` accepts `legacy_sse`; legacy routes/files retained. |
| D-08/D-09 07.2 handoff and candidates | VERIFIED | Handoff file exists and names all required cleanup candidates; ROADMAP has follow-up note. |
| Prohibitions | VERIFIED | No Paladin-side resume conversion, no arbitrary interrupt approval card predicate, no legacy `{ decision: ... }` official payload, no 07.1 cleanup/removal of legacy fallback, no audit/schema/Computer Use behavior expansion observed. |

## Anti-Patterns Found

None blocking.

- No local `agui_approval_bridge` source/test files remain.
- No `/copilotkit` `DeferredToolResults` construction or `deferred_tool_results` argument exists.
- No official frontend path uses legacy `{ decision: ... }` payloads.
- No unreferenced TODO/FIXME/XXX blockers were found in phase-modified files. The only `TBD` hits are ROADMAP placeholders for already-known unplanned phases and Phase 07.1's intentionally noted requirements placeholder.
- `git diff -- apps/agent/src/server/main.py apps/agent/src/agent/hitl.py apps/agent/src/agent/approval_store.py apps/desktop/src/components/approval/ApprovalBridge.tsx apps/desktop/src/components/approval/ApprovalDialog.tsx` produced no diff, so no Phase 07.2 cleanup was accidentally implemented during this verification.

## Human Verification Required

None.

## Gaps Summary

No gaps found. Status is `passed` with 30/30 plan must_haves/prohibitions verified, 0 behavior unverified, and 0 overrides applied.

