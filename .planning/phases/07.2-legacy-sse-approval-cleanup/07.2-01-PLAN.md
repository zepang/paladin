---
phase: 07.2-legacy-sse-approval-cleanup
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/agent/src/agent/paladin_agent.py
  - apps/agent/tests/test_agent.py
autonomous: true
requirements:
  - 07.2-R1
  - 07.2-R3
  - 07.2-R6
user_setup: []
must_haves:
  truths:
    - "D-04 D-05: explicit legacy SSE mode fails fast during backend config parsing."
    - "D-06: omitted hitl.mode still defaults to agui_interrupt with no migration branch."
    - "D-01 D-02: agent creation no longer installs legacy approval callback code, while neutral HITL config validation remains available."
    - "SPEC Edge Coverage: R1/R3 idempotency is covered by stable unsupported-mode behavior and callback symbol absence."
    - "SPEC Prohibitions: must not silently map legacy mode to agui_interrupt; must not weaken official AG-UI approval policy."
  artifacts:
    - path: "apps/agent/src/agent/paladin_agent.py"
      provides: "only supported approval mode parser plus neutral HITL config validation"
      exports: ["_approval_mode", "validate_hitl_config", "create_paladin_agent"]
    - path: "apps/agent/tests/test_agent.py"
      provides: "backend TDD regression for unsupported legacy mode, default official mode, interrupt_on, and Computer Use approval"
  key_links:
    - from: "apps/agent/src/agent/paladin_agent.py"
      to: "pydantic_deep.create_deep_agent"
      via: "interrupt_on remains populated for official deferred approvals"
      pattern: "interrupt_on"
    - from: "apps/agent/tests/test_agent.py"
      to: "apps/agent/src/agent/paladin_agent.py"
      via: "tests import _approval_mode and create_paladin_agent directly"
      pattern: "_approval_mode"
---

<objective>
Make backend approval mode parsing official-only: reject explicit legacy SSE mode, preserve default agui_interrupt, and remove the agent factory branch that would install a legacy callback.

Purpose: This closes the config-level fallback before route/store deletion, covering D-01, D-02, D-04, D-05, D-06, D-11, and D-13 while preserving the official AG-UI path from Phase 07.1.
Output: Updated `paladin_agent.py` and backend tests proving the unsupported mode fails and official deferred approval remains intact.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-CONTEXT.md
@.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-SPEC.md
@.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-RESEARCH.md
@.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-PATTERNS.md
@.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-VALIDATION.md
@apps/agent/src/agent/paladin_agent.py
@apps/agent/src/agent/hitl.py
@apps/agent/tests/test_agent.py
</context>

## Artifacts this phase produces

Deleted files/symbols advanced by this plan:
- Active import and use of `create_approval_callback` from `apps/agent/src/agent/paladin_agent.py`
- Agent factory branch that installs `ToolGuard.approval_callback`
- Agent-attached `_hitl_sse_queue`
- Test expectation that `hitl.mode` can select `legacy_sse`

Retained/preserved symbols:
- `apps/agent/src/agent/paladin_agent.py::_approval_mode`
- `apps/agent/src/agent/paladin_agent.py::validate_hitl_config` or an equivalent neutral helper imported by `paladin_agent.py`
- `hitl.require_approval`, `hitl.blocked`, and `timeout_seconds` config semantics
- `interrupt_on` and `Tool(..., requires_approval=True)` official AG-UI approval behavior

<feature>
  <name>Official-only backend approval mode</name>
  <files>apps/agent/src/agent/paladin_agent.py, apps/agent/tests/test_agent.py</files>
  <behavior>
    RED cases:
    - `_approval_mode({})` returns `agui_interrupt`.
    - `_approval_mode({"hitl": {"mode": "legacy_sse"}})` raises `ValueError` with a message naming the unsupported mode and `agui_interrupt`.
    - Unsupported non-official modes still raise `ValueError`.
    - `create_paladin_agent()` passes `interrupt_on` to `create_deep_agent()` and leaves `ToolGuard.approval_callback` unset.
    - Computer Use tools remain wrapped with native Pydantic `Tool(..., requires_approval=True)`.
  </behavior>
  <implementation>After the RED test fails for the explicit legacy mode, move retained HITL config validation out of the legacy-shaped module into `paladin_agent.py` or a neutral helper, then simplify agent creation so only official deferred approval is wired. Do not add Paladin-side AG-UI resume conversion and do not change approval-required tool policy.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - rewrite backend mode tests for official-only approval</name>
  <read_first>
    - `.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-CONTEXT.md` decisions D-04, D-05, D-06, D-11
    - `.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-PATTERNS.md` "Fail-Fast Mode Validation"
    - `apps/agent/tests/test_agent.py`
  </read_first>
  <files>apps/agent/tests/test_agent.py</files>
  <action>Replace the old fallback-allowed mode assertion with a failing test for explicit legacy mode rejection per D-04 and D-05. Keep the default official mode assertion per D-06, keep unsupported-value coverage, and ensure existing official `interrupt_on` and Computer Use approval tests remain in place for D-11.</action>
  <verify>
    <automated>cd apps/agent && uv run pytest tests/test_agent.py::test_hitl_mode_defaults_to_agui_interrupt tests/test_agent.py::test_hitl_mode_rejects_legacy_sse tests/test_agent.py::test_hitl_mode_rejects_unsupported_value -q</automated>
  </verify>
  <acceptance_criteria>The new explicit legacy-mode test fails before implementation for the right reason, while the intended assertions describe D-04 through D-06 without relying on legacy callback code.</acceptance_criteria>
  <done>Acceptance criteria above are met and the automated verification command passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - enforce official-only mode and remove agent callback branch</name>
  <read_first>
    - `.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-SPEC.md` requirements R1, R3, R6 and Prohibitions
    - `apps/agent/src/agent/paladin_agent.py`
    - `apps/agent/src/agent/hitl.py`
  </read_first>
  <files>apps/agent/src/agent/paladin_agent.py, apps/agent/tests/test_agent.py</files>
  <action>Implement the official-only parser and neutral config validation location per D-01 and D-02. Remove the active backend callback import/use, remove the non-official branch in `create_paladin_agent()`, keep `interrupt_on` populated from configured and Computer Use approval requirements, and keep the official Computer Use native approval wrapping unchanged per D-11.</action>
  <verify>
    <automated>cd apps/agent && uv run pytest tests/test_agent.py::test_hitl_mode_defaults_to_agui_interrupt tests/test_agent.py::test_hitl_mode_rejects_legacy_sse tests/test_agent.py::test_agui_interrupt_mode_passes_require_approval_to_deep_agent_interrupts tests/test_agent.py::test_computer_use_tools_are_registered_with_native_approval -q</automated>
  </verify>
  <acceptance_criteria>Backend mode parsing rejects explicit legacy mode, omitted mode still defaults to official AG-UI approval, `ToolGuard.approval_callback` is not installed, and official approval tests pass.</acceptance_criteria>
  <done>Acceptance criteria above are met and the automated verification command passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR - keep config validation neutral and callback residue absent in agent factory</name>
  <read_first>
    - `.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-RESEARCH.md` "Pitfall 1"
    - `apps/agent/src/agent/paladin_agent.py`
  </read_first>
  <files>apps/agent/src/agent/paladin_agent.py, apps/agent/tests/test_agent.py</files>
  <action>Clean up imports, logging, and test names after the green implementation. Keep validation behavior readable without retaining obsolete approval semantics, and do not introduce new packages or schema/ORM work.</action>
  <verify>
    <automated>cd apps/agent && uv run pytest tests/test_agent.py -q && ! rg -n "create_approval_callback|_hitl_sse_queue" apps/agent/src/agent/paladin_agent.py</automated>
  </verify>
  <acceptance_criteria>`test_agent.py` passes, active agent factory source contains no legacy callback factory or SSE queue bridge reference, and official AG-UI approval behavior is unchanged.</acceptance_criteria>
  <done>Acceptance criteria above are met and the automated verification command passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file -> agent factory | Untrusted or stale local config selects approval mode before tools are registered. |
| agent factory -> Pydantic Deep | Approval policy crosses into official deferred approval runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.2-01 | Tampering | `_approval_mode` | high | mitigate | Reject explicit legacy mode with a clear error and cover it with TDD tests. |
| T-07.2-06 | Elevation of Privilege | `create_paladin_agent` approval wiring | high | mitigate | Preserve `interrupt_on` and Computer Use native approval tests so cleanup cannot bypass required approvals. |
| T-07.2-SC | Tampering | package installs | low | accept | No package-manager install tasks exist in this plan; Package Legitimacy Audit is skipped by research because no new packages are required. |
</threat_model>

<verification>
Run backend targeted tests:

`cd apps/agent && uv run pytest tests/test_agent.py -q`
</verification>

<success_criteria>
- Explicit legacy mode raises a clear `ValueError`.
- Omitted mode remains `agui_interrupt`.
- Agent creation has no active legacy callback branch.
- Official `interrupt_on` and Computer Use approval regressions pass.
</success_criteria>

<output>
Create `.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-01-SUMMARY.md` when done, including RED/GREEN/REFACTOR commits and any validation relocation choice.
</output>
