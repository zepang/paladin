---
phase: 07.2-legacy-sse-approval-cleanup
plan: 02
type: tdd
wave: 2
depends_on:
  - "07.2-01"
files_modified:
  - apps/agent/src/server/main.py
  - apps/agent/src/agent/hitl.py
  - apps/agent/src/agent/approval_store.py
  - apps/agent/tests/test_server.py
  - apps/agent/tests/test_hitl.py
  - apps/agent/tests/test_approval_store.py
autonomous: true
requirements:
  - 07.2-R2
  - 07.2-R3
  - 07.2-R6
user_setup: []
must_haves:
  truths:
    - "D-01 D-03: request-id callback/store implementation is not active backend code."
    - "D-11: old backend approval routes are absent or return 404 while `/copilotkit` delegation and resume forwarding remain intact."
    - "D-13: active backend source/tests do not retain old callback/store/queue symbols."
    - "SPEC Edge Coverage: R2/R3 idempotency and concurrency are covered by route absence and removal of request-id state."
    - "SPEC Prohibitions: must not leave old backend routes reachable; must not keep request-id state as dead active code."
  artifacts:
    - path: "apps/agent/src/server/main.py"
      provides: "official `/copilotkit`, `/health`, `/info`, and no legacy approval transport"
    - path: "apps/agent/tests/test_server.py"
      provides: "route absence and official AG-UI resume regression tests"
    - path: "apps/agent/src/agent/hitl.py"
      provides: "deleted file or no active legacy approval implementation"
    - path: "apps/agent/src/agent/approval_store.py"
      provides: "deleted legacy store"
  key_links:
    - from: "apps/agent/src/server/main.py"
      to: "pydantic_ai.ui.ag_ui.AGUIAdapter.dispatch_request"
      via: "`/copilotkit` request replay"
      pattern: "AGUIAdapter.dispatch_request"
---

<objective>
Delete the backend legacy approval transport and request-id state while proving official `/copilotkit` AG-UI delegation still works.

Purpose: This removes the backend fallback named by D-01, D-03, D-11, and D-13 after Plan 01 has removed the agent-factory callback branch.
Output: Server route cleanup, legacy backend file/test deletion or rewrite, and route/source absence tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-CONTEXT.md
@.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-SPEC.md
@.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-RESEARCH.md
@.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-PATTERNS.md
@.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-VALIDATION.md
@apps/agent/src/server/main.py
@apps/agent/src/agent/hitl.py
@apps/agent/src/agent/approval_store.py
@apps/agent/tests/test_server.py
@apps/agent/tests/test_hitl.py
@apps/agent/tests/test_approval_store.py
</context>

## Artifacts this phase produces

Deleted files/symbols advanced by this plan:
- `apps/agent/src/agent/hitl.py`, unless Plan 01 intentionally kept a neutral helper elsewhere
- `apps/agent/src/agent/approval_store.py`
- `apps/agent/tests/test_approval_store.py`
- Legacy-only callback/queue assertions in `apps/agent/tests/test_hitl.py`
- Backend route/broadcast symbols: `/approval/stream`, `/approval/{request_id}`, `_approval_queues`, `_broadcast_approval_event`, `approval_stream`, `approval_decision`, `_pending_approvals`, `_pending_decisions`, `_sse_queue`, `resolve_approval`

Retained/preserved symbols:
- `apps/agent/src/server/main.py::copilotkit_endpoint`
- `apps/agent/src/server/main.py::copilotkit_info`
- `apps/agent/src/server/main.py::health`
- `AGUIAdapter.dispatch_request`
- Official `resume[]` request body forwarding through `/copilotkit`

<feature>
  <name>Backend route absence and official dispatch preservation</name>
  <files>apps/agent/src/server/main.py, apps/agent/tests/test_server.py, apps/agent/tests/test_hitl.py, apps/agent/tests/test_approval_store.py, apps/agent/src/agent/hitl.py, apps/agent/src/agent/approval_store.py</files>
  <behavior>
    RED cases:
    - Server route table does not contain the old streaming route.
    - Server route table does not contain the old request-id decision route.
    - GET/POST probes for old approval endpoints return 404 after cleanup.
    - `/copilotkit` still dispatches to `AGUIAdapter.dispatch_request()` with replayed body, agent, deps, and unchanged `resume[]`.
  </behavior>
  <implementation>After RED tests describe route absence, remove server broadcast lifecycle, legacy route handlers, request-id approval implementation, and legacy store/tests. Preserve official `/copilotkit` behavior exactly and do not add a replacement approval endpoint.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - add backend route absence tests</name>
  <read_first>
    - `.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-SPEC.md` requirements R2 and R6
    - `.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-PATTERNS.md` "apps/agent/tests/test_server.py"
    - `apps/agent/tests/test_server.py`
  </read_first>
  <files>apps/agent/tests/test_server.py</files>
  <action>Add route-table and 404 regression tests per D-11 proving the old backend approval transport is not registered. Keep existing official dispatch and official resume preservation tests unchanged.</action>
  <verify>
    <automated>cd apps/agent && uv run pytest tests/test_server.py -q</automated>
  </verify>
  <acceptance_criteria>The new route absence tests fail before server cleanup because the old endpoints are still registered, while official `/copilotkit` tests remain present.</acceptance_criteria>
  <done>Acceptance criteria above are met and the automated verification command passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - remove server route bridge and legacy backend modules</name>
  <read_first>
    - `.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-CONTEXT.md` decisions D-01, D-03, D-11, D-13
    - `apps/agent/src/server/main.py`
    - `apps/agent/src/agent/hitl.py`
    - `apps/agent/src/agent/approval_store.py`
  </read_first>
  <files>apps/agent/src/server/main.py, apps/agent/src/agent/hitl.py, apps/agent/src/agent/approval_store.py, apps/agent/tests/test_hitl.py, apps/agent/tests/test_approval_store.py</files>
  <action>Delete the backend route bridge, broadcast lifecycle, request-id approval implementation, legacy approval store, and legacy-only tests per D-01 and D-03. If Plan 01 moved validation into a neutral location, delete the obsolete HITL module completely; otherwise stop and reconcile the validation location before proceeding. Preserve `/copilotkit`, `/copilotkit/info`, `/health`, `/info`, and `/threads` behavior.</action>
  <verify>
    <automated>cd apps/agent && uv run pytest tests/test_server.py tests/test_agent.py -q && ! rg -n "create_approval_callback|_pending_approvals|_pending_decisions|_sse_queue|resolve_approval|approval_store|/approval/stream|/approval/\\{request_id\\}" apps/agent/src apps/agent/tests</automated>
  </verify>
  <acceptance_criteria>Old backend approval routes and request-id state are absent from active backend source/tests; official server and agent tests pass.</acceptance_criteria>
  <done>Acceptance criteria above are met and the automated verification command passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR - tighten backend imports and route absence gates</name>
  <read_first>
    - `.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-RESEARCH.md` "Common Pitfalls"
    - `apps/agent/src/server/main.py`
    - `apps/agent/tests/test_server.py`
  </read_first>
  <files>apps/agent/src/server/main.py, apps/agent/tests/test_server.py</files>
  <action>Remove obsolete imports and comments left by the deleted transport. Keep route tests focused on registered routes and 404 behavior, and keep negative source gates scoped to `apps/agent/src` and `apps/agent/tests` so historical planning records do not invalidate the gate.</action>
  <verify>
    <automated>cd apps/agent && uv run pytest tests/test_server.py -q && ! rg -n "StreamingResponse|_approval_queues|_broadcast_approval_event|approval_stream|approval_decision" apps/agent/src/server/main.py</automated>
  </verify>
  <acceptance_criteria>Server imports and tests are clean, no old transport lifecycle symbols remain in `main.py`, and official `/copilotkit` tests still pass.</acceptance_criteria>
  <done>Acceptance criteria above are met and the automated verification command passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| desktop/client -> FastAPI server | Client requests can reach registered HTTP routes. |
| FastAPI server -> AG-UI adapter | Official approval resume payloads are delegated to the Pydantic AI adapter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.2-02 | Spoofing/Elevation of Privilege | legacy approval endpoints | high | mitigate | Remove old approval endpoints and prove absence via route table and 404 tests. |
| T-07.2-03 | Tampering | request-id callback/store modules | high | mitigate | Delete active callback/store modules and tests; enforce source-search absence in active backend paths. |
| T-07.2-06 | Tampering | `/copilotkit` resume forwarding | medium | mitigate | Keep dispatch regression tests that capture and compare unchanged `resume[]` body forwarding. |
| T-07.2-SC | Tampering | package installs | low | accept | No package-manager install tasks exist in this plan. |
</threat_model>

<verification>
Run backend targeted tests and source gates:

`cd apps/agent && uv run pytest tests/test_agent.py tests/test_server.py -q`

`! rg -n "create_approval_callback|_pending_approvals|_pending_decisions|_sse_queue|resolve_approval|approval_store|/approval/stream|/approval/\\{request_id\\}" apps/agent/src apps/agent/tests`
</verification>

<success_criteria>
- Old backend approval routes are not registered and probe as 404.
- Request-id callback/store implementation is deleted from active backend code/tests.
- Official `/copilotkit` dispatch and resume forwarding still pass tests.
</success_criteria>

<output>
Create `.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-02-SUMMARY.md` when done, including RED/GREEN/REFACTOR commits and deleted backend files.
</output>
