---
phase: 07.2-legacy-sse-approval-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/App.tsx
  - apps/desktop/src/components/ChatToolbar.tsx
  - apps/desktop/src/components/approval/ApprovalBridge.tsx
  - apps/desktop/src/components/approval/ApprovalDialog.tsx
  - apps/desktop/src/components/approval/AguiApprovalInterrupt.tsx
  - apps/desktop/src/components/approval/ApprovalCard.tsx
  - apps/desktop/src/components/ChatArea.tsx
  - apps/desktop/src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx
autonomous: true
requirements:
  - 07.2-R4
  - 07.2-R6
user_setup: []
must_haves:
  truths:
    - "D-07 D-08: desktop legacy approval channel is removed without adding toolbar pending-status UI."
    - "D-09: ChatToolbar keeps non-approval context/tool-call information and drops legacy approval status."
    - "D-10 D-12: official approval cards remain owned by AguiApprovalInterrupt in the chat area and submit official resume payloads."
    - "D-13: active desktop source does not retain legacy bridge/dialog/provider/context symbols or old approval endpoint strings."
    - "UI-SPEC: preserve ApprovalCard visuals, labels, spacing, and payload behavior."
    - "SPEC Edge Coverage: R4 idempotency/concurrency is covered by deleting EventSource/provider queue UI; R6 regression covers official path."
  artifacts:
    - path: "apps/desktop/src/App.tsx"
      provides: "CopilotKitProvider shell without legacy ApprovalProvider/Dialog wrapper"
    - path: "apps/desktop/src/components/ChatToolbar.tsx"
      provides: "non-approval toolbar context/tool-call surface"
    - path: "apps/desktop/src/components/approval/ApprovalBridge.tsx"
      provides: "deleted legacy EventSource bridge"
    - path: "apps/desktop/src/components/approval/ApprovalDialog.tsx"
      provides: "deleted legacy modal"
    - path: "apps/desktop/src/components/approval/AguiApprovalInterrupt.tsx"
      provides: "preserved official AG-UI interrupt owner"
  key_links:
    - from: "apps/desktop/src/components/ChatArea.tsx"
      to: "apps/desktop/src/components/approval/AguiApprovalInterrupt.tsx"
      via: "chat area mount point"
      pattern: "AguiApprovalInterrupt"
    - from: "apps/desktop/src/components/approval/AguiApprovalInterrupt.tsx"
      to: "@copilotkit/react-core/v2"
      via: "copilotkit.setInterruptElement and agent.runAgent({ resume })"
      pattern: "setInterruptElement"
---

<objective>
Remove the desktop EventSource approval bridge, modal dialog, provider, and toolbar status while preserving the official AG-UI approval card flow.

Purpose: This fulfills D-07 through D-10, D-12, D-13, and the approved UI-SPEC without introducing deferred toolbar pending-status UI.
Output: Active desktop source has only the official `AguiApprovalInterrupt` approval surface and passing approval interrupt tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-CONTEXT.md
@.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-SPEC.md
@.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-UI-SPEC.md
@.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-PATTERNS.md
@apps/desktop/src/App.tsx
@apps/desktop/src/components/ChatToolbar.tsx
@apps/desktop/src/components/ChatArea.tsx
@apps/desktop/src/components/approval/ApprovalBridge.tsx
@apps/desktop/src/components/approval/ApprovalDialog.tsx
@apps/desktop/src/components/approval/AguiApprovalInterrupt.tsx
@apps/desktop/src/components/approval/ApprovalCard.tsx
@apps/desktop/src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx
</context>

## Artifacts this phase produces

Deleted files/symbols advanced by this plan:
- `apps/desktop/src/components/approval/ApprovalBridge.tsx`
- `apps/desktop/src/components/approval/ApprovalDialog.tsx`
- `ApprovalProvider`
- `useApprovalContext`
- desktop references to `/approval/stream` and `/approval/{request_id}`
- legacy approval status block in `ChatToolbar`

Retained/preserved symbols:
- `apps/desktop/src/components/approval/AguiApprovalInterrupt.tsx`
- `apps/desktop/src/components/approval/ApprovalCard.tsx`
- `apps/desktop/src/components/ChatArea.tsx` mount of `AguiApprovalInterrupt`
- `copilotkit.setInterruptElement`
- `agent.runAgent({ resume })`
- ApprovalCard labels `Deny` and `Approve`

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy approval provider/dialog from the app shell</name>
  <read_first>
    - `.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-UI-SPEC.md` Interaction Contract
    - `.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-CONTEXT.md` decisions D-07, D-08, D-10
    - `apps/desktop/src/App.tsx`
  </read_first>
  <files>apps/desktop/src/App.tsx, apps/desktop/src/components/approval/ApprovalBridge.tsx, apps/desktop/src/components/approval/ApprovalDialog.tsx</files>
  <action>Remove the legacy provider wrapper, context-consuming dialog component, and deleted component imports per D-08. Keep `CopilotKitProvider`, `Titlebar`, `ChatArea`, `RightPanel`, `StatusBar`, and `Toaster` placement unchanged per UI-SPEC. Delete the bridge and dialog files after imports are removed.</action>
  <verify>
    <automated>cd apps/desktop && pnpm vitest run src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx</automated>
  </verify>
  <acceptance_criteria>App shell renders without legacy approval provider/dialog wiring, toast placement is unchanged, and official approval interrupt tests still pass.</acceptance_criteria>
  <done>Acceptance criteria above are met and the automated verification command passes.</done>
</task>

<task type="auto">
  <name>Task 2: Remove legacy approval status from ChatToolbar</name>
  <read_first>
    - `.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-UI-SPEC.md` Copywriting Contract and Color rules
    - `.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-CONTEXT.md` decisions D-07, D-09, D-10
    - `apps/desktop/src/components/ChatToolbar.tsx`
  </read_first>
  <files>apps/desktop/src/components/ChatToolbar.tsx</files>
  <action>Drop the legacy approval context import, pending-status state, and yellow approval block per D-09. Keep toolbar width, border, context labels, tool-call list, and clear-conversation action unchanged. Do not add any official pending-status toolbar UI in this phase per D-07 and D-10.</action>
  <verify>
    <automated>cd apps/desktop && pnpm vitest run src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx && ! rg -n "ShieldAlert|useApprovalContext|ApprovalProvider|ApprovalBridge|ApprovalDialog" apps/desktop/src/App.tsx apps/desktop/src/components/ChatToolbar.tsx</automated>
  </verify>
  <acceptance_criteria>ChatToolbar contains only non-approval context/tool-call/clear controls and no legacy approval context dependency.</acceptance_criteria>
  <done>Acceptance criteria above are met and the automated verification command passes.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve official AG-UI approval card tests and source ownership</name>
  <read_first>
    - `.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-UI-SPEC.md` Source Ownership Gates
    - `apps/desktop/src/components/ChatArea.tsx`
    - `apps/desktop/src/components/approval/AguiApprovalInterrupt.tsx`
    - `apps/desktop/src/components/approval/ApprovalCard.tsx`
    - `apps/desktop/src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx`
  </read_first>
  <files>apps/desktop/src/components/approval/AguiApprovalInterrupt.tsx, apps/desktop/src/components/approval/ApprovalCard.tsx, apps/desktop/src/components/ChatArea.tsx, apps/desktop/src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx</files>
  <action>Leave the official card visual contract and chat ownership path intact per D-10 and D-12. Add or adjust tests only if needed to prove the official interrupt component remains mounted and submits a single official resume payload. Keep approval labels and denial payload unchanged.</action>
  <verify>
    <automated>cd apps/desktop && pnpm vitest run src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx && ! rg -n "ApprovalBridge|ApprovalDialog|ApprovalProvider|useApprovalContext|/approval/stream|/approval/\\{request_id\\}" apps/desktop/src && rg -n "AguiApprovalInterrupt|ApprovalCard|setInterruptElement|agent.runAgent\\(\\{ resume \\}\\)" apps/desktop/src</automated>
  </verify>
  <acceptance_criteria>Active desktop source has no legacy approval channel references, official approval interrupt/card ownership remains, and targeted Vitest passes.</acceptance_criteria>
  <done>Acceptance criteria above are met and the automated verification command passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| desktop UI -> agent server | Legacy EventSource/HTTP decision channel previously crossed this boundary outside official AG-UI. |
| CopilotKit AG-UI client -> official interrupt component | Official interrupts are rendered and resumed through CopilotKit/AG-UI client state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.2-04 | Spoofing/Tampering | legacy desktop bridge/dialog | high | mitigate | Delete EventSource provider/dialog/status wiring and enforce active desktop source-search absence. |
| T-07.2-06 | Elevation of Privilege | official approval card/resume | high | mitigate | Preserve `AguiApprovalInterrupt`, `ApprovalCard`, one-shot decision behavior, and resume payload tests. |
| T-07.2-SC | Tampering | package installs | low | accept | No package-manager install or new shadcn registry task exists in this plan. |
</threat_model>

<verification>
Run frontend targeted tests and source gates:

`cd apps/desktop && pnpm vitest run src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx`

`! rg -n "ApprovalBridge|ApprovalDialog|ApprovalProvider|useApprovalContext|/approval/stream|/approval/\\{request_id\\}" apps/desktop/src`
</verification>

<success_criteria>
- Legacy desktop bridge/dialog/provider/status UI is removed.
- No deferred toolbar pending-status UI is implemented.
- Official AG-UI interrupt/card path remains mounted in chat and passes targeted tests.
- ApprovalCard visuals/copy/payload contract from UI-SPEC is preserved.
</success_criteria>

<output>
Create `.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-03-SUMMARY.md` when done, listing deleted frontend files and preserved official UI symbols.
</output>
