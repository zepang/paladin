# Phase 07.2: Legacy SSE Approval Cleanup - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-03
**Phase:** 07.2-legacy-sse-approval-cleanup
**Areas discussed:** Backend removal boundary, Legacy mode failure behavior, Frontend cleanup shape, Verification gates

---

## Backend Removal Boundary

| Option | Description | Selected |
|--------|-------------|----------|
| Delete legacy implementation and relocate retained validation | Remove callback/state/store code; move config validation out of a legacy-shaped `hitl.py` if needed. | ✓ |
| Keep `hitl.py` as config-only module | Preserve the module name with only validation helpers. | |
| Disable without deleting implementation | Leave legacy callback/store code present but unreachable. | |

**User's choice:** Approved strict deletion with validation moved to a neutral location if needed.
**Notes:** The key concern is avoiding active dead code that still looks like a supported legacy approval path.

---

## Legacy Mode Failure Behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Fail fast on explicit `legacy_sse` | Raise a clear `ValueError` during agent/config parsing. | ✓ |
| Silently migrate to `agui_interrupt` | Treat old configs as official approval mode. | |
| Accept but ignore legacy mode | Keep compatibility while making no behavioral change. | |

**User's choice:** Approved fail-fast behavior.
**Notes:** Omitting `hitl.mode` still defaults to `agui_interrupt`; explicit `legacy_sse` must not silently migrate.

---

## Frontend Cleanup Shape

| Option | Description | Selected |
|--------|-------------|----------|
| Remove legacy bridge/dialog/provider and toolbar status only | Delete EventSource UI path; leave official `AguiApprovalInterrupt` as the approval UI. | ✓ |
| Replace toolbar status with official AG-UI pending status | Add new UI behavior during cleanup. | |
| Keep hidden provider compatibility | Leave provider/dialog path in place but unused. | |

**User's choice:** Approved cleanup-only frontend scope.
**Notes:** A future official toolbar pending-status feature can be planned separately; 07.2 should not expand into UI redesign.

---

## Verification Gates

| Option | Description | Selected |
|--------|-------------|----------|
| Strict tests plus source-search gates | Backend/frontend regression tests and active-source absence checks are required. | ✓ |
| Tests only | Rely on unit/integration tests without residue searches. | |
| Source cleanup only | Remove references without official-path regression tests. | |

**User's choice:** Approved strict verification gates.
**Notes:** Historical `.planning` references may remain as chronology, but active source/tests/docs must not advertise or depend on legacy SSE.

---

## the agent's Discretion

- Exact destination for retained config validation is left to planning/implementation, constrained by the decision that legacy approval implementation must not remain active.
- Plan file splitting is left to the planner.

## Deferred Ideas

- Official AG-UI pending approval status in `ChatToolbar` is deferred to a future phase.
- `implement-resizable-sidebar.md` was reviewed as a false-positive todo match and not folded into this phase.
