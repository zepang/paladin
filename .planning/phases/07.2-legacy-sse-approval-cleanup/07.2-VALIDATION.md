---
phase: 07.2
slug: legacy-sse-approval-cleanup
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-03
---

# Phase 07.2 - Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Backend framework** | pytest via `apps/agent/pyproject.toml` |
| **Backend config file** | `apps/agent/pyproject.toml`; no separate `pytest.ini` found |
| **Backend quick run command** | `cd apps/agent && uv run pytest tests/test_agent.py tests/test_server.py -q` |
| **Backend legacy-delete command** | `cd apps/agent && uv run pytest tests/test_agent.py::test_hitl_mode_defaults_to_agui_interrupt tests/test_server.py -q` plus source-search gates |
| **Frontend framework** | Vitest via `apps/desktop/package.json` |
| **Frontend quick run command** | `cd apps/desktop && pnpm vitest run src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx` |
| **Full suite command** | Backend: `cd apps/agent && uv run pytest`; Frontend: `cd apps/desktop && pnpm vitest run`; build visibility: `cd apps/desktop && pnpm build` |
| **Estimated runtime** | Backend final gate: 3.46s pytest / 4.41s wall; frontend final gate: 979ms Vitest / 1.85s wall |

---

## Sampling Rate

- **After every task commit:** Run the targeted test for the touched tier plus the relevant source-search gate.
- **After every plan wave:** Run backend or frontend quick suite for the changed tier.
- **Before `$gsd-verify-work`:** Backend pytest subset, frontend approval Vitest, source-search gates, and docs gate must pass.
- **Max feedback latency:** Final Wave 0 gates completed in under 5 seconds for targeted backend/frontend checks.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 07.2-W0-01 | 07.2-01, 07.2-05 | 0/4 | 07.2-R1 | T-07.2-01 | Explicit `legacy_sse` cannot silently enable an obsolete approval path | unit | `cd apps/agent && uv run pytest tests/test_agent.py tests/test_server.py tests/test_agui_dependency_probe.py -q` | rewritten and verified | green |
| 07.2-W0-02 | 07.2-02, 07.2-05 | 0/4 | 07.2-R2 | T-07.2-02 | Legacy approval routes are not reachable | server test | `cd apps/agent && uv run pytest tests/test_agent.py tests/test_server.py tests/test_agui_dependency_probe.py -q` | route absence checks present | green |
| 07.2-W0-03 | 07.2-02, 07.2-05 | 0/4 | 07.2-R3 | T-07.2-03 | Request-id callback/store symbols are absent from active backend code/tests | source gate | `! rg -n "create_approval_callback|_pending_approvals|_pending_decisions|_sse_queue|resolve_approval|approval_store|/approval/stream|/approval/\\{request_id\\}" apps/agent/src apps/agent/tests` | legacy files deleted | green |
| 07.2-W0-04 | 07.2-03, 07.2-05 | 0/4 | 07.2-R4 | T-07.2-04 | Desktop legacy bridge/dialog/provider/status symbols are absent from active desktop code | source gate + Vitest | `cd apps/desktop && pnpm vitest run src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx`; `! rg -n "ApprovalBridge|ApprovalDialog|ApprovalProvider|useApprovalContext|/approval/stream|/approval/\\{request_id\\}" apps/desktop/src` | legacy files deleted | green |
| 07.2-W0-05 | 07.2-04, 07.2-05 | 0/4 | 07.2-R5 | T-07.2-05 | Active docs do not advertise legacy SSE as available | docs source gate | `! rg -n "legacy_sse|/approval/stream|/approval/\\{request_id\\}|ApprovalBridge|ApprovalDialog|approval_store" README.md docs apps/agent/README.md apps/desktop/README.md | rg -v "historical|deprecated|removed|prior behavior|legacy cleanup|07\\.2"` | active docs classified/updated | green |
| 07.2-W0-06 | 07.2-01..05 | 0/4 | 07.2-R6 | T-07.2-06 | Official AG-UI interrupt/resume behavior remains intact | regression | `cd apps/agent && uv run pytest tests/test_agent.py tests/test_server.py tests/test_agui_dependency_probe.py -q`; `cd apps/desktop && pnpm vitest run src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx`; `rg -n "AguiApprovalInterrupt|ApprovalCard|setInterruptElement|agent.runAgent\\(\\{ resume \\}\\)" apps/desktop/src` | present and verified | green |

*Status: pending, green, red, flaky.*

---

## Wave 0 Requirements

- [x] Rewrite `apps/agent/tests/test_agent.py::test_hitl_mode_allows_legacy_sse_fallback` into `test_hitl_mode_rejects_legacy_sse`.
- [x] Add `apps/agent/tests/test_server.py` route absence checks for `/approval/stream` and `/approval/{request_id}`.
- [x] Delete or rewrite `apps/agent/tests/test_hitl.py` and `apps/agent/tests/test_approval_store.py`.
- [x] Add or adjust frontend tests if `App.tsx` or `ChatToolbar.tsx` coverage is needed to prove no provider/context dependency.

## Final Wave 4 Gate Results

- Backend final gate passed: `cd apps/agent && uv run pytest tests/test_agent.py tests/test_server.py tests/test_agui_dependency_probe.py -q` returned `43 passed in 3.46s`.
- Backend active source/test gate passed with no matches for legacy callback/store/route symbols.
- Frontend final gate passed: `cd apps/desktop && pnpm vitest run src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx` returned `10 passed` in one test file.
- Desktop active source gate passed with no legacy bridge/dialog/provider/context or old approval endpoint matches.
- Official frontend preservation gate found `AguiApprovalInterrupt`, `ApprovalCard`, `setInterruptElement`, and `agent.runAgent({ resume })` in active desktop source.
- Active docs fallback gate passed with no unsupported matches outside historical/deprecated/removed/legacy-cleanup context.
- Residual build risk: full frontend build was not required for phase acceptance; known unrelated `Titlebar` TypeScript build errors remain outside 07.2 scope per SPEC if encountered by broader build gates.

---

## Manual-Only Verifications

All phase behaviors should have automated verification. Manual inspection is allowed only to classify historical `.planning` references that intentionally describe prior behavior rather than active support.

---

## Threat Coverage

| Threat Ref | Threat | Required Mitigation |
|------------|--------|---------------------|
| T-07.2-01 | Explicit legacy config silently enables obsolete approval semantics | `_approval_mode({"hitl": {"mode": "legacy_sse"}})` raises `ValueError` naming `legacy_sse` unsupported and `agui_interrupt` supported |
| T-07.2-02 | Legacy endpoint remains reachable and can spoof approval decisions | Remove `/approval/stream` and `/approval/{request_id}` routes and add route absence tests |
| T-07.2-03 | Request-id callback/store code remains reachable through imports | Delete active backend symbols and pass backend source-search gate |
| T-07.2-04 | Desktop legacy bridge still sends HTTP approval decisions | Delete active bridge/dialog/provider/status wiring and pass frontend source-search gate |
| T-07.2-05 | Active docs keep advertising unsupported fallback | Update active docs or classify matches as historical planning references |
| T-07.2-06 | Cleanup breaks official Computer Use approval | Preserve AG-UI interrupt/resume and `Tool(..., requires_approval=True)` regression coverage |

---

## Validation Sign-Off

- [x] All tasks have automated verify commands or Wave 0 dependencies.
- [x] Sampling continuity: no 3 consecutive tasks without automated verify.
- [x] Wave 0 covers all missing references.
- [x] No watch-mode flags in verification commands.
- [x] Feedback latency measured and recorded.
- [x] `nyquist_compliant: true` set in frontmatter after Wave 0 closes.

**Approval:** complete
