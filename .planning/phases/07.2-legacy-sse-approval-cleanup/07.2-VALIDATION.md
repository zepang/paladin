---
phase: 07.2
slug: legacy-sse-approval-cleanup
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-03
---

# Phase 07.2 - Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Backend framework** | pytest via `apps/agent/pyproject.toml` |
| **Backend config file** | `apps/agent/pyproject.toml`; no separate `pytest.ini` found |
| **Backend quick run command** | `cd apps/agent && uv run pytest tests/test_agent.py tests/test_server.py -q` |
| **Backend legacy-delete command** | `cd apps/agent && uv run pytest tests/test_agent.py::test_hitl_mode_defaults_to_agui_interrupt tests/test_server.py -q` plus source-search gates |
| **Frontend framework** | Vitest via `apps/desktop/package.json` |
| **Frontend quick run command** | `cd apps/desktop && pnpm vitest run src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx` |
| **Full suite command** | Backend: `cd apps/agent && uv run pytest`; Frontend: `cd apps/desktop && pnpm vitest run`; build visibility: `cd apps/desktop && pnpm build` |
| **Estimated runtime** | TBD by executor after first run |

---

## Sampling Rate

- **After every task commit:** Run the targeted test for the touched tier plus the relevant source-search gate.
- **After every plan wave:** Run backend or frontend quick suite for the changed tier.
- **Before `$gsd-verify-work`:** Backend pytest subset, frontend approval Vitest, source-search gates, and docs gate must pass.
- **Max feedback latency:** TBD after Wave 0 measures actual command runtimes.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 07.2-W0-01 | TBD | 0 | 07.2-R1 | T-07.2-01 | Explicit `legacy_sse` cannot silently enable an obsolete approval path | unit | `cd apps/agent && uv run pytest tests/test_agent.py::test_hitl_mode_defaults_to_agui_interrupt tests/test_agent.py::test_hitl_mode_rejects_legacy_sse -q` | rewrite needed | pending |
| 07.2-W0-02 | TBD | 0 | 07.2-R2 | T-07.2-02 | Legacy approval routes are not reachable | server test | `cd apps/agent && uv run pytest tests/test_server.py -q` | add route absence checks | pending |
| 07.2-W0-03 | TBD | 0 | 07.2-R3 | T-07.2-03 | Request-id callback/store symbols are absent from active backend code/tests | source gate | `rg -n "create_approval_callback|_pending_approvals|_pending_decisions|_sse_queue|resolve_approval|approval_store" apps/agent/src apps/agent/tests` | existing matches to remove | pending |
| 07.2-W0-04 | TBD | 0 | 07.2-R4 | T-07.2-04 | Desktop legacy bridge/dialog/provider/status symbols are absent from active desktop code | source gate + Vitest | `rg -n "ApprovalBridge|ApprovalDialog|ApprovalProvider|useApprovalContext|/approval/stream|/approval/\\{request_id\\}" apps/desktop/src`; `cd apps/desktop && pnpm vitest run src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx` | existing matches to remove | pending |
| 07.2-W0-05 | TBD | 0 | 07.2-R5 | T-07.2-05 | Active docs do not advertise legacy SSE as available | docs source gate | `rg -n "legacy_sse|/approval/stream|/approval/\\{request_id\\}|ApprovalBridge|ApprovalDialog|approval_store" README.md docs .planning/PROJECT.md .planning/ROADMAP.md .planning/STATE.md` | existing matches to classify/update | pending |
| 07.2-W0-06 | TBD | 0 | 07.2-R6 | T-07.2-06 | Official AG-UI interrupt/resume behavior remains intact | regression | `cd apps/agent && uv run pytest tests/test_agent.py tests/test_server.py tests/test_agui_dependency_probe.py -q`; `cd apps/desktop && pnpm vitest run src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx` | mostly present | pending |

*Status: pending, green, red, flaky.*

---

## Wave 0 Requirements

- [ ] Rewrite `apps/agent/tests/test_agent.py::test_hitl_mode_allows_legacy_sse_fallback` into `test_hitl_mode_rejects_legacy_sse`.
- [ ] Add `apps/agent/tests/test_server.py` route absence checks for `/approval/stream` and `/approval/{request_id}`.
- [ ] Delete or rewrite `apps/agent/tests/test_hitl.py` and `apps/agent/tests/test_approval_store.py`.
- [ ] Add or adjust frontend tests if `App.tsx` or `ChatToolbar.tsx` coverage is needed to prove no provider/context dependency.

---

## Manual-Only Verifications

All phase behaviors should have automated verification. Manual inspection is allowed only to classify historical `.planning` references that intentionally describe prior behavior rather than active support.

---

## Threat Coverage

| Threat Ref | Threat | Required Mitigation |
|------------|--------|---------------------|
| T-07.2-01 | Explicit legacy config silently enables obsolete approval semantics | `_approval_mode({"hitl": {"mode": "legacy_sse"}})` raises `ValueError` naming `legacy_sse` unsupported and `agui_interrupt` supported |
| T-07.2-02 | Legacy endpoint remains reachable and can spoof approval decisions | Remove `/approval/stream` and `/approval/{request_id}` routes and add route absence tests |
| T-07.2-03 | Request-id callback/store code remains reachable through imports | Delete active backend symbols and pass backend source-search gate |
| T-07.2-04 | Desktop legacy bridge still sends HTTP approval decisions | Delete active bridge/dialog/provider/status wiring and pass frontend source-search gate |
| T-07.2-05 | Active docs keep advertising unsupported fallback | Update active docs or classify matches as historical planning references |
| T-07.2-06 | Cleanup breaks official Computer Use approval | Preserve AG-UI interrupt/resume and `Tool(..., requires_approval=True)` regression coverage |

---

## Validation Sign-Off

- [ ] All tasks have automated verify commands or Wave 0 dependencies.
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify.
- [ ] Wave 0 covers all missing references.
- [ ] No watch-mode flags in verification commands.
- [ ] Feedback latency measured and recorded.
- [ ] `nyquist_compliant: true` set in frontmatter after Wave 0 closes.

**Approval:** pending
