---
phase: 07.2-legacy-sse-approval-cleanup
verified: 2026-07-03T10:16:40Z
status: passed
score: 6/6 must-haves verified
behavior_unverified: 0
overrides_applied: 0
gaps: []
---

# Phase 07.2: Legacy SSE Approval Cleanup Verification Report

**Phase Goal:** Remove the retained legacy SSE approval fallback now that the official AG-UI interrupt/resume path is verified.
**Verified:** 2026-07-03T10:16:40Z
**Status:** passed
**Re-verification:** No - initial verification

## Goal Achievement

Goal-backward result: the phase goal is achieved. Active backend source no longer supports the legacy request-id SSE/HTTP approval mode, active desktop source no longer mounts the legacy EventSource/dialog/provider/status path, and the official AG-UI interrupt/card/resume path remains wired and covered by targeted tests.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Explicit `hitl.mode = "legacy_sse"` fails fast; omitted mode defaults to `agui_interrupt`. | VERIFIED | `apps/agent/src/agent/paladin_agent.py:232` accepts only `agui_interrupt` and raises `ValueError` for `legacy_sse`; `create_paladin_agent()` calls `_approval_mode(raw_config)` before model/API-key initialization at `apps/agent/src/agent/paladin_agent.py:320`. `uv run pytest tests/test_agent.py -v` passed: 29 passed. |
| 2 | Backend legacy SSE approval routes, broadcast queues, and route handlers are absent or rejected. | VERIFIED | `apps/agent/src/server/main.py` exposes `/copilotkit`, `/copilotkit/info`, `/health`, `/info`, and thread endpoints only; no `StreamingResponse`, `_approval_queues`, `approval_stream`, or `approval_decision`. `uv run pytest tests/test_server.py tests/test_agui_dependency_probe.py -q` passed: 15 passed. |
| 3 | Request-id callback/store implementation is not active backend code. | VERIFIED | Deleted files are absent: `apps/agent/src/agent/hitl.py`, `apps/agent/src/agent/approval_store.py`, `apps/agent/tests/test_hitl.py`, `apps/agent/tests/test_approval_store.py`. Backend source/test gate produced no matches for `create_approval_callback`, `_pending_approvals`, `_pending_decisions`, `_sse_queue`, `resolve_approval`, `approval_store`, `/approval/stream`, or `/approval/{request_id}`. |
| 4 | Desktop legacy `ApprovalBridge`/`ApprovalDialog`/provider/status path is removed. | VERIFIED | Deleted files are absent: `apps/desktop/src/components/approval/ApprovalBridge.tsx` and `apps/desktop/src/components/approval/ApprovalDialog.tsx`. Desktop source gate produced no matches for `ApprovalBridge`, `ApprovalDialog`, `ApprovalProvider`, `useApprovalContext`, `/approval/stream`, or `/approval/{request_id}`. `ChatToolbar` shows context/tool-call information only at `apps/desktop/src/components/ChatToolbar.tsx:35`. |
| 5 | Official AG-UI approval card path is preserved. | VERIFIED | `ChatArea` mounts `AguiApprovalInterrupt` at `apps/desktop/src/components/ChatArea.tsx:39`; `AguiApprovalInterrupt` filters official `reason === "tool_call"` interrupts at `apps/desktop/src/components/approval/AguiApprovalInterrupt.tsx:51`, renders `ApprovalCard`, and submits `agent.runAgent({ resume })` at `apps/desktop/src/components/approval/AguiApprovalInterrupt.tsx:157`. `pnpm vitest run src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx` passed: 10 passed. |
| 6 | Active docs no longer guide workers toward the deleted legacy path. | VERIFIED | Active-doc gate passed: no unsupported matches outside `historical`, `deprecated`, `removed`, `prior behavior`, `legacy cleanup`, or `07.2` context. Recent commit `4f57ab7` also removed stale `test_hitl.py` guidance and clarified `AguiApprovalInterrupt`/`ApprovalCard` as the only active approval surface. |

**Score:** 6/6 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `apps/agent/src/agent/paladin_agent.py` | Official-only mode parser, neutral HITL validation, official approval wiring | VERIFIED | `_approval_mode` rejects `legacy_sse`; no legacy callback branch; `interrupt_on` remains populated from required tools and Computer Use approval tools. |
| `apps/agent/src/server/main.py` | Official `/copilotkit` adapter endpoint and no legacy approval routes | VERIFIED | `/copilotkit` delegates to `AGUIAdapter.dispatch_request()` with replayed body, agent, and deps. |
| `apps/agent/src/agent/hitl.py` | Deleted legacy request-id callback implementation | VERIFIED | File absent. |
| `apps/agent/src/agent/approval_store.py` | Deleted legacy approval store | VERIFIED | File absent. |
| `apps/desktop/src/components/approval/AguiApprovalInterrupt.tsx` | Official interrupt renderer and resume submitter | VERIFIED | Subscribes to AG-UI run-finished interrupts, renders `ApprovalCard`, submits one `resume[]` entry. |
| `apps/desktop/src/components/approval/ApprovalCard.tsx` | Preserved approval UI surface | VERIFIED | Renders approve/deny controls; targeted Vitest covers rendering and disabled states. |
| `apps/desktop/src/components/approval/ApprovalBridge.tsx` | Deleted legacy EventSource provider | VERIFIED | File absent. |
| `apps/desktop/src/components/approval/ApprovalDialog.tsx` | Deleted legacy dialog | VERIFIED | File absent. |
| `.planning/phases/07.2-legacy-sse-approval-cleanup/07.2-VALIDATION.md` | Final validation status | VERIFIED | Frontmatter has `status: complete`, `nyquist_compliant: true`, and `wave_0_complete: true`; final Wave 4 gate results are recorded. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `paladin_agent.py` | `pydantic_deep.create_deep_agent` | `interrupt_on=interrupt_on` | WIRED | Official deferred approval remains the only runtime approval wiring; `ToolGuard.approval_callback` is not installed. |
| `server/main.py` | `AGUIAdapter.dispatch_request` | `/copilotkit` request replay | WIRED | Server tests verify request, agent, deps, and `resume[]` body are preserved. |
| `ChatArea.tsx` | `AguiApprovalInterrupt` | direct mount beside `CopilotChat` | WIRED | Frontend test verifies official approval interrupt remains mounted in chat area. |
| `AguiApprovalInterrupt.tsx` | `ApprovalCard` and AG-UI resume | `setInterruptElement` and `agent.runAgent({ resume })` | WIRED | Vitest verifies official interrupt filtering, one-shot approve, and resume payload. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Agent approval policy and Computer Use approval remain intact | `cd apps/agent && uv run pytest tests/test_agent.py -v` | 29 passed | PASS |
| Server route absence and official AG-UI adapter/dependency coverage | `cd apps/agent && uv run pytest tests/test_server.py tests/test_agui_dependency_probe.py -q` | 15 passed | PASS |
| Frontend official approval interrupt/card/resume path | `cd apps/desktop && pnpm vitest run src/components/approval/__tests__/AguiApprovalInterrupt.test.tsx` | 1 file, 10 tests passed | PASS |
| Backend active legacy source/test absence | `! rg -n "create_approval_callback|_pending_approvals|_pending_decisions|_sse_queue|resolve_approval|approval_store|/approval/stream|/approval/\\{request_id\\}" apps/agent/src apps/agent/tests` | no matches | PASS |
| Desktop active legacy source absence | `! rg -n "ApprovalBridge|ApprovalDialog|ApprovalProvider|useApprovalContext|/approval/stream|/approval/\\{request_id\\}" apps/desktop/src` | no matches | PASS |
| Active-doc unsupported fallback absence | active docs gate below | no unsupported matches | PASS |

Active docs gate command:

```bash
! (rg -n "legacy_sse|/approval/stream|/approval/\\{request_id\\}|ApprovalBridge|ApprovalDialog|approval_store" README.md docs apps/agent/README.md apps/desktop/README.md | rg -v "historical|deprecated|removed|prior behavior|legacy cleanup|07\\.2")
```

### Probe Execution

No phase-specific `scripts/*/tests/probe-*.sh` probes were declared or discovered for Phase 07.2. Probe execution skipped as not applicable.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| 07.2-R1 | 07.2-01, 07.2-05 | Remove legacy mode selection; explicit legacy mode raises. | SATISFIED | `_approval_mode` and `create_paladin_agent` ordering verified; 29 `test_agent.py` tests passed. |
| 07.2-R2 | 07.2-02, 07.2-05 | Remove backend approval endpoints and broadcast infrastructure. | SATISFIED | Server route/source gates passed; server tests passed. |
| 07.2-R3 | 07.2-01, 07.2-02, 07.2-05 | Remove request-id callback/store code and legacy-only tests. | SATISFIED | Legacy modules/tests absent and backend source gate has no active matches. |
| 07.2-R4 | 07.2-03, 07.2-05 | Remove desktop bridge/dialog/provider/status wiring. | SATISFIED | Legacy desktop files absent and desktop source gate has no active matches. |
| 07.2-R5 | 07.2-04, 07.2-05 | Clean legacy-only tests and stale active documentation. | SATISFIED | Active-doc unsupported fallback gate passed; remaining old terms are historical/deprecated/removed context. |
| 07.2-R6 | 07.2-01, 07.2-02, 07.2-03, 07.2-05 | Preserve official AG-UI approval behavior. | SATISFIED | Backend adapter/resume tests and frontend approval interrupt tests passed. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---:|---|---|---|
| `apps/agent/src/agent/paladin_agent.py` | 195 | `return []` | INFO | Legitimate empty MCP server list when no MCP config exists; not a stub. |
| `apps/desktop/src/components/approval/AguiApprovalInterrupt.tsx` | 146, 178 | `return null` | INFO | Intentional headless interrupt registration component; UI is provided through `setInterruptElement`. |

No blocker debt markers (`TBD`, `FIXME`, `XXX`) were found in the checked 07.2 touched source/test files.

### Known Broader Suite Residuals

I did not rerun full-suite commands because the phase acceptance gates are targeted and already passed. The known broader-suite residuals are accurately scoped outside 07.2:

- `apps/agent` full pytest has unrelated `tests/test_computer_use.py::test_computer_screenshot` failure because `PIL` is not installed.
- `apps/desktop` full npm/Vitest run has an unrelated `Titlebar` drag test failure.

These do not block Phase 07.2 because the approval cleanup target gates passed and the failing areas are not part of legacy SSE approval cleanup.

### Human Verification Required

None. All Phase 07.2 must-haves have automated code, source-search, or targeted test evidence.

### Gaps Summary

No gaps found. Phase 07.2 achieved the goal: legacy SSE approval mode/routes/store/callback and desktop bridge/dialog/provider/status paths are removed from active code, active docs no longer present the deleted fallback as supported, and the official AG-UI interrupt/card/resume path remains preserved and verified.

---

_Verified: 2026-07-03T10:16:40Z_
_Verifier: the agent (gsd-verifier)_
