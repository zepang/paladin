---
phase: 07.3
plan: 07.3-01
title: Rust 依赖增量 + processes.json 配置骨架 + lib.rs 模块注册 (无行为)
type: execute
wave: 1
depends_on: []
requirements:
  - SDC-01
  - SDC-02
files_modified:
  - apps/desktop/src-tauri/Cargo.toml
  - apps/desktop/src-tauri/processes.json
  - apps/desktop/src-tauri/src/process/mod.rs
  - apps/desktop/src-tauri/src/process/supervisor.rs
  - apps/desktop/src-tauri/src/process/config.rs
  - apps/desktop/src-tauri/src/process/commands.rs
  - apps/desktop/src-tauri/src/lib.rs
autonomous: true
user_setup: []
must_haves:
  truths:
    - "SPEC R1 / Edge R1: apps/desktop/src-tauri/processes.json 必须存在，schema 形态满足 dev/packaged 双模式可切换（mode 字段 + processes map + 每个 process 的 cmd: Vec<String>）。"
    - "Cargo.toml 新增 tokio (rt-multi-thread/process/time/io-util/macros/sync features) + reqwest (rustls-tls，无 default-tls)，为 Wave 2 supervisor 提供运行时与探针客户端；不引入 tauri-plugin-shell（D-13 锁定）。"
    - "lib.rs 新增 `mod process;` 与 `use crate::process::ProcessSupervisor;`，process 模块四文件 (mod/supervisor/config/commands) 存在但仅含类型签名骨架，不注册 invoke_handler、不在 setup() 内 manage（避免 Wave 1 编译失败但行为零变更）。"
    - "process/config.rs 定义 ProcessConfig / ProcessEntry / HealthConfig / EndpointConfig / ProcessNameKey serde 结构体，字段命名与 processes.json 一一对应。"
    - "process/supervisor.rs 仅声明 ProcessState 枚举（5 variants: Starting/Running/Degraded/Unhealthy/Stopped，serde rename_all=lowercase）与 ProcessSupervisor 空结构体骨架；具体行为由 Wave 2 plan 06 填充。"
    - "process/commands.rs 文件存在但仅含占位 `// commands registered in plan 06`，6 个 #[tauri::command] 不在此 plan 实现。"
  prohibitions:
    - statement: "MUST NOT 引入 tauri-plugin-shell 依赖 (D-13 锁定，dev spawn 走 tokio::process 自研监督)。"
      status: resolved
      verification: "Cargo.toml grep 不含 tauri-plugin-shell 字串。"
    - statement: "MUST NOT 在本 plan 内 spawn 任何子进程、注册任何 #[tauri::command]、在 setup() 内 app.manage(ProcessSupervisor)（行为零变更，仅为 Wave 2 铺垫）。"
      status: resolved
      verification: "lib.rs setup() 闭包内不出现 ProcessSupervisor 字串；invoke_handler! 宏不新增 process::commands 入口。"
    - statement: "MUST NOT 在 processes.json 中使用单字符串 cmd (SPEC Prohibition P1 命令注入防御)；cmd 字段必须是 JSON 数组形态。"
      status: resolved
      verification: "processes.json 内每个 cmd 字段都是 [\"...\", \"...\"] 数组形态，无 \"cmd\": \"foo bar\" 单串。"
  artifacts:
    - path: "apps/desktop/src-tauri/Cargo.toml"
      provides: "tokio + reqwest 依赖增量"
      exports: []
    - path: "apps/desktop/src-tauri/processes.json"
      provides: "dev 模式 Agent + Go Server 进程描述（cmd/cwd/env/port/health/startup_grace_secs）"
      exports: []
    - path: "apps/desktop/src-tauri/src/process/mod.rs"
      provides: "process 模块入口 (pub mod + 共享类型 re-export)"
      exports: ["process::ProcessState", "process::ProcessName", "process::ProcessSupervisor", "process::ProcessConfig"]
    - path: "apps/desktop/src-tauri/src/process/config.rs"
      provides: "ProcessConfig / ProcessEntry / HealthConfig / EndpointConfig / ProcessNameKey serde 结构体（仅类型，loader 在 plan 03 实现）"
      exports: ["process::config::ProcessConfig", "process::config::ProcessEntry", "process::config::HealthConfig", "process::config::EndpointConfig", "process::config::ProcessNameKey"]
    - path: "apps/desktop/src-tauri/src/process/supervisor.rs"
      provides: "ProcessSupervisor 空骨架 + ProcessState 枚举"
      exports: ["process::supervisor::ProcessSupervisor"]
    - path: "apps/desktop/src-tauri/src/process/commands.rs"
      provides: "命令占位（6 个 #[tauri::command] 在 plan 06 实现）"
      exports: []
  key_links:
    - from: "apps/desktop/src-tauri/src/lib.rs"
      to: "apps/desktop/src-tauri/src/process/mod.rs"
      via: "mod process; 引入新模块"
      pattern: "mod process;"
---

<objective>
为 Wave 2 supervisor 实现 铺垫地基：补齐 Cargo 依赖（tokio + reqwest）、创建 processes.json 配置文件、建立 process/ 模块四文件骨架（mod/supervisor/config/commands）与共享类型定义、在 lib.rs 注册 `mod process;`。本 plan **不实现任何子进程行为**，不注册 invoke_handler，不在 setup() 内 manage(ProcessSupervisor)——仅为后续 plan 提供编译通过的「类型骨架 + 配置文件 + 依赖」。完成后 `cargo check -p paladin-desktop` 通过。

Purpose: PATTERNS §0 已确认 Cargo.toml 零 tokio/reqwest、processes.json 不存在、lib.rs 只有 terminal/file_commands 两模块。本 plan 把这些空白补齐到「可以开始写 supervisor 主体」的最小状态。
Output: cargo check 通过；process/ 模块四文件存在；processes.json dev 模式配置就位；Cargo.toml 含 tokio rt-multi-thread+process+time+io-util+macros+sync + reqwest rustls-tls。
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.3-sidecar-process-management/07.3-CONTEXT.md
@.planning/phases/07.3-sidecar-process-management/07.3-SPEC.md
@.planning/phases/07.3-sidecar-process-management/07.3-PATTERNS.md
@apps/desktop/src-tauri/Cargo.toml
@apps/desktop/src-tauri/src/lib.rs
@apps/desktop/src-tauri/src/terminal/mod.rs
</context>

## Artifacts this phase produces

- `apps/desktop/src-tauri/Cargo.toml` — 新增 `tokio = { version = "1", features = ["rt-multi-thread", "process", "time", "io-util", "macros", "sync"] }` 与 `reqwest = { version = "0.12", default-features = false, features = ["rustls-tls"] }`。
- `apps/desktop/src-tauri/processes.json` — dev 模式配置：`{ "mode": "dev", "processes": { "agent": {...}, "server": {...} }, "backoff_secs": [1,2,4,8,16], "max_restarts": 5, "shutdown_grace_secs": 5 }`。Agent cmd = `["uv", "run", "paladin-agent", "serve", "--dev"]` cwd `../../apps/agent`，port 9876，health.liveness.path=`/health` expect_status=200；Server cmd = `["go", "run", "./cmd/server"]` cwd `../../apps/server`，port 9880，health.liveness.path=`/healthz` + health.readiness.path=`/readyz`。
- `apps/desktop/src-tauri/src/process/mod.rs` — `pub mod commands; pub mod config; pub mod supervisor;` + `pub use config::ProcessConfig; pub use supervisor::{ProcessState, ProcessName, ProcessSupervisor};`。
- `apps/desktop/src-tauri/src/process/config.rs` — serde 结构体：`ProcessConfig { mode, processes, backoff_secs, max_restarts, shutdown_grace_secs }`、`ProcessEntry { cmd: Vec<String>, cwd, env, port, health, startup_grace_secs }`、`HealthConfig { liveness, readiness }`、`EndpointConfig { path, expect_status }`、`enum ProcessNameKey { Agent, Server }`。
- `apps/desktop/src-tauri/src/process/supervisor.rs` — `enum ProcessState { Starting, Running, Degraded, Unhealthy, Stopped }` (serde rename_all=lowercase, Copy, Clone, Eq, PartialEq, Debug, Serialize, Deserialize)；`enum ProcessName { Agent, Server }`；`struct ProcessSupervisor { /* 字段在 plan 06 填充 */ }` 空骨架（含 `pub fn new(...) -> Self` 与 `#[allow(dead_code)]` 标注的字段占位）。
- `apps/desktop/src-tauri/src/process/commands.rs` — 占位注释 `// 6 commands implemented in plan 07.3-06`。
- `apps/desktop/src-tauri/src/lib.rs` — 在 `mod file_commands;` 后追加 `mod process;` 与 `use crate::process::ProcessSupervisor;`（use 暂时空引用，加 `#[allow(unused_imports)]`）。**不**修改 invoke_handler!、**不**修改 setup() 闭包。

<tasks>

<task id="07.3-01-01" type="execute">
  <name>Task 1: 新增 Cargo.toml tokio + reqwest 依赖</name>
  <description>在 apps/desktop/src-tauri/Cargo.toml 的 [dependencies] 段追加 tokio 与 reqwest 两个 crate。tokio 必须含 process feature（用于 spawn 子进程）+ time（探针周期）+ rt-multi-thread + macros + sync + io-util；reqwest 用 rustls-tls 避免 Windows OpenSSL 依赖。**不**引入 tauri-plugin-shell。</description>
  <read_first>
    - `.planning/phases/07.3-sidecar-process-management/07.3-PATTERNS.md` §0 与 §2.7（Cargo 增量建议 + D-13 不引 shell plugin）
    - `.planning/phases/07.3-sidecar-process-management/07.3-SPEC.md` §Constraints (跨平台 Windows 无 SIGTERM，需 rustls-tls 避免 OpenSSL)
    - `apps/desktop/src-tauri/Cargo.toml`（当前依赖列表）
  </read_first>
  <action>在 [dependencies] 末尾追加两行：`tokio = { version = "1", features = ["rt-multi-thread", "process", "time", "io-util", "macros", "sync"] }` 和 `reqwest = { version = "0.12", default-features = false, features = ["rustls-tls"] }`。运行 `cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` 拉取并验证编译。</action>
  <verify>
    <automated>grep -E '^tokio\s*=|^reqwest\s*=' apps/desktop/src-tauri/Cargo.toml &amp;&amp; cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>Cargo.toml 含 `tokio = { version = "1", features = ["rt-multi-thread", "process", "time", "io-util", "macros", "sync"] }`；含 `reqwest = { version = "0.12", default-features = false, features = ["rustls-tls"] }`；不含 `tauri-plugin-shell`；`cargo check` 退出 0。</acceptance_criteria>
  <done>Acceptance criteria above are met.</done>
</task>

<task id="07.3-01-02" type="execute">
  <name>Task 2: 创建 processes.json dev 模式配置文件</name>
  <description>在 apps/desktop/src-tauri/processes.json 写入 dev 模式进程描述。schema 形态对齐 PATTERNS §10 建议（D-14 自决已采纳）。两个进程 cmd 字段都是 string array（SPEC Prohibition P1 命令注入防御）。</description>
  <read_first>
    - `.planning/phases/07.3-sidecar-process-management/07.3-PATTERNS.md` §10 (processes.json schema 建议) 与 §9 (健康端点协议)
    - `.planning/phases/07.3-sidecar-process-management/07.3-SPEC.md` §Constraints (端口 9876/9880 固定、startup_grace_secs=5、shutdown_grace_secs=5、backoff 1/2/4/8/16、max_restarts 5)
    - `apps/agent/src/server/cli.py` 与 `apps/server/cmd/server/main.go` 确认实际命令行参数
  </read_first>
  <action>创建 `apps/desktop/src-tauri/processes.json`，内容为：`{ "mode": "dev", "processes": { "agent": { "cmd": ["uv", "run", "paladin-agent", "serve", "--dev"], "cwd": "../../apps/agent", "env": {}, "port": 9876, "health": { "liveness": { "path": "/health", "expect_status": 200 } }, "startup_grace_secs": 5 }, "server": { "cmd": ["go", "run", "./cmd/server"], "cwd": "../../apps/server", "env": {}, "port": 9880, "health": { "liveness": { "path": "/healthz", "expect_status": 200 }, "readiness": { "path": "/readyz", "expect_status": 200 } }, "startup_grace_secs": 5 } }, "backoff_secs": [1, 2, 4, 8, 16], "max_restarts": 5, "shutdown_grace_secs": 5 }`。验证 JSON 合法：`python3 -c "import json; json.load(open('apps/desktop/src-tauri/processes.json'))"` 或 `jq . apps/desktop/src-tauri/processes.json > /dev/null`。</action>
  <verify>
    <automated>test -f apps/desktop/src-tauri/processes.json &amp;&amp; jq '.processes.agent.cmd | type, .processes.server.cmd | type, .backoff_secs, .max_restarts, .shutdown_grace_secs' apps/desktop/src-tauri/processes.json</automated>
  </verify>
  <acceptance_criteria>processes.json 存在且 JSON 合法；`.processes.agent.cmd` 与 `.processes.server.cmd` 均为 array 类型（非 string）；`.backoff_secs == [1,2,4,8,16]`；`.max_restarts == 5`；`.shutdown_grace_secs == 5`；`.processes.agent.port == 9876`；`.processes.server.port == 9880`；`.processes.agent.health.liveness.path == "/health"`；`.processes.server.health.liveness.path == "/healthz"`；`.processes.server.health.readiness.path == "/readyz"`。</acceptance_criteria>
  <done>Acceptance criteria above are met.</done>
</task>

<task id="07.3-01-03" type="execute">
  <name>Task 3: 创建 process/ 模块四文件骨架 + 共享类型</name>
  <description>在 apps/desktop/src-tauri/src/process/ 下创建 mod.rs / config.rs / supervisor.rs / commands.rs 四文件。仅定义类型签名，不实现任何 spawn/probe/restart 行为。supervisor.rs 内 ProcessSupervisor 为空 struct（字段在 plan 06 填充），加 `#[allow(dead_code)]` 避免 Wave 1 编译告警。</description>
  <read_first>
    - `.planning/phases/07.3-sidecar-process-management/07.3-PATTERNS.md` §2.1 (模块组织) 与 §2.2 (ProcessState 枚举范式) 与 §2.3 (ProcessSupervisor 范式) 与 §10 (Rust serde 草案)
    - `apps/desktop/src-tauri/src/terminal/mod.rs`（参照 `pub mod commands;` 顶层骨架 + TerminalState enum derive 模式）
    - `.planning/phases/07.3-sidecar-process-management/07.3-SPEC.md` §Acceptance (5 状态名 starting/running/degraded/unhealthy/stopped, lowercase)
  </read_first>
  <action>创建 `src/process/mod.rs`：声明三个子模块并 re-export 关键类型 `pub use config::ProcessConfig; pub use supervisor::{ProcessName, ProcessState, ProcessSupervisor};`。创建 `src/process/config.rs`：用 serde derive 定义 `ProcessConfig { pub mode: String, pub processes: HashMap<ProcessNameKey, ProcessEntry>, pub backoff_secs: Vec<u64>, pub max_restarts: u32, pub shutdown_grace_secs: u64 }`、`ProcessEntry { pub cmd: Vec<String>, pub cwd: Option<PathBuf>, pub env: HashMap<String,String>, pub port: u16, pub health: HealthConfig, pub startup_grace_secs: u64 }`、`HealthConfig { pub liveness: EndpointConfig, pub readiness: Option<EndpointConfig> }`、`EndpointConfig { pub path: String, pub expect_status: u16 }`、`#[derive(Hash,Eq,PartialEq)] #[serde(rename_all="lowercase")] enum ProcessNameKey { Agent, Server }`，全部 `#[derive(Debug, Clone, Deserialize)]`（ProcessConfig 额外加 Serialize 以便前端透传）。创建 `src/process/supervisor.rs`：`#[derive(Debug,Clone,Copy,PartialEq,Eq,Serialize,Deserialize)] #[serde(rename_all="lowercase")] pub enum ProcessState { Starting, Running, Degraded, Unhealthy, Stopped }`；`#[derive(Debug,Clone,Copy,PartialEq,Eq,Serialize,Deserialize)] #[serde(rename_all="lowercase")] pub enum ProcessName { Agent, Server }`；`pub struct ProcessSupervisor { /* placeholder; fields added in plan 06 */ }` 加 `#[allow(dead_code)]`。创建 `src/process/commands.rs`：仅一行注释 `// 6 commands implemented in plan 07.3-06`。</action>
  <verify>
    <automated>test -f apps/desktop/src-tauri/src/process/mod.rs &amp;&amp; test -f apps/desktop/src-tauri/src/process/config.rs &amp;&amp; test -f apps/desktop/src-tauri/src/process/supervisor.rs &amp;&amp; test -f apps/desktop/src-tauri/src/process/commands.rs &amp;&amp; grep -q 'pub enum ProcessState' apps/desktop/src-tauri/src/process/supervisor.rs &amp;&amp; grep -q 'pub struct ProcessConfig' apps/desktop/src-tauri/src/process/config.rs</automated>
  </verify>
  <acceptance_criteria>四个文件均存在；supervisor.rs 含 `pub enum ProcessState` 且 variants 数=5（Starting/Running/Degraded/Unhealthy/Stopped），serde rename_all=lowercase；config.rs 含 `pub struct ProcessConfig` 与 `pub struct ProcessEntry`，ProcessEntry.cmd 字段类型为 `Vec<String>`；mod.rs 含三条 `pub use`；commands.rs 仅含注释。</acceptance_criteria>
  <done>Acceptance criteria above are met.</done>
</task>

<task id="07.3-01-04" type="execute">
  <name>Task 4: 在 lib.rs 注册 mod process; 与 use 引入（零行为变更）</name>
  <description>在 apps/desktop/src-tauri/src/lib.rs 顶部模块声明区追加 `mod process;` 与 `#[allow(unused_imports)] use crate::process::ProcessSupervisor;`。**不**修改 invoke_handler! 宏、**不**修改 setup() 闭包——本 plan 仅让模块纳入编译，避免 dead_code 告警升级为错误。</description>
  <read_first>
    - `apps/desktop/src-tauri/src/lib.rs`（当前 mod 声明 + use 引入 + invoke_handler! + setup()）
    - `.planning/phases/07.3-sidecar-process-management/07.3-PATTERNS.md` §2.6 (lib.rs 改造点 — 仅注册部分)
  </read_first>
  <action>在 `mod terminal;` 与 `mod file_commands;` 之后追加 `mod process;` 一行；在 `use crate::terminal::TerminalManager;` 之后追加 `#[allow(unused_imports)] use crate::process::ProcessSupervisor;`。运行 `cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` 验证编译通过、无 error（warning 允许，dead_code 在 Wave 2 plan 06 实现后消失）。</action>
  <verify>
    <automated>grep -E '^mod process;' apps/desktop/src-tauri/src/lib.rs &amp;&amp; grep -E 'use crate::process::ProcessSupervisor' apps/desktop/src-tauri/src/lib.rs &amp;&amp; cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>lib.rs 含 `mod process;` 与 `use crate::process::ProcessSupervisor;`；invoke_handler! 列表未新增 process::commands 入口（即不含 `process::commands::restart_agent` 等字串）；setup() 闭包内不含 `ProcessSupervisor::new` 或 `app.manage` 调用 ProcessSupervisor；`cargo check` 退出 0。</acceptance_criteria>
  <done>Acceptance criteria above are met.</done>
</task>

</tasks>

<verification>
执行 Wave 1 静态验收命令：

`cd apps/desktop/src-tauri && cargo check`

`grep -E '^tokio\s*=|^reqwest\s*=' Cargo.toml`

`grep -c 'tauri-plugin-shell' Cargo.toml` (期望 0)

`jq '.backoff_secs, .max_restarts, .processes.agent.cmd | type' processes.json`

`test -f src/process/mod.rs && test -f src/process/config.rs && test -f src/process/supervisor.rs && test -f src/process/commands.rs`

`grep -E '^mod process;|use crate::process::ProcessSupervisor' src/lib.rs`

`grep -c 'process::commands::' src/lib.rs` (期望 0 — 命令未注册)

完成后无运行时行为变更，桌面端启动后**仍不会**自动 spawn 子进程（这是 Wave 2 plan 06 的职责）。
</verification>

<success_criteria>
- Cargo.toml 含 tokio (process/time/rt-multi-thread/macros/sync/io-util features) 与 reqwest (rustls-tls, default-features=false)；不含 tauri-plugin-shell。
- processes.json 存在且为合法 JSON；cmd 字段均为数组；backoff_secs=[1,2,4,8,16]；max_restarts=5；shutdown_grace_secs=5；端口 9876/9880；健康端点 /health、/healthz、/readyz。
- process/ 模块四文件存在；ProcessState 枚举 5 variants 含 serde rename_all=lowercase；ProcessConfig/ProcessEntry/HealthConfig/EndpointConfig/ProcessNameKey serde 结构体就位。
- lib.rs 含 `mod process;` 与 use 引入；invoke_handler! 与 setup() 未修改。
- `cargo check` 退出 0，无 error。
</success_criteria>

<output>
Create `.planning/phases/07.3-sidecar-process-management/07.3-01-SUMMARY.md` when done, listing the exact tokio/reqwest version resolved by Cargo.lock, the processes.json content, and the four module file headers.
</output>
