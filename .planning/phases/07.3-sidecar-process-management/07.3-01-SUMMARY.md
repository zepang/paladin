---
phase: 07.3-sidecar-process-management
plan: 01
subsystem: infra
tags: [tauri, tokio, reqwest, sidecar, rust, process-supervisor]

# Dependency graph
requires:
  - phase: 08-go-server
    provides: Go Server /healthz + /readyz 端点 (端口 9880) 与命令行入口
  - phase: 07.1-official-ag-ui-deferred-approval
    provides: Python Agent `serve --dev` CLI (端口 9876) 与 /health 端点
provides:
  - Cargo 依赖 tokio (rt-multi-thread/process/time/io-util/macros/sync) + reqwest (rustls-tls)
  - processes.json dev 模式进程描述 (cmd 数组形态, 9876/9880, /health//healthz//readyz)
  - process/ 模块四文件骨架 (mod/supervisor/config/commands) 与 serde 共享类型
  - lib.rs 内 `mod process;` 注册 (零行为变更)
affects: [07.3-03 (config loader), 07.3-06 (supervisor 主体 + 6 commands), phase-10-packaging (processes.json mode 切换)]

# Tech tracking
tech-stack:
  added: [tokio 1.52.3, reqwest 0.12.28]
  patterns: [Tauri mod 注册零行为铺垫, serde rename_all=lowercase 状态机枚举, processes.json cmd 数组形态 (命令注入防御 P1)]

key-files:
  created:
    - apps/desktop/src-tauri/processes.json
    - apps/desktop/src-tauri/src/process/mod.rs
    - apps/desktop/src-tauri/src/process/config.rs
    - apps/desktop/src-tauri/src/process/supervisor.rs
    - apps/desktop/src-tauri/src/process/commands.rs
  modified:
    - apps/desktop/src-tauri/Cargo.toml
    - apps/desktop/src-tauri/Cargo.lock
    - apps/desktop/src-tauri/src/lib.rs

key-decisions:
  - "Cargo.lock 解析版本: tokio 1.52.3, reqwest 0.12.28 (rustls-tls, default-features=false)"
  - "不引入 tauri-plugin-shell (D-13 锁定, dev spawn 走 tokio::process 自研监督)"
  - "processes.json cwd 用 ../../apps/agent 与 ../../apps/server (相对 src-tauri)"

patterns-established:
  - "Pattern: 骨架 plan 先建类型 + 配置 + 依赖,行为在后续 plan 填充,lib.rs 仅 mod 注册避免行为泄漏"
  - "Pattern: 配置层 ProcessNameKey (Hash/Eq) 与运行时 ProcessName 解耦,经 impl From 桥接 (待 plan 06)"
  - "Pattern: cmd 字段强制 Vec<String> (SPEC Prohibition P1),tokio::process::Command 默认不走 shell"

requirements-completed: [SDC-01, SDC-02]

# Coverage metadata — 每个交付物一项,均为自动化验证通过
coverage:
  - id: D1
    description: "Cargo.toml 新增 tokio (process/time/rt-multi-thread/macros/sync/io-util) + reqwest (rustls-tls) 依赖,不含 tauri-plugin-shell"
    requirement: "SDC-01"
    verification:
      - kind: other
        ref: "grep -E '^tokio\\s*=|^reqwest\\s*=' Cargo.toml && grep -c tauri-plugin-shell (==0)"
        status: pass
      - kind: integration
        ref: "cargo check (exit 0)"
        status: pass
    human_judgment: false
  - id: D2
    description: "processes.json dev 模式配置: agent (uv run, 9876, /health) + server (go run, 9880, /healthz+/readyz), cmd 均为数组, backoff [1,2,4,8,16], max_restarts 5, shutdown_grace 5"
    requirement: "SDC-02"
    verification:
      - kind: other
        ref: "python3 json assertions (cmd type=array, backoff_secs, max_restarts, shutdown_grace_secs, ports, health paths)"
        status: pass
    human_judgment: false
  - id: D3
    description: "process/ 模块四文件骨架: mod.rs (pub use 4 类型) + config.rs (ProcessConfig/ProcessEntry/HealthConfig/EndpointConfig/ProcessNameKey serde) + supervisor.rs (ProcessState 5 variants serde lowercase + ProcessName + ProcessSupervisor 空骨架) + commands.rs (注释占位)"
    requirement: "SDC-01"
    verification:
      - kind: other
        ref: "test -f 四文件 + grep pub enum ProcessState (5 variants) + grep pub struct ProcessConfig/ProcessEntry (Vec<String> cmd) + rename_all=lowercase"
        status: pass
      - kind: integration
        ref: "cargo check (exit 0, 13 dead_code warnings 预期)"
        status: pass
    human_judgment: false
  - id: D4
    description: "lib.rs 注册 mod process; + use crate::process::ProcessSupervisor (#[allow(unused_imports)]);invoke_handler! 与 setup() 未修改,零运行时行为变更"
    requirement: "SDC-01"
    verification:
      - kind: other
        ref: "grep '^mod process;' + grep 'use crate::process::ProcessSupervisor' + grep -c process::commands:: (==0) + grep -c ProcessSupervisor::new in lib (==0)"
        status: pass
      - kind: integration
        ref: "cargo check (exit 0)"
        status: pass
    human_judgment: false

# Metrics
duration: 7min
completed: 2026-07-05
status: complete
---

# Phase 07.3 Plan 01: Rust 依赖增量 + processes.json 配置骨架 + lib.rs 模块注册 Summary

**为 Wave 2 supervisor 主体实现铺垫地基:补齐 tokio (1.52.3) + reqwest (0.12.28) 依赖、就位 processes.json dev 模式进程描述、建立 process/ 模块四文件类型骨架并在 lib.rs 注册 `mod process;`——零运行时行为变更,`cargo check` 通过。**

## Performance

- **Duration:** 7 min
- **Started:** 2026-07-05T08:19:33Z
- **Completed:** 2026-07-05T08:26:23Z
- **Tasks:** 4
- **Files modified:** 8 (5 created, 3 modified)

## Accomplishments

- **Cargo 依赖增量**:`apps/desktop/src-tauri/Cargo.toml` 新增 `tokio = { version = "1", features = ["rt-multi-thread", "process", "time", "io-util", "macros", "sync"] }` 与 `reqwest = { version = "0.12", default-features = false, features = ["rustls-tls"] }`,Cargo.lock 解析为 tokio 1.52.3 / reqwest 0.12.28。**不**引入 `tauri-plugin-shell`(D-13 锁定)。
- **processes.json dev 模式配置**:`apps/desktop/src-tauri/processes.json` 就位——agent (`uv run paladin-agent serve --dev`, 9876, `/health`) + server (`go run ./cmd/server`, 9880, `/healthz` + `/readyz`);`cmd` 字段均为数组形态(SPEC Prohibition P1);`backoff_secs=[1,2,4,8,16]`,`max_restarts=5`,`shutdown_grace_secs=5`(继承自 Phase 7a 锁定约束)。
- **process 模块四文件骨架**:`src/process/{mod,config,supervisor,commands}.rs` 建立——`mod.rs` re-export `ProcessConfig`/`ProcessState`/`ProcessName`/`ProcessSupervisor`;`config.rs` 定义 5 个 serde 结构体(`ProcessConfig`/`ProcessEntry`/`HealthConfig`/`EndpointConfig`/`ProcessNameKey`);`supervisor.rs` 声明 `ProcessState` 5 variants(serde rename_all="lowercase")+ `ProcessName` + `ProcessSupervisor` 空骨架(`#[allow(dead_code)]`);`commands.rs` 仅占位注释(6 commands 在 plan 07.3-06)。
- **lib.rs 零行为注册**:`mod process;` 与 `#[allow(unused_imports)] use crate::process::ProcessSupervisor;` 就位;`invoke_handler!` 与 `setup()` 闭包**未**修改——桌面端启动后**仍不会** spawn 任何子进程(Wave 2 plan 06 的职责)。

## Task Commits

每个任务原子提交:

1. **Task 1: 新增 Cargo.toml tokio + reqwest 依赖** — `a527b79` (chore)
2. **Task 2: 创建 processes.json dev 模式配置文件** — `97076b3` (chore)
3. **Task 3: 创建 process/ 模块四文件骨架 + 共享类型** — `806a809` (feat)
4. **Task 4: 在 lib.rs 注册 mod process; 与 use 引入(零行为变更)** — `5722dfe` (feat)

**Plan metadata:** 待本 SUMMARY 提交生成。

## Files Created/Modified

- `apps/desktop/src-tauri/Cargo.toml` — 新增 tokio + reqwest 依赖行
- `apps/desktop/src-tauri/Cargo.lock` — 依赖锁定(tokio 1.52.3 / reqwest 0.12.28,含传递依赖)
- `apps/desktop/src-tauri/processes.json` — dev 模式进程描述(agent + server,cmd 数组形态)
- `apps/desktop/src-tauri/src/process/mod.rs` — 模块入口 + 共享类型 re-export
- `apps/desktop/src-tauri/src/process/config.rs` — `ProcessConfig`/`ProcessEntry`/`HealthConfig`/`EndpointConfig`/`ProcessNameKey` serde 结构体
- `apps/desktop/src-tauri/src/process/supervisor.rs` — `ProcessState` 5 variants + `ProcessName` + `ProcessSupervisor` 空骨架
- `apps/desktop/src-tauri/src/process/commands.rs` — 占位注释(6 commands 在 plan 07.3-06)
- `apps/desktop/src-tauri/src/lib.rs` — `mod process;` + `use` 引入(`#[allow(unused_imports)]`)

## Decisions Made

- **Cargo.lock 解析版本**:tokio 1.52.3、reqwest 0.12.28(由 `cargo check` 拉取锁定,记入 SUMMARY 供 plan 06/Phase 10 引用)。
- **processes.json cwd 路径**:采用 `../../apps/agent` 与 `../../apps/server`(相对 `src-tauri` 目录),与 PLAN task action 指定一致。
- **ProcessSupervisor 保留 `new()` 占位 + `Default` impl**:空 struct 上提供无参 `new()` 与 `Default`,plan 06 替换为真实构造器(app_handle/config/log_dir)。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking compile] 给 config.rs 非 ProcessConfig 类型补 Serialize derive**
- **Found during:** Task 3(process 模块骨架)
- **Issue:** PLAN action 指定"全部 `#[derive(Debug, Clone, Deserialize)]`(ProcessConfig 额外加 Serialize)"。但 `ProcessConfig` derive `Serialize` 时,其字段 `processes: HashMap<ProcessNameKey, ProcessEntry>` 的 key/value 类型必须也实现 `Serialize`,否则 `cargo check` 编译失败(Task 4 plan 级硬门)。
- **Fix:** 给 `ProcessNameKey`/`ProcessEntry`/`HealthConfig`/`EndpointConfig` 均额外加 `Serialize` derive。
- **Files modified:** `apps/desktop/src-tauri/src/process/config.rs`
- **Verification:** `cargo check` 退出 0(零 error);前端透传场景(SPEC R7 `get_process_status`)也需要这些类型可序列化,改动方向正确。
- **Committed in:** `806a809`(Task 3 commit)

**2. [Rule 1 - Plan bug] cargo check 包名纠正**
- **Found during:** Plan-level verification
- **Issue:** PLAN `<verification>` 段与 prompt 验收期望 `cargo check -p paladin-desktop`,但 `Cargo.toml` 的 `name = "paladin"`(无 `-desktop` 后缀),`-p paladin-desktop` 会报 "package not found"。
- **Fix:** 改用 `cd apps/desktop/src-tauri && cargo check`(等价且正确,task action 内的命令本身就是这种写法)。
- **Files modified:** 无(仅验证命令选择)
- **Verification:** `cargo check` 退出 0。
- **Committed in:** N/A(验证命令层面,无源码改动)

**3. [Rule 1 - Workflow instruction conflicts with factual correctness] 跳过 `requirements.mark-complete SDC-01 SDC-02`**
- **Found during:** Step 10(update_requirements)
- **Issue:** PLAN frontmatter `requirements: [SDC-01, SDC-02]`,但 PLAN objective 与 prohibitions 明确声明本 plan "不实现任何子进程行为"、"不在 setup() 内 manage(ProcessSupervisor)"。SDC-01/02 的实际完成(spawn + 健康监测 + 重启)需待 plan 06 与后续 plans。机械执行 `mark-complete` 会把 REQUIREMENTS.md 的 SDC-01/02 勾选为已完成,记录与实际不符的状态。
- **Fix:** SUMMARY frontmatter `requirements-completed` 仍按模板规则 verbatim 复制 `[SDC-01, SDC-02]`(元数据追溯),但**不**调用 `gsd query requirements.mark-complete`。SDC-01/02/03 的勾选留待 phase 07.3 全部 plan(plan 06 实现命令、UAT 通过)后由 phase 级 close-out 处理。
- **Files modified:** 无(状态机层面,跳过一次工具调用)
- **Verification:** REQUIREMENTS.md 的 SDC-01/02 仍为 `[ ]`(未勾选),与"零运行时行为"事实一致。
- **Committed in:** N/A

---

**Total deviations:** 3 auto-fixed(2 Rule 1 plan bug,1 Rule 3 blocking compile)
**Impact on plan:** 全部为编译/事实正确性所必需,无 scope creep。Rule 3 是 serde derive 完整性补全;Rule 1 #2 仅修正验证命令选择;Rule 1 #3 避免记录虚假完成状态。Plan 的核心交付(依赖 + 配置 + 骨架 + lib.rs 注册)完全按规范实现。

## Issues Encountered

- **`jq` 不可用**:plan verification 与 task 2 `<verify>` 用 `jq` 断言 processes.json。环境无 jq,改用 `python3 -c` 做等价 JSON 断言(全部通过)。已在 deviation #N/A 记录——非源码问题,仅验证工具替换。

## User Setup Required

None — 无外部服务配置需要。本 plan 仅铺垫 Rust 依赖与配置骨架,不 spawn 子进程,不需要 PG/Redis 或 Agent/Server 运行。

## Next Phase Readiness

- **Ready for plan 07.3-03**(processes.json loader + 校验):`ProcessConfig` serde 结构体已就位,loader 可直接 `serde_json::from_reader` + 校验 `mode` 字段与 cmd 数组约束。
- **Ready for plan 07.3-06**(supervisor 主体 + 6 commands):`ProcessSupervisor` 空骨架 + `ProcessState`/`ProcessName` 枚举已声明,plan 06 填充字段(agent/server ManagedProcess、config、app_handle、log_dir)、实现 spawn/probe/restart/backoff/shutdown/log,并在 commands.rs 实现 6 个 `#[tauri::command]`、在 lib.rs `invoke_handler!` 注册、在 setup() `app.manage(ProcessSupervisor::new(...))`。
- **零行为变更已验证**:桌面端启动行为与 plan 01 前完全一致(`process::commands::` 在 lib.rs 命中 0 次,setup() 闭包不含 `ProcessSupervisor::new`/`app.manage`)。
- **无阻塞**:13 个 dead_code 警告全部源自本骨架的未使用类型/方法,plan 06 实现后自然消失,不影响 plan 01 验收。

## Self-Check: PASSED

- ✅ `[ -f ]` 四个 process 模块文件均存在
- ✅ `git log --oneline --grep="07.3-01"` 返回 ≥ 4 个 commit(a527b79 / 97076b3 / 806a809 / 5722dfe)
- ✅ 重跑全部 task `<acceptance_criteria>`:Task 1(tokio/reqwest/无 shell plugin/cargo check 0)、Task 2(processes.json 10 项断言)、Task 3(4 文件 + ProcessState 5 variants + Vec<String> cmd + rename_all)、Task 4(mod/use + 0 commands 注册 + 0 manage + cargo check 0)——全部 PASS
- ✅ 重跑 plan 级 `<verification>` 7 条命令:全部 PASS(详见上方)

---
*Phase: 07.3-sidecar-process-management*
*Completed: 2026-07-05*
