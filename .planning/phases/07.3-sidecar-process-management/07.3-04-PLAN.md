---
phase: 07.3
plan: 07.3-04
title: shadcn Popover 基础组件补建 (@base-ui/react)
type: execute
wave: 1
depends_on: []
requirements:
  - SDC-03
files_modified:
  - apps/desktop/src/components/ui/popover.tsx
autonomous: true
user_setup: []
must_haves:
  truths:
    - "PATTERNS §0 关键纠偏: apps/desktop/src/components/ui/popover.tsx 此前不存在（全仓 Grep Popover 0 命中），CONTEXT D-11 假设的「Phase 5.1 已引入」不成立。本 plan 补齐该 GAP，为 Wave 4 的 ProcessLight 控制菜单（D-11）提供基础组件。"
    - "components.json style=base-nova + package.json @base-ui/react ^1.5.0 已确认；popover.tsx MUST 以 @base-ui/react/popover 为底层（已核实导出: Root/Trigger/Portal/Positioner/Popup/Backdrop/Close/Title/Description），NOT @radix-ui/react-popover。"
    - "popover.tsx 导出三个具名组件 Popover / PopoverTrigger / PopoverContent，签名与 alert-dialog.tsx 同构（函数式组件 + data-slot + cn 合并 className + 透传 props），可被 ProcessLight 用 render={<Button/>} as-child 模式触发。"
    - "PopoverContent 默认样式与 alert-dialog.tsx 内容区视觉一致 (z-50 / rounded-md / border-border / bg-popover / text-popover-foreground / shadow-md)，含 data-starting-style / data-ending-style 透明度过渡。"
  prohibitions:
    - statement: "MUST NOT 引入 @radix-ui/react-popover 或任何 radix 依赖 (components.json style=base-nova 锁定 base-ui 底层)。"
      status: resolved
      verification: "popover.tsx import 行只含 @base-ui/react/popover，无 @radix-ui 字串；package.json 不新增 radix 依赖。"
    - statement: "MUST NOT 在本 plan 内实现任何业务逻辑（restart/stop 命令调用、状态读取）；Popover 仅是受控浮层原语，业务消费在 plan 08 ProcessLight。"
      status: resolved
      verification: "popover.tsx 不含 invoke() / useProcessStore / restart_agent 字串；仅做 Root/Trigger/Portal/Positioner/Popup 透传。"
  artifacts:
    - path: "apps/desktop/src/components/ui/popover.tsx"
      provides: "Popover / PopoverTrigger / PopoverContent 三个具名导出，base-ui 受控浮层原语"
      exports: ["./popover#Popover", "./popover#PopoverTrigger", "./popover#PopoverContent"]
  key_links:
    - from: "apps/desktop/src/components/ui/popover.tsx"
      to: "apps/desktop/src/components/StatusBar/ProcessLight.tsx"
      via: "plan 08 ProcessLight 用 Popover 包裹 restart/stop/查看日志 控制菜单 (D-11)"
      pattern: "import { Popover, PopoverTrigger, PopoverContent }"
---

<objective>
补建 PATTERNS §0 确认缺失的 shadcn Popover 基础组件。CONTEXT D-11 依赖该组件实现进程灯点击弹控制菜单，但仓库内 components/ui/ 下只有 alert-dialog/button/scroll-area/sheet/sonner 五个组件，popover.tsx 不存在。本 plan 以项目已装的 @base-ui/react ^1.5.0 为底层（与 alert-dialog.tsx 同源），手写一个三导出的 popover.tsx，为 Wave 4 ProcessLight 解锁。

Purpose: 把「基础组件缺失」从 Wave 4 的阻塞路径提前到 Wave 1，让 ProcessLight (plan 08) 与 LogsPanel (plan 09) 可并行消费。
Output: popover.tsx 存在且 tsc --noEmit 通过；导出 Popover / PopoverTrigger / PopoverContent；不引入 radix。
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.3-sidecar-process-management/07.3-PATTERNS.md
@.planning/phases/07.3-sidecar-process-management/07.3-CONTEXT.md
@apps/desktop/src/components/ui/alert-dialog.tsx
@apps/desktop/components.json
@apps/desktop/package.json
</context>

## Artifacts this phase produces

- `apps/desktop/src/components/ui/popover.tsx` — 三具名导出：
  - `Popover`（包 `@base-ui/react/popover` 的 `Root`，透传 `Root.Props`，加 `data-slot="popover"`）
  - `PopoverTrigger`（包 `Trigger`，透传 `Trigger.Props`，加 `data-slot="popover-trigger"`，支持 base-ui 的 `render` prop as-child 模式）
  - `PopoverContent`（组合 `Portal` + `Positioner`（`sideOffset` 默认 4）+ `Popup`，`className` 用 `cn` 合并 `z-50 w-72 rounded-md border border-border bg-popover p-4 text-popover-foreground shadow-md` 与 `data-starting-style:opacity-0 data-ending-style:opacity-0` 过渡）
- 不新增 package.json 依赖（@base-ui/react 已在）。

<tasks>

<task id="07.3-04-01" type="execute">
  <name>Task 1: 创建 popover.tsx 三导出基础组件</name>
  <description>参照 alert-dialog.tsx 的函数式组件 + data-slot + cn 模式，用 @base-ui/react/popover 的 Root/Trigger/Portal/Positioner/Popup 五个原语组装 Popover/PopoverTrigger/PopoverContent。实施前用 `ls node_modules/@base-ui/react/popover/` 核实导出名（已预检：root/trigger/portal/positioner/popup/backdrop/close/title/description/arrow/viewport 都在）。</description>
  <read_first>
    - `apps/desktop/src/components/ui/alert-dialog.tsx`（同源模板：函数式组件 + data-slot + cn + base-ui 透传 props 模式）
    - `.planning/phases/07.3-sidecar-process-management/07.3-PATTERNS.md` §0（Popover GAP 确认）+ §6.1（base-ui Popover 途径 A 写法）
    - `apps/desktop/components.json`（style=base-nova，确认 base-ui 底层）
    - `apps/desktop/package.json`（确认 @base-ui/react ^1.5.0 已装、无 radix）
  </read_first>
  <action>创建 `apps/desktop/src/components/ui/popover.tsx`：顶部 `import { Popover as PopoverPrimitive } from '@base-ui/react/popover';` + `import type * as React from 'react';` + `import { cn } from '@/lib/utils';`。定义 `function Popover({ ...props }: PopoverPrimitive.Root.Props) { return <PopoverPrimitive.Root data-slot="popover" {...props} />; }`；定义 `function PopoverTrigger({ ...props }: PopoverPrimitive.Trigger.Props) { return <PopoverPrimitive.Trigger data-slot="popover-trigger" {...props} />; }`；定义 `function PopoverContent({ className, ...props }: PopoverPrimitive.Popup.Props) { return ( <PopoverPrimitive.Portal> <PopoverPrimitive.Positioner sideOffset={4}> <PopoverPrimitive.Popup data-slot="popover-content" className={cn('z-50 w-72 rounded-md border border-border bg-popover p-4 text-popover-foreground shadow-md data-starting-style:opacity-0 data-ending-style:opacity-0', className)} {...props} /> </PopoverPrimitive.Positioner> </PopoverPrimitive.Portal> ); }`。底部 `export { Popover, PopoverTrigger, PopoverContent };`。若 base-ui 类型名与预检不一致（如 `Positioner` 实际叫 `Positioner` 还是别的），以 `node_modules/@base-ui/react/popover/index.d.ts` 实际导出为准微调。</action>
  <verify>
    <automated>cd apps/desktop &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -10 &amp;&amp; grep -E '^export \{ Popover' src/components/ui/popover.tsx &amp;&amp; grep -c '@radix-ui' src/components/ui/popover.tsx</automated>
  </verify>
  <acceptance_criteria>apps/desktop/src/components/ui/popover.tsx 存在；含 `import { Popover as PopoverPrimitive } from '@base-ui/react/popover'`；不含 `@radix-ui` 字串（grep 计数 0）；导出 Popover / PopoverTrigger / PopoverContent 三个具名；`npx tsc --noEmit` 在 apps/desktop 退出 0 无类型错误；package.json 未新增依赖（git diff package.json 仅可能含 lockfile 间接变化，无新 dependencies 条目）。</acceptance_criteria>
  <done>Acceptance criteria above are met.</done>
</task>

<task id="07.3-04-02" type="execute">
  <name>Task 2: 视觉与交互自检 — render Storybook 风格最小验证</name>
  <description>项目无 Storybook，本 task 用一次性最小 React 元素 + biome/lint 自检替代：在 popover.tsx 之外不新建文件，仅用 grep 确认组件可被 import 路径解析，并用 biome check 验证文件风格一致。若 base-ui Trigger 的 as-child prop 名不是 `render`（PATTERNS §6.2 注释了这点），在此 task 修正 ProcessLight 将要用的 prop 名记录到 SUMMARY。</description>
  <read_first>
    - `apps/desktop/src/components/ui/popover.tsx`（Task 1 产物）
    - `apps/desktop/src/components/ui/alert-dialog.tsx`（对照 biome 风格）
    - `apps/desktop/node_modules/@base-ui/react/popover/trigger/index.d.ts`（核实 as-child prop 名 — 用 `cat` 查看 TriggerProps 的 children/render/RenderProps 字段名）
  </read_first>
  <action>运行 `cd apps/desktop && npx biome check src/components/ui/popover.tsx`（0 error）。运行 `cat node_modules/@base-ui/react/popover/trigger/index.d.ts | head -40` 确认 Trigger 的 as-child prop 实际名（若为 `render` 则与 PATTERNS §6.2 一致；若为 `asChild` 或别的，记录差异）。运行 `grep -r "from '@/components/ui/popover'" src/` 确认 0 业务命中（本 plan 不消费，仅声明可用）。</action>
  <verify>
    <automated>cd apps/desktop &amp;&amp; npx biome check src/components/ui/popover.tsx 2>&amp;1 | tail -5 &amp;&amp; grep -rc "from '@/components/ui/popover'" src/ | grep -v ':0' || echo "no_consumers_ok"</automated>
  </verify>
  <acceptance_criteria>`npx biome check src/components/ui/popover.tsx` 退出 0 无 error；`grep "from '@/components/ui/popover'" src/` 当前 0 业务命中（消费在 plan 08）；Trigger as-child prop 名已核实并记录（render 或 asChild 之一）。</acceptance_criteria>
  <done>Acceptance criteria above are met.</done>
</task>

</tasks>

<verification>
执行 Wave 1 Popover 补建验收命令：

`test -f apps/desktop/src/components/ui/popover.tsx`

`grep -E "from '@base-ui/react/popover'" apps/desktop/src/components/ui/popover.tsx`

`grep -c '@radix-ui' apps/desktop/src/components/ui/popover.tsx` (期望 0)

`cd apps/desktop && npx tsc --noEmit && npx biome check src/components/ui/popover.tsx`

完成后 Popover 基础组件就位，plan 08 ProcessLight 与 plan 09 LogsPanel 可直接 `import { Popover, PopoverTrigger, PopoverContent } from '@/components/ui/popover'`。
</verification>

<success_criteria>
- apps/desktop/src/components/ui/popover.tsx 存在，导出 Popover / PopoverTrigger / PopoverContent。
- 底层为 @base-ui/react/popover（NOT radix），与 alert-dialog.tsx 同构。
- tsc --noEmit + biome check 双绿。
- PATTERNS §0 GAP 闭环。
</success_criteria>

<output>
Create `.planning/phases/07.3-sidecar-process-management/07.3-04-SUMMARY.md` when done, listing the three exports, the base-ui primitives used, and the Trigger as-child prop name (render vs asChild) for plan 08 consumption.
</output>
