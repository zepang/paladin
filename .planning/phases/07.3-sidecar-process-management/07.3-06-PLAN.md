---
phase: 07.3
plan: 07.3-06
title: ProcessSupervisor 主体 + 6 Tauri commands + setup() wiring + 日志捕获/落盘/脱敏
type: execute
wave: 2
depends_on:
  - 07.3-01
  - 07.3-02
  - 07.3-03
  - 07.3-05
requirements:
  - SDC-01
  - SDC-02
  - SDC-03
files_modified:
  - apps/desktop/src-tauri/src/process/supervisor.rs
  - apps/desktop/src-tauri/src/process/commands.rs
  - apps/desktop/src-tauri/src/process/mod.rs
  - apps/desktop/src-tauri/src/lib.rs
  - apps/desktop/src-tauri/Cargo.toml
autonomous: true
user_setup:
  - "UAT 需要本机具备 uv（apps/agent）与 go（apps/server）工具链；若缺失，spawn 会进入启动失败 → stopped 路径，可观察但仍需真实工具链做 healthy 验证。"
  - "UAT 需要 Postgres+Redis 运行（docker-compose.server.yml）才能让 Go /readyz 通过；缺失时 Go 应稳定在 degraded，不重启。"
must_haves:
  truths:
    - "SPEC R1 / Edge R1: setup() 内 ProcessConfig::load_from_path(processes.json) 失败时返回 Err，supervisor 不 spawn 任何进程，Tauri 启动失败暴露配置错误（plan 03 loader 已校验，本 plan 在 setup 闭包 ? 传播）。"
    - "SPEC R1 / Edge R9: spawn_one(name) 在 tokio::process::Command 前 is_port_available(port) 预检，false 时返回 PortInUse 错误、不 spawn、emit {name, state: Stopped, last_error: '端口 9876 已被占用'}；true 时 spawn 并记录 tracked Child handle 到 ManagedProcess（P4：kill 只针对此 handle，绝不来自 stale PID 文件）。"
    - "SPEC R2 / Edge R2: probe_loop 每 10s 用 reqwest::blocking（或 async）GET liveness endpoint（Agent /health、Go /healthz）；非 200 或连接失败计 fail_count+1；fail_count >= 3（PROBE_FAIL_THRESHOLD）触发 Unhealthy + backoff 重启。Go /readyz 非 200 仅置 Degraded，不计 fail_count、不重启（SPEC R3）。"
    - "SPEC R3 / Edge R3: spawn 后 tokio::time::Instant 计时，5s 窗口内 child exit → SpawnFailed 事件 → transition 返回 Stopped（不走 backoff）；>5s exit → Crashed 事件 → transition 返回 Unhealthy + restart_count+1 → backoff 序列重启。"
    - "SPEC R4 / Prohibition P3: 5 次 backoff 全失败（restart_count >= max_restarts=5）→ Stopped 终态，emit stopped；之后 5 分钟内无自动 spawn（无后台 timer 唤醒 stopped 进程，只有手动 restart_agent/server 或 app 重启才脱离）。"
    - "SPEC R5 / Edge R5 empty / Prohibition P4: Drop::drop 调用 graceful_shutdown_all — 先 stop probe/log task（AtomicBool 旗标），再对每个 tracked Child handle 发 SIGTERM（Unix: nix crate 或 libc::kill；Windows: child.kill() 走 Job Object/TerminateProcess），等 5s grace（shutdown_grace_secs），超时 child.kill().await 强制 kill。关闭顺序 Agent → Server（与启动相反）。SIGTERM 对已死进程为 no-op（child.wait() 立即返回）。"
    - "SPEC R6 / Edge R6 / D-04: log capture task 读 child stdout/stderr（tokio::AsyncBufRead），每行 redact_log_line(&line) 脱敏后同时（a）append 到 Tauri app_local_data_dir/log/{paladin-agent.log, paladin-server.log}（D-04 锁定的落盘位置：macOS ~/Library/Application Support/com.worsmer.paladin/logs/、Windows %LOCALAPPDATA%/com.worsmer.paladin/logs、Linux ~/.local/share/com.worsmer.paladin/logs，跨平台由 Tauri API 自动解析，dev 与 packaged 一致）（b）app.emit('process-log', LogChunk{process, stream, line, ts}）。log_dir 不可写时 create_dir_all 失败 → 降级为仅 emit + eprintln warning，不阻塞 spawn（SPEC Edge R6）。"
    - "SPEC R7 / Edge R7: 6 个 #[tauri::command] — restart_agent / stop_agent / restart_server / stop_server / get_process_status / get_runtime_config。get_process_status 首探完成前返回 {agent: {state: Starting}, server: {state: Starting}}；restart_X 内部用 tokio::sync::Mutex 串行化，已在 restart 中时返回 Ok(()) with in-progress 标志不二次 spawn；手动 restart 清零 fail_count + restart_count（state_machine RestartRequested 已实现）。"
    - "SPEC R8 / D-07: get_runtime_config 返回 {agent_url: 'http://localhost:9876', server_url: 'http://localhost:9880'}，URL 来自 ProcessConfig.processes.{agent,server}.port 构造，单一 Rust 真相源。"
    - "SPEC Prohibition P1: spawn_one 的 Command::new(cmd[0]).args(cmd[1..]) 仅来自 ProcessConfig 静态字段，runtime chat/agent 输出无法影响 cmd；不走 shell（tokio::process::Command 默认不引 shell）。"
    - "SPEC Prohibition P2: 每次 state 变 Unhealthy/Stopped 时 emit 'process-status' payload 含 last_error（退出原因）+ stderr_tail（末尾 N 行 stderr 摘要），UI 必须显示「为什么重启」（前端消费在 plan 07/08）。"
    - "SPEC Edge R5 empty 启动顺序: setup() 先 spawn Server → 等待 /readyz 通过（或 5s startup grace 超时降级 degraded）→ 再 spawn Agent（Agent 不依赖 PG/Redis 但 CopilotKit 走 Agent，提前暴露端口）。"
  prohibitions:
    - statement: "MUST NOT 执行来自不可信/远程输入的命令串；只有随应用发布的本地 processes.json 可作为命令源；运行时 chat/agent 输出 MUST NOT 影响子进程命令行 (SPEC Prohibition P1)。"
      status: resolved
      verification: "spawn_one 函数签名仅接受 &ProcessEntry（来自 ProcessConfig），不接受运行时 String；grep spawn_one 不含 format!/push! 拼接 cmd 元素；integration test 断言改变 Tauri invoke 参数不影响 cmd。"
    - statement: "MUST NOT 静默重启而不把最近退出原因 + 末尾 stderr 暴露给用户；UI 必须显示「为什么重启」(SPEC Prohibition P2)。"
      status: resolved
      verification: "每次 Unhealthy/Stopped 状态迁移时 app.emit('process-status', {name, state, last_error: Some(...), stderr_tail: Some(...)}); integration test 断言 emit 的 payload 含非空 last_error 与 stderr_tail。"
    - statement: "处于 stopped 状态的进程 MUST NOT 在无显式用户动作时自动恢复 spawn；后台定时器 MUST NOT 唤醒 stopped 进程 (SPEC Prohibition P3)。"
      status: resolved
      verification: "supervisor 不注册任何基于 interval 的 stopped→starting 定时器；probe_loop 在 state==Stopped 时退出循环；integration test 让进程进入 stopped 后 sleep 5s，断言无新 spawn（PID 不变）。"
    - statement: "MUST NOT 凭裸 PID 跨会话 kill 进程；kill 只能针对当前会话自己 spawn 的 tracked Child handle，绝不来自 stale 文件/匹配的 PID (SPEC Prohibition P4)。"
      status: resolved
      verification: "graceful_shutdown 只调用 ManagedProcess.child.start_kill()/kill()（tokio::process::Child 方法），不读 PID 文件、不调用系统 kill 命令匹配 PID；负向测试：子进程死后 PID 被哨兵复用，Drop 时不杀哨兵。"
  artifacts:
    - path: "apps/desktop/src-tauri/src/process/supervisor.rs"
      provides: "ProcessSupervisor 主体 — spawn_one / probe_loop / restart_with_backoff / graceful_shutdown / emit_status / log_capture_task；ManagedProcess { child: Option<tokio::process::Child>, state, snapshot, stderr_ring_buffer }"
      exports: ["process::supervisor::ProcessSupervisor", "process::supervisor::ManagedProcess", "process::supervisor::ProcessStatusSnapshot", "process::supervisor::LogChunk", "process::supervisor::RuntimeConfig"]
    - path: "apps/desktop/src-tauri/src/process/commands.rs"
      provides: "6 个 #[tauri::command]: restart_agent / stop_agent / restart_server / stop_server / get_process_status / get_runtime_config"
      exports: ["process::commands::restart_agent", "process::commands::stop_agent", "process::commands::restart_server", "process::commands::stop_server", "process::commands::get_process_status", "process::commands::get_runtime_config"]
    - path: "apps/desktop/src-tauri/src/lib.rs"
      provides: "setup() 内 ProcessSupervisor::new + app.manage + state.start()；invoke_handler! 追加 6 命令"
      exports: []
    - path: "apps/desktop/src-tauri/Cargo.toml"
      provides: "nix = \"0.29\" (Unix SIGTERM，cfg(unix)) — Windows 用 tokio Child::kill 走 Job Object 无需额外 crate"
      exports: []
  key_links:
    - from: "apps/desktop/src-tauri/src/process/supervisor.rs"
      to: "apps/desktop/src-tauri/src/process/state_machine.rs"
      via: "supervisor 每 event 调 transition(current, event, &snap) 决策下一步状态"
      pattern: "state_machine::transition"
    - from: "apps/desktop/src-tauri/src/process/supervisor.rs"
      to: "apps/desktop/src-tauri/src/process/port_probe.rs"
      via: "spawn_one 调 is_port_available(port) 预检"
      pattern: "port_probe::is_port_available"
    - from: "apps/desktop/src-tauri/src/process/supervisor.rs"
      to: "apps/desktop/src-tauri/src/process/log_redact.rs"
      via: "log_capture_task 每行 redact_log_line 后 emit + append"
      pattern: "log_redact::redact_log_line"
    - from: "apps/desktop/src-tauri/src/lib.rs"
      to: "apps/desktop/src-tauri/src/process/supervisor.rs"
      via: "setup() app.manage(ProcessSupervisor::new(app.handle().clone(), cfg, log_dir)) + state.start()"
      pattern: "ProcessSupervisor::new"
---

<objective>
实现 Phase 07.3 的核心交付物 —— Rust 监督器主体。本 plan 把 plan 01/02/03/05 已验证的纯函数（state machine、config loader、port probe、log redact）与 tokio::process 子进程管理、Tauri Channel/State/Emit、reqwest 探针、文件落盘、跨平台终止机制组装成一个完整的 ProcessSupervisor，并在 lib.rs setup() 注册。

启动时序（SPEC Edge R5）：load processes.json → 创建 log_dir → ProcessSupervisor::new → app.manage → state.start() → spawn Server → 等 /readyz 或 5s grace → spawn Agent → 启动各自 probe_loop + log_capture_task。运行期：probe_loop 10s 周期探针，event 经 transition 决策，状态变化 emit 'process-status'；child exit 监听走 Crashed/SpawnFailed 分支；手动 restart/stop 命令串行化（tokio::sync::Mutex）。关闭：Drop 旗标 stop task → 对每个 tracked Child SIGTERM → 5s grace → 强制 kill，顺序 Agent→Server。

Purpose: 让 Tauri 成为 Python Agent 与 Go Server 的唯一托管者，开发者无需手动 uv run / go run。
Output: 桌面端启动后 5s 内两子进程可见（ps）；kill 子进程 ≤30s 收到 unhealthy 事件、≤~40s 恢复 healthy；关闭桌面端 ps 无残留。
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07.3-sidecar-process-management/07.3-SPEC.md
@.planning/phases/07.3-sidecar-process-management/07.3-CONTEXT.md
@.planning/phases/07.3-sidecar-process-management/07.3-PATTERNS.md
@apps/desktop/src-tauri/src/terminal/mod.rs
@apps/desktop/src-tauri/src/lib.rs
@apps/desktop/src-tauri/processes.json
@apps/desktop/src-tauri/src/process/state_machine.rs
@apps/desktop/src-tauri/src/process/config.rs
@apps/desktop/src-tauri/src/process/port_probe.rs
@apps/desktop/src-tauri/src/process/log_redact.rs
</context>

## Artifacts this phase produces

**Rust 类型与函数（supervisor.rs）：**
- `pub struct ManagedProcess { pub name: ProcessName, pub state: ProcessState, pub snapshot: SupervisorSnapshot, pub child: Option<tokio::process::Child>, pub stderr_tail: std::collections::VecDeque<String>, pub shutdown_flag: Arc<AtomicBool>, pub restart_in_progress: Arc<tokio::sync::Mutex<bool>> }`
- `pub struct ProcessSupervisor { agent: tokio::sync::Mutex<ManagedProcess>, server: tokio::sync::Mutex<ManagedProcess>, config: ProcessConfig, app_handle: AppHandle, log_dir: PathBuf }`
- `#[derive(Serialize, Clone)] pub struct ProcessStatusSnapshot { pub agent: ProcessInfoDTO, pub server: ProcessInfoDTO }` / `ProcessInfoDTO { state: ProcessState, last_error: Option<String>, stderr_tail: Option<String>, last_restart_at: Option<i64> }`
- `#[derive(Serialize, Clone)] pub struct LogChunk { pub process: ProcessName, pub stream: LogStream, pub line: String, pub ts: i64 }` / `enum LogStream { Stdout, Stderr }`
- `#[derive(Serialize, Clone)] pub struct RuntimeConfig { pub agent_url: String, pub server_url: String }`
- `impl ProcessSupervisor { pub fn new(app_handle, config, log_dir) -> Self; pub fn start(&self) -> Result<(), String>; pub fn restart(&self, name) -> Result<(), String>; pub fn stop(&self, name) -> Result<(), String>; pub fn status(&self) -> ProcessStatusSnapshot; pub fn runtime_config(&self) -> RuntimeConfig; fn spawn_one(&self, name) -> Result<(), String>; fn probe_loop(&self, name); fn log_capture_task(&self, name, stdout: ChildStdout, stderr: ChildStderr); fn wait_exit(&self, name); fn graceful_shutdown_one(&self, mp: &mut ManagedProcess); fn graceful_shutdown_all(&self); fn emit_status(&self, name, last_error: Option<String>); }`
- `impl Drop for ProcessSupervisor { fn drop(&mut self) { self.graceful_shutdown_all(); } }`

**Tauri 命令（commands.rs）：**
- `restart_agent(state: State<ProcessSupervisor>) -> Result<(), String>`
- `stop_agent(state: State<ProcessSupervisor>) -> Result<(), String>`
- `restart_server(state: State<ProcessSupervisor>) -> Result<(), String>`
- `stop_server(state: State<ProcessSupervisor>) -> Result<(), String>`
- `get_process_status(state: State<ProcessSupervisor>) -> ProcessStatusSnapshot`
- `get_runtime_config(state: State<ProcessSupervisor>) -> RuntimeConfig`

**Tauri 事件名（emit）：**
- `process-status` — payload `ProcessInfoDTO { name, state, last_error, stderr_tail, last_restart_at }`（每次状态变化）
- `process-log` — payload `LogChunk { process, stream, line, ts }`（每行子进程输出，脱敏后）

**lib.rs 集成：**
- `mod process;`（plan 01 已加）+ `use crate::process::ProcessSupervisor;`（plan 01 已加，去掉 `#[allow(unused_imports)]`）
- `invoke_handler!` 追加 6 个 process::commands 入口
- `setup()` 内在 `app.manage(TerminalManager::new(...))` 之后追加：加载 processes.json → 计算 log_dir → `app.manage(ProcessSupervisor::new(...))` → `app.state::<ProcessSupervisor>().start()`

**Cargo.toml 增量：**
- `nix = { version = "0.29", features = ["signal"] }`（Unix only，cfg(unix)）— Windows 用 tokio Child::kill 走 Job Object

**日志文件（运行时产物）：**
- `app_local_data_dir/log/paladin-agent.log` / `paladin-server.log`（无界追加，v1 限制）

<tasks>

<task id="07.3-06-01" type="execute">
  <name>Task 1: supervisor.rs 类型骨架 + spawn_one + port 预检 + emit_status</name>
  <description>把 plan 01 的空骨架 ProcessSupervisor 填充为完整类型定义 + spawn_one 实现。spawn_one 调 is_port_available 预检（plan 05），用 tokio::process::Command::new(cmd[0]).args(cmd[1..]) spawn（不走 shell），child stdout/stderr 通过 tokio spawn 两个 log_capture_task（具体脱敏 + emit 在 Task 4），child handle 存入 ManagedProcess.child。spawn 后记录 spawn_time 用于 5s 启动失败窗口判定。</description>
  <read_first>
    - `.planning/phases/07.3-sidecar-process-management/07.3-SPEC.md` §Requirements R1 + §Prohibitions P1/P4 + §Edge Coverage R1/R9
    - `.planning/phases/07.3-sidecar-process-management/07.3-PATTERNS.md` §2.3（ProcessSupervisor 范式）+ §2.4（spawn + Channel）+ §2.9（跨平台终止）
    - `apps/desktop/src-tauri/src/terminal/mod.rs`（TerminalManager spawn_command + thread + emit 范式）
    - `apps/desktop/src-tauri/src/process/state_machine.rs`（plan 02 — transition 决策内核）
    - `apps/desktop/src-tauri/src/process/port_probe.rs`（plan 05 — is_port_available）
    - `apps/desktop/src-tauri/src/process/config.rs`（plan 03 — ProcessConfig 结构）
    - `apps/desktop/src-tauri/src/process/supervisor.rs`（plan 01 空骨架 + ProcessState enum）
  </read_first>
  <action>在 supervisor.rs 追加：定义 ManagedProcess / ProcessStatusSnapshot / ProcessInfoDTO / LogChunk / LogStream / RuntimeConfig 结构体（derive Serialize Clone，字段如 artifacts 所列）。实现 ProcessSupervisor::new(app_handle, config, log_dir) 初始化两个 ManagedProcess（state=Starting, child=None, stderr_tail=VecDeque::with_capacity(50), shutdown_flag=Arc::new(AtomicBool::new(false)), restart_in_progress=Arc::new(Mutex::new(false))）。实现 spawn_one(&self, name) -> Result<(), String>：（1）从 config.processes 取对应 ProcessEntry；（2）port_probe::is_port_available(entry.port) 为 false 时返回 Err 并 self.emit_status(name, Some(format!("端口 {} 已被占用", entry.port)))；（3）tokio::process::Command::new(&entry.cmd[0]).args(&entry.cmd[1..])，cwd 设置 entry.cwd，envs 设置 entry.env.iter()，stdout(Stdio::piped()).stderr(Stdio::piped()).spawn()；（4）取 child.stdout / child.stderr，tokio::spawn 两个 log_capture_task（Task 4 填充内部，本 task 先占位 tokio::spawn(async move {})）；（5）tokio::spawn wait_exit 任务（Task 3 填充）；（6）记录 child handle 到 ManagedProcess.child = Some(child)，state = Starting，spawn_time = Instant::now。实现 emit_status(&self, name, last_error) — 构造 ProcessInfoDTO（含 stderr_tail 从 VecDeque 末尾拼接），self.app_handle.emit("process-status", &dto)。实现 runtime_config(&self) — 从 config.processes.agent.port 构造 agent_url=http://localhost:{port}，server 同理。实现 status(&self) — 锁两个 mutex 返回 snapshot（首探完成前 state=Starting，即 child 已 spawn 但首次 probe 未返回）。在 Cargo.toml 追加 `nix = { version = "0.29", features = ["signal"] }`。运行 cargo check。</action>
  <verify>
    <automated>cd apps/desktop/src-tauri &amp;&amp; cargo check 2>&amp;1 | tail -10 &amp;&amp; grep -E 'pub fn spawn_one|pub fn emit_status|pub struct ManagedProcess' src/process/supervisor.rs</automated>
  </verify>
  <acceptance_criteria>supervisor.rs 含 pub struct ManagedProcess / ProcessStatusSnapshot / ProcessInfoDTO / LogChunk / RuntimeConfig；含 pub fn spawn_one / emit_status / runtime_config / status / new；spawn_one 调 port_probe::is_port_available 预检；Command::new(&entry.cmd[0]).args(&entry.cmd[1..]) 不走 shell；Cargo.toml 含 nix 0.29；`cargo check` 退出 0。</acceptance_criteria>
  <done>Acceptance criteria above are met.</done>
</task>

<task id="07.3-06-02" type="execute">
  <name>Task 2: probe_loop — 10s 周期探针 + state transition + emit</name>
  <description>实现 probe_loop(&self, name) — tokio::spawn 一个 loop，每 10s（tokio::time::interval(Duration::from_secs(10))）用 reqwest GET liveness endpoint（Agent http://127.0.0.1:9876/health、Server http://127.0.0.1:9880/healthz）。结果转 ProcessEvent（ProbeOk / ProbeFail），调 state_machine::transition(current, event, &snap) 得到新状态 + 新 snapshot，若状态变化则 emit_status。Server 额外探 /readyz：200 保持/转 Running；非 200 用 ProbeDegraded event（transition 转 Degraded，不计 fail_count）。loop 在 shutdown_flag 为 true 或 state==Stopped 时退出。</description>
  <read_first>
    - `.planning/phases/07.3-sidecar-process-management/07.3-SPEC.md` §Requirements R2/R3 + §Constraints（探针 10s、3 连败、Go /readyz 只 degraded）+ §Edge Coverage R2
    - `.planning/phases/07.3-sidecar-process-management/07.3-PATTERNS.md` §9（健康端点协议表）
    - `apps/desktop/src-tauri/src/process/state_machine.rs`（plan 02 — ProbeOk/ProbeFail/ProbeDegraded events + transition）
  </read_first>
  <action>实现 probe_loop(&self, name)：构造 reqwest::Client（超时 5s）；loop { tokio::select! { _ = tokio::time::sleep(Duration::from_secs(10)) => { let url = format!("http://127.0.0.1:{}/{}", port, liveness_path); match client.get(&url).send().await { Ok(resp) if resp.status().as_u16() == expect_status => event = ProbeOk, _ => event = ProbeFail }; 若 name==Server 再探 readiness：GET /readyz，非 200 用 ProbeDegraded 覆盖 event（仅当 liveness 已 Ok）；锁 mutex，transition(state, event, &snap) → (new_state, new_snap)；若 new_state != state 则 emit_status；存回 new_state/new_snap } _ = shutdown_flag_signal => break } 若 state==Stopped break }。注意 Server liveness 失败优先于 readiness（liveness 失败 = ProbeFail 计入阈值；readiness 失败 = ProbeDegraded 不计入）。运行 cargo check。</action>
  <verify>
    <automated>cd apps/desktop/src-tauri &amp;&amp; cargo check 2>&amp;1 | tail -5 &amp;&amp; grep -E 'fn probe_loop|tokio::time::sleep|reqwest::Client' src/process/supervisor.rs</automated>
  </verify>
  <acceptance_criteria>supervisor.rs 含 fn probe_loop；用 tokio::time::sleep(Duration::from_secs(10)) 周期；用 reqwest::Client::get 探 liveness + readiness；调 state_machine::transition；状态变化时 emit_status；loop 在 shutdown_flag 或 state==Stopped 时退出；`cargo check` 退出 0。</acceptance_criteria>
  <done>Acceptance criteria above are met.</done>
</task>

<task id="07.3-06-03" type="execute">
  <name>Task 3: wait_exit + 启动失败 vs 运行崩溃区分 + backoff 重启</name>
  <description>实现 wait_exit(&self, name) — tokio::spawn 一个 task 等 child.wait().await。exit 后计算 elapsed = spawn_time.elapsed()：若 elapsed <= 5s（STARTUP_GRACE_SECS）→ SpawnFailed event → transition 返回 Stopped（不走 backoff）；若 > 5s → Crashed event → transition 返回 Unhealthy + restart_count+1 → 进入 backoff 重启循环。backoff 循环：取 backoff_delay(restart_count, max_restarts) → 若 Some(d) 则 tokio::time::sleep(d) 后重新 spawn_one + 重置 spawn_time；若 None（耗尽）→ Stopped 终态。每次重启都 emit_status 含 last_error（exit reason）+ stderr_tail。Stopped 后 wait_exit 退出，不再自动 spawn（P3）。</description>
  <read_first>
    - `.planning/phases/07.3-sidecar-process-management/07.3-SPEC.md` §Requirements R3/R4 + §Acceptance（退避 1/2/4/8/16s、5 次后 stopped、首 5s exit = 启动失败）+ §Prohibitions P2/P3
    - `.planning/phases/07.3-sidecar-process-management/07.3-CONTEXT.md` §decisions D-13（不引 shell plugin，自研）+ Phase 7a 继承 I2（退避序列）
    - `apps/desktop/src-tauri/src/process/state_machine.rs`（plan 02 — Crashed/SpawnFailed/RestartRequested events + transition + backoff_delay）
  </read_first>
  <action>实现 wait_exit(&self, name)：let spawn_time = mp.spawn_time; loop { if let Some(ref mut child) = mp.child { match child.wait().await { Ok(status) => { drop(child take)；let elapsed = spawn_time.elapsed().as_secs_f64(); let event = if elapsed <= state_machine::STARTUP_GRACE_SECS { ProcessEvent::SpawnFailed{exited_after_secs: elapsed} } else { ProcessEvent::Crashed{exited_after_secs: elapsed} }; let last_error = format!("进程退出 code={:?} after {:.1}s", status.code(), elapsed); let (new_state, new_snap) = transition(state, event, &snap); mp.state = new_state; mp.snapshot = new_snap; mp.child = None; self.emit_status(name, Some(last_error)); match new_state { Stopped => break, Unhealthy => { let attempt = new_snap.restart_count; match state_machine::backoff_delay(attempt - 1, config.max_restarts) { Some(d) => { tokio::time::sleep(d).await; if self.spawn_one(name).is_ok() { mp.spawn_time = Instant::now(); /* 重启 probe_loop 与 wait_exit */ } else { /* spawn 失败（如端口仍占）→ 累加 restart_count 再 backoff 或转 Stopped */ } } None => { mp.state = Stopped; self.emit_status(name, Some("退避 5 次耗尽")); break; } } } _ => break } } Err(_) => break } } else { break } } }。注意 spawn_one 内部已重新启动 probe_loop/wait_exit task（避免递归 — 用 tokio::spawn 分离）。运行 cargo test（确认不破坏 plan 02/03/05 测试）+ cargo check。</action>
  <verify>
    <automated>cd apps/desktop/src-tauri &amp;&amp; cargo check 2>&amp;1 | tail -5 &amp;&amp; grep -E 'fn wait_exit|STARTUP_GRACE_SECS|backoff_delay' src/process/supervisor.rs</automated>
  </verify>
  <acceptance_criteria>supervisor.rs 含 fn wait_exit；用 spawn_time.elapsed() <= STARTUP_GRACE_SECS 区分 SpawnFailed vs Crashed；调 transition 得到 Stopped（启动失败）或 Unhealthy（崩溃）；Unhealthy 时调 backoff_delay(restart_count-1, max_restarts)，Some sleep 后 spawn_one，None 转 Stopped；每次状态变化 emit_status 含 last_error；Stopped 后 break 不自动 spawn（P3 闭环）；`cargo check` 退出 0；plan 02/03/05 测试仍全绿。</acceptance_criteria>
  <done>Acceptance criteria above are met.</done>
</task>

<task id="07.3-06-04" type="execute">
  <name>Task 4: log_capture_task — 脱敏 + emit + 落盘 + logs-dir-not-writable 降级</name>
  <description>实现 log_capture_task(&self, name, stdout, stderr) — 两个 tokio task 各持一个 BufReader<ChildStdout/ChildStderr>，按行读（tokio::io::AsyncBufReadExt::lines），每行：（1）redact_log_line(&line) 脱敏（plan 05，DEEPSEEK_API_KEY/JWT_SECRET/PALADIN_JWT_SECRET/Auth/password 五类）；（2）append 到 log_dir/{paladin-agent.log, paladin-server.log}（tokio::fs::OpenOptions append + write）；（3）emit 'process-log' LogChunk{process:name, stream:Stdout|Stderr, line, ts:chrono::Utc::now().timestamp_millis()}；（4）若是 stderr 且属末尾，push 到 ManagedProcess.stderr_tail VecDeque（保留最近 50 行，供 emit_status P2 退出原因暴露）。log_dir create_dir_all 失败或 open append 失败时降级：仅 emit + eprintln!("[supervisor] log_dir not writable: {}", e) + emit 一条 warning LogChunk，不 panic、不阻塞 spawn（SPEC Edge R6）。</description>
  <read_first>
    - `.planning/phases/07.3-sidecar-process-management/07.3-SPEC.md` §Requirements R6 + §Acceptance（前端 1s 可见 + 落盘 logs 目录）+ §Edge Coverage R6（不可写降级）
    - `.planning/phases/07.3-sidecar-process-management/07.3-PATTERNS.md` §2.4（spawn + Channel）+ §2.8（落盘路径 + 敏感约束 D-15）
    - `.planning/phases/08-go-server/08-CONTEXT.md` D-15（Logs 绝不含明文密码或 JWT）
    - `apps/desktop/src-tauri/src/process/log_redact.rs`（plan 05 — redact_log_line + SECRET_PATTERNS）
  </read_first>
  <action>实现 log_capture_task（泛型 over AsyncBufRead）：async fn capture_lines<R: AsyncRead + Unpin + Send + 'static>(&self, name: ProcessName, stream: LogStream, reader: R, log_path: PathBuf) { let mut lines = tokio::io::BufReader::new(reader).lines(); let mut file = match tokio::fs::OpenOptions::new().create(true).append(true).open(&log_path).await { Ok(f) => Some(f), Err(e) => { eprintln!("[supervisor] log_dir not writable: {}", e); self.app_handle.emit("process-log", LogChunk{process:name, stream:Stderr, line: format!("[warn] 日志目录不可写，仅前端可见: {}", e), ts: now()}); None } }; while let Ok(Some(line)) = lines.next_line().await { let redacted = log_redact::redact_log_line(&line); if let Some(ref mut f) = file { use tokio::io::AsyncWriteExt; let _ = f.write_all(format!("{}\n", redacted).as_bytes()).await; } self.app_handle.emit("process-log", LogChunk{process:name, stream, line: redacted.clone(), ts: now()}); if matches!(stream, LogStream::Stderr) { let mut mp = self.mutex_of(name).lock().await; mp.stderr_tail.push_back(redacted); if mp.stderr_tail.len() > 50 { mp.stderr_tail.pop_front(); } } if self.shutdown_flag_of(name).load(Ordering::Relaxed) { break; } } }。spawn_one 在拿到 child.stdout/child.stderr 后 tokio::spawn(self.capture_lines(name, Stdout, stdout, log_dir.join("paladin-agent.log"))) + 同理 stderr。log_dir 在 new() 时 std::fs::create_dir_all 失败不阻塞（保留 PathBuf，capture_lines 内再尝试 open）。运行 cargo check + cargo test。</action>
  <verify>
    <automated>cd apps/desktop/src-tauri &amp;&amp; cargo check 2>&amp;1 | tail -5 &amp;&amp; grep -E 'fn capture_lines|redact_log_line|OpenOptions|process-log' src/process/supervisor.rs</automated>
  </verify>
  <acceptance_criteria>supervisor.rs 含 fn capture_lines（或 log_capture_task）；每行调 redact_log_line；append 到 paladin-agent.log/paladin-server.log；emit 'process-log' LogChunk；stderr push 到 stderr_tail VecDeque（容量 50）；open 失败时 eprintln + emit warning 不 panic；shutdown_flag 为 true 时 break；`cargo check` 退出 0；plan 02/03/05 测试仍全绿。</acceptance_criteria>
  <done>Acceptance criteria above are met.</done>
</task>

<task id="07.3-06-05" type="execute">
  <name>Task 5: graceful_shutdown — Drop + SIGTERM(Unix)/Job Object(Win) + 5s grace + 强制 kill + 启动顺序</name>
  <description>实现 graceful_shutdown_all（Drop 调用）+ graceful_shutdown_one。关闭顺序 Agent → Server（与启动相反）。每个：先 shutdown_flag.store(true) 停 probe/log task；再对 tracked child 发 SIGTERM（Unix: nix::sys::signal::kill(Pid::from_raw(child.id() as i32), SIGTERM)；Windows: 用 child.start_kill() 直接走 Job Object/TerminateProcess — Windows 无 SIGTERM 概念，直接走强制 kill 路径但记 grace 等待）；tokio::time::timeout(Duration::from_secs(shutdown_grace_secs), child.wait())；超时则 child.kill().await 兜底。SIGTERM 对已死进程为 no-op（wait 立即返回 Ok）。不读 PID 文件、不系统 kill 命令匹配 PID（P4）。同时实现 start() 启动顺序：先 spawn Server → 等 /readyz 或 5s grace → 再 spawn Agent。</description>
  <read_first>
    - `.planning/phases/07.3-sidecar-process-management/07.3-SPEC.md` §Requirements R5 + §Acceptance（无孤儿残留、SIGTERM 5s grace）+ §Edge Coverage R5 empty + §Prohibitions P4
    - `.planning/phases/07.3-sidecar-process-management/07.3-PATTERNS.md` §2.9（跨平台终止机制骨架）
    - `.planning/phases/07.3-sidecar-process-management/07.3-CONTEXT.md` §decisions D-14（Windows 终止三选一，planner 选 tokio Child::kill 走 Job Object）
    - `apps/desktop/src-tauri/src/terminal/mod.rs`（Drop impl + close_all 范式）
  </read_first>
  <action>实现 graceful_shutdown_one(&self, mp: &mut ManagedProcess)：mp.shutdown_flag.store(true, Ordering::Relaxed)；if let Some(ref mut child) = mp.child { #[cfg(unix)] { let pid = child.id() as i32; let _ = nix::sys::signal::kill(nix::unistd::Pid::from_raw(pid), nix::sys::signal::Signal::SIGTERM); } #[cfg(windows)] { /* Windows 无 SIGTERM，直接进入 grace wait，超时 child.kill() */ } match tokio::time::timeout(Duration::from_secs(self.config.shutdown_grace_secs), child.wait()).await { Ok(_) => { /* 已退出，SIGTERM 对已死进程为 no-op */ } Err(_) => { let _ = child.kill().await; /* 强制 kill 兜底 */ } } mp.child = None; mp.state = Stopped; }。实现 graceful_shutdown_all(&self)：顺序 self.graceful_shutdown_one(agent mutex.lock().await) 然后 server（与启动相反）。实现 Drop for ProcessSupervisor { fn drop(&mut self) { /* 同步路径：用 std thread 或 tokio runtime handle block_on — Tauri Drop 内不宜 await，用 std::process 旗标 + 已 spawn 的 task 自退出 */ for mp in [&mut self.agent, &mut self.server] { mp.shutdown_flag.store(true, Ordering::Relaxed); if let Some(ref mut child) = mp.child { let _ = child.start_kill(); /* 异步信号，立即返回 */ } } /* grace 等待由 child 在 wait_exit task 内感知 shutdown_flag 自退出，Drop 不 block 5s（避免 Tauri 退出卡顿）；强 kill 由 OS 在 app exit 时回收 */ } }。实现 start(&self) -> Result<(), String>：（1）self.spawn_one(Server)?；（2）tokio::time::timeout(Duration::from_secs(5), self.wait_for_readyz()).await — readyz 通过或超时都继续（SPEC Edge R5：超时降级 degraded 不阻塞 Agent 启动）；（3）self.spawn_one(Agent)?。运行 cargo check（注意 cfg(unix)/cfg(windows) 双路径编译验证 — 用 `cargo check --target x86_64-pc-windows-msvc` 若工具链在；否则仅 unix 路径 + 信任 windows 分支）。</action>
  <verify>
    <automated>cd apps/desktop/src-tauri &amp;&amp; cargo check 2>&amp;1 | tail -5 &amp;&amp; grep -E 'fn graceful_shutdown|impl Drop for ProcessSupervisor|cfg\(unix\)|SIGTERM|start_kill' src/process/supervisor.rs</automated>
  </verify>
  <acceptance_criteria>supervisor.rs 含 fn graceful_shutdown_one / graceful_shutdown_all；impl Drop for ProcessSupervisor；cfg(unix) 路径用 nix::sys::signal::kill SIGTERM；timeout(shutdown_grace_secs) 超时 child.kill() 兜底；关闭顺序 Agent→Server；启动顺序 Server→等 readyz→Agent；不读 PID 文件不系统 kill 匹配（P4）；`cargo check` 退出 0。</acceptance_criteria>
  <done>Acceptance criteria above are met.</done>
</task>

<task id="07.3-06-06" type="execute">
  <name>Task 6: commands.rs 6 命令 + 并发幂等 + 手动 restart 清零</name>
  <description>实现 commands.rs 六个 #[tauri::command]。restart_X 内部先抢 restart_in_progress Mutex（try_lock 失败返回 Ok(()) 标 in-progress，不二次 spawn — SPEC Edge R7 并发幂等），成功后调 supervisor.restart(name) — restart 调 stop（drop child + wait exit 退出）再 spawn_one，且清零 snapshot.fail_count + snapshot.restart_count（state_machine RestartRequested 已实现，supervisor 在 restart 入口 transition(state, RestartRequested, &snap) 得到 (Starting, default snapshot)）。stop_X 调 supervisor.stop(name) — stop 设 shutdown_flag + child.start_kill + state=Stopped（不 backoff、不自动 spawn，P3）。</description>
  <read_first>
    - `.planning/phases/07.3-sidecar-process-management/07.3-SPEC.md` §Requirements R7/R8 + §Acceptance（5 命令、get_process_status starting、并发幂等、手动 restart 清零）+ §Edge Coverage R7
    - `.planning/phases/07.3-sidecar-process-management/07.3-CONTEXT.md` §decisions D-07（get_runtime_config 单一 URL 真相源）
    - `.planning/phases/07.3-sidecar-process-management/07.3-PATTERNS.md` §2.5（6 命令薄包装范式）
    - `apps/desktop/src-tauri/src/terminal/commands.rs`（#[tauri::command] + State<TerminalManager> 范式）
  </read_first>
  <action>在 commands.rs 实现：`#[tauri::command] pub async fn restart_agent(state: State<'_, ProcessSupervisor>) -> Result<(), String> { state.restart(ProcessName::Agent).await }`，同理 stop_agent / restart_server / stop_server。`#[tauri::command] pub fn get_process_status(state: State<'_, ProcessSupervisor>) -> ProcessStatusSnapshot { state.status() }`（首探完成前 status 返回 Starting）。`#[tauri::command] pub fn get_runtime_config(state: State<'_, ProcessSupervisor>) -> RuntimeConfig { state.runtime_config() }`。在 supervisor.rs 实现 restart(&self, name)：let mp = self.mutex_of(name).lock().await; let in_progress = mp.restart_in_progress.clone(); let mut guard = in_progress.lock().await; if *guard { return Ok(()); } *guard = true; drop(guard); self.stop_internal(&mut mp).await; let (new_state, new_snap) = transition(mp.state, ProcessEvent::RestartRequested, &mp.snapshot); mp.state = new_state; mp.snapshot = new_snap; let r = self.spawn_one(name).await; *in_progress.lock().await = false; r。实现 stop(&self, name)：let mut mp = self.mutex_of(name).lock().await; self.stop_internal(&mut mp).await; mp.state = Stopped; drop(mp); self.emit_status(name, Some("手动停止"))。stop_internal：mp.shutdown_flag.store(true); if let Some(ref mut child) = mp.child { let _ = child.start_kill(); let _ = child.wait().await; mp.child = None; }。运行 cargo check。</action>
  <verify>
    <automated>cd apps/desktop/src-tauri &amp;&amp; cargo check 2>&amp;1 | tail -5 &amp;&amp; grep -cE '#\[tauri::command\]' src/process/commands.rs</automated>
  </verify>
  <acceptance_criteria>commands.rs 含 6 个 #[tauri::command]（restart_agent/stop_agent/restart_server/stop_server/get_process_status/get_runtime_config）；restart 用 restart_in_progress Mutex 串行化，重入返回 Ok(()) 不二次 spawn；restart 调 transition(RestartRequested) 清零 snapshot；stop 设 Stopped 不 backoff；get_process_status 首探前返回 Starting；get_runtime_config 返回 {agent_url, server_url}；`cargo check` 退出 0。</acceptance_criteria>
  <done>Acceptance criteria above are met.</done>
</task>

<task id="07.3-06-07" type="execute">
  <name>Task 7: lib.rs setup() wiring + invoke_handler! 注册 6 命令 + 集成 smoke test</name>
  <description>在 lib.rs setup() 闭包内、app.manage(TerminalManager) 之后追加 ProcessSupervisor 初始化：用 app.path().app_local_data_dir()?.join("log") 算 log_dir；用 include_str! 或 app.path().resource_dir() 定位 processes.json（dev 模式从 src-tauri/processes.json 读，packaged 从 resource_dir 读 — 本 plan 先用 app_local_data_dir/processes.json fallback 到 CARGO_MANIFEST_DIR/processes.json）；ProcessConfig::load_from_path 失败时 setup 返回 Err（SPEC Edge R1）；app.manage(ProcessSupervisor::new(...))；app.state::<ProcessSupervisor>().start()（spawn Server→readyz→Agent）。invoke_handler! 追加 6 命令。最后跑一次 `cargo test` + `cargo build` 全量验证。</description>
  <read_first>
    - `apps/desktop/src-tauri/src/lib.rs`（当前 invoke_handler! + setup() + tray）
    - `.planning/phases/07.3-sidecar-process-management/07.3-PATTERNS.md` §2.6（setup 改造点）+ §2.8（app_local_data_dir 取路径）
    - `.planning/phases/07.3-sidecar-process-management/07.3-SPEC.md` §Edge Coverage R1（配置错误不 spawn）
    - `apps/desktop/src-tauri/src/process/config.rs`（plan 03 — load_from_path + ConfigError）
  </read_first>
  <action>在 lib.rs 顶部把 `#[allow(unused_imports)] use crate::process::ProcessSupervisor;` 改为 `use crate::process::ProcessSupervisor;`（去掉 allow）。invoke_handler! 列表追加 `process::commands::restart_agent, process::commands::stop_agent, process::commands::restart_server, process::commands::stop_server, process::commands::get_process_status, process::commands::get_runtime_config,`。setup() 闭包在 `app.manage(TerminalManager::new(app.handle().clone()));` 之后追加：`let log_dir = app.path().app_local_data_dir()?.join("log"); let _ = std::fs::create_dir_all(&log_dir); let cfg_path = std::env::var("PALADIN_PROCESSES_CFG").map(PathBuf::from).unwrap_or_else(|_| PathBuf::from(env!("CARGO_MANIFEST_DIR")).join("processes.json")); let process_cfg = ProcessConfig::load_from_path(&cfg_path).map_err(|e| format!("processes.json 加载失败: {:?}", e))?; let supervisor = ProcessSupervisor::new(app.handle().clone(), process_cfg, log_dir); app.manage(supervisor); let state: tauri::State<ProcessSupervisor> = app.state(); state.start()?;`（注意 start 是 async，用 tauri::async_runtime::block_on 或把 start 改为同步 spawn — 推荐 start 内部 tokio::spawn 异步任务、本身同步返回）。运行 `cargo build --manifest-path apps/desktop/src-tauri/Cargo.toml` + `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml`。</action>
  <verify>
    <automated>cd apps/desktop/src-tauri &amp;&amp; cargo build 2>&amp;1 | tail -5 &amp;&amp; cargo test 2>&amp;1 | tail -10 &amp;&amp; grep -cE 'process::commands::' src/lib.rs</automated>
  </verify>
  <acceptance_criteria>lib.rs invoke_handler! 含 6 个 process::commands:: 入口；setup() 含 ProcessConfig::load_from_path + app.manage(ProcessSupervisor) + state.start()；setup 配置错误返回 Err；`cargo build` 退出 0；`cargo test` 全绿（含 plan 02/03/05 测试）；grep process::commands:: src/lib.rs 计数 >= 6。</acceptance_criteria>
  <done>Acceptance criteria above are met.</done>
</task>

</tasks>

<verification>
执行 Wave 2 supervisor 集成验收命令：

`cd apps/desktop/src-tauri && cargo build`

`cd apps/desktop/src-tauri && cargo test`

`cd apps/desktop/src-tauri && cargo clippy -- -D warnings 2>&1 | grep -c 'warning:'` (期望 0)

`grep -cE 'process::commands::' apps/desktop/src-tauri/src/lib.rs` (期望 >= 6)

`grep -E 'pub fn start|pub fn restart|pub fn stop|pub fn spawn_one|pub fn status|pub fn runtime_config|impl Drop for ProcessSupervisor' apps/desktop/src-tauri/src/process/supervisor.rs`

`grep -E 'tokio::process::Command|is_port_available|redact_log_line|state_machine::transition|backoff_delay' apps/desktop/src-tauri/src/process/supervisor.rs`

UAT 手动验收（SPEC Acceptance，需 uv + go + docker-compose.server.yml 运行）：
- 桌面端启动后 5s 内 `ps aux | grep -E 'paladin-agent|paladin-server'` 两进程可见
- 预占 9876（`python3 -m http.server 9876`）后启动桌面端，前端显示 Agent 端口占用错误态、不 spawn Agent
- `kill -9 <agent_pid>` 后 ≤30s 前端收到 unhealthy 事件、≤~40s 恢复 healthy
- `kill -9 <server_pid>` 退避间隔实测 1/2/4/8/16s（用日志时间戳）
- 连续制造 5 次启动失败（如 cmd 改为不存在命令）后状态停 stopped，5 分钟内无自动 spawn
- 关闭桌面端后 `ps aux | grep paladin` 无残留
- 停 PG（`docker stop`）后 Go 状态 degraded，不重启
</verification>

<success_criteria>
- ProcessSupervisor + ManagedProcess + 6 命令 + Drop + setup() wiring 就位。
- spawn_one 走 port_probe 预检 + tokio::process::Command（不走 shell，P1）+ tracked Child handle（P4）。
- probe_loop 10s 周期 + transition 决策 + emit_status（含 last_error + stderr_tail，P2）。
- wait_exit 区分 SpawnFailed(<=5s)/Crashed(>5s) + backoff_delay 序列重启 + Stopped 不自动 spawn（P3）。
- log_capture_task redact_log_line + append log_dir + emit process-log + logs-dir-not-writable 降级（Edge R6 + D-15）。
- graceful_shutdown SIGTERM(Unix)/Job Object(Win) + 5s grace + 强 kill + 关闭顺序 Agent→Server（R5 + P4）。
- 6 命令并发幂等 + 手动 restart 清零（R7 + Edge R7）。
- 启动顺序 Server→readyz→Agent（Edge R5 empty）。
- SPEC R1/R2/R3/R4/R5/R6/R7/R8 + Edge R1/R2/R3/R4/R5/R6/R7/R9 + Prohibitions P1/P2/P3/P4 在 supervisor 层闭环。
</success_criteria>

<output>
Create `.planning/phases/07.3-sidecar-process-management/07.3-06-SUMMARY.md` when done, listing the ProcessSupervisor public API surface, the 6 command signatures, the 2 emit event names + payload schemas, and the UAT results matrix.
</output>
