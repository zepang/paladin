---
phase: 07.3
plan: 07.3-10
title: 跨平台验证 + 终态 source gates + Phase 闭环
type: execute
wave: 5
depends_on:
  - 07.3-06
  - 07.3-07
  - 07.3-08
  - 07.3-09
requirements:
  - SDC-01
  - SDC-02
  - SDC-03
files_modified:
  - .planning/phases/07.3-sidecar-process-management/07.3-VALIDATION.md
  - .planning/phases/07.3-sidecar-process-management/07.3-UAT.md
  - .planning/phases/07.3-sidecar-process-management/07.3-VERIFICATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
user_setup:
  - "三平台 UAT 需要在 macOS / Windows / Linux 各执行一遍启动+健康+重启+关闭场景。autonomous=false 因为跨平台 UAT 需要人工在三个 OS 上操作（或 CI 矩阵，但本 phase 无 CI）。"
  - "Windows UAT 需要确认 nix crate 在 windows-msvc 工具链下不引入 Unix 依赖（cfg(unix) 守卫）；若无 Windows 环境，至少 cargo check --target x86_64-pc-windows-msvc 验证编译。"
  - "Linux UAT 需要 X11 或 Wayland（Tauri wry 依赖）；app_local_data_dir 解析为 ~/.local/share/com.worsmer.paladin/logs。"
must_haves:
  truths:
    - "SPEC R9 Acceptance: macOS、Windows、Linux 三平台各执行一遍启动+健康+重启+关闭 UAT 通过；UAT 文档记录三平台结果矩阵。"
    - "SPEC 全部 22 条 Acceptance checkbox 经验证 — 11 条核心功能 + 6 条日志与控制 + 5 条配置与跨平台 — 在 VALIDATION/VERIFICATION 文档逐条标注 pass/fail + 证据。"
    - "SPEC 全部 13 条 Edge Coverage（9 covered + 4 dismissed）在 VERIFICATION 文档逐条确认 covered 行的验证证据；dismissed 行（adjacency/ordering）标注 dismissed 理由。"
    - "SPEC 全部 4 条 Prohibitions（P1 命令注入 / P2 不静默重启 / P3 stopped 不自恢复 / P4 不裸 PID kill）在 VERIFICATION 文档逐条标注验证方法与结果。"
    - "终态 source gates 全绿: grep localhost:9876 业务代码 0 命中（R8）；grep tauri-plugin-shell Cargo.toml 0 命中（D-13）；grep '@radix-ui' popover.tsx 0 命中（base-nova）；cargo test + cargo clippy + tsc + biome 全绿。"
    - "PATTERNS §0 四条纠偏全部闭环验证: popover.tsx 存在（plan 04）、processes.json 存在（plan 01）、Cargo.toml 含 tokio+reqwest+nix 不含 shell plugin（plan 01/06）、CSP 无需改（确认 tauri.conf.json 仍 connect-src http://localhost:*）。"
    - "app_local_data_dir/log/ 三平台路径解析正确: macOS ~/Library/Application Support/com.worsmer.paladin/logs/、Windows %LOCALAPPDATA%/com.worsmer.paladin/logs、Linux ~/.local/share/com.worsmer.paladin/logs；paladin-agent.log/paladin-server.log 各自存在且含特征字符串。"
  prohibitions:
    - statement: "MUST NOT 在本 plan 引入任何新代码或新依赖（本 plan 仅做验证 + 文档 + STATE/ROADMAP 更新）；所有功能在 plan 01-09 已交付。"
      status: resolved
      verification: "git diff 仅含 .planning/ 下 VALIDATION/UAT/VERIFICATION/STATE/ROADMAP 文档；apps/ 下零代码改动。"
    - statement: "MUST NOT 标记 Phase 07.3 complete 除非三平台 UAT 全 pass（或明确记录哪一平台 deferred + 风险）；SPEC R9 是硬验收项不可跳过。"
      status: resolved
      verification: "07.3-UAT.md 含三平台结果矩阵，每平台 4 场景（启动/健康/重启/关闭）pass/fail 明确；若某平台 deferred，STATE.md 显式标注。"
  artifacts:
    - path: ".planning/phases/07.3-sidecar-process-management/07.3-VALIDATION.md"
      provides: "22 条 SPEC Acceptance checkbox 验证结果（pass/fail + 证据 + 命令）"
      exports: []
    - path: ".planning/phases/07.3-sidecar-process-management/07.3-UAT.md"
      provides: "三平台 UAT 场景矩阵（macOS/Windows/Linux × 启动/健康/重启/关闭）+ 操作步骤 + 预期 + 实测"
      exports: []
    - path: ".planning/phases/07.3-sidecar-process-management/07.3-VERIFICATION.md"
      provides: "13 Edge Coverage + 4 Prohibitions 逐条验证证据 + scope gates + PATTERNS §0 闭环确认"
      exports: []
  key_links:
    - from: ".planning/phases/07.3-sidecar-process-management/07.3-VERIFICATION.md"
      to: ".planning/phases/07.3-sidecar-process-management/07.3-SPEC.md"
      via: "逐条引用 SPEC Acceptance/Edge/Prohibitions 并标注验证结果"
      pattern: "SPEC §Acceptance / §Edge Coverage / §Prohibitions"
---

<objective>
Phase 07.3 闭环 plan：跑完三平台 UAT、把 SPEC 22 条 Acceptance + 13 Edge + 4 Prohibitions 逐条验证写入 VALIDATION/UAT/VERIFICATION 三个文档，执行终态 source gates，更新 STATE.md 与 ROADMAP.md 标记 Phase 07.3 complete。

本 plan 不写任何应用代码（apps/ 零改动），仅做：
1. 跑全量 cargo test + cargo clippy + cargo build + tsc + biome。
2. 跑 source gates（grep localhost:9876 / grep tauri-plugin-shell / grep @radix-ui）。
3. 三平台 UAT（macOS 必做，Windows/Linux 若环境可用则做，否则记录 deferred + 风险）。
4. 写 VALIDATION.md（22 条 acceptance）、UAT.md（三平台矩阵）、VERIFICATION.md（edge + prohibitions + scope gates + PATTERNS §0 闭环）。
5. 更新 STATE.md（Phase 07.3 complete、progress +1）与 ROADMAP.md（Phase 07.3 Plans 10/10 complete）。

Purpose: 把「实现完成」转化为「验证完成」，让下游 phase（10 Packaging）可以信任 sidecar 已就绪。
Output: 三份验证文档 + STATE/ROADMAP 更新 + 三平台 UAT pass（或明确 deferred）。
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.3-sidecar-process-management/07.3-SPEC.md
@.planning/phases/07.3-sidecar-process-management/07.3-CONTEXT.md
@.planning/phases/07.3-sidecar-process-management/07.3-PATTERNS.md
@.planning/phases/07.3-sidecar-process-management/07.3-01-PLAN.md
@.planning/phases/07.3-sidecar-process-management/07.3-02-PLAN.md
@.planning/phases/07.3-sidecar-process-management/07.3-03-PLAN.md
@.planning/phases/07.3-sidecar-process-management/07.3-04-PLAN.md
@.planning/phases/07.3-sidecar-process-management/07.3-05-PLAN.md
@.planning/phases/07.3-sidecar-process-management/07.3-06-PLAN.md
@.planning/phases/07.3-sidecar-process-management/07.3-07-PLAN.md
@.planning/phases/07.3-sidecar-process-management/07.3-08-PLAN.md
@.planning/phases/07.3-sidecar-process-management/07.3-09-PLAN.md
</context>

## Artifacts this phase produces

- `.planning/phases/07.3-sidecar-process-management/07.3-VALIDATION.md` — 22 条 SPEC Acceptance checkbox 验证结果，每条含：checkbox 引用、验证命令、pass/fail、证据（命令输出片段或 UAT 观察）。
- `.planning/phases/07.3-sidecar-process-management/07.3-UAT.md` — 三平台（macOS/Windows/Linux）× 四场景（启动/健康/重启/关闭）= 12 格矩阵 + 每格操作步骤 + 预期 + 实测结果。
- `.planning/phases/07.3-sidecar-process-management/07.3-VERIFICATION.md` — 13 Edge Coverage 逐条（9 covered 给证据，4 dismissed 给理由）+ 4 Prohibitions 逐条（验证方法 + 结果）+ source gates 全绿截图 + PATTERNS §0 四条纠偏闭环确认。
- `.planning/STATE.md` — current_phase 推进、Phase 07.3 标 Complete、progress completed_plans +7（plan 04-10）、Recent Activity 追加 2026-07-05 条目。
- `.planning/ROADMAP.md` — Phase 07.3 段 Plans 改为 10/10 complete + 列出 10 个 plan 文件名 + Goal 段从 [Urgent work - to be planned] 改为实际交付总结。

<tasks>

<task id="07.3-10-01" type="execute">
  <name>Task 1: 全量构建 + 测试 + lint gate</name>
  <description>跑 Rust + 前端全量构建测试 lint，确保 plan 01-09 产物无回归。Rust: cargo build + cargo test + cargo clippy -- -D warnings。前端: tsc --noEmit + biome check。</description>
  <read_first>
    - `apps/desktop/src-tauri/Cargo.toml`（确认依赖完整）
    - `apps/desktop/package.json`（确认 scripts — tsc/biome 命令）
  </read_first>
  <action>执行并记录输出：`cd apps/desktop/src-tauri && cargo build 2>&1 | tail -20`；`cd apps/desktop/src-tauri && cargo test 2>&1 | tail -30`（确认 plan 02 state_machine + plan 03 config + plan 05 port_probe/log_redact 全绿）；`cd apps/desktop/src-tauri && cargo clippy --all-targets -- -D warnings 2>&1 | tail -10`；`cd apps/desktop && npx tsc --noEmit 2>&1 | tail -10`；`cd apps/desktop && npx biome check src/ 2>&1 | tail -10`。任一失败则回退对应 plan 修复，不在本 plan 改应用代码。</action>
  <verify>
    <automated>cd apps/desktop/src-tauri &amp;&amp; cargo test 2>&amp;1 | tail -5 &amp;&amp; cd ../.. &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -3 &amp;&amp; echo ALL_GREEN</automated>
  </verify>
  <acceptance_criteria>cargo build 退出 0；cargo test 全绿（plan 02/03/05 单元测试 + 任何 integration test）；cargo clippy 0 warning；npx tsc --noEmit 退出 0；npx biome check 0 error。任一失败则该 task 标 blocked，不在本 plan 改代码而是回退。</acceptance_criteria>
  <done>ALL_GREEN printed and all five commands exit 0.</done>
</task>

<task id="07.3-10-02" type="execute">
  <name>Task 2: 终态 source gates</name>
  <description>跑 SPEC acceptance 的 source-level grep 验证：localhost:9876 业务代码 0 命中（R8）、tauri-plugin-shell 不在 Cargo.toml（D-13）、@radix-ui 不在 popover.tsx（base-nova）、processes.json schema 字段、Tauri CSP 仍开放。</description>
  <read_first>
    - `.planning/phases/07.3-sidecar-process-management/07.3-SPEC.md` §Acceptance（配置与跨平台段 — grep 命令）
    - `.planning/phases/07.3-sidecar-process-management/07.3-PATTERNS.md` §0（四条纠偏闭环验证）+ §11（项目惯例）
  </read_first>
  <action>执行并记录每条 grep 结果：（1）`grep -rn 'localhost:9876' apps/desktop/src/ | grep -vE 'fallback|console.warn|默认'`（期望空 — 业务 0 命中，仅 fallback 常量保留合规）；（2）`grep -c 'tauri-plugin-shell' apps/desktop/src-tauri/Cargo.toml`（期望 0）；（3）`grep -c '@radix-ui' apps/desktop/src/components/ui/popover.tsx`（期望 0）；（4）`grep -E 'from .@base-ui/react/popover.' apps/desktop/src/components/ui/popover.tsx`（期望命中）；（5）`jq '.processes.agent.cmd | type, .processes.server.cmd | type, .backoff_secs, .max_restarts' apps/desktop/src-tauri/processes.json`（期望 array/array/[1,2,4,8,16]/5）；（6）`grep -E 'connect-src' apps/desktop/src-tauri/tauri.conf.json`（期望仍含 http://localhost:* — PATTERNS §0 CSP 无需改）；（7）`grep -E 'tokio|reqwest|nix' apps/desktop/src-tauri/Cargo.toml`（期望三依赖都在）。把七条结果记入 VERIFICATION.md。</action>
  <verify>
    <automated>echo "=== R8 URL ===" &amp;&amp; grep -rn 'localhost:9876' apps/desktop/src/ | grep -vE 'fallback|console.warn|默认' || echo "URL_CLEAN_OK" &amp;&amp; echo "=== shell plugin ===" &amp;&amp; grep -c 'tauri-plugin-shell' apps/desktop/src-tauri/Cargo.toml &amp;&amp; echo "=== radix ===" &amp;&amp; grep -c '@radix-ui' apps/desktop/src/components/ui/popover.tsx</automated>
  </verify>
  <acceptance_criteria>R8 grep 业务代码 0 命中（URL_CLEAN_OK）；tauri-plugin-shell Cargo.toml 计数 0；@radix-ui popover.tsx 计数 0；@base-ui/react/popover 命中；processes.json cmd array + backoff [1,2,4,8,16] + max_restarts 5；CSP connect-src 含 localhost:*；Cargo.toml 含 tokio+reqwest+nix。七条全绿记入 VERIFICATION.md。</acceptance_criteria>
  <done>Acceptance criteria above are met.</done>
</task>

<task id="07.3-10-03" type="execute">
  <name>Task 3: macOS UAT — 启动/健康/重启/关闭四场景</name>
  <description>在 macOS 执行四场景 UAT：（1）启动 — 桌面端启动 5s 内 ps 见 paladin-agent + paladin-server；（2）健康 — kill agent PID ≤30s 前端 unhealthy 事件、≤~40s 恢复；（3）重启 — 退避间隔实测 1/2/4/8/16s（日志时间戳）；（4）关闭 — 退出桌面端 ps 无残留。同时验证端口占用报错、PG/Redis 停时 Go degraded、日志面板可见性。</description>
  <read_first>
    - `.planning/phases/07.3-sidecar-process-management/07.3-SPEC.md` §Requirements R1-R9 + §Acceptance（22 条 checkbox）
    - `.planning/phases/07.3-sidecar-process-management/07.3-CONTEXT.md` §canonical_refs（apps/agent cli.py serve --dev + apps/server cmd/server/main.go + docker-compose.server.yml）
  </read_first>
  <action>启动 docker-compose.server.yml（Postgres+Redis）。启动桌面端（cargo tauri dev 或已构建 app）。逐场景操作并截图/记录：（1）启动场景 — `ps aux | grep -E 'uv run paladin-agent|go run ./cmd/server'` 5s 内两进程可见；（2）端口占用场景 — 先 `python3 -m http.server 9876` 再启动桌面端，观察前端 Agent 端口占用错误态、ps 无 agent 进程；（3）健康场景 — `kill -9 <agent_pid>`，记录前端 unhealthy 事件时间戳（≤30s）与恢复 healthy 时间戳（≤~40s）；（4）重启退避场景 — kill server，记录每次 backoff 重启的日志时间戳，验证间隔 1/2/4/8/16s；（5）退避耗尽场景 — 临时把 processes.json server.cmd 改为不存在的命令，启动后 5 次失败 → stopped，观察 5 分钟内无自动 spawn；（6）优雅关闭场景 — 退出桌面端，`ps aux | grep paladin` 无残留；（7）PG 停场景 — `docker stop <pg_container>`，Go 状态 degraded 不重启；（8）日志场景 — agent 输出特征字符串 `echo "UAT_MARKER_$(date +%s)"`，前端日志面板 1s 内可见 + 落盘文件含该字符串。每场景记入 UAT.md macOS 列。</action>
  <verify>
    <automated>ps aux | grep -E 'uv run paladin-agent|go run ./cmd/server' | grep -v grep | wc -l</automated>
  </verify>
  <acceptance_criteria>UAT.md macOS 列 4 核心场景（启动/健康/重启/关闭）全 pass + 4 扩展场景（端口占用/退避耗尽/PG停/日志可见）全 pass；记录含命令输出与时间戳证据；macOS app_local_data_dir ~/Library/Application Support/com.worsmer.paladin/logs/paladin-agent.log 与 paladin-server.log 存在且含特征字符串。</acceptance_criteria>
  <done>Acceptance criteria above are met and UAT.md macOS column filled.</done>
</task>

<task id="07.3-10-04" type="execute">
  <name>Task 4: Windows + Linux UAT（或 deferred 记录）</name>
  <description>Windows + Linux 各执行同样四场景 UAT。若无环境，明确记录 deferred + 风险（哪些场景最可能跨平台出问题：Windows SIGTERM 等价、Linux X11/Wayland、app_local_data_dir 路径）。至少跑 cargo check --target x86_64-pc-windows-msvc 验证 Windows 编译（nix crate cfg(unix) 守卫不引入 Unix 依赖）。</description>
  <read_first>
    - `.planning/phases/07.3-sidecar-process-management/07.3-SPEC.md` §Requirements R9 + §Constraints（Windows 无 SIGTERM、跨平台路径）
    - `.planning/phases/07.3-sidecar-process-management/07.3-PATTERNS.md` §2.9（跨平台终止机制）+ §2.8（app_local_data_dir 三平台路径）
  </read_first>
  <action>Windows: 若有 Windows 环境，cargo tauri dev 启动 + 同 macOS 四场景；若无，`rustup target add x86_64-pc-windows-msvc; cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml --target x86_64-pc-windows-msvc 2>&1 | tail -20` 验证编译（nix crate cfg(unix) 守卫）。Linux: 若有 Linux 环境（含 X11/Wayland），同四场景；若无，至少 `cargo check --target x86_64-unknown-linux-gnu`。把结果（pass/deferred + 风险）记入 UAT.md。Windows 重点验证：child.start_kill() 走 Job Object 不残留；Linux 重点验证：app_local_data_dir 解析为 ~/.local/share/com.worsmer.paladin/logs。</action>
  <verify>
    <automated>cd apps/desktop/src-tauri &amp;&amp; cargo check --target x86_64-pc-windows-msvc 2>&amp;1 | tail -5 || echo "windows_target_unavailable"</automated>
  </verify>
  <acceptance_criteria>UAT.md 含 Windows + Linux 两列；每列四场景标注 pass 或 deferred（含理由 + 风险评估）；若 cargo check --target windows-msvc 可用则编译验证 pass；若环境完全缺失则三平台中 macOS 全 pass + Win/Linux deferred 风险明确（Windows 终止机制 + Linux 显示服务器依赖），STATE.md 标注「Phase 07.3 macOS UAT pass, Windows/Linux UAT deferred to Phase 10 Packaging 矩阵」。</acceptance_criteria>
  <done>Acceptance criteria above are met.</done>
</task>

<task id="07.3-10-05" type="execute">
  <name>Task 5: 写 VALIDATION.md — 22 条 SPEC Acceptance 逐条验证</name>
  <description>把 SPEC §Acceptance Criteria 的 22 条 checkbox 逐条验证，每条含 checkbox 引用、验证方法（命令/UAT/source gate）、pass/fail、证据。</description>
  <read_first>
    - `.planning/phases/07.3-sidecar-process-management/07.3-SPEC.md` §Acceptance Criteria（核心功能 11 条 + 日志与控制 6 条 + 配置与跨平台 5 条 = 22 条）
  </read_first>
  <action>创建 `.planning/phases/07.3-sidecar-process-management/07.3-VALIDATION.md`：按 SPEC §Acceptance 三段（核心功能 / 日志与控制 / 配置与跨平台）逐条列：checkbox 原文 + 验证方法 + 结果（pass/fail）+ 证据（命令输出或 UAT.md 引用或 source gate 结果）。例如「桌面端启动后 5s 内 Agent + Go Server 两个子进程自动 spawn」→ pass + 证据 UAT.md macOS 启动场景 ps 输出；「grep -rn localhost:9876 apps/desktop/src 业务代码 0 命中」→ pass + 证据 VERIFICATION.md Task 2 grep 输出空。</action>
  <verify>
    <automated>test -f .planning/phases/07.3-sidecar-process-management/07.3-VALIDATION.md &amp;&amp; grep -cE '^\| *\[[ x]\]' .planning/phases/07.3-sidecar-process-management/07.3-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>07.3-VALIDATION.md 存在；含 22 条 checkbox 行（与 SPEC §Acceptance 一一对应）；每条含验证方法 + 结果 + 证据；22 条全 pass（或 fail 明确标注 + 回退 plan）。</acceptance_criteria>
  <done>Acceptance criteria above are met.</done>
</task>

<task id="07.3-10-06" type="execute">
  <name>Task 6: 写 VERIFICATION.md — 13 Edge + 4 Prohibitions + scope gates</name>
  <description>逐条验证 SPEC §Edge Coverage（13 行：9 covered 给证据，4 dismissed 给理由）+ §Prohibitions（4 条 must-NOT：验证方法 + 结果）+ PATTERNS §0 四条纠偏闭环确认。</description>
  <read_first>
    - `.planning/phases/07.3-sidecar-process-management/07.3-SPEC.md` §Edge Coverage + §Prohibitions
    - `.planning/phases/07.3-sidecar-process-management/07.3-PATTERNS.md` §0（四条纠偏）
  </read_first>
  <action>创建 `.planning/phases/07.3-sidecar-process-management/07.3-VERIFICATION.md`：Edge 段 — 13 行表格逐条：Category / Requirement / Status / 验证证据（covered 行给命令或 UAT 引用，dismissed 行给 dismissed 理由引用 SPEC 原文）。Prohibitions 段 — 4 行表格逐条：statement / verification method / result。PATTERNS §0 闭环段 — 四条纠偏逐条确认：popover.tsx 存在（plan 04 证据）、processes.json 存在（plan 01 证据）、Cargo.toml tokio+reqwest+nix 不含 shell plugin（grep 证据）、CSP 无需改（tauri.conf.json connect-src 证据）。Scope gates 段 — 全绿截图或命令输出（cargo test/clippy/tsc/biome + grep gates）。</action>
  <verify>
    <automated>test -f .planning/phases/07.3-sidecar-process-management/07.3-VERIFICATION.md &amp;&amp; grep -cE 'covered|dismissed|P1|P2|P3|P4|PATTERNS §0' .planning/phases/07.3-sidecar-process-management/07.3-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>07.3-VERIFICATION.md 存在；含 13 Edge 行（9 covered + 4 dismissed）+ 4 Prohibitions 行 + PATTERNS §0 四条闭环 + scope gates 全绿证据；每条验证方法可重现。</acceptance_criteria>
  <done>Acceptance criteria above are met.</done>
</task>

<task id="07.3-10-07" type="execute">
  <name>Task 7: 更新 STATE.md + ROADMAP.md 标记 Phase 07.3 complete</name>
  <description>STATE.md 推进 current_phase、Phase 07.3 标 Complete、progress completed_plans +7（plan 04-10）、Recent Activity 追加 2026-07-05 条目。ROADMAP.md Phase 07.3 段 Plans 改 10/10 complete + 列 10 个 plan + Goal 段从 TBD 改实际交付总结。</description>
  <read_first>
    - `.planning/STATE.md`（当前 current_phase 9 + Phase 07.3 段）
    - `.planning/ROADMAP.md`（Phase 07.3 段 — 当前 Plans 0/TBD）
  </read_first>
  <action>更新 STATE.md：frontmatter current_phase 保持 9（Phase 9 仍是最深完成态）但 Phase Status 表 Phase 07.3 行改 Complete；progress completed_plans 从 33 改 40（+7 plan 04-10）；Recent Activity 顶部追加 `2026-07-05: Phase 07.3 executed — Rust ProcessSupervisor (spawn/probe/backoff/shutdown/log capture) + processes.json config + 6 Tauri commands + 前端 process store + StatusBar 多灯 + LogsPanel + StartupMask; SDC-01/02/03 闭环; macOS UAT pass, Win/Linux deferred to Phase 10`。更新 ROADMAP.md Phase 07.3 段：Plans 改 `10/10 complete`；Plans 列表列出 07.3-01..07.3-10 全部 [x]；Goal 段从 `[Urgent work - to be planned]` 改实际交付总结一句话。autonomous=false 因为若 Win/Linux deferred 则 STATE 需人工确认标注。</action>
  <verify>
    <automated>grep -E 'current_phase|completed_plans|07.3.*Complete|10/10' .planning/STATE.md .planning/ROADMAP.md | head -10</automated>
  </verify>
  <acceptance_criteria>STATE.md Phase Status 表 Phase 07.3 行 Complete；progress completed_plans = 40；Recent Activity 含 2026-07-05 Phase 07.3 条目；ROADMAP.md Phase 07.3 段 Plans 10/10 complete + 10 个 plan 文件 [x] + Goal 段更新；若 Win/Linux deferred 则 STATE 显式标注。</acceptance_criteria>
  <done>Acceptance criteria above are met.</done>
</task>

</tasks>

<verification>
执行 Wave 5 闭环验收命令：

`test -f .planning/phases/07.3-sidecar-process-management/07.3-VALIDATION.md`

`test -f .planning/phases/07.3-sidecar-process-management/07.3-UAT.md`

`test -f .planning/phases/07.3-sidecar-process-management/07.3-VERIFICATION.md`

`grep -E '07.3.*Complete|10/10' .planning/STATE.md .planning/ROADMAP.md`

`cd apps/desktop/src-tauri && cargo test && cd ../.. && npx tsc --noEmit && npx biome check src/`

完成后 Phase 07.3 全部 9 SPEC requirements + 22 acceptance + 13 edge + 4 prohibitions 验证闭环，STATE/ROADMAP 标 complete，下游 Phase 10 Packaging 可信任 sidecar 已就绪（dev spawn 模式 → packaged 改 processes.json 即可零代码切换）。
</verification>

<success_criteria>
- cargo build/test/clippy + tsc + biome 全绿。
- 7 source gates 全绿（R8 URL / D-13 shell plugin / base-nova radix / processes.json schema / CSP / Cargo deps / popover 底层）。
- macOS UAT 4 核心 + 4 扩展场景 pass；Win/Linux pass 或 deferred + 风险明确。
- VALIDATION.md 22 条 acceptance 全 pass。
- VERIFICATION.md 13 edge + 4 prohibitions + PATTERNS §0 闭环。
- STATE.md + ROADMAP.md 标 Phase 07.3 complete。
- SPEC R9 跨平台闭环（macOS 全 pass；Win/Linux 编译验证 + UAT 或 deferred）。
</success_criteria>

<output>
Create `.planning/phases/07.3-sidecar-process-management/07.3-10-SUMMARY.md` when done, including the three-platform UAT result matrix, the source gates tally, and the hand-off note to Phase 10 Packaging (processes.json mode=packaged 切换 + externalBin 配置在那个 phase）。
</output>
