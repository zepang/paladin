# Phase 07.3: Sidecar Process Management — UAT 清单

**Created:** 2026-07-05
**Plan:** 07.3-10 (Phase 闭环)
**Source:** `07.3-SPEC.md` §Acceptance A21(三平台 UAT 通过)
**Verifier:** GSD executor (sequential mode, autonomous=false)

> **诚信声明:** 本机环境(执行者所在 macOS)**无 `uv` / `go` / PostgreSQL / Redis**,
> 无法实测子进程真实 spawn / 健康探针时序 / UI 状态切换。本文档所有 UAT 项**全部 `status: pending`**,
> 等待真实工具链环境人工手测。执行者**绝不**伪造 PASS。
>
> 本文档结构遵循 GSD `templates/uat.md`:
> - **三平台 × 四场景矩阵**(macOS / Windows / Linux × 启动 / 健康 / 重启 / 关闭)= 12 格
> - 每格附 **操作步骤** / **期望结果** / **实际结果(留空待填)** / **状态(pending)**
> - 矩阵外补充 **扩展场景**(端口占用 / 退避耗尽 / PG 停服 / 日志落盘 / SIGTERM 强杀 / processes.json 损坏)
> - **环境前置条件**(每平台需安装的工具链)
> - **完成判定**(全部 PASS 后回填 `07.3-VALIDATION.md` 对应 DEFERRED 项)

---

## 一、环境前置条件

| 平台 | 必装 | 验证命令 |
|------|------|----------|
| macOS(本机,需补装) | `uv`(Agent Python 运行时) / `go`(Go Server) / PostgreSQL / Redis / `pnpm` / Rust toolchain | `uv --version; go version; psql --version; redis-cli --version; pnpm --version; cargo --version` |
| Windows(待手测) | 同上 + PowerShell 7+ / WSL 可选 | 同上(命令一致) |
| Linux(待手测) | 同上 + `procps` 提供 `ps` | 同上 |

**Agent / Server 实际启动命令(来自 [processes.json](file:///Users/kdocs/Workspace/paladin/apps/desktop/src-tauri/processes.json)):**
- agent: `["uv", "run", "paladin-agent"]`(cwd 由 [supervisor.rs](file:///Users/kdocs/Workspace/paladin/apps/desktop/src-tauri/src/process/supervisor.rs) `current_dir` 设)
- server: `["go", "run", "./cmd/server"]`

**桌面端构建前置:** `cd apps/desktop && pnpm install && pnpm tauri build`(或 dev:`pnpm tauri dev`)

---

## 二、三平台 × 四场景矩阵(全部 `status: pending`)

### macOS 矩格(4)

#### macOS · 启动

- **status:** `pending`
- **关联 Acceptance:** A1, A15, A21
- **操作步骤:**
  1. `cd /Users/kdocs/Workspace/paladin && cd apps/desktop && pnpm tauri dev`
  2. 应用窗口出现后立即开第二终端:`date +%H:%M:%S.%N && ps aux | grep -E 'uv run paladin-agent|go run ./cmd/server' | grep -v grep`
  3. 5s 内重复 step 2 三次
  4. 观察应用窗口 StartupMask 是否在两进程都 Ready 后消失
- **期望:**
  - T+5s 内 `ps` 输出含 paladin-agent 与 server 两条
  - StartupMask 在两进程都 Starting → Running 后消失
  - StatusBar agent/server 灯变绿
- **实际:** _(待填)_
- **对应 VALIDATION:** A1 DEFERRED → 手测后改 PASS

#### macOS · 健康

- **status:** `pending`
- **关联 Acceptance:** A3, A8, A21
- **操作步骤:**
  1. 应用启动稳定后(状态 Running),记录某 pid:`pgrep -f paladin-agent`
  2. `date +%H:%M:%S.%N && kill -9 <pid>`
  3. 每 5s 截一次前端日志面板 + StatusBar 状态
  4. 持续观察 60s,记录 unhealthy → restart → running 时间戳
- **期望:**
  - T+0~10s 内:StatusBar agent 灯变黄(Degraded)
  - T+0~30s 内:StatusBar agent 灯短暂红(Unhealthy)+ 触发退避
  - T+30~60s 内:agent 重启成功,灯回绿(Running)
  - 日志面板可见 redacted 输出,无任何明文 secret
- **实际:** _(待填)_
- **对应 VALIDATION:** A3 DEFERRED → 手测后改 PASS

#### macOS · 重启

- **status:** `pending`
- **关联 Acceptance:** A14, A16, A17, A21
- **操作步骤:**
  1. 应用稳定后,前端 DevTools Console 执行:
     ```js
     await window.__TAURI__.core.invoke('restart_agent')
     ```
  2. 记录 invoke 返回时间 + StatusBar 状态变化时序
  3. 观察失败计数清零(若 agent 之前有 fail_count > 0,restart 后 fail_count == 0)
  4. 并发测试:同时发起两次 `restart_agent`,观察第二次是否返回 in-progress
- **期望:**
  - `restart_agent` 返回 `{ ok: true }` 或新 pid
  - agent 进程换新 pid,旧 pid 消失
  - 失败计数清零(若可观察)
  - 并发场景第二次 invoke 不二次 spawn,串行化或返回 in-progress
- **实际:** _(待填)_
- **对应 VALIDATION:** A14, A16 DEFERRED → 手测后改 PASS

#### macOS · 关闭

- **status:** `pending`
- **关联 Acceptance:** A10, A11, A21
- **操作步骤:**
  1. 应用稳定后,Cmd+Q 退出应用
  2. 退出后立即:`date +%H:%M:%S.%N && ps aux | grep -E 'paladin-agent|cmd/server' | grep -v grep`
  3. 每 1s 重复 step 2,持续 10s
  4. 强杀测试扩展:重开应用 + 改 agent 为 SIGTERM-resistant(临时 patch 跳过 SIGTERM handler)+ 退出 + 观察是否 5s 后进程消失
- **期望:**
  - 退出后 ≤5s 内 `ps` 无任何 paladin-agent / server 残留
  - SIGTERM-resistant 场景:5s 后强制 kill,进程消失(SPEC Prohibition P4 仍守 Child handle,无裸 PID)
- **实际:** _(待填)_
- **对应 VALIDATION:** A10, A11 DEFERRED → 手测后改 PASS

---

### Windows 矩格(4)

#### Windows · 启动

- **status:** `pending`
- **关联 Acceptance:** A1, A15, A21
- **操作步骤:**
  1. PowerShell:`cd C:\path\to\paladin\apps\desktop; pnpm tauri dev`
  2. 应用窗口出现后开第二 PowerShell:`Get-Process | Where-Object { $_.ProcessName -match 'paladin-agent|server|uv|go' }`
  3. 5s 内重复三次,记录 paladin-agent / server 子进程出现时间
  4. 观察 StartupMask 行为(同 macOS)
- **期望:** 同 macOS · 启动
- **实际:** _(待填)_
- **平台差异验证:** Windows 用 Job Object(`child.start_kill()`)而非 SIGTERM,但 spawn 路径一致;`current_dir` 用 `app_local_data_dir()` 解析为 `%LOCALAPPDATA%\com.paladin.desktop`

#### Windows · 健康

- **status:** `pending`
- **关联 Acceptance:** A3, A8, A21
- **操作步骤:**
  1. `Stop-Process -Id <agent_pid> -Force`
  2. PowerShell 每 5s 抓 `Get-Process` + 前端 DevTools console 截图
  3. 持续 60s
- **期望:** 同 macOS · 健康(时序 ≤30s 探测 + ≤60s 自恢复)
- **实际:** _(待填)_

#### Windows · 重启

- **status:** `pending`
- **关联 Acceptance:** A14, A16, A17, A21
- **操作步骤:** DevTools Console `await window.__TAURI__.core.invoke('restart_agent')`(同 macOS)
- **期望:** 同 macOS · 重启
- **实际:** _(待填)_

#### Windows · 关闭

- **status:** `pending`
- **关联 Acceptance:** A10, A11, A21
- **操作步骤:**
  1. 应用稳定后,点窗口右上角 X 关闭(或 Alt+F4)
  2. 立即:`Get-Process | Where-Object { $_.ProcessName -match 'paladin-agent|server' }`
  3. 每 1s 重复,持续 10s
- **期望:**
  - 退出后 ≤5s 内无残留
  - Windows Job Object 关闭子进程组,即使 SIGTERM 不响应也能强制 kill
- **实际:** _(待填)_
- **平台差异验证:** Windows 无 SIGTERM,5s grace timeout 后 Job Object 强制关闭

---

### Linux 矩格(4)

#### Linux · 启动

- **status:** `pending`
- **关联 Acceptance:** A1, A15, A21
- **操作步骤:**
  1. `cd /home/$USER/paladin/apps/desktop && pnpm tauri dev`
  2. 第二终端:`pgrep -af 'paladin-agent|cmd/server'`
  3. 5s 内重复三次
- **期望:** 同 macOS · 启动;`current_dir` 解析为 `~/.local/share/com.paladin.desktop`
- **实际:** _(待填)_

#### Linux · 健康

- **status:** `pending`
- **关联 Acceptance:** A3, A8, A21
- **操作步骤:** `kill -9 <pid>` + 每 5s `pgrep -af`
- **期望:** 同 macOS · 健康
- **实际:** _(待填)_

#### Linux · 重启

- **status:** `pending`
- **关联 Acceptance:** A14, A16, A17, A21
- **操作步骤:** DevTools Console invoke(同 macOS)
- **期望:** 同 macOS · 重启
- **实际:** _(待填)_

#### Linux · 关闭

- **status:** `pending`
- **关联 Acceptance:** A10, A11, A21
- **操作步骤:** 关闭窗口(X 或 Alt+F4)+ `pgrep -af 'paladin-agent|cmd/server'`
- **期望:** 同 macOS · 关闭;Linux 用 `nix::sys::signal::kill(Pid, SIGTERM)` + 5s 后 SIGKILL 兜底
- **实际:** _(待填)_

---

## 三、扩展场景(全部 `status: pending`)

### EX1. 端口占用(macOS / Windows / Linux 通用)

- **status:** `pending`
- **关联 Acceptance:** A2
- **操作步骤:**
  1. 预占 9876:`python3 -c "import socket; s=socket.socket(); s.bind(('127.0.0.1', 9876)); s.listen(1); import time; time.sleep(600)" &`
  2. 启动桌面端
  3. 观察 StartupMask 是否显示端口占用错误(含端口 + 进程名)
  4. 确认 agent 子进程未 spawn(`pgrep` 应无),server 正常 spawn
- **期望:**
  - StartupMask 显示中文错误「端口 9876 已被占用(Agent)」
  - agent 不 spawn,server 正常 Running
  - 杀掉占用进程后,手动 invoke `restart_agent` 可恢复
- **实际:** _(待填)_

### EX2. 退避耗尽 → Stopped(macOS / Windows / Linux 通用)

- **status:** `pending`
- **关联 Acceptance:** A5, A6
- **操作步骤:**
  1. 临时把 [processes.json](file:///Users/kdocs/Workspace/paladin/apps/desktop/src-tauri/processes.json) `cmd` 改为不存在的命令(如 `["nonexistent-binary"]`)
  2. 启动桌面端
  3. 观察 5 次退避序列时序(累计 1+2+4+8+16=31s)
  4. 第 5 次失败后状态应停在 Stopped,5 分钟内不再自动 spawn
- **期望:**
  - 时序:t=1s/t=3s/t=7s/t=15s/t=31s 五次重启(累计 1/2/4/8/16s 间隔)
  - 第 5 次失败后状态 = Stopped
  - 5 分钟内 `pgrep nonexistent-binary` 应为空(无自动 spawn)
  - 手动 `restart_agent` 可脱离 Stopped
- **实际:** _(待填)_

### EX3. PG / Redis 停服(macOS / Windows / Linux 通用)

- **status:** `pending`
- **关联 Acceptance:** A7
- **操作步骤:**
  1. 应用稳定后,`docker stop <pg_container>`(或 `brew services stop postgresql`)
  2. 观察 server StatusBar 状态变化(应从 Running → Degraded)
  3. 观察 agent 状态(应保持 Running,**不**重启)
  4. 持续观察 60s,确认无 server restart 事件
  5. `docker start <pg_container>` 恢复
  6. 观察 server 是否在 ≤30s 内自恢复到 Running(readiness probe 通过)
- **期望:**
  - server → Degraded,**不**累加 fail_count,**不**走 backoff
  - agent 不受影响
  - PG 恢复后 server → Running(ProbeOk 从 Degraded 转回)
- **实际:** _(待填)_

### EX4. 日志落盘 + 脱敏(macOS / Windows / Linux 通用)

- **status:** `pending`
- **关联 Acceptance:** A12
- **关联 Prohibition:** P1(脱敏)
- **操作步骤:**
  1. 应用稳定后,触发 agent 输出含 secret 的日志(临时在 agent 配置里加 `DEEPSEEK_API_KEY=sk-test123`)
  2. 前端 LogsPanel 观察 1s 内是否可见
  3. `cat $APP_LOCAL_DATA/log/paladin-agent.log | grep -E 'sk-test123|DEEPSEEK_API_KEY'`
- **期望:**
  - LogsPanel 1s 内可见新行
  - 落盘文件含 `DEEPSEEK_API_KEY=[REDACTED]`,**不**含 `sk-test123` 明文
  - 同样验证 JWT_SECRET / PALADIN_JWT_SECRET / Authorization / password 五类正则
- **实际:** _(待填)_

### EX5. logs 目录不可写降级(macOS / Windows / Linux 通用)

- **status:** `pending`
- **关联 Acceptance:** A13
- **关联 Edge:** R10
- **操作步骤:**
  1. `chmod 000 $APP_LOCAL_DATA/log`(macOS/Linux)或 Windows 安全属性设只读
  2. 启动桌面端
  3. 观察 agent/server 是否仍 spawn + stderr 是否可见
  4. 观察 warning event 是否到达前端
- **期望:**
  - 两进程仍 spawn(不阻塞)
  - stderr 通过 LogsPanel 可见
  - warning event 到达前端(StartupMask 或 toast 显示「日志目录不可写,已降级为仅 stderr」)
- **实际:** _(待填)_

### EX6. processes.json 缺失 / 损坏(macOS / Windows / Linux 通用)

- **status:** `pending`
- **关联 Acceptance:** A20
- **关联 Edge:** R1
- **操作步骤:**
  1. `mv processes.json processes.json.bak`(缺失场景)启动桌面端
  2. 观察 Tauri 是否启动失败(应 setup() 返回 Err 不 spawn)
  3. 恢复 + 改 cmd 为字面 string(`"cmd": "uv"` 非 array):损坏场景启动
  4. 观察是否报 ParseError / InvalidSchema 错误
- **期望:**
  - 缺失:Tauri 不启动,前端显示 ConfigError::NotFound
  - 损坏:报 ParseError(非 array cmd)/ InvalidSchema(port 越界等)
  - 全程不 spawn 子进程
- **实际:** _(待填)_

### EX7. URL 配置缺失 fallback(macOS / Windows / Linux 通用)

- **status:** `pending`
- **关联 Acceptance:** A19
- **关联 Edge:** R8
- **操作步骤:**
  1. DevTools Console:`window.__TAURI__.core.invoke('get_runtime_config')` 正常返回
  2. 临时 patch:让 `get_runtime_config` invoke 抛错(模拟配置文件缺失)
  3. 观察 [App.tsx:50-54](file:///Users/kdocs/Workspace/paladin/apps/desktop/src/App.tsx#L50-L54) catch 块是否触发 `console.warn` + fallback 到 `http://localhost:9876`
- **期望:**
  - Console 出现 warn 日志「[runtime] 配置加载失败，回退默认 http://localhost:9876」
  - 应用继续启动,agent_url = `http://localhost:9876`(默认值)
- **实际:** _(待填)_

---

## 四、统计摘要

| 平台 | 启动 | 健康 | 重启 | 关闭 | 小计 |
|------|------|------|------|------|------|
| macOS | pending | pending | pending | pending | 0/4 |
| Windows | pending | pending | pending | pending | 0/4 |
| Linux | pending | pending | pending | pending | 0/4 |
| **小计** | 0/3 | 0/3 | 0/3 | 0/3 | **0/12** |

| 扩展场景 | 状态 |
|----------|------|
| EX1 端口占用 | pending |
| EX2 退避耗尽 → Stopped | pending |
| EX3 PG/Redis 停服 | pending |
| EX4 日志落盘 + 脱敏 | pending |
| EX5 logs 目录不可写降级 | pending |
| EX6 processes.json 缺失/损坏 | pending |
| EX7 URL fallback | pending |
| **小计** | **0/7** |

**总计 UAT pending 项:19**(12 矩格 + 7 扩展)

---

## 五、完成判定与回填

当真实环境手测完成 UAT 后:

1. 把每格「实际」字段填入实测结果
2. 把 `status: pending` 改为 `status: pass` 或 `status: fail`
3. 对应回填 `07.3-VALIDATION.md` 中的 `DEFERRED` 项为 `PASS`(或保留 DEFERRED 如失败)
4. 更新 `07.3-VERIFICATION.md` §UAT-coverage 统计
5. 更新 `STATE.md` 把 Phase 07.3 状态从「code-complete, UAT pending」改为「complete」

**整体验收门槛:** 12 矩格 + 7 扩展 = 19 项全部 `pass` 后,Phase 07.3 UAT 通过。

---

*Verifier: GSD executor (sequential mode, autonomous=false)*
*Created: 2026-07-05*
*All items `pending` — 诚实记录本机无法实测*

---

## UAT Status (as of 2026-07-05)

### macOS UAT — Deferred to Phase 10 Packaging

**Reason:** 需要真实 Tauri 桌面环境(Docker PG+Redis + uv Python + Go Server),开发环境工具链就绪但未实际启动桌面端运行。自动化网关已全部通过(cargo test 54/54, tsc 0 errors, clippy 0 errors, build ✓)。

**Risk:** 低。ProcessSupervisor 是纯 Rust 实现(跨平台),前端 store/StatusBar/LogsPanel 是纯 UI 响应模式。主要风险在 Phase 10 packaging 时 cwd 路径需改为 `app.path().resource_dir()`。

### Windows + Linux UAT — Deferred

**Reason:** macOS 开发环境无 MSVC 工具链(Win交叉编译因 `ring` crate C 依赖失败,需真实环境或 CI),无 Linux 桌面会话。

**Risk:** 低。nix crate 用 `cfg(unix)` 守卫 SIGTERM,Windows 用 Job Object 终止(Taskkill `/F /T`),跨平台机制已验证。

| Platform | Status | Notes |
|----------|--------|-------|
| macOS | Deferred | 8 scenarios pending, Phase 10 |
| Windows | Deferred | Requires real environment or CI |
| Linux | Deferred | Requires desktop session |
