---
status: partial
phase: 07.3-sidecar-process-management
source:
  - 07.3-04-SUMMARY.md
  - 07.3-06-SUMMARY.md
  - 07.3-07-SUMMARY.md
  - 07.3-08-SUMMARY.md
  - 07.3-09-SUMMARY.md
  - 07.3-10-SUMMARY.md
started: 2026-07-06T08:44:20Z
updated: 2026-07-11T08:03:45Z
---

## Current Test

[testing paused — 1 item outstanding]

## Tests

### 1. Cold Start Sidecar Smoke Test
expected: Start the desktop app from a clean state with uv, Go, PostgreSQL, and Redis available. Within 5 seconds the app automatically starts both sidecars from processes.json: paladin-agent on port 9876 and the Go server on port 9880. The startup mask clears once both are ready, and the StatusBar shows green process lights for agent and server.
result: issue
reported: "User manually started agent and server successfully, but the desktop UI still shows Agent stopped: restart spawn failed: spawn Agent failed: No such file or directory (os error 2)."
severity: blocker

### 2. Health Detection and Automatic Restart
expected: Kill the running agent process after the app is stable. Within 30 seconds the UI reports the agent as degraded or unhealthy, exposes the latest error/stderr, then the supervisor restarts the agent with the configured backoff and the StatusBar returns to green once it is healthy again.
result: pass
source: human
reported: "User completed the abnormal/process recovery group and confirmed it passed."

### 3. Manual Process Controls
expected: Open the agent process light popover and use restart_agent, stop_agent, and get_process_status. Restart changes the agent PID and clears failure counters; stop removes the process; status returns agent and server states using starting/running/degraded/unhealthy/stopped values. A concurrent double restart is serialized or reported as in progress, with no duplicate spawn.
result: pass
source: human
reported: "User completed the desktop supervisor basics group and confirmed restart, stop, status, and concurrent restart behavior passed."

### 4. Graceful Shutdown Cleanup
expected: Quit the desktop app normally. Within 5 seconds no paladin-agent or Go server child process remains. If a child ignores graceful termination, the supervisor force-kills it after the grace window without killing unrelated processes.
result: pass
source: human
reported: "User completed the desktop supervisor basics group and confirmed shutdown cleanup passed."

### 5. Port Occupied Handling
expected: Pre-bind port 9876 or 9880 before launching the app. The matching sidecar does not spawn, the UI shows a clear port occupied error including the port and process name, and the other sidecar can still start if its port is free.
result: pass
source: human
reported: "User completed the abnormal/process recovery group and confirmed port occupied handling passed."

### 6. Backoff Exhaustion and Stopped State
expected: Configure one sidecar command to fail repeatedly. The supervisor retries using 1s, 2s, 4s, 8s, and 16s delays, then leaves the process in stopped state. For the next 5 minutes it does not auto-spawn again until the user triggers restart.
result: pass
source: human
reported: "User confirmed the final UI stopped at Agent stopped with error: Agent 退避 5 次耗尽,放弃自恢复. Go remained supervisor-owned and running."

### 7. Go Readiness Degraded Without Restart
expected: Stop PostgreSQL or Redis while the Go server is running. The server process remains alive, the UI reports degraded readiness, the agent remains running, and once the dependency is restored the server returns to running without a process restart.
result: pass
source: human
reported: "User replied pass."

### 8. Logs Panel Live Output and Redaction
expected: Open the Logs tab and trigger stdout/stderr from both sidecars. New lines appear in the correct agent/server tab within 1 second, stderr is visually distinguished, pause and clear controls behave predictably, log files are written to the app log directory, and secrets such as DEEPSEEK_API_KEY, JWT_SECRET, PALADIN_JWT_SECRET, Authorization bearer tokens, and password values are redacted in both UI and files.
result: pass
source: human
reported: "User followed the temporary sidecar log command flow and provided UI screenshot plus log file path. Verified /Users/kdocs/Library/Logs/com.worsmer.paladin/paladin-agent.log contains stdout and stderr lines, with DEEPSEEK_API_KEY, JWT_SECRET, PALADIN_JWT_SECRET, Authorization bearer token, and password values redacted as [REDACTED]."

### 9. Logs Directory Unwritable Fallback
expected: Make the app log directory unwritable and start the app. Both sidecars still spawn, stderr output remains visible in the Logs panel, and the UI surfaces a warning that log persistence degraded instead of blocking startup.
result: issue
reported: "With the app log directory unwritable, Agent and Go still started and the Agent log panel continued to show live logs, but no user-visible warning about degraded log persistence was observed."
severity: minor

### 10. Config Missing or Invalid
expected: Launch with processes.json missing or invalid, including a non-array cmd field. The supervisor reports a configuration error, no sidecar process is spawned, and the user sees the config failure instead of a silent startup hang.
result: pass
source: human
reported: "User verified missing processes.json path. UI displayed runtime config invalid, NotFound path details, and Agent/Go both remained unowned and stopped."

### 11. Runtime URL Fallback
expected: Simulate get_runtime_config failing. The app logs the configured console warning, falls back to http://localhost:9876 for the agent URL, and continues rendering without any remaining business-code hardcoded localhost URL dependency.
result: pass
source: human
reported: "User temporarily invoked get_runtime_config_broken. Screenshot showed console warning '[runtime] 配置加载失败，回退默认 http://localhost:9876' with command-not-found detail, and the UI continued rendering."

### 12. Cross-Platform Sidecar Smoke
expected: On macOS, Windows, and Linux, run the same start, health, restart, and shutdown flow. Process paths and log directories resolve to platform-appropriate locations, Windows termination uses the equivalent process-tree mechanism, Unix termination uses the SIGTERM plus force-kill fallback, and all four flows pass on each platform.
result: blocked
blocked_by: physical-device
reason: "macOS pass, Windows/Linux blocked"

### 13. Automated Code and Source Gates
expected: Automated checks from the phase summaries remain green: cargo test --lib reports 54 passed, TypeScript reports 0 errors, clippy reports 0 errors for the 07.3 scope, source gates confirm no tauri-plugin-shell, commands remain array-based in processes.json, URL business-code hardcoding is removed, and SPEC Edge/Prohibition verification remains recorded in VERIFICATION.md.
result: pass
source: automated

## Summary

total: 13
passed: 10
issues: 2
pending: 0
skipped: 0
blocked: 1

## Gaps

- truth: "Start the desktop app from a clean state with uv, Go, PostgreSQL, and Redis available. Within 5 seconds the app automatically starts both sidecars from processes.json: paladin-agent on port 9876 and the Go server on port 9880. The startup mask clears once both are ready, and the StatusBar shows green process lights for agent and server."
  status: failed
  reason: "User reported: manually started agent and server successfully, but desktop UI still shows Agent stopped: restart spawn failed: spawn Agent failed: No such file or directory (os error 2)."
  severity: blocker
  test: 1
  root_cause: "Two spawn-time path assumptions failed. First, processes.json dev cwd values were written as ../../apps/agent and ../../apps/server, but supervisor.rs resolves relative cwd from apps/desktop/src-tauri, so those paths became apps/apps/agent and apps/apps/server. Second, Tauri/cargo-run dev processes may not inherit a login-shell PATH containing /usr/local/go/bin or ~/.local/bin, so Command::new(\"go\")/Command::new(\"uv\") can also fail with os error 2."
  artifacts:
    - path: "apps/desktop/src-tauri/processes.json"
      issue: "agent/server cwd pointed to non-existent directories from src-tauri"
    - path: "apps/desktop/src-tauri/src/process/config_tests.rs"
      issue: "coverage lacked a regression test proving configured cwd paths resolve from CARGO_MANIFEST_DIR"
    - path: "apps/desktop/src-tauri/src/process/supervisor.rs"
      issue: "spawn environment assumed the parent PATH already contained common uv/go install directories"
    - path: "apps/desktop/src-tauri/src/process/supervisor_tests.rs"
      issue: "new regression coverage for augmented spawn PATH"
  missing:
    - "Update dev cwd to ../../agent and ../../server"
    - "Add a config regression test that resolves every dev cwd from src-tauri and asserts the directory exists"
    - "Augment child process PATH with common dev tool directories before spawn without invoking a shell"
    - "Add a supervisor regression test proving /usr/local/go/bin and ~/.local/bin are present in the spawn PATH"
  debug_session: "inline systematic-debugging 2026-07-06"
- truth: "Make the app log directory unwritable and start the app. Both sidecars still spawn, stderr output remains visible in the Logs panel, and the UI surfaces a warning that log persistence degraded instead of blocking startup."
  status: failed
  reason: "User reported: With the app log directory unwritable, Agent and Go still started and the Agent log panel continued to show live logs, but no user-visible warning about degraded log persistence was observed."
  severity: minor
  test: 9
  artifacts: []
  missing: []
