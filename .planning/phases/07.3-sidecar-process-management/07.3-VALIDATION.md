# Phase 07.3: Sidecar Process Management — VALIDATION

**Created:** 2026-07-05
**Plan:** 07.3-10 (Phase 闭环)
**Source:** `07.3-SPEC.md` §Acceptance Criteria
**Verifier:** GSD executor (sequential mode, autonomous=false)

> 本文档逐条验证 SPEC `07.3-SPEC.md` §Acceptance Criteria 的 21 条 checkbox。
>
> **重要计数偏差(详见 07.3-10-SUMMARY.md §Deviations #1):** SPEC frontmatter 与 PLAN frontmatter
> 均声称「22 条 acceptance」,但实际数 SPEC §Acceptance Criteria 的 checkbox 是 **21 条**
> (核心功能 11 + 日志与控制 6 + 配置与跨平台 4)。本文按实际 21 条验证。
>
> **状态语义:**
> - `PASS` —— 静态证据(源码 / 配置 / 类型 / 单元测试 / source gate grep)已**完整**证明该条
> - `DEFERRED` —— 决策/契约层有静态证据,但完整验收需要真实运行时(子进程实际 spawn、健康
>   探针时序、UI 状态切换、跨平台行为),本机环境无 `uv` / `go` / PostgreSQL / Redis,无法实测,
>   留给 `07.3-UAT.md` 三平台手测
>
> **诚信底线:** 本机无法实测的运行时项**绝不**标 `PASS`。所有 `DEFERRED` 项的代码层证据仍记录
> 在「证据」列,供 UAT 执行时对照。

---

## 统计摘要

| 段 | 总数 | PASS | DEFERRED |
|---|------|------|----------|
| 核心功能 | 11 | 5 | 6 |
| 日志与控制 | 6 | 4 | 2 |
| 配置与跨平台 | 4 | 2 | 2 |
| **合计** | **21** | **11** | **10** |

> SPEC frontmatter 声称的「22 条」与实际 21 条不符,记为偏差(详见 SUMMARY)。

---

## 一、核心功能(11 条)

### A1. 桌面端启动后 5s 内 Agent + Go Server 两个子进程自动 spawn,PID 可查

- **状态:** `DEFERRED`
- **验证方法:** 真实启动桌面端 + `ps aux | grep -E 'uv run paladin-agent|go run ./cmd/server'`
- **代码层证据:**
  - [lib.rs](file:///Users/kdocs/Workspace/paladin/apps/desktop/src-tauri/src/lib.rs) `setup()` 内 `app.manage(ProcessSupervisor::new(...))` + 顶层 tokio task 调 `supervisor.start()`
  - [supervisor.rs](file:///Users/kdocs/Workspace/paladin/apps/desktop/src-tauri/src/process/supervisor.rs) `start()` 按 D-08 Server-first 顺序 spawn,`spawn_child_to_slot` 用 `tokio::process::Command::new(&entry.cmd[0]).args(&entry.cmd[1..])`
  - plan 06 SUMMARY §Accomplishments 1: 「spawn → 端口预检 → probe_loop → wait_exit」完整循环
- **UAT 待办:** macOS `ps` 5s 内两进程可见(详见 `07.3-UAT.md` macOS 启动场景)

### A2. 端口 9876 或 9880 被占时,监督器报错且不 spawn 该进程,前端显示错误态

- **状态:** `DEFERRED`
- **验证方法:** 预占端口 + 启动桌面端 + 观察前端遮罩/StatusBar 状态
- **代码层证据:**
  - [port_probe.rs](file:///Users/kdocs/Workspace/paladin/apps/desktop/src-tauri/src/process/port_probe.rs) `is_port_available(port)` 用 `std::net::TcpListener::bind(("127.0.0.1", port))` + 立即 drop;`check_port_or_error` 包装中文错误消息
  - 7 个单元测试覆盖(plan 05 SUMMARY):空闲 true / 占用 false / drop 后可重检 / err 消息含端口与进程名 / port=0/u16::MAX 不 panic
  - `spawn_child_to_slot` 在 spawn 前调 `check_port_or_error`,失败时 emit port-in-use 事件
  - 前端 [StartupMask.tsx](file:///Users/kdocs/Workspace/paladin/apps/desktop/src/components/StartupMask.tsx) 渲染 last_error + stderr_tail
- **UAT 待办:** 真实端口占用场景 + 前端遮罩状态人眼验证(详见 `07.3-UAT.md` macOS 端口占用场景)

### A3. kill 子进程后 ≤30s 内监督器检测并触发重启(10s×3 探针)

- **状态:** `DEFERRED`
- **验证方法:** `kill -9 <pid>` + 记录前端 unhealthy 事件时间戳
- **代码层证据:**
  - [state_machine.rs](file:///Users/kdocs/Workspace/paladin/apps/desktop/src-tauri/src/process/state_machine.rs) `PROBE_FAIL_THRESHOLD: u32 = 3`(常量锁定)
  - `supervisor.rs` `probe_loop` 用 `tokio::time::interval(Duration::from_secs(10))` 10s 周期
  - state_machine 测试 `test_probe_fail_threshold_3_consecutive` 断言 3 连败 → Unhealthy
- **UAT 待办:** macOS 真实 kill + 时间戳测量(≤30s)(详见 `07.3-UAT.md` macOS 健康场景)

### A4. 重启退避间隔依次为 1s / 2s / 4s / 8s / 16s

- **状态:** `PASS`
- **验证方法:** 静态常量 + 单元测试
- **证据:**
  - [state_machine.rs:28](file:///Users/kdocs/Workspace/paladin/apps/desktop/src-tauri/src/process/state_machine.rs#L28) `pub const BACKOFF_SEQUENCE_MS: [u64; 5] = [1000, 2000, 4000, 8000, 16000];`
  - [processes.json](file:///Users/kdocs/Workspace/paladin/apps/desktop/src-tauri/processes.json) `"backoff_secs": [1, 2, 4, 8, 16]` 配置层锁定
  - state_machine 测试 `test_backoff_sequence_all_5_steps` 断言 5 步序列
  - plan 02 SUMMARY:18 测试覆盖,5×6 矩阵 + 边界

### A5. 连续 5 次重启失败后状态停在 stopped,之后 5 分钟内无自动 spawn

- **状态:** `PASS`
- **验证方法:** 纯函数状态机单元测试(决策逻辑)+ Stopped 终态无 timer 唤醒(架构证明)
- **证据:**
  - state_machine 测试 `test_backoff_exhausted_returns_none`:`backoff_delay(5, 5) == None` → 触发 Stopped
  - state_machine 测试 `test_stopped_ignores_backoff_tick` / `test_stopped_ignores_probe_fail` / `test_stopped_ignores_probe_ok`:Stopped 终态对 BackoffTick / ProbeFail / ProbeOk 全免疫(SPEC Prohibition P3)
  - `supervisor.rs` Stopped 状态下 `probe_loop` 与 `wait_exit` 都 `break`,**无任何后台 timer** 唤醒 Stopped 进程
  - 唯一脱离路径:`RestartRequested`(手动 restart 命令)+ StopRequested(已 Stopped 是 no-op)

### A6. 启动首 5s 内 exit 视为启动失败,直接 stopped 不走退避

- **状态:** `PASS`
- **验证方法:** 纯函数状态机 + 边界测试
- **证据:**
  - [state_machine.rs:34](file:///Users/kdocs/Workspace/paladin/apps/desktop/src-tauri/src/process/state_machine.rs#L34) `pub const STARTUP_GRACE_SECS: f64 = 5.0;`
  - state_machine 测试 `test_spawn_failed_within_grace_goes_stopped`(4.9s → Stopped,**不**消费 backoff 槽位)
  - state_machine 测试 `test_spawn_failed_at_grace_boundary_goes_stopped`(5.0s 边界 → Stopped)
  - `supervisor.rs` `wait_exit` 在 child 退出时按 `elapsed <= STARTUP_GRACE_SECS` 分类为 `SpawnFailed` vs `Crashed`

### A7. 停 PG/Redis,Go Server 不重启,仅状态变 degraded

- **状态:** `DEFERRED`(决策逻辑 PASS,运行时验证 DEFERRED)
- **验证方法:** `docker stop <pg_container>` + 观察 Go 状态 + 确认无 restart 事件
- **代码层证据:**
  - state_machine 测试 `test_probe_degraded_no_fail_count_increment`:`ProbeDegraded` 事件**不**累加 fail_count
  - `supervisor.rs` `probe_loop` 区分 liveness(`/healthz`,计入 fail_count)与 readiness(`/readyz`,只置 Degraded)
  - transition 表:Degraded 状态下 ProbeOk → Running(自恢复),不消费 backoff
- **UAT 待办:** 真实 PG/Redis 停服场景(详见 `07.3-UAT.md` macOS PG 停场景)

### A8. Agent 端点非 200 或 body 异常计为一次探针失败(计入 3 连败阈值)

- **状态:** `PASS`
- **验证方法:** 纯函数状态机 + transition 表
- **证据:**
  - state_machine `transition(_, ProbeFail, _)` 在所有非 Stopped 状态下累加 fail_count,达 `PROBE_FAIL_THRESHOLD=3` → Unhealthy
  - 测试 `test_probe_fail_threshold_3_consecutive`、`test_probe_fail_2_stays_running`、`test_probe_fail_from_degraded_increments_fail_count`
  - `supervisor.rs` `probe_loop` 把 reqwest 非 200 / 网络异常都映射为 `ProbeFail` 事件

### A9. 退出时刻 t≤5s 归启动失败;任何终止原因(非零退出 / 信号 / 崩溃)均算

- **状态:** `PASS`
- **验证方法:** supervisor.rs wait_exit 时间窗分类 + state_machine SpawnFailed/Crashed 区分
- **证据:**
  - `supervisor.rs` `wait_exit`:`let elapsed = start_time.elapsed().as_secs_f64(); if elapsed <= STARTUP_GRACE_SECS { SpawnFailed } else { Crashed }`
  - 不论 exit code(非 0 / 信号 / 崩溃),`child.wait().await` 都会返回,统一进入分类逻辑
  - state_machine `SpawnFailed` 直接 → Stopped(不消费 backoff);`Crashed` → Unhealthy + restart_count+1(走 backoff)
  - 测试 `test_spawn_failed_within_grace_goes_stopped` + `test_spawn_failed_at_grace_boundary_goes_stopped`

### A10. 正常关闭桌面端后 ps 无残留 paladin-agent / paladin-server

- **状态:** `DEFERRED`
- **验证方法:** 退出桌面端 + `ps aux | grep paladin`
- **代码层证据:**
  - `supervisor.rs` `impl Drop for ProcessSupervisor` 调 `graceful_shutdown_all`
  - `graceful_shutdown_slot` 先 SIGTERM(Unix)/`start_kill`(Windows Job Object)→ 5s grace timeout → `child.kill()` 强制兜底
  - `Command::kill_on_drop(true)` 防 child handle drop 时泄漏
- **UAT 待办:** macOS 真实关闭 + ps 检查(详见 `07.3-UAT.md` macOS 关闭场景)

### A11. SIGTERM 无响应时 5s 后强制 kill 验证

- **状态:** `DEFERRED`
- **验证方法:** 启动 SIGTERM-resistant 子进程 + 退出桌面端 + 确认 5s 后进程消失
- **代码层证据:**
  - `supervisor.rs` `graceful_shutdown_slot`:`tokio::time::timeout(Duration::from_secs(5), child.wait())`,超时则 `child.kill().await`
  - Unix:`#[cfg(unix)] nix::sys::signal::kill(Pid, SIGTERM)`(supervisor.rs:601-604, 620-623)
  - Windows:`child.start_kill()`(Job Object 关闭子进程组)
  - 5s 是 `processes.json` 的 `shutdown_grace_secs: 5` + SPEC Constraints 锁定
- **UAT 待办:** 需要构造 SIGTERM-resistant 测试进程(详见 `07.3-UAT.md` macOS 关闭场景扩展)

---

## 二、日志与控制(6 条)

### A12. 子进程 stdout/stderr 在前端日志面板 1s 内可见,并落盘 logs 目录

- **状态:** `DEFERRED`
- **验证方法:** 子进程输出特征字符串 + 前端日志面板观察 + 落盘文件 grep
- **代码层证据:**
  - `supervisor.rs` `capture_lines` 用 `tokio::io::BufReader` 行循环,每行经 `redact_log_line` 脱敏后 `app.emit("process-log", &chunk)` + `tokio::fs::OpenOptions::append` 落盘
  - 前端 [LogsPanel.tsx](file:///Users/kdocs/Workspace/paladin/apps/desktop/src/components/panel/LogsPanel.tsx) `listen<LogChunk>('process-log', ...)` 按 `payload.process` 路由到 agent/server 独立 buffer
  - 落盘路径:`app_local_data_dir/log/paladin-{agent,server}.log`(D-04)
- **UAT 待办:** macOS 真实子进程输出 + 1s 内可见 + 文件 grep(详见 `07.3-UAT.md` macOS 日志场景)

### A13. logs 目录不可写时退化为仅 stderr + 发 warning 事件,不阻塞 spawn

- **状态:** `DEFERRED`(代码路径 PASS,warning event emit 运行时 DEFERRED)
- **验证方法:** chmod logs 目录不可写 + 启动 + 观察 spawn 仍发生
- **代码层证据:**
  - `supervisor.rs` `capture_lines`:`tokio::fs::OpenOptions::new().append(true).create(true).open(&path).await` 失败时 `file = None`,loop 继续 emit;后续写失败也降级为 None(避免反复失败占用 CPU,SPEC Edge R10)
  - `spawn_child_to_slot` 不依赖文件 IO 结果,文件降级不阻塞 spawn
- **UAT 待办:** 真实 chmod 场景 + warning event 实际 emit 验证(目前 warning 走 emit_status_to 的 last_error 字段)

### A14. restart_agent / stop_agent / restart_server / stop_server / get_process_status 命令工作

- **状态:** `DEFERRED`(命令注册 PASS,运行时 invoke 工作 DEFERRED)
- **验证方法:** 前端 invoke 各命令 + 观察进程状态变化
- **代码层证据:**
  - [commands.rs](file:///Users/kdocs/Workspace/paladin/apps/desktop/src-tauri/src/process/commands.rs) 6 个 `#[tauri::command]`(行 18/26/33/41/48/56)
  - [lib.rs:37-42](file:///Users/kdocs/Workspace/paladin/apps/desktop/src-tauri/src/lib.rs#L37-L42) `invoke_handler!` 注册全部 6 命令
  - plan 06 SUMMARY §Accomplishments 5:「6 Tauri 命令通过 invoke_handler! 注册」
- **UAT 待办:** 真实 invoke + 状态变化观察(详见 `07.3-UAT.md` macOS 重启场景)

### A15. get_process_status 在首次探针完成前返回 starting

- **状态:** `PASS`
- **验证方法:** ProcessState 初始值 + status snapshot 路径
- **证据:**
  - [supervisor.rs](file:///Users/kdocs/Workspace/paladin/apps/desktop/src-tauri/src/process/supervisor.rs) `ManagedProcess::new` 初始化 `state: ProcessState::Starting`
  - `get_process_status` 命令直接读 `slot.mp.state` 序列化返回
  - 首次 probe_loop tick 在 spawn 后 10s 才发生,期间状态保持 Starting

### A16. 并发 restart 同一进程返回 in-progress,不二次 spawn

- **状态:** `PASS`
- **验证方法:** supervisor.rs restart_lock 模式
- **证据:**
  - `supervisor.rs` `restart_one(name)` 内 `let _guard = self.restart_lock(name).lock().await;`(每进程 `tokio::sync::Mutex<()>`)
  - 并发调用排队串行化:第二个调用等第一个 release 后才进入,此时 `slot.mp.state` 已被前者改为 Starting,restart_count 已清零,再次执行是 no-op 或 idempotent
  - plan 06 SUMMARY §patterns-established:「SPEC Edge R7 并发幂等 — restart_one 持 restart_lock」

### A17. 手动 restart 命令把该进程失败计数清零

- **状态:** `PASS`
- **验证方法:** 纯函数状态机单元测试
- **证据:**
  - state_machine 测试 `test_manual_restart_clears_counters`:`transition(any_state, RestartRequested, snap)` 清零 `fail_count` 与 `restart_count`
  - `supervisor.rs` `restart_one(name)` 调 `transition(mp.state, ProcessEvent::RestartRequested, &mp.snapshot)`
  - plan 02 SUMMARY §transition 表:任意状态 + RestartRequested → Starting(snapshot 全字段归 0)

---

## 三、配置与跨平台(4 条)

### A18. grep -rn "localhost:9876" apps/desktop/src 业务代码 0 命中

- **状态:** `PASS`
- **验证方法:** source-level grep(Task 2 G1 已执行)
- **证据:**
  ```bash
  $ grep -rn 'localhost:9876' apps/desktop/src/ | grep -vE 'fallback|console.warn|默认'
  URL_CLEAN_OK   # 业务代码 0 命中
  ```
  - 全部 localhost:9876 命中仅 2 处,均在 [App.tsx](file:///Users/kdocs/Workspace/paladin/apps/desktop/src/App.tsx) 的 catch 块:
    - 行 51:`console.warn('[runtime] 配置加载失败，回退默认 http://localhost:9876', e);`(SPEC Edge R8 警告)
    - 行 53:`agent_url: 'http://localhost:9876', // fallback default SPEC Edge R8`(SPEC Edge R8 默认值常量)
  - 命中 lint 过滤词 `fallback` / `console.warn` / `默认`,符合 D-07 单一 Rust 真相源契约

### A19. URL 配置缺失时回退默认 http://localhost:9876 + console 警告

- **状态:** `PASS`
- **验证方法:** 静态源码 inspect
- **证据:**
  - [App.tsx:50-54](file:///Users/kdocs/Workspace/paladin/apps/desktop/src/App.tsx#L50-L54) `invoke('get_runtime_config')` catch 块:`console.warn` + `setRuntimeConfig({ agent_url: 'http://localhost:9876', server_url: 'http://localhost:9880' })`
  - plan 07 SUMMARY §Accomplishments 3:「SPEC R8 + Edge R8 闭环 — 三处 localhost:9876 业务硬编码全部清零(仅 fallback catch 块默认值保留)」

### A20. processes.json 缺失/格式错时监督器报配置错误、不 spawn、前端显示配置错误态

- **状态:** `PASS`
- **验证方法:** config loader 单元测试 + lib.rs setup() 错误传播
- **证据:**
  - [config.rs](file:///Users/kdocs/Workspace/paladin/apps/desktop/src-tauri/src/process/config.rs) `ConfigError` 4 variants:`NotFound` / `IoError` / `ParseError` / `InvalidSchema`
  - `ProcessConfig::load_from_path(impl AsRef<Path>)` 三步流:exists 检查 → `std::fs::read` → `serde_json::from_slice` → `validate`
  - 14 个单元测试覆盖(plan 03 SUMMARY):round-trip + NotFound + JSON 损坏 + cmd 单串 + env 字面值 + port=0/65536 + cmd 空 + 缺 liveness + backoff 太长 + max_restarts 太高 + shutdown_grace 越界 + readiness 范围
  - [lib.rs](file:///Users/kdocs/Workspace/paladin/apps/desktop/src-tauri/src/lib.rs) `setup()` 内 `ProcessConfig::load_from_path(...)?` 失败时返回 Err,setup 失败 → Tauri 启动失败 → 不 spawn
  - SPEC Prohibition P1 type-system 锁:`AsRef<Path>` 编译期排除 `reqwest::Url` 与 Tauri IPC 字符串(plan 03 测试 `_assert_signature_accepts<T: AsRef<Path>>`)

### A21. macOS、Windows、Linux 三平台启动 + 健康 + 重启 + 关闭 UAT 通过

- **状态:** `DEFERRED`
- **验证方法:** 三平台各执行启动 + 健康 + 重启 + 关闭四场景 UAT
- **代码层证据(跨平台抽象已就位):**
  - Unix:`#[cfg(unix)] nix::sys::signal::kill(Pid, SIGTERM)` + SIGKILL 兜底
  - Windows:`child.start_kill()`(Job Object 关闭子进程组,D-14 锁定)
  - 路径:`app.path().app_local_data_dir()` 跨平台自动解析(macOS ~/Library/Application Support / Win %LOCALAPPDATA% / Linux ~/.local/share)
  - `Cargo.toml` `nix = { version = "0.29", features = ["signal"] }` 仅 `cfg(unix)` 编译,Windows 不引入 Unix 依赖
- **UAT 待办:** 三平台完整 12 格矩阵(详见 `07.3-UAT.md`)

---

## 四、source gates 总览(本 plan Task 1 + Task 2 实测)

| Gate | 命令 | 期望 | 实测 |
|------|------|------|------|
| cargo check | `cd apps/desktop/src-tauri && cargo check --lib` | exit 0 | ✅ exit 0(5 个 dead_code warning,plan 06 SUMMARY 已记录的既存项) |
| cargo test | `cargo test --lib` | 54 passed | ✅ 54 passed; 0 failed(plan 02 state_machine 18 + plan 03 config 14 + plan 05 port_probe 7 + log_redact 15) |
| cargo clippy | `cargo clippy --lib --no-deps` | exit 0(0 error) | ✅ exit 0(6 个 warning 全是 dead_code,非 error) |
| tsc | `cd apps/desktop && npx tsc --noEmit` | exit 0 | ✅ exit 0(0 errors) |
| biome | `npx biome check src/` | exit 0 | ⚠ exit 1(29 errors,**全部既存**,非 07.3 引入;详见 VERIFICATION.md §scope-gates) |
| R8 URL | `grep -rn 'localhost:9876' apps/desktop/src \| grep -vE 'fallback\|console.warn\|默认'` | 空 | ✅ URL_CLEAN_OK |
| shell plugin | `grep -c 'tauri-plugin-shell' Cargo.toml` | 0 | ✅ 0 |
| radix | `grep -c '@radix-ui' popover.tsx` | 0 | ✅ 0 |
| base-ui | `grep "from '@base-ui/react/popover'" popover.tsx` | 命中 | ✅ 1 命中 |
| processes.json | cmd array / backoff [1,2,4,8,16] / max_restarts 5 | 全中 | ✅ python3 assertions 全 PASS |
| CSP | `grep 'connect-src' tauri.conf.json` | 含 localhost:* | ✅ `connect-src ipc: http://ipc.localhost http://localhost:* https://*.copilotkit.ai` |
| Cargo deps | tokio + reqwest + nix 都在 | 全在 | ✅ 三依赖行都在,无 tauri-plugin-shell |

---

## 五、与 SPEC 的计数偏差记录

| 来源 | 声称 | 实测 | 处置 |
|------|------|------|------|
| SPEC frontmatter `Ambiguity Report` | 22 acceptance | 21 | 偏差 #1(SUMMARY §Deviations) |
| PLAN frontmatter `must_haves.truths` | 22 acceptance | 21 | 同上 |
| PLAN Task 5 acceptance | 22 条 checkbox | 21 | 本文档按 21 条实际验证 |

SPEC §Acceptance Criteria 三段实际 checkbox 数:核心功能 11 + 日志与控制 6 + 配置与跨平台 4 = **21**。
SPEC/PLAN 文档层的「22」属笔误,不影响验收完整性 —— 21 条全部覆盖,无遗漏。

---

*Validator: GSD executor (sequential mode)*
*Validated: 2026-07-05*
*Next: `07.3-UAT.md`(三平台手测清单)+ `07.3-VERIFICATION.md`(13 Edge + 4 Prohibitions + PATTERNS §0)*
