---
phase: "07.4"
plan: "07.4-04"
type: "execute"
wave: 4
depends_on: ["07.4-01", "07.4-02", "07.4-03"]
files_modified:
  - ".planning/phases/07.4-sidecar-runtime-mode/07.4-VALIDATION.md"
  - ".planning/phases/07.4-sidecar-runtime-mode/07.4-VERIFICATION.md"
  - ".planning/phases/07.4-sidecar-runtime-mode/07.4-UAT.md"
  - ".planning/phases/07.4-sidecar-runtime-mode/07.4-04-SUMMARY.md"
  - ".planning/ROADMAP.md"
  - ".planning/STATE.md"
autonomous: true
requirements: [TBD]
must_haves:
  truths:
    - "Automated verification matrix is the primary proof for Phase 07.4."
    - "Full manual desktop/packaging UAT remains secondary and may be deferred to Phase 10."
    - "Missing any required matrix category fails this plan."
    - "ROADMAP requirement IDs are TBD/null for this phase."
  prohibitions:
    - "Do not mark Phase 07.4 complete if any GOAL/CONTEXT/RESEARCH/SPEC source item is unplanned or unverified."
    - "Do not implement source code changes in this closure plan except tiny compile/test fixes required by verification."
    - "Do not claim packaged binaries, externalBin, installers, or persisted preference are complete."
  artifacts:
    - "07.4-VALIDATION.md updated with evidence."
    - "07.4-VERIFICATION.md source coverage and prohibitions evidence."
    - "07.4-UAT.md created or updated with manual/deferred notes."
    - "07.4-04-SUMMARY.md closure summary."
    - "ROADMAP/STATE scoped status updates only after gates pass."
  key_links:
    - ".planning/phases/07.4-sidecar-runtime-mode/07.4-CONTEXT.md"
    - ".planning/phases/07.4-sidecar-runtime-mode/07.4-SPEC.md"
    - ".planning/phases/07.4-sidecar-runtime-mode/07.4-RESEARCH.md"
    - ".planning/phases/07.4-sidecar-runtime-mode/07.4-VALIDATION.md"
---

# 07.4-04 Plan: Verification Matrix And Phase Closure

<objective>
执行 Phase 07.4 的最终验证矩阵、source coverage audit、prohibition audit 与文档闭环。只有 dev attach、dev spawn、conflict、packaged supervisor-owned、external action disabled、failure classes 六类自动化覆盖全部有证据时，才更新阶段验证文档和 ROADMAP/STATE。
</objective>

<execution_context>
This plan depends on all implementation plans. It is mostly documentation and verification. Do not make feature changes here; if tests reveal defects, fix only the minimal source issue needed to make the already-planned behavior pass, then rerun the relevant gates.
</execution_context>

<context>
Read these first:
- `.planning/phases/07.4-sidecar-runtime-mode/07.4-CONTEXT.md`
- `.planning/phases/07.4-sidecar-runtime-mode/07.4-SPEC.md`
- `.planning/phases/07.4-sidecar-runtime-mode/07.4-RESEARCH.md`
- `.planning/phases/07.4-sidecar-runtime-mode/07.4-PATTERNS.md`
- `.planning/phases/07.4-sidecar-runtime-mode/07.4-VALIDATION.md`
- The summaries or commits from 07.4-01, 07.4-02, and 07.4-03
- `.planning/phases/07.3-sidecar-process-management/07.3-10-SUMMARY.md`

Implementation notes:
- Preserve Phase 07.3's pattern of automated gates plus deferred real packaging/UAT notes.
- Keep ROADMAP/STATE edits scoped to Phase 07.4 plan completion/status.
- If a required source audit item is not covered, stop and create an explicit gap note instead of declaring completion.
</context>

## Artifacts This Phase Produces

- Updated `07.4-VALIDATION.md` with per-task verification evidence and `nyquist_compliant: true` only after evidence exists
- New or updated `07.4-VERIFICATION.md` mapping SPEC acceptance, Edge Coverage, and Prohibitions to tests/source gates
- New or updated `07.4-UAT.md` with manual dev attach observations if performed and Phase 10 deferred packaging/UAT items
- `07.4-04-SUMMARY.md`
- Scoped ROADMAP/STATE phase progress updates after gates pass

<tasks>
<task type="auto" tdd="false">
  <name>Run Rust process/runtime verification matrix</name>
  <files>
    apps/desktop/src-tauri/src/process/**
    .planning/phases/07.4-sidecar-runtime-mode/07.4-VALIDATION.md
  </files>
  <read_first>
    1. `07.4-VALIDATION.md` Per-Task Verification Map.
    2. SPEC Requirement 6 acceptance criteria.
    3. Test files added in 07.4-01 and 07.4-02.
  </read_first>
  <action>
    Run Rust process-focused tests, full Rust tests, and clippy. Capture exact pass/fail counts and commands in `07.4-VALIDATION.md`. If failures occur, fix only the behavior already specified by 07.4-01/02 and rerun.
  </action>
  <verify>
    <automated>cd apps/desktop/src-tauri && cargo test process:: --lib</automated>
    <automated>cd apps/desktop/src-tauri && cargo test</automated>
    <automated>cd apps/desktop/src-tauri && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Rust tests cover tuple legality, dev attach, dev spawn, conflict, packaged validation/branch, external action safety, and per-service concurrency/idempotency.
    - `cargo test process:: --lib` passes.
    - `cargo test` passes.
    - `cargo clippy --all-targets -- -D warnings` passes or any preexisting unrelated failure is documented with scope proof.
  </acceptance_criteria>
  <done>Rust verification evidence is recorded with exact command outputs.</done>
</task>

<task type="auto" tdd="false">
  <name>Run frontend status UX verification matrix</name>
  <files>
    apps/desktop/src/**
    .planning/phases/07.4-sidecar-runtime-mode/07.4-VALIDATION.md
  </files>
  <read_first>
    1. Tests added in 07.4-03.
    2. SPEC Requirements 5 and 6.
    3. CONTEXT D-09 through D-12 and D-19.
  </read_first>
  <action>
    Run focused frontend tests if present, then all desktop tests, then production build. Record evidence in `07.4-VALIDATION.md`. If tests were not added, stop and classify this as a validation gap unless an equivalent source assertion was explicitly created and accepted earlier.
  </action>
  <verify>
    <automated>pnpm --filter @paladin/desktop test -- --run</automated>
    <automated>pnpm --filter @paladin/desktop build</automated>
  </verify>
  <acceptance_criteria>
    - Frontend tests/build cover process tuple store merge, external disabled controls, redetect action, conflict diagnostics, failure classes, and Go-not-blocking rule.
    - `pnpm --filter @paladin/desktop test -- --run` passes if frontend tests are added.
    - `pnpm --filter @paladin/desktop build` passes.
  </acceptance_criteria>
  <done>Frontend verification evidence is recorded with exact command outputs.</done>
</task>

<task type="auto" tdd="false">
  <name>Run source gates for prohibitions and deferred boundary</name>
  <files>
    apps/desktop/src-tauri/src/process/**
    apps/desktop/src/**
    .planning/phases/07.4-sidecar-runtime-mode/07.4-VERIFICATION.md
  </files>
  <read_first>
    1. SPEC Prohibitions table.
    2. RESEARCH "Don't Hand-Roll" and "Security Domain".
    3. CONTEXT deferred ideas.
  </read_first>
  <action>
    Run focused source checks or manual grep review to prove: no PID/port/process-name kill was added; `restart_*` is not used for redetect; frontend does not poll `/health`; packaged branch does not call `augmented_spawn_path`; `cmd` schema was not split; no persisted external/managed preference was introduced. Record commands and results in `07.4-VERIFICATION.md`.
  </action>
  <verify>
    <automated>rg "redetect_|restart_|augmented_spawn_path|/health|/healthz|/readyz|external_not_owned|kill\\(|SIGKILL|SIGTERM|cmd:" apps/desktop/src-tauri/src/process apps/desktop/src</automated>
  </verify>
  <acceptance_criteria>
    - All four SPEC prohibitions have evidence.
    - Deferred ideas are absent from source changes.
    - Source audit explicitly states no real frozen binaries, externalBin packaging, installer validation, persisted preference, or kill-based switch-to-managed was implemented.
  </acceptance_criteria>
  <done>Prohibition and deferred-boundary audit is recorded.</done>
</task>

<task type="auto" tdd="false">
  <name>Complete required source coverage audit</name>
  <files>
    .planning/phases/07.4-sidecar-runtime-mode/07.4-VERIFICATION.md
    .planning/phases/07.4-sidecar-runtime-mode/07.4-04-SUMMARY.md
  </files>
  <read_first>
    1. Mandatory `required_source_audit` from the planner prompt.
    2. GOAL bullets in ROADMAP Phase 07.4.
    3. Every locked decision D-01 through D-20 in CONTEXT.
    4. RESEARCH responsibility map and validation architecture.
    5. SPEC Edge Coverage and Prohibitions.
  </read_first>
  <action>
    Create an audit table mapping every required item to 07.4-01/02/03/04 evidence. If any source item lacks a plan or verification result, do not mark complete; write `## ⚠ Source Audit: Unplanned Items Found` in the verification artifact and stop for planner remediation.
  </action>
  <verify>
    <automated>test -f .planning/phases/07.4-sidecar-runtime-mode/07.4-VERIFICATION.md && rg "GOAL|CONTEXT|RESEARCH|SPEC|D-01|D-20|Prohibition|Edge" .planning/phases/07.4-sidecar-runtime-mode/07.4-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Every ROADMAP bullet is mapped.
    - Every locked CONTEXT decision D-01..D-20 is mapped.
    - Every RESEARCH named area from the prompt is mapped.
    - Every SPEC Edge Coverage and resolved Prohibition is mapped.
    - No deferred idea appears as implemented scope.
  </acceptance_criteria>
  <done>Required source audit is complete or explicit gap marker blocks completion.</done>
</task>

<task type="auto" tdd="false">
  <name>Write closure artifacts and scoped roadmap/state update</name>
  <files>
    .planning/phases/07.4-sidecar-runtime-mode/07.4-VALIDATION.md
    .planning/phases/07.4-sidecar-runtime-mode/07.4-VERIFICATION.md
    .planning/phases/07.4-sidecar-runtime-mode/07.4-UAT.md
    .planning/phases/07.4-sidecar-runtime-mode/07.4-04-SUMMARY.md
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <read_first>
    1. 07.4-VALIDATION.md after Tasks 1-2.
    2. 07.4-VERIFICATION.md after Tasks 3-4.
    3. Phase 07.3 closure summary for style.
    4. ROADMAP Phase 07.4 section.
  </read_first>
  <action>
    If all gates pass and source audit is complete, set validation frontmatter to `nyquist_compliant: true`, write UAT/deferred notes, write 07.4-04-SUMMARY, and update ROADMAP/STATE with Phase 07.4 completion status and plan checklist. Keep edits scoped. If any gate fails, do not update completion status.
  </action>
  <verify>
    <automated>rg "07.4|Sidecar Runtime Mode|nyquist_compliant|deferred|complete" .planning/phases/07.4-sidecar-runtime-mode/07.4-VALIDATION.md .planning/phases/07.4-sidecar-runtime-mode/07.4-VERIFICATION.md .planning/phases/07.4-sidecar-runtime-mode/07.4-04-SUMMARY.md .planning/ROADMAP.md .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - Closure artifacts exist and reference exact verification evidence.
    - ROADMAP/STATE are updated only after gates pass.
    - UAT notes keep Phase 10 packaging/manual matrix deferred where appropriate.
    - Requirement IDs are documented as TBD/null, not fabricated.
  </acceptance_criteria>
  <done>Phase 07.4 has evidence-backed closure artifacts and scoped planning-state updates.</done>
</task>
</tasks>

<threat_model>
- Threat: false completion despite missing matrix category. Mitigation: explicit six-category matrix and source audit gate.
- Threat: documentation claims out-of-scope packaging work. Mitigation: deferred-boundary source gate and UAT note.
- Threat: test failures hidden as unrelated. Mitigation: exact command evidence and scope proof for any preexisting failure.
- Threat: phase state advances with unverified code. Mitigation: ROADMAP/STATE update only after gates pass.
</threat_model>

<verification>
Required final commands:
- `cd apps/desktop/src-tauri && cargo test process:: --lib`
- `cd apps/desktop/src-tauri && cargo test`
- `cd apps/desktop/src-tauri && cargo clippy --all-targets -- -D warnings`
- `pnpm --filter @paladin/desktop build`
- `pnpm --filter @paladin/desktop test -- --run`
</verification>

<success_criteria>
- All required automated gates pass or any unrelated preexisting failure is documented with proof.
- Verification matrix covers dev attach, dev spawn, conflict, packaged supervisor-owned, external action disabled, and failure classes.
- Required source audit has no unplanned items.
- Closure docs and ROADMAP/STATE are consistent.
</success_criteria>

## Source Coverage Audit

- GOAL covered: all Phase 07.4 ROADMAP bullets verified across 07.4-01/02/03 and audited here.
- CONTEXT covered: D-01 through D-20 must be mapped to evidence before completion.
- RESEARCH covered: runtime tuple/status model, dev attach/spawn/conflict, external action safety, packaged validation, config diagnostics, frontend status UX, validation architecture, and security boundary.
- SPEC covered: all requirements, every covered/backstop Edge Coverage item, and every resolved Prohibition.
- Deferred ideas excluded: real frozen binaries, Tauri externalBin, installers, persisted preference, one-click kill switch-to-managed, Tauri-started PG/Redis, remote/multi-machine support, log rotation/compression.

<output>
Verification and closure artifacts only after evidence exists. If any source item is unplanned or unverified, output an explicit audit gap instead of phase completion.
</output>
