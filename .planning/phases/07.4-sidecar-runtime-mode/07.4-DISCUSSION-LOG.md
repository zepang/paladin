# Phase 07.4: sidecar-runtime-mode - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-06
**Phase:** 07.4-sidecar-runtime-mode
**Areas discussed:** Runtime mode config boundary, External attach health and redetect semantics, UI copy and control menu shape, State model and concurrency ownership boundary, Verification strategy and test doubles

---

## Runtime Mode Config Boundary

| Question | Options Presented | Selected |
|----------|-------------------|----------|
| How strict should packaged mode configuration validation be? | Strictly reject dev commands; Allow with warning; Planner decides; Other | Strictly reject dev commands |
| Where should dev PATH augmentation live? | Dev spawn path only; Global augmentation with packaged validation; Explicit config env only; Other | Dev spawn path only |
| How should packaged command source be represented? | Keep `cmd: string[]` with validation; Split `dev_cmd` / `packaged_cmd`; Introduce `binary`; Other | Keep `cmd: string[]` with validation |
| How should configuration errors surface? | Global config error blocks supervisor and shows UI diagnostic; Single-service isolated errors; stderr/log only; Other | Global config error blocks supervisor and shows UI diagnostic |

**Notes:** Packaged mode should be strict and dev-tool-free. The current schema remains, but validation and runtime branching must enforce the boundary.

---

## External Attach Health and Redetect Semantics

| Question | Options Presented | Selected |
|----------|-------------------|----------|
| Should Rust keep probing an attached external service? | Continue probing without restart; Probe only once at attach; Frontend probes external; Other | Continue probing without restart |
| How should a previously attached external service be shown after health fails? | `state=unhealthy, owner=external, health=failed`; `state=conflict, owner=none, health=failed`; `state=stopped, owner=external, health=failed`; Other | `state=unhealthy, owner=external, health=failed` |
| What form should redetect take? | New Tauri commands plus UI button; Reuse restart command; Only automatic redetect; Other | New Tauri commands plus UI button |
| Can redetect switch from external to supervisor-owned? | Yes, spawn when port is free; No, external stays external for the session; Ask confirmation before spawn; Other | Yes, spawn when port is free |

**Notes:** `conflict` is reserved for preflight/redetect classification. Once a service has been attached, later health failure keeps the external ownership visible.

---

## UI Copy and Control Menu Shape

| Question | Options Presented | Selected |
|----------|-------------------|----------|
| How should ProcessLight present stop/restart for `owner=external`? | Disable stop/restart with reason; Hide stop/restart; Show but explain on click; Other | Disable stop/restart with reason |
| What should the StatusBar/ProcessLight main label prioritize? | State + owner dual labels; State only, owner in popover; Only show abnormal details; Other | State + owner dual labels |
| When should StartupMask block the workspace? | Agent availability only; Agent or Go abnormal blocks; Only supervisor-owned Agent blocks; Other | Agent availability only |
| How detailed should failure-class diagnostics be? | Short title + diagnostics + action; Raw error string only; Detailed runbook; Other | Short title + diagnostics + action |

**Notes:** The UI should stay compact but explicit. Go status should be visible and diagnostic without blocking the CopilotKit workspace.

---

## State Model and Concurrency Ownership Boundary

| Question | Options Presented | Selected |
|----------|-------------------|----------|
| Should the implementation define a legal state/owner/health combination table? | Explicit legal combination table with tests; Enums only; Frontend tolerates any combination; Other | Explicit legal combination table with tests |
| What should stop/restart return for `owner=external`? | Typed error, no action; Ok no-op; Convert to redetect; Other | Typed error, no action |
| What should graceful shutdown/app quit do with external services? | Only stop supervisor-owned tracked child; Skip external and emit warning; Ask external service to shut down; Other | Only stop supervisor-owned tracked child |
| Where should concurrency serialization live? | Per-service runtime lock covers all lifecycle operations; Only `restart_lock`; Global supervisor lock; Other | Per-service runtime lock covers all lifecycle operations |

**Notes:** Ownership is the safety boundary. Frontend state is not trusted as the only defense; Rust commands must enforce external ownership restrictions.

---

## Verification Strategy and Test Doubles

| Question | Options Presented | Selected |
|----------|-------------------|----------|
| How should dev attach/conflict be tested in Rust? | Local fake HTTP server + ephemeral port; Mock reqwest/client trait; Pure function only; Other | Local fake HTTP server + ephemeral port |
| How should packaged no-dev-deps be proven? | Config validate + spawn path branch tests; Config validate only; Grep/source gate only; Other | Config validate + spawn path branch tests |
| How much frontend UI state coverage is required? | Types/build + lightweight component behavior tests; tsc/build only; Playwright visual/UAT; Other | Types/build + lightweight component behavior tests |
| How should final gates be combined? | Rust tests + frontend type/build + focused source gates; Existing full gates only; Full manual UAT matrix; Other | Rust tests + frontend type/build + focused source gates |

**Notes:** Verification is automated-first. Manual/visual UAT can remain secondary and is not the primary proof for this phase.

---

## the agent's Discretion

- Planner may choose exact Rust function/module names and typed error shape.
- Planner may choose exact lightweight frontend test mechanism.
- Planner may refine Chinese UI copy, icons, and colors within existing shadcn/lucide patterns.

## Deferred Ideas

- Real frozen Python/Go sidecar binaries and Tauri `externalBin` remain Phase 10.
- Persisted external/managed startup preference is out of scope.
- One-click switch-to-managed flow is out of scope.
- Full Playwright visual/UAT matrix is not the primary proof for this phase.
