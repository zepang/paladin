---
status: complete
phase: 07.4-sidecar-runtime-mode
source:
  - .planning/phases/07.4-sidecar-runtime-mode/07.4-01-SUMMARY.md
  - .planning/phases/07.4-sidecar-runtime-mode/07.4-02-SUMMARY.md
  - .planning/phases/07.4-sidecar-runtime-mode/07.4-03-SUMMARY.md
  - .planning/phases/07.4-sidecar-runtime-mode/07.4-04-SUMMARY.md
started: 2026-07-06T15:32:36Z
updated: 2026-07-11T08:18:17Z
---

# Phase 07.4 UAT

## Current Test

[testing complete]

## Tests

### 1. Cold Start And Clean Exit
expected: After old Paladin Agent/Go processes are stopped and the desktop app is started from scratch, the app should enter the main chat UI without requiring "Restart Agent". The Agent status light should become green/running. The Go status light should become running or degraded if PostgreSQL/Redis readiness is unavailable, but it should not fail with a stale port conflict from the previous desktop session. After closing the desktop app, supervisor-owned Agent/Go processes should be stopped so ports 9876 and 9880 are not left occupied by Paladin-spawned processes.
result: pass

### 2. Dev External Attach
expected: If Agent and/or Go are already running and their health endpoints pass before desktop startup, the desktop should attach to them as external services instead of trying to spawn new processes. The status bar should show owner external semantics and should not report stopped/conflict just because the services are not supervisor children.
result: pass
source: human
reported: "User replied pass."

### 3. External Action Limits
expected: For an externally attached service, restart and stop actions should be disabled or rejected, while redetect remains available. Clicking disabled controls should not kill or replace the externally managed process.
result: pass
source: human
reported: "User verified external Agent and Go show owner=external, restart/stop are disabled, redetect remains available, and external services are not killed or replaced. User also observed that View Logs remains clickable for external services but opens an empty log view because Paladin does not capture external process logs."

### 4. Conflict Classification
expected: If port 9876 or 9880 is occupied by a process that does not pass the configured Paladin health check, redetect/startup should show a conflict diagnostic that includes the affected port and health-check context. It should not collapse into a generic stopped state or attempt to spawn over the occupied port.
result: pass
source: human
reported: "User replied pass."

### 5. Go Readiness Degraded
expected: If Go liveness `/healthz` passes but readiness `/readyz` fails because PostgreSQL/Redis is unavailable, the Go service should display degraded health rather than stopped. This degraded state should not block Agent startup or imply that the supervisor failed to own the process.
result: pass
source: human
reported: "User replied pass."

### 6. Packaged/Config Diagnostic Boundary
expected: If runtime config is invalid for packaged mode or contains dev-only assumptions such as `uv`, `go`, or repo-relative cwd values, the desktop should surface a readable diagnostic instead of failing before React can render. Real packaged binaries and installer behavior remain deferred to Phase 10.
result: pass
source: human
reported: "User verified packaged mode with dev command. UI rendered a readable runtime config invalid diagnostic and reported InvalidSchema(\"Agent packaged mode rejects dev command \\\"uv\\\"\") instead of white-screening."

## Automated Coverage

### Runtime Tuple Legality
expected: Rust DTO/state tests prove `state + owner + health` combinations serialize and reject invalid ownership tuples.
result: pass
source: automated
verification:
  - cargo test process:: --lib

### Dev Preflight Attach/Conflict
expected: Rust preflight tests cover healthy external Agent attach, degraded Go attach, and occupied unhealthy conflict.
result: pass
source: automated
verification:
  - test_dev_preflight_attaches_healthy_external_agent
  - test_dev_preflight_attaches_degraded_external_server
  - test_dev_preflight_reports_conflict_for_occupied_unhealthy_port

### Packaged No Dev Dependencies
expected: Packaged validation rejects `uv`, `go`, and repo-relative dev cwd, and packaged mode skips dev PATH augmentation.
result: pass
source: automated
verification:
  - test_packaged_rejects_uv_command
  - test_packaged_rejects_go_command
  - test_packaged_rejects_repo_relative_dev_cwd
  - test_spawn_path_for_mode_is_dev_only

### Frontend Tuple Consumption
expected: Frontend types/build accept `conflict`, `owner`, and `health`, and the existing desktop test suite remains green.
result: pass
source: automated
verification:
  - pnpm --filter @paladin/desktop build
  - pnpm --filter @paladin/desktop test -- --run

### Process Tree And Port Probe Regression
expected: Supervisor-owned `go run`/`uv` children are terminated as a process group on close/restart, and port probing detects unspecified-address listeners such as `:9880`.
result: pass
source: automated
verification:
  - cargo test
  - test_is_port_available_returns_false_when_unspecified_addr_occupied

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "For externally attached services, user-facing controls should accurately reflect what Paladin can manage or observe."
  status: fixed
  reason: "User reported: View Logs remains clickable for external Agent/Go services, but Paladin does not capture external process logs, so the logs page opens empty."
  severity: minor
  test: 3
  root_cause: "ProcessLight disabled restart/stop for external services but left the View Logs button active even though external processes are not spawned with piped stdout/stderr by Paladin."
  artifacts:
    - path: "apps/desktop/src/components/StatusBar/ProcessLight.tsx"
      issue: "External process popover allowed opening an empty Paladin log view."
  missing:
    - "Disable View Logs for external services and explain that external logs live in the terminal that launched the service."
  fix: "Updated the external-service helper text and disabled the View Logs button for external services with a tooltip pointing users to the launching terminal."
  verification:
    - "pnpm --filter @paladin/desktop build"
