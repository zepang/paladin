---
phase: 07.4
slug: sidecar-runtime-mode
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-06
---

# Phase 07.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust `cargo test` + Vitest/TypeScript build |
| **Config file** | `apps/desktop/src-tauri/Cargo.toml`, `apps/desktop/package.json` |
| **Quick run command** | `cd apps/desktop/src-tauri && cargo test process:: --lib` |
| **Full suite command** | `cd apps/desktop/src-tauri && cargo test && pnpm --filter @paladin/desktop build` |
| **Estimated runtime** | ~60-180 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd apps/desktop/src-tauri && cargo test process:: --lib`
- **After every plan wave:** Run `cd apps/desktop/src-tauri && cargo test && pnpm --filter @paladin/desktop build`
- **Before `$gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 180 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 07.4-01-01 | TBD | 1 | Runtime tuple legality | T-07.4-01 | External ownership cannot be stopped/restarted/killed | unit | `cd apps/desktop/src-tauri && cargo test process::` | ❌ W0 | ⬜ pending |
| 07.4-01-02 | TBD | 1 | Dev attach/spawn/conflict preflight | T-07.4-02 | Healthy external attach never spawns or kills; unhealthy occupied port becomes conflict | integration | `cd apps/desktop/src-tauri && cargo test process::` | ❌ W0 | ⬜ pending |
| 07.4-02-01 | TBD | 2 | Packaged mode validation | T-07.4-03 | Packaged mode rejects dev commands/repo cwd/PATH augmentation | unit | `cd apps/desktop/src-tauri && cargo test process::config` | ✅ | ⬜ pending |
| 07.4-03-01 | TBD | 3 | Frontend ownership/status UX | T-07.4-04 | UI disables external stop/restart and exposes redetect/conflict diagnostics | component/build | `pnpm --filter @paladin/desktop build` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Add or extend Rust tests for legal `state + owner + health` tuples, dev attach/spawn/conflict classification, external lifecycle rejection, and packaged validation.
- [ ] Add or extend frontend tests if the desktop package already has a Vitest path; otherwise rely on TypeScript build plus focused source assertions in the plan.
- [ ] Ensure fake HTTP server fixtures use ephemeral ports and do not require real Agent/Go sidecars.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Dev external attach with manually started services | Healthy external services show as external and are not killed | Requires developer-started sidecars for end-to-end observation | Start Agent/Go manually, launch desktop dev mode, confirm status labels and disabled controls |
| Packaged-mode user-facing diagnostic copy | Packaged config failure displays concise desktop diagnostic | Real packaging is out of scope for 07.4 | Use test config/mode fixture or dev diagnostic path to inspect title, diagnostic block, and suggested action |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 180s
- [ ] `nyquist_compliant: true` set in frontmatter after execution evidence exists

**Approval:** pending
