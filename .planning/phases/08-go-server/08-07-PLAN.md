---
phase: 08
plan: 08-07
title: Local-dev README (Podman-first) + .env polish + end-to-end integration + validation sign-off
type: execute
wave: 5
depends_on:
  - "08-05"
  - "08-06"
requirements:
  - SRV-01
files_modified:
  - apps/server/README.md
  - apps/server/.env.example
  - .planning/phases/08-go-server/08-VALIDATION.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - "D-24: README leads with Podman commands and documents Docker as a fallback; it does not assume Docker is the only runtime."
    - "D-26: .env.example documents every env var with safe placeholders (database URL, Redis URL, JWT secret, bootstrap admin email/password, server port, JWT TTL, bcrypt cost)."
    - "SPEC R6 acceptance: a developer can start PG+Redis from documented config, run migrations, start the Go server, and observe successful dependency health checks."
    - "SPEC R6 idempotency edge: re-starting local dependencies and re-checking health is stable for development use (compose down/up + /readyz 200 is repeatable)."
    - "SPEC R6 concurrency edge (dismissed): concurrent local dependency startup orchestration is outside the Go server contract (explicit assumption — surfaced, not dropped)."
    - "SPEC acceptance (whole phase): every R1..R6 acceptance bullet is exercised by the end-to-end smoke in this plan or by the unit/integration tests in 08-03..08-06."
    - "D-27: verification uses Go `testing` + `httptest` + Podman-backed integration; no `testcontainers-go` dependency is introduced."
  prohibitions:
    - statement: "MUST NOT implement audit logs, quota management, or Tauri sidecar process management in Phase 8 (R1/R6)."
      status: resolved
      verification: "scope-review grep over apps/server asserts no audit_log table, no Redis INCR/HINCRBY quota counter, and no tauri/sidecar process-management symbol."
    - statement: "MUST NOT commit real secrets in .env.example or README (D-14)."
      status: resolved
      verification: "grep asserts placeholder markers (change-me, your-, example.local) and no 32+ char high-entropy literal."
    - statement: "MUST NOT advertise legacy SSE approval or Phase 9 admin features as available in this server (scope boundary)."
      status: resolved
      verification: "README and .env.example contain no audit/quota/sidecar claim; a scope section explicitly defers them to Phase 9/10."
  artifacts:
    - path: "apps/server/README.md"
      provides: "Podman-first local-dev quick start, Docker fallback, dirty-state recovery, test commands, and scope boundary"
    - path: "apps/server/.env.example"
      provides: "polished placeholder-only environment documentation with the >=32-byte JWT note"
    - path: ".planning/phases/08-go-server/08-VALIDATION.md"
      provides: "Wave 0 marked complete, task IDs filled, nyquist_compliant set true"
  key_links:
    - from: "apps/server/README.md"
      to: "docker-compose.server.yml (plan 08-02) and apps/server/migrations (plan 08-02)"
      via: "README documents podman compose up and migrate up as the canonical bring-up"
      pattern: "podman compose"
---

<objective>
Close Phase 08 with developer-facing documentation and a full end-to-end integration pass: author the Podman-first `apps/server/README.md`, polish `.env.example`, run the complete bring-up (compose -> migrate -> server -> health/auth/RBAC/WS smoke) including the R6 dependency-down failure path, perform the Phase 9/sidecar scope-review grep, and update `08-VALIDATION.md` to mark Wave 0 complete and Nyquist compliant.

Purpose: This plan is the SRV-01/R6 acceptance gate and the only place that proves the whole stack works together with documented commands. It also enforces the Phase 9 scope boundary (Prohibition 4) so the auth foundation does not silently absorb audit/quota/sidecar work.
Output: A README a new developer can follow to a green `/readyz`, a passing end-to-end smoke capturing each R1..R6 acceptance bullet, a clean scope grep, and a signed-off validation record.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-go-server/08-CONTEXT.md
@.planning/phases/08-go-server/08-SPEC.md
@.planning/phases/08-go-server/08-RESEARCH.md
@.planning/phases/08-go-server/08-PATTERNS.md
@.planning/phases/08-go-server/08-VALIDATION.md
@apps/agent/README.md
@references/wps-cowork/apps/api/README.md
@docker-compose.server.yml
@apps/server/.env.example
</context>

## Artifacts this phase produces

New/updated files/symbols created by this plan:
- `apps/server/README.md` — sections: Prerequisites (Go >= 1.26, sqlc v1.31.1, golang-migrate v4.19.x, Podman preferred / Docker fallback); Quick start (Podman-first `podman compose -f docker-compose.server.yml up -d`, `cp .env.example .env`, `migrate -path apps/server/migrations -database "$PALADIN_DATABASE_URL" up`, `go run ./cmd/server`, `curl localhost:9880/healthz`); Docker fallback block; Dirty-state recovery (`migrate ... force <version>`); Test commands (`go test ./... -race -count=1`, `PALADIN_DB_INTEGRATION=1`); Configuration table mirroring `.env.example`; Scope boundary (audit/quota -> Phase 9, sidecar -> later, packaging -> Phase 10).
- `apps/server/.env.example` — polished: grouped/ordered keys, the >=32-byte JWT secret comment, TTL/cost notes, no real secrets.
- `.planning/phases/08-go-server/08-VALIDATION.md` — frontmatter `wave_0_complete: true`, `nyquist_compliant: true`; Per-Task Verification Map task IDs filled (08-01-01 etc.); checklists ticked.

<tasks>

<task id="08-07-01" type="execute">
  <name>Task 1: Polish .env.example</name>
  <description>Ensure .env.example is grouped, ordered, fully documented, and contains no real secrets, with the >=32-byte JWT note prominent.</description>
  <read_first>
    - `.planning/phases/08-go-server/08-CONTEXT.md` decisions D-14, D-26
    - `.planning/phases/08-go-server/08-RESEARCH.md` "Common Pitfalls" #4, #5
    - `apps/server/.env.example` (from 08-01)
    - `apps/server/internal/config/config.go` (authoritative key list)
  </read_first>
  <action>Re-edit `apps/server/.env.example` to group keys under three comment headers: "# Server", "# PostgreSQL", "# Redis", "# Auth", "# Bootstrap admin". Confirm the nine keys exist with the documented defaults: PALADIN_PORT=9880, PALADIN_DATABASE_URL, PALADIN_REDIS_URL, PALADIN_JWT_SECRET (with a comment ">= 32 bytes required; HS256 needs >= 256 bits of entropy"), PALADIN_JWT_TTL=15m, PALADIN_BCRYPT_COST=10, PALADIN_ADMIN_EMAIL=admin@example.local, PALADIN_ADMIN_PASSWORD=change-me-admin-password, PALADIN_AUTO_MIGRATE=true. Ensure no value is a 32+ char high-entropy literal and all sensitive values use change-me/your-/example.local placeholders.</action>
  <verify>
    <automated>cd apps/server &amp;&amp; grep -E 'PALADIN_PORT=9880|PALADIN_DATABASE_URL=|PALADIN_REDIS_URL=|PALADIN_JWT_SECRET=|PALADIN_JWT_TTL=15m|PALADIN_BCRYPT_COST=10|PALADIN_ADMIN_EMAIL=admin@example.local|PALADIN_ADMIN_PASSWORD=change-me-admin-password|PALADIN_AUTO_MIGRATE=true' .env.example &amp;&amp; grep -E '32 bytes|256 bits' .env.example &amp;&amp; ! grep -E 'PALADIN_JWT_SECRET=[A-Za-z0-9+/]{32,}' .env.example</automated>
  </verify>
  <acceptance_criteria>All nine keys are present with the documented defaults/placeholders; the >=32-byte JWT note is present; no JWT secret value is a 32+ char high-entropy literal; sensitive values use placeholder markers; keys are grouped under the listed headers.</acceptance_criteria>
  <done>Acceptance criteria above are met and the automated verification command passes.</done>
</task>

<task id="08-07-02" type="execute">
  <name>Task 2: Author apps/server/README.md (Podman-first)</name>
  <description>Write the developer quick-start that leads with Podman, documents Docker as fallback, covers dirty-state recovery and tests, and states the Phase 9/10 scope boundary.</description>
  <read_first>
    - `.planning/phases/08-go-server/08-CONTEXT.md` decisions D-23, D-24, D-25
    - `.planning/phases/08-go-server/08-RESEARCH.md` "Common Pitfalls" #2 (dirty state), "Environment Availability"
    - `.planning/phases/08-go-server/08-PATTERNS.md` file #5
    - `apps/agent/README.md`
    - `references/wps-cowork/apps/api/README.md`
  </read_first>
  <action>Create `apps/server/README.md` with the sections described in the artifacts block. The Quick start must lead with `podman compose -f docker-compose.server.yml up -d` and provide a clearly marked Docker fallback (`docker compose -f docker-compose.server.yml up -d`). Include the migration command `migrate -path apps/server/migrations -database "$PALADIN_DATABASE_URL" up` and the run command `go run ./cmd/server`. Include `curl -sf localhost:9880/healthz` and `curl -sf localhost:9880/readyz` as the verification step. Add a Dirty-state recovery subsection: `migrate -path apps/server/migrations -database "$PALADIN_DATABASE_URL" force <version>` then re-run `up`. Add a Testing subsection: `go test ./... -race -count=1` and `PALADIN_DB_INTEGRATION=1 go test ./internal/db/...`. Add a Scope boundary subsection explicitly deferring audit logs, quota management, and sidecar management to Phase 9 / later sidecar work, and packaging/TLS to Phase 10.</action>
  <verify>
    <automated>cd apps/server &amp;&amp; grep -E 'podman compose -f docker-compose.server.yml up -d|docker compose -f docker-compose.server.yml up -d|migrate -path apps/server/migrations|go run ./cmd/server|localhost:9880/healthz|localhost:9880/readyz|force <version>|go test ./\.\.\. -race' README.md &amp;&amp; grep -E 'Scope|Phase 9|Phase 10|sidecar' README.md</automated>
  </verify>
  <acceptance_criteria>README leads with the Podman command and includes the Docker fallback; it documents migrate up, go run, and both health curls; it includes the dirty-state force recovery; it documents the -race test command; it states the Phase 9/10/sidecar scope boundary; Podman appears before Docker in the Quick start.</acceptance_criteria>
  <done>Acceptance criteria above are met and the automated verification command passes.</done>
</task>

<task id="08-07-03" type="execute">
  <name>Task 3: End-to-end integration smoke (R1..R6 acceptance)</name>
  <description>Run the full bring-up against the compose stack and capture each acceptance bullet: health up/down, register/login/me/admin, WebSocket auth.</description>
  <read_first>
    - `.planning/phases/08-go-server/08-SPEC.md` Acceptance Criteria and R6 manual verification
    - `.planning/phases/08-go-server/08-VALIDATION.md` Manual-Only Verifications
    - `apps/server/README.md`
    - `docker-compose.server.yml`
  </read_first>
  <action>Perform the following sequence and record outputs. (1) `podman compose -f docker-compose.server.yml up -d` (or docker fallback), wait healthy. (2) `migrate -path apps/server/migrations -database "$PALADIN_DATABASE_URL" up`. (3) Start the server with valid env in the background. (4) `curl -sf localhost:9880/healthz` -> expect 200 `{"status":"ok"}`. (5) `curl -sf localhost:9880/readyz` -> expect 200 with postgres+redis up. (6) R1 failure path: `podman compose stop postgres` then `curl -sf localhost:9880/readyz` -> expect 503 with postgres:down; restart postgres and re-confirm 200. (7) `POST /auth/register` with a new email -> expect 201 roles ["user"]. (8) Register the same email again -> expect 409 email_taken. (9) `POST /auth/login` with valid creds -> expect 200 with a token; parse `expires_at`. (10) `GET /me` with the Bearer token -> expect 200. (11) `GET /admin/health` with the user token -> expect 403. (12) Log in as the bootstrapped admin (PALADIN_ADMIN_EMAIL/PASSWORD) and call `/admin/health` -> expect 200. (13) Dial `ws://localhost:9880/ws`, send no auth message -> expect close within 5s; dial again with `{"type":"auth","token":"<jwt>"}` -> expect the connection to stay open. Capture each step's status code in the summary.</action>
  <verify>
    <automated>podman compose -f docker-compose.server.yml up -d &amp;&amp; sleep 6 &amp;&amp; PGURL="$PALADIN_DATABASE_URL" &amp;&amp; migrate -path apps/server/migrations -database "$PGURL" up &amp;&amp; curl -sf localhost:9880/healthz &amp;&amp; curl -sf localhost:9880/readyz</automated>
  </verify>
  <acceptance_criteria>The health/ready 200 path succeeds; stopping postgres makes /readyz return 503 with postgres:down and restarting restores 200; register returns 201 then 409 on duplicate; login returns 200 with a token; /me returns 200 with the bearer token; /admin/health returns 403 for a user and 200 for the admin; an unauth WS connection closes within ~5s and a valid-token WS connection stays open.</acceptance_criteria>
  <done>Acceptance criteria above are met for every numbered step and the automated verification command passes; all captured status codes are recorded in the summary.</done>
</task>

<task id="08-07-04" type="execute">
  <name>Task 4: Phase 9 / sidecar scope-review grep</name>
  <description>Prove the Prohibition-4 boundary: no audit log, no Redis quota counter, and no Tauri sidecar process management exists in apps/server.</description>
  <read_first>
    - `.planning/phases/08-go-server/08-SPEC.md` Prohibitions (row 4) and Out of scope
    - `.planning/phases/08-go-server/08-CONTEXT.md` Out of scope
    - `apps/server/` source tree
  </read_first>
  <action>Run grep checks over `apps/server` and confirm absence: (a) no `audit_log`/`audit_logs` table in migrations and no `audit` package; (b) no Redis quota-counter usage — no `INCR`, `HINCRBY`, `INCRBY` call on the redis client and no `quota` symbol; (c) no Tauri/sidecar process-management code — no `tauri`, `sidecar`, `exec.Command` launching the desktop or agent. If any match appears, remove or defer it before completing the phase. Document the grep result in the summary.</action>
  <verify>
    <automated>! grep -RInE 'audit_log|audit_logs|package audit|INCR|HINCRBY|INCRBY|quota|tauri|sidecar|exec.Command' apps/server/internal apps/server/cmd apps/server/migrations</automated>
  </verify>
  <acceptance_criteria>The grep returns no matches across apps/server internal/cmd/migrations for audit_log, audit package, Redis INCR/HINCRBY/INCRBY, quota, tauri, sidecar, or exec.Command; the Prohibition-4 boundary holds.</acceptance_criteria>
  <done>Acceptance criteria above are met and the automated verification command passes.</done>
</task>

<task id="08-07-05" type="execute">
  <name>Task 5: Update 08-VALIDATION.md to Wave 0 complete and Nyquist compliant</name>
  <description>Fill the validation task IDs, close the Wave 0 checklist, and flip the frontmatter flags now that every acceptance path is proven.</description>
  <read_first>
    - `.planning/phases/08-go-server/08-VALIDATION.md`
    - `.planning/phases/08-go-server/08-SPEC.md` Acceptance Criteria
  </read_first>
  <action>Edit `.planning/phases/08-go-server/08-VALIDATION.md`. In the frontmatter set `wave_0_complete: true` and `nyquist_compliant: true`. In the Per-Task Verification Map replace each TBD-NN with the owning plan/task id (e.g. SRV-01 health -> 08-04-05, SRV-01 compose -> 08-02-05, SRV-02 schema -> 08-02-03/04, SRV-02 register/login -> 08-05-02, SRV-03 RBAC -> 08-05-02, SRV-04 WS -> 08-06-02) and set Status to ✅ green. Tick every Wave 0 Requirements checkbox and every Validation Sign-Off checkbox. Set Approval to approved.</action>
  <verify>
    <automated>grep -E 'wave_0_complete: true|nyquist_compliant: true' .planning/phases/08-go-server/08-VALIDATION.md &amp;&amp; ! grep -E 'TBD-NN|⬜ pending' .planning/phases/08-go-server/08-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>Frontmatter has wave_0_complete: true and nyquist_compliant: true; no TBD-NN placeholder or pending checkbox remains; every Per-Task row maps to a real plan/task id with ✅; the Validation Sign-Off checklist is fully ticked; Approval is approved.</acceptance_criteria>
  <done>Acceptance criteria above are met and the automated verification command passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documented commands -> developer environment | Misleading or Docker-only docs would violate D-24 and block Podman users. |
| scope boundary -> Phase 9 | Audit/quota/sidecar leakage would inflate Phase 8 scope and break Prohibition 4. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-07-01 | Repudiation/Tampering | scope creep | medium | mitigate | Scope-review grep asserts no audit/quota/sidecar symbols (Prohibition 4). |
| T-08-07-02 | Information Disclosure | secrets in docs | medium | mitigate | .env.example + README placeholder grep asserts no real secret (D-14). |
| T-08-07-03 | Denial of Service | dirty migration state | low | mitigate | README documents `migrate force` recovery (RESEARCH Pitfall #2). |
| T-08-07-SC | Tampering | package installs | low | accept | No new packages in this plan; runtime tooling documented only. |
</threat_model>

<verification>
Run the phase-closing gates:

`cd apps/server && go test ./... -race -count=1`

End-to-end smoke (compose up): healthz 200, readyz 200, readyz 503 with postgres down, register 201/409, login 200, /me 200, /admin/health 403(user)/200(admin), WS no-auth close / valid-auth open.

Scope grep: `! grep -RInE 'audit_log|audit_logs|package audit|INCR|HINCRBY|INCRBY|quota|tauri|sidecar|exec.Command' apps/server/internal apps/server/cmd apps/server/migrations`

Validation frontmatter: `wave_0_complete: true` and `nyquist_compliant: true`.
</verification>

<success_criteria>
- README leads with Podman, documents Docker fallback, covers migration/run/health/test, dirty-state recovery, and the Phase 9/10 scope boundary.
- `.env.example` documents all nine keys with placeholders and the >=32-byte JWT note; no real secret is committed.
- The end-to-end smoke captures every R1..R6 acceptance bullet (health up/down, register/login/me/admin, WS auth).
- The scope-review grep confirms no audit/quota/sidecar code in apps/server.
- `08-VALIDATION.md` is Wave 0 complete, Nyquist compliant, with task IDs filled and approval set.
- `go test ./... -race` is green across the whole module.
</success_criteria>

<output>
Create `.planning/phases/08-go-server/08-07-SUMMARY.md` when done, including the captured status codes from the end-to-end smoke and the scope-grep result.
</output>
