---
phase: 09
plan: 09-05
title: End-to-end integration verification + scope gates + README + VALIDATION update
type: execute
wave: 3
depends_on:
  - "09-01"
  - "09-02"
  - "09-03"
  - "09-04"
requirements:
  - ADM-01
  - ADM-02
  - ADM-03
files_modified:
  - apps/server/README.md
  - apps/server/.env.example
  - .planning/phases/09-admin-systems/09-VALIDATION.md
  - .planning/phases/09-admin-systems/09-UAT.md
  - .planning/phases/09-admin-systems/09-VERIFICATION.md
autonomous: true
user_setup:
  - "Podman running with PostgreSQL + Redis via docker-compose.server.yml (compose up)"
  - "PALADIN_QUOTA_LIMIT and PALADIN_QUOTA_WINDOW set for the live smoke if defaults (50/1h) are undesirable for fast testing"
must_haves:
  truths:
    - "All SPEC R1-R6 acceptance criteria are covered by automated tests from plans 09-01..09-04 or live smoke steps in this plan."
    - "SPEC Prohibition: Phase 9 does not implement Tauri sidecar management, a desktop admin console, OAuth, or packaging — verified by source grep."
    - "Phase 8 regression: all Phase 8 tests still pass unchanged (auth, RBAC, health, WebSocket MVP)."
    - "Full module: go test ./... -race passes, go vet clean, gofmt clean, sqlc generate produces no diff."
  prohibitions:
    - statement: "MUST NOT implement Tauri sidecar management, desktop admin UI, OAuth, or packaging in Phase 9 (SPEC out-of-scope)."
      status: resolved
      verification: "scope-gate grep finds no sidecar/desktop/oauth/packaging code added under apps/server or apps/desktop in Phase 9."
    - statement: "MUST NOT regress Phase 8 auth/RBAC/health/WebSocket behavior."
      status: resolved
      verification: "go test ./... -race passes the full suite including all Phase 8 tests unchanged."
  artifacts:
    - path: "apps/server/README.md"
      provides: "updated local-run docs covering audit/quota env vars, admin API, and quota gate"
      exports: []
    - path: ".planning/phases/09-admin-systems/09-VALIDATION.md"
      provides: "validation matrix mapping each SPEC acceptance criterion to its test/smoke proof"
      exports: []
    - path: ".planning/phases/09-admin-systems/09-VERIFICATION.md"
      provides: "release criteria sign-off for Phase 9"
      exports: []
  key_links: []
---

<objective>
Run the full Phase 9 verification matrix: confirm every SPEC R1-R6 acceptance criterion is proven by automated tests or live smoke, enforce the scope gates (no sidecar/desktop/OAuth/packaging), confirm Phase 8 regression, update the README with audit/quota env vars and admin API docs, and produce the VALIDATION + VERIFICATION artifacts.

Purpose: This plan is the Phase 9 closeout gate. It does not add features — it proves the features from 09-01..09-04 meet the SPEC and that no out-of-scope creep occurred.
Output: A green `go test ./... -race`, clean `go vet`/`gofmt`/`sqlc generate`, passing scope gates, updated README, and VALIDATION/VERIFICATION artifacts.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-admin-systems/09-SPEC.md
@.planning/phases/09-admin-systems/09-CONTEXT.md
@.planning/phases/09-admin-systems/09-01-PLAN.md
@.planning/phases/09-admin-systems/09-02-PLAN.md
@.planning/phases/09-admin-systems/09-03-PLAN.md
@.planning/phases/09-admin-systems/09-04-PLAN.md
@apps/server/README.md
@apps/server/.env.example
</context>

## Artifacts this phase produces

- `apps/server/README.md` — updated with: PALADIN_QUOTA_LIMIT/WINDOW env vars, the quota gate behavior, the GET /admin/audit-logs endpoint, and the WebSocket admin event stream.
- `.planning/phases/09-admin-systems/09-VALIDATION.md` — validation matrix: each SPEC acceptance criterion → test name or smoke step that proves it.
- `.planning/phases/09-admin-systems/09-UAT.md` — manual UAT checklist for end-of-phase human verification.
- `.planning/phases/09-admin-systems/09-VERIFICATION.md` — release criteria sign-off.

<tasks>

<task id="09-05-01" type="execute">
  <name>Task 1: Run full module test suite + quality gates</name>
  <description>Run the complete Go test suite under -race and all quality gates to confirm no regressions and no drift.</description>
  <read_first>
    - `.planning/phases/09-admin-systems/09-SPEC.md` Acceptance Criteria
    - `apps/server/README.md` (existing test commands)
  </read_first>
  <action>Run `cd apps/server && sqlc generate && git diff --exit-code internal/db/sqlc/` (no codegen drift). Run `cd apps/server && go test ./... -race -count=1`. Run `cd apps/server && go vet ./...`. Run `cd apps/server && gofmt -l .` (must be empty). Record pass/fail for each. If any gate fails, do NOT mark this task done — surface the failure for a repair task.</action>
  <verify>
    <automated>cd apps/server &amp;&amp; sqlc generate &amp;&amp; git diff --exit-code internal/db/sqlc/ &amp;&amp; go test ./... -race -count=1 &amp;&amp; go vet ./... &amp;&amp; test -z "$(gofmt -l .)" &amp;&amp; echo ALL_GATES_GREEN</automated>
  </verify>
  <acceptance_criteria>`sqlc generate` produces no diff; `go test ./... -race` passes (all Phase 8 + Phase 9 tests); `go vet ./...` clean; `gofmt -l .` empty; ALL_GATES_GREEN printed.</acceptance_criteria>
  <done>Acceptance criteria above are met and the automated verification command prints ALL_GATES_GREEN.</done>
</task>

<task id="09-05-02" type="execute">
  <name>Task 2: Enforce scope gates (no sidecar/desktop/OAuth/packaging creep)</name>
  <description>Grep the codebase to confirm Phase 9 did not introduce any out-of-scope work.</description>
  <read_first>
    - `.planning/phases/09-admin-systems/09-SPEC.md` Out of scope
    - `.planning/phases/09-admin-systems/09-CONTEXT.md` Deferred Ideas
  </read_first>
  <action>Run grep checks: no new Tauri sidecar config under apps/desktop/src-tauri referencing the Go server process management; no new OAuth provider code under apps/server; no new desktop admin UI components under apps/desktop/src/components/admin; no packaging/bundling changes. The checks should find ONLY documentation references in .planning/ (which are allowed). Also assert no `auth/refresh` route exists (Phase 8 prohibition preserved).</action>
  <verify>
    <automated>! git diff --name-only HEAD~10 apps/desktop/src-tauri/tauri.conf.json | grep -q . ; ! grep -RIn 'oauth\|oauth2\|golang.org/x/oauth' apps/server/internal/ ; ! grep -RIn 'auth/refresh' apps/server/internal/http/server.go ; echo SCOPE_CLEAN</automated>
  </verify>
  <acceptance_criteria>No Tauri sidecar config changes for Go server process management; no OAuth code under apps/server; no auth/refresh route; no desktop admin UI components added; SCOPE_CLEAN printed.</acceptance_criteria>
  <done>Acceptance criteria above are met and the automated verification command prints SCOPE_CLEAN.</done>
</task>

<task id="09-05-03" type="execute">
  <name>Task 3: Live end-to-end smoke against Podman stack</name>
  <description>Run the manual smoke flow against a live server to prove the three subsystems integrate end-to-end.</description>
  <read_first>
    - `.planning/phases/09-admin-systems/09-SPEC.md` Acceptance Criteria
    - `apps/server/README.md`
    - `docker-compose.server.yml`
  </read_first>
  <action>Start the stack: `podman compose -f docker-compose.server.yml up -d`. Run migrations. Start the server with a low quota for fast testing: `PALADIN_QUOTA_LIMIT=5 PALADIN_QUOTA_WINDOW=1m ...`. Smoke flow: (1) register a user + login → confirm auth.register/auth.login audit rows via GET /admin/audit-logs as admin; (2) hit POST /ai/mock 6 times with the user token → confirm the 6th returns 429 quota_exceeded with X-RateLimit-Remaining: 0; (3) confirm a quota.exceeded audit row exists; (4) connect an admin WS client → trigger another audit event → confirm the admin receives an "audit.log" envelope; (5) confirm the user-role WS client does NOT receive admin events; (6) hit /ai/mock as admin → confirm no 429 (bypass). Record results.</action>
  <verify>
    <automated>podman compose -f docker-compose.server.yml ps | grep -qE 'postgres|redis' &amp;&amp; curl -sf http://localhost:9880/healthz | grep -q ok &amp;&amp; echo SMOKE_READY</automated>
  </verify>
  <acceptance_criteria>The stack is running; /healthz returns ok; the 6-step smoke flow completes with the expected audit rows, 429 on the 6th AI call, admin WS receiving "audit.log", user WS excluded, and admin bypass confirmed. Results recorded for the summary.</acceptance_criteria>
  <done>Acceptance criteria above are met and the smoke results are recorded.</done>
</task>

<task id="09-05-04" type="execute">
  <name>Task 4: Update README + produce VALIDATION/UAT/VERIFICATION artifacts</name>
  <description>Document the new admin capabilities and produce the Phase 9 closeout artifacts.</description>
  <read_first>
    - `.planning/phases/09-admin-systems/09-SPEC.md` full Acceptance Criteria list
    - `apps/server/README.md`
    - `apps/server/.env.example`
    - `.planning/phases/08-go-server/08-VALIDATION.md` (artifact format)
  </read_first>
  <action>Update `apps/server/README.md`: add a "Phase 9 — Admin Systems" section documenting PALADIN_QUOTA_LIMIT/PALADIN_QUOTA_WINDOW, the quota gate behavior (429 + headers), GET /admin/audit-logs (cursor + filters), and the WebSocket admin event stream (audit.log, quota.exceeded envelopes). Update `.env.example` if any new vars are missing. Create `.planning/phases/09-admin-systems/09-VALIDATION.md` mapping each SPEC acceptance criterion to its proof (test name or smoke step). Create `09-UAT.md` with a manual checklist. Create `09-VERIFICATION.md` with release criteria sign-off.</action>
  <verify>
    <automated>test -f .planning/phases/09-admin-systems/09-VALIDATION.md &amp;&amp; test -f .planning/phases/09-admin-systems/09-UAT.md &amp;&amp; test -f .planning/phases/09-admin-systems/09-VERIFICATION.md &amp;&amp; grep -q 'PALADIN_QUOTA_LIMIT' apps/server/.env.example &amp;&amp; grep -q 'admin/audit-logs' apps/server/README.md &amp;&amp; echo ARTIFACTS_READY</automated>
  </verify>
  <acceptance_criteria>README documents quota env vars, the audit API, and the admin WS event stream; .env.example includes PALADIN_QUOTA_LIMIT/WINDOW; VALIDATION.md maps every SPEC acceptance criterion to a proof; UAT.md and VERIFICATION.md exist.</acceptance_criteria>
  <done>Acceptance criteria above are met and the automated verification command prints ARTIFACTS_READY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| full module -> release | The closeout gate decides whether Phase 9 is shippable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05-01 | Tampering | scope creep | medium | mitigate | Scope-gate grep confirms no sidecar/desktop/OAuth/packaging code was introduced. |
| T-09-05-02 | Repudiation | untested acceptance criterion | medium | mitigate | VALIDATION.md maps every SPEC criterion to a test or smoke proof. |
| T-09-05-03 | Denial of Service | Phase 8 regression | high | mitigate | Full `go test ./... -race` includes all Phase 8 tests unchanged. |
</threat_model>

<verification>
Final Phase 9 gates:

`cd apps/server && sqlc generate && git diff --exit-code internal/db/sqlc/`

`cd apps/server && go test ./... -race -count=1`

`cd apps/server && go vet ./... && test -z "$(gofmt -l .)"`

Scope gates (Task 2 commands) + live smoke (Task 3) + artifacts present (Task 4).
</verification>

<success_criteria>
- `go test ./... -race` passes (all Phase 8 + Phase 9 tests green).
- `sqlc generate` produces no diff; `go vet` clean; `gofmt -l .` empty.
- Scope gates confirm no sidecar/desktop/OAuth/packaging creep; no auth/refresh route.
- Live smoke proves: audit rows on auth/RBAC/quota events, 429 on over-limit, admin WS event fan-out, user WS exclusion, admin bypass.
- README documents quota env vars, audit API, and admin WS event stream.
- VALIDATION.md maps every SPEC R1-R6 acceptance criterion to its proof; UAT.md and VERIFICATION.md exist.
</success_criteria>

<output>
Create `.planning/phases/09-admin-systems/09-05-SUMMARY.md` when done, including the gate results (ALL_GATES_GREEN, SCOPE_CLEAN), the live smoke results table, and a link to VALIDATION.md. Update STATE.md to mark Phase 9 Complete and Phase 10 Ready to plan.
</output>
