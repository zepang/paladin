---
phase: 10-packaging
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-packaging/10-VALIDATION.md
  - apps/desktop/scripts/release.mjs
  - apps/desktop/scripts/release.test.mjs
  - apps/agent/paladin-agent-sidecar.spec
  - apps/desktop/package.json
  - package.json
autonomous: true
requirements: [PKG-01, PKG-02]
must_haves:
  truths:
    - "R1/Edge: macOS DMG 可从仓库构建并在全新安装位置启动，且不读取源码或开发工具。"
    - "R2/Edge: MSI 构建、Windows UAT 与 release-ready 是三个独立状态。"
    - "R3/Edge: 两个逻辑 sidecar 名可映射 target triple 与 Windows .exe。"
    - "R4/Edge: 环境只在进程启动时采样，运行中不热加载。"
    - "R5/Edge: PG/Redis 三种缺失组合仅令 Go degraded，Agent 与工作区非阻塞。"
    - "R6/Edge: 日志在完整行边界执行 10 MB x 5 轮转，写盘失败不影响 UI 流。"
    - "R7/Edge: 文档逐平台分列 build、installed UAT、release-ready。"
  prohibitions:
    - "MUST NOT 将仅构建或 UAT 未完成/失败的平台标记为 release-ready。"
    - "MUST NOT 将继承的 API Key、数据库、Redis 或 JWT 值写入日志、诊断、文档或 bundle。"
    - "MUST NOT 在 packaged 诊断要求运行开发命令或编辑源码配置。"
    - "MUST NOT 按端口/进程名终止外部进程；仅操作 supervisor 持有句柄。"
  artifacts:
    - path: .planning/phases/10-packaging/10-VALIDATION.md
      provides: "执行前有效的当前 Nyquist task/evidence map"
    - path: apps/desktop/scripts/release.mjs
      provides: "单一 release 入口、sidecar 构建、输入审计与产物清单"
      exports: [main, buildAgent, buildServer, auditBundleInputs]
    - path: apps/agent/paladin-agent-sidecar.spec
      provides: "PyInstaller paladin-agent-sidecar 规格"
    - path: apps/desktop/scripts/release.test.mjs
      provides: "release pipeline 合约测试"
  key_links:
    - from: package.json
      to: apps/desktop/scripts/release.mjs
      via: "package-manager release 入口"
      pattern: "release"
    - from: apps/desktop/scripts/release.mjs
      to: apps/agent/paladin-agent-sidecar.spec
      via: "PyInstaller spec invocation"
      pattern: "paladin-agent-sidecar"
---

<objective>
建立可测试的单一 release 构建入口，生成 PyInstaller Agent 与 Go Server 原生 sidecar，并在 Tauri 启动前 fail-closed 审计产物和 `.env` 输入。

Purpose: 为 macOS 与 Windows 原生构建提供同一、无 shell 注入且可审计的构建链（D-01、D-02、D-13）。
Output: release 脚本、测试、PyInstaller spec 与 package-manager 入口。
</objective>

<execution_context>
@/Users/kdocs/.codex/gsd-core/workflows/execute-plan.md
@/Users/kdocs/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-packaging/10-SPEC.md
@.planning/phases/10-packaging/10-CONTEXT.md
@.planning/phases/10-packaging/10-RESEARCH.md
@.planning/phases/10-packaging/10-PATTERNS.md
@apps/desktop/scripts/tauri-cli.mjs
@apps/agent/pyproject.toml
@apps/agent/src/server/cli.py
@apps/server/cmd/server/main.go
</context>

## Artifacts This Plan Produces

- `apps/desktop/scripts/release.mjs`: `main`, `buildAgent`, `buildServer`, `auditBundleInputs`, stable logical-name staging.
- `apps/desktop/scripts/release.test.mjs`: ordering, missing artifact, `.env`, suffix/triple tests.
- `apps/agent/paladin-agent-sidecar.spec`: PyInstaller executable definition.

<tasks>

<task type="auto" id="T0">
  <name>Task 0: 在实现前重写 Phase 10 Validation 合同</name>
  <files>.planning/phases/10-packaging/10-VALIDATION.md</files>
  <read_first>当前全部 Phase 10 PLAN；10-SPEC Edge/Prohibitions；10-CONTEXT D-01..D-25；10-UI-SPEC。</read_first>
  <action>维护已提前写入磁盘的 Validation 合同：任何计划 ID/任务、gate 或依赖变化必须同步更新；macOS 四场景仍是唯一 installed-app blocking gate，Windows 只保留 native buildability evidence。合同完整不等于 task 已执行。</action>
  <verify><automated>node -e "const fs=require('fs');const s=fs.readFileSync('.planning/phases/10-packaging/10-VALIDATION.md','utf8');const current=['10-01/T0','10-01/T1','10-01/T2','10-02/T1','10-02/T2','10-03/T1','10-03/T2','10-04/T1','10-04/T2','10-05/T1','10-05/T2','10-05/T3','10-06/T1','10-06/T2','10-06/T3','10-07/T1','10-07/T2','10-08/T1','10-08/T2','10-09/T1','10-09/T2','10-09/T3'];const banned=['10-W0-01','10-PKG-01','10-PKG-02','10-SDC-01','10-SDC-02','10-LOG-01','10-UAT-PREREQ','10-UAT-01','10-CLOSE-01','Dual-platform hard gate','Windows installed-app blocking','Windows installed UAT blocking','Windows installed-app UAT hard gate'];for(const x of current)if(!s.includes(x))process.exit(1);for(const x of banned)if(s.includes(x))process.exit(1);for(const x of ['wave_0_complete: true','nyquist_compliant: true','buildability-only','installed-app blocking'])if(!s.includes(x))process.exit(1)"</automated></verify>
  <acceptance_criteria>所有 22 个当前 task IDs 恰有合同映射；历史 ID 与双平台硬门措辞均不存在；quick/focused/full 延迟层级明确。</acceptance_criteria>
  <done>执行者从 Wave 1 即使用当前 Nyquist 合同。</done>
</task>

<task type="auto" id="T1" tdd="true">
  <name>Task 1: 用 RED→GREEN 锁定 release orchestration</name>
  <files>apps/desktop/scripts/release.test.mjs, apps/desktop/scripts/release.mjs</files>
  <read_first>10-SPEC.md R1-R3；10-CONTEXT.md D-01、D-02、D-13；tauri-cli.mjs 的 shell:false 模式。</read_first>
  <behavior>先写失败测试：Agent→Server→Tauri 顺序；缺任一 sidecar 时不调用 Tauri；任何将进入 PyInstaller/Tauri 输入的 `.env` 令构建失败；逻辑名映射当前 triple/Windows 后缀。</behavior>
  <action>按 D-02 实现单入口和显式 argv spawn，按 D-13 对实际输入树 fail-closed，禁止把 secret 值写入输出；只在测试 RED 后实现 GREEN，再整理重复路径逻辑。</action>
  <verify><automated>node --test apps/desktop/scripts/release.test.mjs</automated></verify>
  <acceptance_criteria>测试真实证明排序、阻断点、输入审计与平台命名，不以 dry-run 文本代替行为断言。</acceptance_criteria>
  <done>release orchestration 合约测试全部通过。</done>
</task>

<task type="auto" id="T2">
  <name>Task 2: 接入真实 Agent/Server 构建与 package 入口</name>
  <files>apps/agent/paladin-agent-sidecar.spec, apps/desktop/scripts/release.mjs, apps/desktop/package.json, package.json</files>
  <read_first>T1 输出；pyproject.toml 的 CLI entry；server main.go；CONTEXT D-01/D-02。</read_first>
  <action>用 PyInstaller spec 生成逻辑名 `paladin-agent-sidecar`，用 `go build` 生成 `paladin-server-sidecar`；按目标平台 staging 为 Tauri 需要的 triple/`.exe` 名。根脚本只委托 release.mjs，保留 dev processes.json，不引入 schema push。</action>
  <verify><automated>pnpm release -- --sidecars-only --target current --verify</automated></verify>
  <acceptance_criteria>两个 sidecar 均由 repo source 生成；缺失产物或 `.env` 输入使命令非零退出；无需 uv/go run 作为运行时命令。</acceptance_criteria>
  <done>当前平台 sidecar build/verify 通过，统一入口可供后续 installer 与 CI 使用。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| source/build inputs→bundle | 不可信文件可能夹带 secret 或错误二进制 |
| package script→child tools | 参数必须保持 argv、不得经 shell |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-01-01 | Tampering | bundle inputs | high | mitigate | D-13 fail-closed `.env` 审计、存在性与命名校验 |
| T-10-01-02 | Elevation | release.mjs spawn | high | mitigate | shell:false、固定工具与 argv |
| T-10-01-03 | Information Disclosure | build output | high | mitigate | 不打印环境值，不打包 `.env` |
</threat_model>

<verification>执行 node tests、当前平台 sidecar build/verify；确认无 schema 文件或 schema push。</verification>
<success_criteria>单入口真实生成两种 sidecar，输入审计在 Tauri 前阻断，逻辑名与平台名可追踪。</success_criteria>
<output>Create `.planning/phases/10-packaging/10-01-SUMMARY.md` when done.</output>
