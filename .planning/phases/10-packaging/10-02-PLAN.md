---
phase: 10-packaging
plan: "02"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - apps/desktop/src-tauri/processes.packaged.json
  - apps/desktop/src-tauri/tauri.conf.json
  - apps/desktop/src-tauri/src/lib.rs
  - apps/desktop/src-tauri/src/process/config.rs
  - apps/desktop/src-tauri/src/process/config_tests.rs
autonomous: true
requirements: [PKG-01, PKG-02]
must_haves:
  truths:
    - "R1/Edge: DMG 安装位置不依赖源码或开发工具。"
    - "R2/Edge: MSI build、Windows UAT、release-ready 独立记录。"
    - "R3/Edge: runtime 解析 target triple 与 Windows .exe。"
    - "R4/Edge: 环境仅于 sidecar 启动采样。"
    - "R5/Edge: PG/Redis 缺失不阻塞 Agent/工作区。"
    - "R6/Edge: 10 MB x 5 完整行轮转且 UI 流独立。"
    - "R7/Edge: 平台发布事实分列。"
  prohibitions:
    - "MUST NOT 错标 release-ready。"
    - "MUST NOT 泄露继承配置值。"
    - "MUST NOT 在 packaged 诊断要求开发命令或源码编辑。"
    - "MUST NOT 杀死 supervisor 未持有的外部进程。"
  artifacts:
    - path: apps/desktop/src-tauri/processes.packaged.json
      provides: "packaged cmd:string[] 配置"
    - path: apps/desktop/src-tauri/src/process/config.rs
      provides: "resource config 与 sidecar path resolution"
      exports: [resolve_process_config_path, resolve_packaged_executable]
  key_links:
    - from: apps/desktop/src-tauri/tauri.conf.json
      to: apps/desktop/src-tauri/processes.packaged.json
      via: bundle resources
      pattern: "processes.packaged.json"
    - from: apps/desktop/src-tauri/src/lib.rs
      to: apps/desktop/src-tauri/src/process/config.rs
      via: installed resource lookup
      pattern: "resolve_process_config_path"
---
<objective>将独立 packaged config 与两种 sidecar 接入 Tauri bundle，并从安装资源确定性加载/解析。（D-01、D-03）</objective>
<execution_context>@/Users/kdocs/.codex/gsd-core/workflows/execute-plan.md
@/Users/kdocs/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/10-packaging/10-SPEC.md
@.planning/phases/10-packaging/10-CONTEXT.md
@.planning/phases/10-packaging/10-PATTERNS.md
@apps/desktop/src-tauri/processes.json
@apps/desktop/src-tauri/src/process/config.rs
@apps/desktop/src-tauri/src/lib.rs</context>
## Artifacts This Plan Produces
- `processes.packaged.json`: stable `cmd: string[]`, null cwd, locked logical names.
- `resolve_process_config_path` and `resolve_packaged_executable`: resource-dir-aware pure helpers.
<tasks>
<task type="auto" id="T1" tdd="true"><name>Task 1: RED→GREEN packaged lookup/path contracts</name><files>apps/desktop/src-tauri/src/process/config_tests.rs, apps/desktop/src-tauri/src/process/config.rs</files><read_first>SPEC R3；CONTEXT D-01/D-03；现有 packaged validation tests。</read_first><behavior>测试 dev config 不变；packaged config 从 resource_dir；两个逻辑名解析 triple/Windows suffix；uv/go/repo cwd 被拒；路径越界和缺文件失败。</behavior><action>先提交失败测试，再实现纯 helper；不改变 `cmd: Vec<String>` schema，不回退 PATH 或 CARGO_MANIFEST_DIR。</action><verify><automated>cd apps/desktop/src-tauri && cargo test process::config --lib</automated></verify><acceptance_criteria>所有路径分支由测试覆盖，安装资源缺失进入诊断失败而非 dev fallback。</acceptance_criteria><done>packaged lookup 和 executable resolution 测试通过。</done></task>
<task type="auto" id="T2"><name>Task 2: bundle resources/externalBin 与 setup 接线</name><files>apps/desktop/src-tauri/processes.packaged.json, apps/desktop/src-tauri/tauri.conf.json, apps/desktop/src-tauri/src/lib.rs</files><read_first>T1 helpers；tauri.conf；CONTEXT D-01/D-03。</read_first><action>保留 processes.json；新增 packaged config，配置 Tauri externalBin/等效资源；setup 依据真实 app resource_dir 加载 packaged config，使用诊断 fallback 报缺失资源。</action><verify><automated>cd apps/desktop/src-tauri && cargo test process:: --lib && cargo check</automated></verify><acceptance_criteria>bundle 声明两个 sidecar 与 packaged config；dev 仍走原配置；installed lookup 无源码假设。</acceptance_criteria><done>Tauri bundle 与 runtime config selection 接线完成。</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| installed resources→Rust config | bundle 内容必须验证后使用 |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-02-01 | Tampering | resource lookup | high | mitigate | canonical resource root、固定逻辑名、存在性校验 |
| T-10-02-02 | Elevation | executable resolution | high | mitigate | 无 PATH fallback/无 shell |
</threat_model>
<verification>cargo config/process tests、cargo check、bundle JSON inspection。</verification>
<success_criteria>安装资源可确定性加载，dev config 原样保留，sidecar 名正确解析。</success_criteria>
<output>Create `.planning/phases/10-packaging/10-02-SUMMARY.md` when done.</output>
