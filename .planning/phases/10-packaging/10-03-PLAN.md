---
phase: 10-packaging
plan: "03"
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - apps/desktop/src-tauri/src/process/supervisor.rs
  - apps/desktop/src-tauri/src/process/supervisor_tests.rs
  - apps/agent/src/server/cli.py
  - apps/agent/tests/test_cli.py
  - apps/server/cmd/server/main.go
  - apps/server/cmd/server/main_test.go
autonomous: true
requirements: [PKG-01, PKG-02]
must_haves:
  truths: ["R1/Edge: installed runtime 无开发依赖。", "R2/Edge: Windows 构建不等于 UAT。", "R3/Edge: 平台 sidecar 路径正确。", "R4/Edge: 启动时环境快照且不热加载。", "R5/Edge: 依赖缺失仅令 Go degraded。", "R6/Edge: 日志轮转失败不影响 UI。", "R7/Edge: 平台状态诚实分列。"]
  prohibitions: ["MUST NOT 错标 release-ready。", "MUST NOT 泄露配置值。", "MUST NOT 给 packaged 用户开发命令。", "MUST NOT 终止非持有外部进程。"]
  artifacts:
    - path: apps/desktop/src-tauri/src/process/supervisor.rs
      provides: "显式 env allowlist、forced packaged mode、resolved executable spawn"
      exports: [environment_for_process]
    - path: apps/agent/tests/test_cli.py
      provides: "packaged 禁用 dotenv 测试"
    - path: apps/server/cmd/server/main_test.go
      provides: "Go packaged 禁用 dotenv 测试"
  key_links:
    - from: apps/desktop/src-tauri/src/process/supervisor.rs
      to: apps/agent/src/server/cli.py
      via: PALADIN_RUNTIME_MODE and allowlisted env
      pattern: "PALADIN_RUNTIME_MODE"
---
<objective>实现 packaged sidecar 的显式环境边界、强制 runtime marker 与 Python/Go 禁用 dotenv。（D-09..D-12）</objective>
<execution_context>@/Users/kdocs/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/10-packaging/10-SPEC.md
@.planning/phases/10-packaging/10-CONTEXT.md
@apps/desktop/src-tauri/src/process/supervisor.rs
@apps/agent/src/server/cli.py
@apps/server/cmd/server/main.go
@apps/server/internal/config/config.go</context>
## Artifacts This Plan Produces
- `environment_for_process`: explicit business/system allowlist and forced mode.
- Python/Go packaged dotenv negative tests.
<tasks>
<task type="auto" id="T1" tdd="true"><name>Task 1: RED→GREEN 环境快照与 ownership-safe lifecycle</name><files>apps/desktop/src-tauri/src/process/supervisor_tests.rs, apps/desktop/src-tauri/src/process/supervisor.rs</files><read_first>CONTEXT D-09/D-10/D-12；SPEC external-owner prohibition；现有 spawn/stop/restart/shutdown/redetect。</read_first><behavior>空 business 值视为 missing；未列 parent vars 不透传；最小 system baseline 透传；parent runtime marker 无法覆盖 packaged。新增精确测试名：`lifecycle_owner_external_stop_is_rejected_without_termination`、`lifecycle_owner_external_restart_only_redetects`、`lifecycle_owner_external_shutdown_is_rejected_without_termination`、`lifecycle_owner_supervisor_uses_tracked_child`；前三者证明无 kill/terminate/port/name scan，最后一项保留 tracked-child 正向行为。</behavior><action>先扩充失败测试，再实现 allowlist builder；生命周期操作只读取 owner 与 tracked child slot，external 分支不得调用终止/查杀 adapter，supervisor 分支只操作持有句柄。</action><verify><automated>cd apps/desktop/src-tauri && LIST=$(cargo test lifecycle_owner --lib -- --list) && printf '%s\n' "$LIST" | grep -q 'lifecycle_owner_external_stop_is_rejected_without_termination' && printf '%s\n' "$LIST" | grep -q 'lifecycle_owner_external_restart_only_redetects' && printf '%s\n' "$LIST" | grep -q 'lifecycle_owner_external_shutdown_is_rejected_without_termination' && printf '%s\n' "$LIST" | grep -q 'lifecycle_owner_supervisor_uses_tracked_child' && COUNT=$(printf '%s\n' "$LIST" | grep -c 'lifecycle_owner_.*: test$') && test "$COUNT" -eq 4 && cargo test lifecycle_owner --lib</automated></verify><acceptance_criteria>列表门先证明精确四个测试存在且非零，再实际运行；三种 external 负向行为和 supervisor-owned 正向行为均通过。</acceptance_criteria><done>环境边界与外部进程保护 focused tests green。</done></task>
<task type="auto" id="T2" tdd="true"><name>Task 2: RED→GREEN 禁用 packaged dotenv</name><files>apps/agent/src/server/cli.py, apps/agent/tests/test_cli.py, apps/server/cmd/server/main.go, apps/server/cmd/server/main_test.go</files><read_first>CONTEXT D-11/D-12；当前 load_dotenv/godotenv calls。</read_first><behavior>dev 保留 dotenv；packaged 忽略工作目录 `.env`；仅 supervisor-forwarded env 生效；缺失配置错误不含值。</behavior><action>分别先写 Python/Go 失败测试，再按 `PALADIN_RUNTIME_MODE=packaged` 跳过 dotenv；保持服务产品语义不变。</action><verify><automated>cd apps/agent && uv run pytest tests/test_cli.py -q && cd ../server && go test ./cmd/server ./internal/config</automated></verify><acceptance_criteria>packaged tests 用诱饵 `.env` 证明未读取，dev convenience 保持。</acceptance_criteria><done>两种 sidecar 的 dotenv boundary 均通过。</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| desktop parent env→sidecars | secret/config 必须最小授权 |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-03-01 | Information Disclosure | inherited env | high | mitigate | env_clear + explicit allowlists |
| T-10-03-02 | Tampering | runtime marker | high | mitigate | supervisor 强制注入 D-12 |
| T-10-03-03 | Elevation | dotenv | high | mitigate | packaged 完全禁用 dotenv |
</threat_model>
<verification>Rust、pytest、Go focused tests。</verification>
<success_criteria>packaged 只接收显式环境，dotenv 不可绕过，诊断不泄密。</success_criteria>
<output>Create `.planning/phases/10-packaging/10-03-SUMMARY.md` when done.</output>
