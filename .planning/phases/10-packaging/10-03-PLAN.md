---
phase: "10"
plan: "10-03"
type: "execute"
wave: 3
depends_on: ["10-02"]
files_modified:
  - "apps/desktop/src-tauri/src/lib.rs"
  - "apps/desktop/src-tauri/src/process/config.rs"
  - "apps/desktop/src-tauri/src/process/config_tests.rs"
  - "apps/desktop/src-tauri/src/process/supervisor.rs"
  - "apps/desktop/src-tauri/src/process/supervisor_tests.rs"
  - "apps/desktop/src-tauri/src/process/log_redact.rs"
  - "apps/desktop/src-tauri/src/process/log_redact_tests.rs"
  - "apps/desktop/src-tauri/src/process/mod.rs"
autonomous: true
requirements: ["PKG-01", "PKG-02", "PKG-03"]
must_haves:
  truths:
    - "Installed macOS builds must launch without pnpm, cargo, uv, go run, or a repo checkout."
    - "Installed Windows builds must launch without pnpm, cargo, uv, go run, or a repo checkout."
    - "Packaged sidecar binaries must exist as paladin-agent-sidecar and paladin-server-sidecar, and packaged validation must reject uv, go, and repo-relative cwd."
    - "Installed-app UAT must record Agent liveness, Go liveness, Go readiness with PostgreSQL/Redis available, and degraded but non-blocking Go behavior when dependencies are unavailable."
    - "Packaged logs must remain bounded at 10 MB x 5 retained files per service."
    - "Rotation must preserve complete log lines and keep redaction active before bytes are persisted."
    - "Rotation must not interrupt the live Logs panel stream."
    - "User-facing documentation must cover install, first launch, readiness expectations, log locations, common failures, and UAT status."
    - "Linux is non-blocking for this phase; macOS and Windows are blocking targets."
    - "No ORM or schema push work is required because this phase does not modify schema files."
  prohibitions:
    - "MUST NOT mark a dev-only or failed-UAT package as release-ready; any platform with missing or failed UAT must be explicitly marked not release-ready."
    - "MUST NOT expose raw secrets, tokens, or environment values in packaged logs, UI diagnostics, or documentation examples."
    - "MUST NOT stop, restart, or shut down user-managed external processes by matching ports or process names; lifecycle actions may only target supervisor-owned child handles."
  artifacts:
    - "Packaged config lookup helper that resolves bundled config instead of CARGO_MANIFEST_DIR."
    - "Packaged sidecar path resolution that maps logical binary names to installed absolute paths."
    - "10 MB x 5 retained-file log rotation integrated into the supervisor persistence path without interrupting UI events."
    - "Rust tests for packaged lookup, packaged spawn path, redaction-before-rotation, line preservation, and rotation failure degradation."
  key_links:
    - ".planning/phases/10-packaging/10-SPEC.md"
    - ".planning/phases/10-packaging/10-CONTEXT.md"
    - ".planning/phases/10-packaging/10-RESEARCH.md"
    - ".planning/phases/10-packaging/10-PATTERNS.md"
    - ".planning/phases/10-packaging/10-VALIDATION.md"
    - ".planning/phases/07.4-sidecar-runtime-mode/07.4-CONTEXT.md"
    - ".planning/phases/07.3-sidecar-process-management/07.3-CONTEXT.md"
---

# 10-03 Plan: Packaged Runtime Lookup And Bounded Log Rotation

<objective>
实现 packaged runtime 的 Rust truth source：安装后从 bundle/resource context 解析 packaged config 与 sidecar executable 绝对路径，保持 packaged validation 拒绝 dev tools/repo cwd，并在 `capture_lines` 附近加入 10 MB x 5 的 redacted line-preserving log rotation，同时保证 `process-log` 事件流不间断。
</objective>

<execution_context>
This is the core packaged-runtime plan. TDD is mandatory for config/path helpers, log rotation behavior, and failure degradation behavior. The plan must preserve Phase 07.4 ownership safety and must not regress external-process protections.
</execution_context>

<context>
Read these first:
- `.planning/phases/10-packaging/10-SPEC.md`
- `.planning/phases/10-packaging/10-CONTEXT.md`
- `.planning/phases/10-packaging/10-RESEARCH.md`
- `.planning/phases/10-packaging/10-PATTERNS.md`
- `.planning/phases/10-packaging/10-VALIDATION.md`
- `.planning/phases/07.4-sidecar-runtime-mode/07.4-CONTEXT.md`
- `.planning/phases/07.3-sidecar-process-management/07.3-CONTEXT.md`
- `apps/desktop/src-tauri/src/lib.rs`
- `apps/desktop/src-tauri/src/process/config.rs`
- `apps/desktop/src-tauri/src/process/config_tests.rs`
- `apps/desktop/src-tauri/src/process/supervisor.rs`
- `apps/desktop/src-tauri/src/process/supervisor_tests.rs`
- `apps/desktop/src-tauri/src/process/log_redact.rs`
- `apps/desktop/src-tauri/src/process/log_redact_tests.rs`

Implementation notes:
- D-02 and D-03 require installed-path lookup, no dev PATH augmentation, and no source-tree fallback for packaged sessions.
- D-14, D-15, and D-16 require rotation after redaction, complete-line preservation, and uninterrupted UI event emission.
- Preserve Phase 07.4 safety: packaged lifecycle actions remain supervisor-owned only; no PID/port/process-name kill heuristics.
</context>

## Artifacts This Plan Produces

- Pure or near-pure helpers for packaged config lookup and packaged sidecar executable lookup
- Rust runtime branch that uses installed bundle/resource context in packaged mode
- Log rotation helper(s) and tests for threshold, retention, line preservation, and failure degradation

<tasks>
<task type="auto" id="T1" tdd="true">
  <name>Write failing tests for packaged config lookup and sidecar path resolution</name>
  <files>
    apps/desktop/src-tauri/src/process/config_tests.rs
    apps/desktop/src-tauri/src/process/supervisor_tests.rs
    apps/desktop/src-tauri/src/lib.rs
    apps/desktop/src-tauri/src/process/config.rs
    apps/desktop/src-tauri/src/process/supervisor.rs
  </files>
  <read_first>
    1. `.planning/phases/10-packaging/10-CONTEXT.md` decisions D-02, D-03, D-07, D-08, D-09.
    2. `.planning/phases/10-packaging/10-RESEARCH.md` sections "Architecture Patterns", "Codebase Findings", and "Validation Architecture".
    3. `apps/desktop/src-tauri/src/lib.rs`.
    4. `apps/desktop/src-tauri/src/process/config_tests.rs`.
    5. `apps/desktop/src-tauri/src/process/supervisor_tests.rs`.
  </read_first>
  <action>
    Add failing Rust tests that lock the packaged-runtime lookup contract: packaged sessions must load the bundled packaged config instead of `CARGO_MANIFEST_DIR/processes.json`, must resolve `paladin-agent-sidecar` and `paladin-server-sidecar` from the installed app resource/sidecar location per D-02, and must not call dev PATH augmentation in packaged mode per D-03. Include regression assertions that packaged validation still rejects `uv`, `go`, and repo-relative `cwd` per D-09.
  </action>
  <verify>
    <automated>cd apps/desktop/src-tauri && cargo test process::config --lib</automated>
    <automated>cd apps/desktop/src-tauri && cargo test process::supervisor --lib</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before runtime helper implementation because packaged lookup still depends on source-tree assumptions or missing path translation.
    - The contract distinguishes config-location resolution from sidecar-executable resolution.
    - Existing packaged validation tests remain in scope and explicit.
  </acceptance_criteria>
  <done>Packaged lookup behavior is captured in failing Rust tests before implementation.</done>
</task>

<task type="auto" id="T2" tdd="true">
  <name>Implement packaged config lookup and sidecar path translation</name>
  <files>
    apps/desktop/src-tauri/src/lib.rs
    apps/desktop/src-tauri/src/process/config.rs
    apps/desktop/src-tauri/src/process/config_tests.rs
    apps/desktop/src-tauri/src/process/supervisor.rs
    apps/desktop/src-tauri/src/process/supervisor_tests.rs
    apps/desktop/src-tauri/src/process/mod.rs
  </files>
  <read_first>
    1. Results from T1.
    2. `.planning/phases/10-packaging/10-PATTERNS.md` mappings for `lib.rs`, `config.rs`, and `supervisor.rs`.
    3. Phase 07.4 context decisions D-01 through D-03 and D-14 through D-16.
  </read_first>
  <action>
    Implement the runtime adapter implied by D-02/D-03/D-08/D-09: resolve packaged config from bundle resources, resolve logical sidecar names to installed absolute paths before `Command::new`, keep `cmd: string[]` unchanged, and ensure packaged mode never augments PATH or depends on repo-relative `cwd`. Surface lookup or validation failures through the existing diagnostic fallback path rather than panicking.
  </action>
  <verify>
    <automated>cd apps/desktop/src-tauri && cargo test process::config --lib</automated>
    <automated>cd apps/desktop/src-tauri && cargo test process::supervisor --lib</automated>
  </verify>
  <acceptance_criteria>
    - Packaged runtime uses bundle-aware lookup helpers rather than source-tree constants.
    - `spawn_child_to_slot` receives an installed absolute executable path in packaged mode while preserving `cmd[1..]`.
    - Packaged mode continues rejecting dev commands and repo-relative cwd values.
    - External-process safety from Phase 07.4 is preserved.
  </acceptance_criteria>
  <done>Rust packaged runtime can load bundled config and spawn bundled sidecars without dev-tool dependencies.</done>
</task>

<task type="auto" id="T3" tdd="true">
  <name>Write failing tests for redacted bounded log rotation</name>
  <files>
    apps/desktop/src-tauri/src/process/supervisor_tests.rs
    apps/desktop/src-tauri/src/process/log_redact_tests.rs
    apps/desktop/src-tauri/src/process/supervisor.rs
    apps/desktop/src-tauri/src/process/log_redact.rs
  </files>
  <read_first>
    1. `.planning/phases/10-packaging/10-CONTEXT.md` decisions D-14, D-15, D-16.
    2. `.planning/phases/10-packaging/10-SPEC.md` Requirement 5, Edge Coverage, and Prohibitions.
    3. `.planning/phases/10-packaging/10-RESEARCH.md` sections "Common Pitfalls", "Security Considerations", and "Validation Architecture".
    4. `apps/desktop/src-tauri/src/process/log_redact.rs`.
    5. `apps/desktop/src-tauri/src/process/supervisor.rs` around `capture_lines`.
  </read_first>
  <action>
    Add failing Rust tests that define the bounded-log contract required by D-14/D-15/D-16: rotation happens after line-level redaction, the retention window is exactly 10 MB x 5 files per service, complete lines are preserved, `process-log` emission continues during rotation, and file-rotation failure degrades only disk persistence while UI streaming continues.
  </action>
  <verify>
    <automated>cd apps/desktop/src-tauri && cargo test process:: --lib log -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because no bounded rotation exists yet.
    - At least one test proves secrets are redacted before rotated bytes are written.
    - At least one test proves complete-line preservation across a threshold boundary.
    - At least one test proves UI event flow survives file-write or rotate failure.
  </acceptance_criteria>
  <done>Rotation behavior is precisely locked by failing tests before code changes.</done>
</task>

<task type="auto" id="T4" tdd="true">
  <name>Implement bounded log rotation in the supervisor persistence path</name>
  <files>
    apps/desktop/src-tauri/src/process/supervisor.rs
    apps/desktop/src-tauri/src/process/supervisor_tests.rs
    apps/desktop/src-tauri/src/process/log_redact.rs
    apps/desktop/src-tauri/src/process/log_redact_tests.rs
    apps/desktop/src-tauri/src/process/mod.rs
  </files>
  <read_first>
    1. Results from T3.
    2. `.planning/phases/10-packaging/10-PATTERNS.md` mappings for `supervisor.rs` and `log_redact.rs`.
    3. `.planning/phases/07.3-sidecar-process-management/07.3-CONTEXT.md` D-04 and logging patterns.
  </read_first>
  <action>
    Implement the 10 MB x 5 retained-file policy inside or adjacent to `capture_lines` as required by D-14/D-15/D-16: redact each full line first, emit to the UI stream, append to the current service log, rotate only at line boundaries, retain at most five files, and preserve the existing graceful-degradation path when disk logging fails. Keep ownership and process-control code unchanged except where tests prove helper extraction is necessary.
  </action>
  <verify>
    <automated>cd apps/desktop/src-tauri && cargo test process:: --lib</automated>
  </verify>
  <acceptance_criteria>
    - The bounded-log tests pass.
    - No rotated file contains raw secrets or truncated complete log lines.
    - `process-log` streaming semantics remain uninterrupted during rotation and rotation failure.
    - No code path introduces shell invocation or external-process killing heuristics.
  </acceptance_criteria>
  <done>Packaged log persistence is redacted, bounded, line-safe, and UI-safe.</done>
</task>
</tasks>

<threat_model>
- ASVS level: 1
- Block on severity: high
- Threat: installed app resolves config or binaries from the repo tree, causing hidden dev dependency or path confusion. Mitigation: D-02/D-03/D-08/D-09 require bundle-aware lookup helpers and packaged-path tests.
- Threat: raw secrets leak into rotated logs or diagnostics. Mitigation: D-15 enforces redaction-before-persist and tests assert it.
- Threat: log rotation drops or splits lines, obscuring operational evidence. Mitigation: D-14/D-16 require line-boundary rotation and uninterrupted event emission.
- Threat: packaged runtime regresss external-process safety by reintroducing port/PID kill logic. Mitigation: preserve Phase 07.4 supervisor-owned-handle-only behavior and regression tests.
</threat_model>

<verification>
Run after each task:
- `cd apps/desktop/src-tauri && cargo test process::config --lib`
- `cd apps/desktop/src-tauri && cargo test process::supervisor --lib`

Run before marking this plan complete:
- `cd apps/desktop/src-tauri && cargo test process:: --lib`
</verification>

<success_criteria>
- Packaged config and sidecar lookup are bundle-aware and dev-tool-free.
- Bounded log rotation is implemented with exact retention, redaction, line preservation, and graceful degradation.
- No release-honesty or UAT claims are made in this plan; it delivers runtime capability only.
</success_criteria>

## Source Coverage Audit

- GOAL covered: packaged startup behavior, bundled sidecar runtime, bounded logs, and non-blocking log stream behavior.
- REQ covered: PKG-01 and PKG-02 runtime prerequisites; PKG-03 log and troubleshooting foundations.
- RESEARCH covered: runtime adapter recommendation, separation of config vs executable lookup, rotate-on-disk-not-event-path guidance, security considerations, and validation architecture.
- CONTEXT covered: D-02, D-03, D-07, D-08, D-09, D-14, D-15, D-16 directly; Phase 07.4 external-ownership protections are preserved by design.
- SPEC covered: Requirement 3 packaged validation half, Requirement 5 in full, and the secret-exposure / external-process prohibitions.
- Unplanned items in this plan: installer UAT execution, user docs, and frontend copy alignment are handled by 10-04 and 10-05.

<output>
Rust packaged-runtime lookup and log rotation only. Do not close the phase or mark any platform release-ready in this plan.
</output>
