---
phase: "10"
plan: "10-04"
type: "execute"
wave: 4
depends_on: ["10-03"]
files_modified:
  - "apps/desktop/src/stores/process.ts"
  - "apps/desktop/src/components/StartupMask.tsx"
  - "apps/desktop/src/components/StatusBar/ProcessLight.tsx"
  - "apps/desktop/src/components/panel/LogsPanel.tsx"
autonomous: true
requirements: ["PKG-01", "PKG-02", "PKG-03"]
must_haves:
  truths:
    - "Installed macOS builds must launch without pnpm, cargo, uv, go run, or a repo checkout."
    - "Installed Windows builds must launch without pnpm, cargo, uv, go run, or a repo checkout."
    - "Packaged sidecar binaries must exist as paladin-agent-sidecar and paladin-server-sidecar, and packaged validation must reject uv, go, and repo-relative cwd."
    - "Installed-app UAT must record Agent liveness, Go liveness, Go readiness with PostgreSQL/Redis available, and degraded but non-blocking Go behavior when dependencies are unavailable."
    - "Packaged logs must remain bounded at 10 MB x 5 retained files per service."
    - "Rotation must preserve complete log lines and keep redaction active before bytes are persisted."
    - "Rotation must not interrupt the live Logs panel stream."
    - "User-facing documentation must cover install, first launch, readiness expectations, log locations, common failures, and UAT status."
    - "Linux is non-blocking for this phase; macOS and Windows are blocking targets."
    - "No ORM or schema push work is required because this phase does not modify schema files."
  prohibitions:
    - "MUST NOT mark a dev-only or failed-UAT package as release-ready; any platform with missing or failed UAT must be explicitly marked not release-ready."
    - "MUST NOT expose raw secrets, tokens, or environment values in packaged logs, UI diagnostics, or documentation examples."
    - "MUST NOT stop, restart, or shut down user-managed external processes by matching ports or process names; lifecycle actions may only target supervisor-owned child handles."
  artifacts:
    - "Packaged-mode diagnostic copy aligned with UI-SPEC for StartupMask and ProcessLight."
    - "LogsPanel behavior that treats rotation as invisible during healthy streaming."
    - "Focused frontend/store tests or equivalent compile-time checks for packaged diagnostic semantics."
  key_links:
    - ".planning/phases/10-packaging/10-UI-SPEC.md"
    - ".planning/phases/10-packaging/10-SPEC.md"
    - ".planning/phases/10-packaging/10-CONTEXT.md"
    - ".planning/phases/10-packaging/10-RESEARCH.md"
    - ".planning/phases/07.4-sidecar-runtime-mode/07.4-CONTEXT.md"
---

# 10-04 Plan: Packaged Diagnostics And UI Contract Alignment

<objective>
把 packaged startup、Go degraded、log rotation、ownership safety 的既有 UI 表达对齐到 Phase 10 UI-SPEC：Agent 仍是 blocking service，Go degraded 非阻塞，StartupMask/ProcessLight/LogsPanel 文案和交互必须准确、简洁、可执行，并避免泄露源码路径、shell 细节或 secret 值。
</objective>

<execution_context>
This plan is UI-contract-only: extend existing surfaces without creating new product UI. TDD applies to store/component behavior that can be tested cheaply; copy/config glue may use standard task execution. Keep the existing compact shadcn/lucide diagnostic pattern.
</execution_context>

<context>
Read these first:
- `.planning/phases/10-packaging/10-UI-SPEC.md`
- `.planning/phases/10-packaging/10-SPEC.md`
- `.planning/phases/10-packaging/10-CONTEXT.md`
- `.planning/phases/10-packaging/10-RESEARCH.md`
- `.planning/phases/07.4-sidecar-runtime-mode/07.4-CONTEXT.md`
- `apps/desktop/src/stores/process.ts`
- `apps/desktop/src/components/StartupMask.tsx`
- `apps/desktop/src/components/StatusBar/ProcessLight.tsx`
- `apps/desktop/src/components/panel/LogsPanel.tsx`

Implementation notes:
- Reuse existing surfaces only; do not create a new settings page, diagnostic center, or hero layout.
- The packaged UI must never instruct users to run `uv`, `go`, `pnpm`, or edit dev-only source paths.
- Rotation is operational behavior, not a user-facing event banner.
</context>

## Artifacts This Plan Produces

- Packaged diagnostic copy updates in `StartupMask`
- Ownership and degraded-state labeling updates in `ProcessLight`
- LogsPanel behavior/copy that treats rotation as invisible during healthy streams
- Focused tests or equivalent behavior checks for the non-blocking Go rule and disabled external controls

<tasks>
<task type="auto" id="T1" tdd="true">
  <name>Write failing behavior checks for packaged diagnostic semantics</name>
  <files>
    apps/desktop/src/stores/process.ts
    apps/desktop/src/components/StartupMask.tsx
    apps/desktop/src/components/StatusBar/ProcessLight.tsx
    apps/desktop/src/components/panel/LogsPanel.tsx
  </files>
  <read_first>
    1. `.planning/phases/10-packaging/10-UI-SPEC.md` sections "Interaction Contract", "Copywriting Contract", and "Logs Rotation UX Contract".
    2. `.planning/phases/10-packaging/10-CONTEXT.md` decisions D-10 through D-16.
    3. `.planning/phases/07.4-sidecar-runtime-mode/07.4-CONTEXT.md` decisions D-09 through D-15.
  </read_first>
  <action>
    Add failing frontend/store checks that lock the packaged semantics required by the UI-SPEC and D-10 through D-16: Agent blocking states drive `StartupMask`, Go degraded never blocks the workspace, external owner controls remain disabled with explanatory copy, packaged config/executable/cwd failures use installed-app language rather than dev-source instructions, and log rotation does not surface as a normal-user banner.
  </action>
  <verify>
    <automated>pnpm --filter @paladin/desktop test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - The checks fail before UI updates because current copy or behavior does not fully match Phase 10 packaged semantics.
    - At least one check proves the Go degraded path is explicitly non-blocking.
    - At least one check proves external controls stay disabled and explanatory.
  </acceptance_criteria>
  <done>Packaged UI contract is encoded in failing behavior checks before copy changes begin.</done>
</task>

<task type="auto" id="T2" tdd="false">
  <name>Align StartupMask packaged diagnostics with installed-app terminology</name>
  <files>
    apps/desktop/src/components/StartupMask.tsx
    apps/desktop/src/stores/process.ts
  </files>
  <read_first>
    1. `.planning/phases/10-packaging/10-UI-SPEC.md` sections "Packaged Startup Diagnostic Tone" and "StartupMask Copy Hierarchy".
    2. `.planning/phases/10-packaging/10-CONTEXT.md` decisions D-03, D-09, D-10, D-13.
    3. Packaged runtime failure shapes from 10-03 outputs.
  </read_first>
  <action>
    Update `StartupMask` and any supporting process-store mapping so packaged config invalid, executable missing, packaged cwd invalid, port conflict, startup-window exit, and generic stopped states all use the installed-app copy locked by the UI-SPEC. Reference D-03/D-09/D-10/D-13 explicitly in the implementation notes so the UI stays truthful about packaged ownership and release status without leaking source-tree paths or shell details.
  </action>
  <verify>
    <automated>pnpm --filter @paladin/desktop build</automated>
  </verify>
  <acceptance_criteria>
    - StartupMask uses installed-app vocabulary for packaged failures.
    - No packaged diagnostic tells the user to run dev commands or edit source-only files.
    - Agent remains the only blocking startup surface.
  </acceptance_criteria>
  <done>StartupMask copy now matches the packaged diagnostic contract.</done>
</task>

<task type="auto" id="T3" tdd="false">
  <name>Align ProcessLight and LogsPanel with packaged ownership and rotation semantics</name>
  <files>
    apps/desktop/src/components/StatusBar/ProcessLight.tsx
    apps/desktop/src/components/panel/LogsPanel.tsx
    apps/desktop/src/stores/process.ts
  </files>
  <read_first>
    1. `.planning/phases/10-packaging/10-UI-SPEC.md` sections "ProcessLight Actions", "Go Degraded Copy", and "LogsPanel Copy".
    2. `.planning/phases/10-packaging/10-CONTEXT.md` decisions D-11 through D-16.
    3. `.planning/phases/07.4-sidecar-runtime-mode/07.4-CONTEXT.md` decisions D-09, D-10, D-14, D-15.
  </read_first>
  <action>
    Update `ProcessLight`, `LogsPanel`, and the process store to show compact ownership/state text that stays truthful in packaged sessions, keeps external control disabling explanations, explicitly marks Go degraded as non-blocking, and keeps log rotation invisible unless the stream actually fails. Reference D-11/D-12/D-14/D-15/D-16 in the task summary so ownership safety and release honesty remain visible.
  </action>
  <verify>
    <automated>pnpm --filter @paladin/desktop test -- --run</automated>
    <automated>pnpm --filter @paladin/desktop build</automated>
  </verify>
  <acceptance_criteria>
    - ProcessLight text shows ownership and health/state compactly and truthfully.
    - External controls remain disabled with explanation; `查看日志` remains available.
    - LogsPanel does not treat successful rotation as a user-visible exception.
  </acceptance_criteria>
  <done>Existing status/log surfaces match the packaged ownership and rotation contract.</done>
</task>
</tasks>

<threat_model>
- ASVS level: 1
- Block on severity: high
- Threat: UI tells installed-app users to run dev-only commands or inspect source-only files. Mitigation: packaged copy is rewritten to installed-app terminology only.
- Threat: Go degraded is misrepresented as a global startup failure. Mitigation: UI-SPEC forces Agent-only blocking and explicit non-blocking Go copy.
- Threat: external-process safety becomes hidden or misleading in the UI. Mitigation: disabled controls and explanatory copy remain visible.
- Threat: logs rotation leaks operational internals or appears as noise to end users. Mitigation: rotation is invisible unless actual stream failure occurs.
</threat_model>

<verification>
Run after each task:
- `pnpm --filter @paladin/desktop test -- --run`
- `pnpm --filter @paladin/desktop build`

Run before marking this plan complete:
- `pnpm --filter @paladin/desktop test -- --run && pnpm --filter @paladin/desktop build`
</verification>

<success_criteria>
- StartupMask, ProcessLight, and LogsPanel match the approved packaged UI contract.
- Go degraded is non-blocking and clearly explained.
- No new UI surfaces are introduced and no release-ready claim is made.
</success_criteria>

## Source Coverage Audit

- GOAL covered: packaged startup diagnostics, non-blocking Go behavior, and log-stream UX continuity.
- REQ covered: PKG-01/PKG-02 user-visible installed-app semantics and PKG-03 troubleshooting/documentation alignment groundwork.
- RESEARCH covered: frontend diagnostic pattern, release honesty, security considerations around copy, and installed-app behavior emphasis.
- CONTEXT covered: D-10 through D-16 directly; inherits Phase 07.4 ownership-safety UI from D-09 through D-15.
- SPEC covered: Requirement 4 degraded-path visibility, Requirement 5 Logs panel continuity, Requirement 6 user-facing troubleshooting truthfulness, and both secret / release-honesty prohibitions.
- Unplanned items in this plan: installer UAT execution and docs publication are handled by 10-05.

<output>
UI contract alignment only. Do not modify Rust runtime logic, installer scripts, or phase closure artifacts in this plan.
</output>
