---
phase: 10-packaging
plan: "04"
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src-tauri/src/process/log_rotate.rs
  - apps/desktop/src-tauri/src/process/log_rotate_tests.rs
  - apps/desktop/src-tauri/src/process/mod.rs
  - apps/desktop/src-tauri/src/process/supervisor.rs
  - apps/desktop/src-tauri/src/process/supervisor_tests.rs
autonomous: true
requirements: [PKG-03]
must_haves:
  truths: ["R1/Edge: DMG 安装无开发依赖。", "R2/Edge: Windows build/UAT/readiness 分离。", "R3/Edge: 平台命名可解析。", "R4/Edge: 环境启动快照。", "R5/Edge: Go degraded 非阻塞。", "R6/Edge: 恰达/超过 10 MB 时在完整行边界轮转，保留最多 5 文件，写盘失败不影响 UI。", "R7/Edge: 文档平台状态分列。"]
  prohibitions: ["MUST NOT 错标 release-ready。", "MUST NOT 在轮转落盘泄露 secret。", "MUST NOT 在 packaged 诊断给开发命令。", "MUST NOT 终止非持有进程。"]
  artifacts:
    - path: apps/desktop/src-tauri/src/process/log_rotate.rs
      provides: "有界完整行日志 writer"
      exports: [RotatingLineWriter, RotationPolicy]
    - path: apps/desktop/src-tauri/src/process/log_rotate_tests.rs
      provides: "10 MB x 5、顺序、失败连续性测试"
  key_links:
    - from: apps/desktop/src-tauri/src/process/supervisor.rs
      to: apps/desktop/src-tauri/src/process/log_rotate.rs
      via: "redact→emit→persist/rotate"
      pattern: "RotatingLineWriter"
---
<objective>以 TDD 实现 Rust supervisor 内 10 MB × 5 的完整行轮转，并证明 redaction 先于落盘、磁盘失败不影响 process-log 事件。（D-24、D-25）</objective>
<execution_context>@/Users/kdocs/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/10-packaging/10-SPEC.md
@.planning/phases/10-packaging/10-CONTEXT.md
@apps/desktop/src-tauri/src/process/supervisor.rs
@apps/desktop/src-tauri/src/process/log_redact.rs</context>
## Artifacts This Plan Produces
- `RotationPolicy`, `RotatingLineWriter::write_line` and focused deterministic tests.
<tasks>
<task type="auto" id="T1" tdd="true"><name>Task 1: RED→GREEN bounded line writer</name><files>apps/desktop/src-tauri/src/process/log_rotate_tests.rs, apps/desktop/src-tauri/src/process/log_rotate.rs, apps/desktop/src-tauri/src/process/mod.rs</files><read_first>SPEC R6；CONTEXT D-24；capture_lines ordering。</read_first><behavior>小阈值 fixture 模拟恰达/超过 10MB；完整行不拆；active+4 archives；旧文件淘汰；顺序可恢复；rename/write failure 返回错误且 writer 可继续尝试。</behavior><action>先写失败测试，再实现 injectable policy；生产常量 10*1024*1024 与 5；轮转只发生于写入一整条已脱敏行之前。</action><verify><automated>cd apps/desktop/src-tauri && cargo test process::log_rotate --lib</automated></verify><acceptance_criteria>边界、文件数、行完整性和失败路径均为行为断言。</acceptance_criteria><done>rotation helper tests green。</done></task>
<task type="auto" id="T2" tdd="true"><name>Task 2: RED→GREEN capture_lines 接线与事件连续性</name><files>apps/desktop/src-tauri/src/process/supervisor_tests.rs, apps/desktop/src-tauri/src/process/supervisor.rs</files><read_first>T1 API；CONTEXT D-25；redact_log_line。</read_first><behavior>输入 secret 行时 event 与 disk 均脱敏；rotate/write failure 后下一行仍 emit；stderr tail 保持；UI 不收到 rotation banner/filename。</behavior><action>先加失败 integration-style tests；保持 `redact -> emit -> stderr_tail -> persist/rotate`，磁盘错误只更新低优先诊断并继续读流。</action><verify><automated>cd apps/desktop/src-tauri && cargo test process:: --lib log -- --nocapture</automated></verify><acceptance_criteria>故障注入证明事件计数/内容连续，disk bytes 无敏感值。</acceptance_criteria><done>capture_lines 满足 D-24/D-25。</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| raw sidecar output→disk/UI | 不可信日志可能含 secret 或超大行 |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-04-01 | Information Disclosure | log persistence | high | mitigate | line redaction before writer |
| T-10-04-02 | Denial of Service | unbounded logs | high | mitigate | 10 MB x 5 retention |
| T-10-04-03 | Denial of Service | disk failure | medium | mitigate | emit path independent from persistence |
</threat_model>
<verification>focused rotation/log tests plus full cargo test。</verification>
<success_criteria>bounded logs、完整行、脱敏落盘和 UI 连续性自动证明。</success_criteria>
<output>Create `.planning/phases/10-packaging/10-04-SUMMARY.md` with RED/GREEN/REFACTOR evidence.</output>
