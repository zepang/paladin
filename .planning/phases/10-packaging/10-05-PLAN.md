---
phase: 10-packaging
plan: "05"
type: execute
wave: 4
depends_on: ["10-01", "10-02", "10-03"]
files_modified:
  - .github/workflows/packaging-windows.yml
  - apps/desktop/scripts/build-manifest.mjs
  - apps/desktop/scripts/build-manifest.test.mjs
  - docs/packaging.md
  - README.md
  - .planning/phases/10-packaging/evidence/windows-build.json
autonomous: false
requirements: [PKG-02, PKG-03]
must_haves:
  truths: ["R1/Edge: macOS installed UAT 为阻塞事实。", "R2/Edge: MSI build、Windows UAT、release-ready 分离。", "R3/Edge: x64 Windows 文件名/后缀正确。", "R4/Edge: 启动环境不热加载。", "R5/Edge: dependency degraded 非阻塞。", "R6/Edge: bounded logs。", "R7/Edge: docs 逐平台三状态。"]
  prohibitions: ["MUST NOT 以 Windows native CI build 声称 release-ready。", "MUST NOT 在 manifest/docs 写 secret。", "MUST NOT 给 packaged 用户开发命令。", "MUST NOT 描述按端口/进程名 kill 外部服务。"]
  artifacts:
    - path: .github/workflows/packaging-windows.yml
      provides: "windows-latest x64 MSI buildability proof"
    - path: apps/desktop/scripts/build-manifest.mjs
      provides: "non-secret SHA-256 build manifest"
      exports: [createBuildManifest]
    - path: docs/packaging.md
      provides: "安装、环境、诊断与逐平台状态文档"
    - path: README.md
      provides: "仓库 Packaging/安装入口并链接详细文档"
    - path: .planning/phases/10-packaging/evidence/windows-build.json
      provides: "native workflow run URL/commit/artifact identity evidence"
  key_links:
    - from: .github/workflows/packaging-windows.yml
      to: package.json
      via: "same pnpm release entrypoint"
      pattern: "pnpm release"
---
<objective>在 native Windows CI 证明 x86_64 MSI 可构建并生成审计 manifest，同时建立诚实的安装/排障文档。（D-14、D-15、D-16、D-17、D-18）</objective>
<execution_context>@/Users/kdocs/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/10-packaging/10-SPEC.md
@.planning/phases/10-packaging/10-CONTEXT.md
@.planning/phases/10-packaging/10-UI-SPEC.md
@package.json</context>
## Artifacts This Plan Produces
- Windows workflow；`createBuildManifest`；`docs/packaging.md` platform status table.
<tasks>
<task type="auto" id="T1" tdd="true"><name>Task 1: RED→GREEN non-secret manifest</name><files>apps/desktop/scripts/build-manifest.test.mjs, apps/desktop/scripts/build-manifest.mjs</files><read_first>CONTEXT D-16/D-17/D-18。</read_first><behavior>manifest 含 SHA、runner OS、x86_64 arch、sidecar/MSI filenames、sizes、SHA-256；拒绝目录/缺文件；输出不含 env。</behavior><action>先写失败测试，再实现只读取明确 artifact paths 的 manifest generator。</action><verify><automated>node --test apps/desktop/scripts/build-manifest.test.mjs</automated></verify><acceptance_criteria>字段齐全且 deterministic；不上传整个 build tree。</acceptance_criteria><done>manifest tests green。</done></task>
<task type="auto" id="T2"><name>Task 2: native Windows workflow 与统一安装文档入口</name><files>.github/workflows/packaging-windows.yml, docs/packaging.md, README.md</files><read_first>CONTEXT D-05、D-14..D-18；SPEC PKG-03/R2/R7；UI-SPEC copy contract；10-06 wrapper contract。</read_first><action>workflow 使用 windows-latest、manual dispatch 与 packaging-path PR filter，构建真实 x64 MSI。README 新增 Packaging/安装入口并链接 `docs/packaging.md`。详细文档明确首次运行和 macOS UAT 都使用 `scripts/launch-paladin-macos.sh`，默认 `/Applications/Paladin.app`，自定义位置使用 `--app &lt;path&gt;`；Windows 明确 installed UAT deferred 且 non-release-ready。覆盖环境变量名称、readiness、日志路径与故障文案，不复制 secret 值。</action><verify><automated>node -e "const fs=require('fs');const y=fs.readFileSync('.github/workflows/packaging-windows.yml','utf8');const d=fs.readFileSync('docs/packaging.md','utf8');const r=fs.readFileSync('README.md','utf8');for(const x of ['windows-latest','workflow_dispatch','x86_64-pc-windows-msvc'])if(!y.includes(x))process.exit(1);for(const x of ['scripts/launch-paladin-macos.sh','/Applications/Paladin.app','--app','首次运行','UAT','deferred','non-release-ready'])if(!d.includes(x))process.exit(1);if(!/docs\/packaging\.md/.test(r)||!/Packaging|安装/.test(r))process.exit(1)"</automated></verify><acceptance_criteria>README 可发现详细安装文档；first-run 与 UAT 使用同一 wrapper；Windows 状态诚实且三项关键用法可机器检查。</acceptance_criteria><done>PKG-03/D-05 文档入口和 native Windows workflow 可审计。</done></task>
<task type="checkpoint:human-verify" id="T3" gate="blocking"><name>Task 3: 触发并验证 native Windows MSI workflow</name><files>.planning/phases/10-packaging/evidence/windows-build.json</files><read_first>packaging-windows.yml；build manifest schema；CONTEXT D-14..D-18。</read_first><action>使用 GitHub CLI 对当前 commit 触发 workflow_dispatch，轮询到 completed；若 conclusion 非 success 则阻塞。下载精确命名的 artifact 到临时目录，只允许一个 MSI 和一个 manifest；校验 manifest commit SHA、windows runner OS、x86_64 target、两个 sidecar 与 MSI filename/size/SHA-256，并重新计算所有哈希。将 run URL、run id、commit、artifact id/name、MSI identity、manifest hash 写入 phase-local JSON；删除下载的 installer，不提交二进制。该证据状态只能写 built/buildability-only，Windows installed UAT 与 release-ready 必须为 false/deferred。</action><verify><automated>node -e "const e=require('./.planning/phases/10-packaging/evidence/windows-build.json');if(e.conclusion!=='success'||e.status!=='buildability-only'||e.release_ready!==false||!/^https:\/\/github.com\//.test(e.run_url)||!e.commit_sha||!e.artifact_id||!e.msi?.sha256||!e.manifest_sha256)process.exit(1)"</automated><human-check>确认 run URL 对应 windows-latest 当前 commit 的成功运行，artifact 仅含 MSI+manifest，且状态没有声称 installed UAT/release-ready。</human-check></verify><acceptance_criteria>native Windows run、精确 artifact 与 hashes 可独立审计；失败、取消、超时、缺 artifact/field 均阻塞。</acceptance_criteria><done>Windows x64 buildability 得到真实 CI 证据，但保持 non-release-ready。</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| CI runner→uploaded artifacts | 产物身份/完整性需可验证 |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-05-01 | Tampering | MSI upload | high | mitigate | SHA-256 manifest + exact paths |
| T-10-05-02 | Repudiation | release status | high | mitigate | build/UAT/release-ready 分列 |
| T-10-05-03 | Information Disclosure | CI manifest | high | mitigate | fixed non-secret schema |
</threat_model>
<verification>manifest tests、workflow static gate、docs section gate；真实 workflow 由 PR/manual run 证明。</verification>
<success_criteria>Windows x64 原生链可执行，产物可审计，但未被误称 release-ready。</success_criteria>
<output>Create `.planning/phases/10-packaging/10-05-SUMMARY.md` when done.</output>
