---
phase: "10"
plan: "10-05"
type: "execute"
wave: 5
depends_on: ["10-03", "10-04"]
files_modified:
  - ".planning/phases/10-packaging/10-VALIDATION.md"
  - ".planning/phases/10-packaging/10-UAT.md"
  - ".planning/phases/10-packaging/10-VERIFICATION.md"
  - ".planning/phases/10-packaging/10-05-SUMMARY.md"
  - ".planning/ROADMAP.md"
  - ".planning/STATE.md"
  - "README.md"
  - "docs/**"
autonomous: true
requirements: ["PKG-01", "PKG-02", "PKG-03"]
must_haves:
  truths:
    - "Installed macOS builds must launch without pnpm, cargo, uv, go run, or a repo checkout."
    - "Installed Windows builds must launch without pnpm, cargo, uv, go run, or a repo checkout."
    - "Packaged sidecar binaries must exist as paladin-agent-sidecar and paladin-server-sidecar, and packaged validation must reject uv, go, and repo-relative cwd."
    - "Installed-app UAT must record Agent liveness, Go liveness, Go readiness with PostgreSQL/Redis available, and degraded but non-blocking Go behavior when dependencies are unavailable."
    - "Packaged logs must remain bounded at 10 MB x 5 retained files per service."
    - "Rotation must preserve complete log lines and keep redaction active before bytes are persisted."
    - "Rotation must not interrupt the live Logs panel stream."
    - "User-facing documentation must cover install, first launch, readiness expectations, log locations, common failures, and UAT status."
    - "Linux is non-blocking for this phase; macOS and Windows are blocking targets."
    - "No ORM or schema push work is required because this phase does not modify schema files."
  prohibitions:
    - "MUST NOT mark a dev-only or failed-UAT package as release-ready; any platform with missing or failed UAT must be explicitly marked not release-ready."
    - "MUST NOT expose raw secrets, tokens, or environment values in packaged logs, UI diagnostics, or documentation examples."
    - "MUST NOT stop, restart, or shut down user-managed external processes by matching ports or process names; lifecycle actions may only target supervisor-owned child handles."
  artifacts:
    - "Manual installed-app UAT records for macOS and Windows, including PG/Redis available and unavailable paths."
    - "Validation and verification artifacts with exact automated commands and release-honesty audit."
    - "User-facing install and first-run documentation that explicitly marks release-ready vs not release-ready status."
    - "Scoped ROADMAP/STATE updates only after evidence gates pass."
  key_links:
    - ".planning/phases/10-packaging/10-SPEC.md"
    - ".planning/phases/10-packaging/10-CONTEXT.md"
    - ".planning/phases/10-packaging/10-VALIDATION.md"
    - ".planning/phases/10-packaging/10-UI-SPEC.md"
    - ".planning/STATE.md"
    - ".planning/ROADMAP.md"
---

# 10-05 Plan: Installed-App UAT, Release Honesty, And User Documentation

<objective>
完成 Phase 10 的证据闭环：在 macOS 与 Windows 上执行真实 installed-app UAT，记录 PG/Redis 可用与不可用两条路径，更新 README/docs/phase artifacts，严格区分“installer 能构建”和“平台已通过 installed-app UAT”，并且只有在证据完整时才更新 ROADMAP/STATE。
</objective>

<execution_context>
This is the phase-closure plan. It is documentation- and evidence-heavy, but still requires automated gates before and after manual UAT. Do not expand scope into signing, notarization, auto-update, or Linux blocking validation.
</execution_context>

<context>
Read these first:
- `.planning/phases/10-packaging/10-SPEC.md`
- `.planning/phases/10-packaging/10-CONTEXT.md`
- `.planning/phases/10-packaging/10-VALIDATION.md`
- `.planning/phases/10-packaging/10-UI-SPEC.md`
- `.planning/STATE.md`
- `.planning/ROADMAP.md`
- `README.md`
- Existing docs covering supervisor/runtime startup chain

Implementation notes:
- D-10 through D-13 define the installed-app UAT and release-honesty boundary.
- Documentation must include macOS install, Windows install, first launch, PG/Redis readiness expectations, log locations, sidecar startup failure, packaged config failure, and UAT status.
- If any source item or platform evidence is missing, write an explicit audit gap and keep that platform not release-ready.
</context>

## Artifacts This Plan Produces

- Updated `10-VALIDATION.md` with exact automated commands and evidence rows
- Locked packaged env/config/assets UAT checklist covering Agent API key, Go DB/Redis/JWT inputs, and installed-app failure wording
- `10-UAT.md` with installed macOS and Windows runs, plus PG/Redis available/unavailable scenarios
- `10-VERIFICATION.md` mapping GOAL/REQ/RESEARCH/CONTEXT/SPEC items to evidence
- `10-05-SUMMARY.md`
- Updated `README.md` and/or docs pages with install, first-run, troubleshooting, log locations, and UAT status
- Scoped `ROADMAP.md` and `STATE.md` updates if and only if evidence gates pass

<tasks>
<task type="auto" id="T1" tdd="false">
  <name>Run final automated packaging and runtime gates before manual UAT</name>
  <files>
    .planning/phases/10-packaging/10-VALIDATION.md
    apps/desktop/src-tauri/src/process/**
    apps/desktop/src/**
  </files>
  <read_first>
    1. `.planning/phases/10-packaging/10-VALIDATION.md` Per-Task Verification Map and Wave 0 requirements.
    2. Outputs from 10-01 through 10-04.
  </read_first>
  <action>
    Run the final automated command set needed to support D-10 through D-13: release-sidecar build probes, runtime-contract verification, Rust process/config/log tests, desktop tests/build, and installer build requests. Record exact commands and results in `10-VALIDATION.md`, replacing the current `TBD` plan references with final plan/task IDs from this phase. When every automated validation row is green and every remaining manual row is explicitly marked manual gate pending or manual gate complete, update the validation sign-off checklist text to show `wave_0_complete: true` and later `nyquist_compliant: true` are the required closure edits.
  </action>
  <verify>
    <automated>pnpm --filter @paladin/desktop test -- --run</automated>
    <automated>cd apps/desktop/src-tauri && cargo test && cargo clippy --all-targets -- -D warnings</automated>
    <automated>pnpm --filter @paladin/desktop exec node apps/desktop/scripts/tauri-cli.mjs release-installers --platform macos --out-dir apps/desktop/src-tauri/target/release/bundle && find apps/desktop/src-tauri/target/release/bundle/dmg -name '*.dmg' -print -quit | grep -q .</automated>
    <automated>pnpm --filter @paladin/desktop exec node apps/desktop/scripts/tauri-cli.mjs release-installers --platform windows --out-dir apps/desktop/src-tauri/target/release/bundle && find apps/desktop/src-tauri/target/release/bundle/msi -name '*.msi' -print -quit | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - Validation rows reference concrete plan/task IDs instead of `TBD`.
    - Automated gates are green or any unrelated preexisting failure is explicitly scoped and evidenced.
    - Installer build commands, sidecar-build commands, and runtime-contract verification commands are captured exactly.
    - `10-VALIDATION.md` explicitly states when `wave_0_complete: true` must be flipped and what remains before `nyquist_compliant: true`.
  </acceptance_criteria>
  <done>Phase 10 validation artifact reflects final automated gates and concrete plan references.</done>
</task>

<task type="manual" id="T2" tdd="false">
  <name>Record installed-app env, config, and asset prerequisites as a pre-UAT manual gate</name>
  <files>
    .planning/phases/10-packaging/10-UAT.md
    .planning/phases/10-packaging/10-VALIDATION.md
  </files>
  <read_first>
    1. `10-02/T4` packaged runtime contract.
    2. `.planning/phases/10-packaging/10-RESEARCH.md` sections "Common Pitfalls" and Planner Recommendation 8.
    3. `.planning/phases/10-packaging/10-CONTEXT.md` decisions D-10 through D-13.
  </read_first>
  <action>
    Before installed-app UAT begins, write the exact manual preconditions into `10-UAT.md` and `10-VALIDATION.md`: where macOS and Windows testers obtain `DEEPSEEK_API_KEY`; where they obtain `PALADIN_DATABASE_URL`, `PALADIN_REDIS_URL`, and `PALADIN_JWT_SECRET`; how the installed app reads or receives those values; what packaged runtime assets/config files must be present for Agent `serve`; and what failure wording the UI/docs must show when any prerequisite is absent. Mark this task as a manual gate that must be complete on both blocking platforms before closure work can continue.
  </action>
  <verify>
    <manual>Confirm `10-UAT.md` contains explicit macOS and Windows prerequisite sections for Agent API key, Go DB/Redis/JWT env, packaged runtime assets/config presence, and failure wording expectations before any platform run is marked started.</manual>
  </verify>
  <acceptance_criteria>
    - The installed-app config/env source chain is documented before UAT execution starts.
    - Agent runtime assets/prompts/config expectations are explicitly documented for installed mode.
    - Missing prerequisites are described as blocking or degraded with installed-app wording, not dev instructions.
    - This task is explicitly labeled a manual gate and cannot be satisfied by file existence alone.
  </acceptance_criteria>
  <done>Installed-app preconditions are locked as a manual gate before any platform UAT evidence is accepted.</done>
</task>

<task type="manual" id="T3" tdd="false">
  <name>Execute installed-app UAT on macOS and Windows with both dependency states</name>
  <files>
    .planning/phases/10-packaging/10-UAT.md
  </files>
  <read_first>
    1. `.planning/phases/10-packaging/10-CONTEXT.md` decisions D-10, D-11, D-12, D-13.
    2. `.planning/phases/10-packaging/10-SPEC.md` Requirement 4 and Acceptance Criteria.
    3. `.planning/phases/10-packaging/10-VALIDATION.md` manual verification table.
    4. Prerequisite gate output from T2.
  </read_first>
  <action>
    Build installer artifacts, install the macOS app and Windows app, launch each installed app outside repo assumptions, and record two scenarios per platform: PostgreSQL/Redis available and PostgreSQL/Redis unavailable. For each run, capture Agent `/health`, Go `/healthz`, Go `/readyz`, the visible degraded/non-blocking behavior, installer filename, OS version, and any startup diagnostics. Reference D-10/D-11/D-12/D-13 in the UAT notes and mark any missing scenario or failed platform as not release-ready.
  </action>
  <verify>
    <manual>Confirm `10-UAT.md` contains four completed evidence sections: macOS+deps-available, macOS+deps-unavailable, Windows+deps-available, Windows+deps-unavailable; each section must include installer filename, OS version, Agent `/health`, Go `/healthz`, Go `/readyz`, visible UI state, and explicit release-ready/not-release-ready result.</manual>
  </verify>
  <acceptance_criteria>
    - macOS UAT records installed-app launch without dev commands or repo checkout.
    - Windows UAT records installed-app launch without dev commands or repo checkout.
    - Both dependency states are documented for both platforms.
    - Any missing or failed scenario is explicitly marked not release-ready.
  </acceptance_criteria>
  <done>Installed-app UAT evidence exists for both platforms and both dependency states, or explicit not-release-ready gaps are recorded.</done>
</task>

<task type="auto" id="T4" tdd="false">
  <name>Publish user-facing install, first-run, troubleshooting, and UAT-status documentation</name>
  <files>
    README.md
    docs/**
    .planning/phases/10-packaging/10-UAT.md
  </files>
  <read_first>
    1. `.planning/phases/10-packaging/10-SPEC.md` Requirement 6 and Prohibitions.
    2. `.planning/phases/10-packaging/10-UI-SPEC.md` packaged diagnostic tone.
    3. UAT evidence from T3.
  </read_first>
  <action>
    Update `README.md` and/or the relevant docs pages so users can verify installation and first-run expectations without source-code knowledge. Include macOS install, Windows install, first launch, PG/Redis readiness expectations, log locations, common sidecar startup failure, packaged config failure, and current UAT status. Reference D-11 through D-13 in the release-honesty language and remove or rewrite any example that would expose secrets, tokens, or raw environment values.
  </action>
  <verify>
    <automated>rg -n "macOS|Windows|first launch|readyz|log|sidecar|config|UAT|release-ready|not release-ready" README.md docs .planning/phases/10-packaging</automated>
  </verify>
  <acceptance_criteria>
    - Docs cover every user-facing section locked in Requirement 6.
    - Docs state whether each blocking platform is release-ready or not release-ready based on actual UAT evidence.
    - No doc example includes raw secrets or misleading dev-only instructions for installed users.
  </acceptance_criteria>
  <done>User-facing docs and troubleshooting are complete and evidence-backed.</done>
</task>

<task type="manual" id="T5" tdd="false">
  <name>Enforce dual-platform hard gate before source audit and phase closure</name>
  <files>
    .planning/phases/10-packaging/10-UAT.md
    .planning/phases/10-packaging/10-VALIDATION.md
  </files>
  <read_first>
    1. Completed evidence from T2 and T3.
    2. `.planning/phases/10-packaging/10-CONTEXT.md` D-10 through D-13.
  </read_first>
  <action>
    Before source audit or closure starts, verify that both blocking platforms have completed the prerequisite gate from T2 and the four evidence scenarios from T3. If any macOS or Windows section is missing, incomplete, or failed, stop here, mark the platform not release-ready, and do not proceed to closure updates. Record this outcome in `10-VALIDATION.md` as a hard gate result.
  </action>
  <verify>
    <manual>Confirm `10-VALIDATION.md` and `10-UAT.md` explicitly show macOS gate complete, Windows gate complete, macOS two-scenario UAT complete, and Windows two-scenario UAT complete before T6 begins.</manual>
  </verify>
  <acceptance_criteria>
    - macOS evidence completeness is explicitly checked before closure work.
    - Windows evidence completeness is explicitly checked before closure work.
    - Any single-platform gap blocks source audit, ROADMAP/STATE updates, and Nyquist closure.
  </acceptance_criteria>
  <done>Dual-platform installed-app evidence is enforced as a hard gate for closure.</done>
</task>

<task type="auto" id="T6" tdd="false">
  <name>Run source-coverage and prohibition audit, then update phase status if eligible</name>
  <files>
    .planning/phases/10-packaging/10-VERIFICATION.md
    .planning/phases/10-packaging/10-05-SUMMARY.md
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <read_first>
    1. GOAL in `10-SPEC.md`.
    2. PKG-01 through PKG-03 in `.planning/REQUIREMENTS.md`.
    3. `.planning/phases/10-packaging/10-RESEARCH.md`.
    4. `.planning/phases/10-packaging/10-CONTEXT.md` decisions D-01 through D-16.
    5. `10-SPEC.md` Requirements 1 through 6, Acceptance Criteria, Edge Coverage rows, and Prohibitions.
    6. Hard-gate result from T5.
  </read_first>
  <action>
    Create a verification matrix that maps every GOAL, every locked requirement (`PKG-01`, `PKG-02`, `PKG-03`), every research recommendation that constrains execution, every context decision `D-01` through `D-16`, every Requirement 1 through 6 acceptance criterion, every Edge Coverage row, and every prohibition row to a concrete plan, artifact, or test result using stable keys in the matrix. If any item lacks coverage, write `## ⚠ Source Audit: Unplanned Items Found` in `10-VERIFICATION.md` and stop before status updates. If all items are covered and the T5 hard gate passed, write `10-05-SUMMARY.md`, update `ROADMAP.md` and `STATE.md`, set `wave_0_complete: true` and `nyquist_compliant: true` in `10-VALIDATION.md`, and explicitly state which platforms are release-ready vs not release-ready based on D-13.
  </action>
  <verify>
    <automated>rg -n "D-01|D-02|D-03|D-04|D-05|D-06|D-07|D-08|D-09|D-10|D-11|D-12|D-13|D-14|D-15|D-16|PKG-01|PKG-02|PKG-03|Requirement 1|Requirement 2|Requirement 3|Requirement 4|Requirement 5|Requirement 6|release-ready|not release-ready|must-NOT|R1|R2|R3|R4|R5|R6" .planning/phases/10-packaging/10-VERIFICATION.md .planning/phases/10-packaging/10-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Every source family item is mapped or an explicit audit gap is written.
    - The matrix proves coverage for `D-01` through `D-16`, `PKG-01` through `PKG-03`, Requirements 1 through 6, every edge row, and every prohibition row.
    - ROADMAP/STATE are updated only after the audit is complete and evidence-backed.
    - `10-VALIDATION.md` frontmatter and sign-off checklist are updated only after the audit is complete and both blocking platforms passed or are truthfully marked not release-ready.
    - Release honesty is explicit per platform.
  </acceptance_criteria>
  <done>Phase closure artifacts and status updates are conditioned on complete evidence and source audit coverage.</done>
</task>
</tasks>

<threat_model>
- ASVS level: 1
- Block on severity: high
- Threat: installer artifacts are described as release-ready without installed-app proof. Mitigation: D-10 through D-13 require platform UAT and explicit release-honesty wording.
- Threat: docs or UAT records leak real credentials or environment values. Mitigation: scrub examples and record only redacted diagnostics.
- Threat: missing source items are silently dropped during closure. Mitigation: mandatory GOAL/REQ/RESEARCH/CONTEXT/SPEC audit with explicit warning marker.
- Threat: status files advance despite failed or partial UAT. Mitigation: `ROADMAP.md` and `STATE.md` update only after evidence gates pass.
</threat_model>

<verification>
Required final commands:
- `pnpm --filter @paladin/desktop test -- --run`
- `cd apps/desktop/src-tauri && cargo test && cargo clippy --all-targets -- -D warnings`
- `pnpm --filter @paladin/desktop exec node apps/desktop/scripts/tauri-cli.mjs release-installers --platform macos --out-dir apps/desktop/src-tauri/target/release/bundle && find apps/desktop/src-tauri/target/release/bundle/dmg -name '*.dmg' -print -quit | grep -q .`
- `pnpm --filter @paladin/desktop exec node apps/desktop/scripts/tauri-cli.mjs release-installers --platform windows --out-dir apps/desktop/src-tauri/target/release/bundle && find apps/desktop/src-tauri/target/release/bundle/msi -name '*.msi' -print -quit | grep -q .`
- `rg -n "macOS|Windows|UAT|release-ready|not release-ready" README.md docs .planning/phases/10-packaging`

Manual required verification:
- Complete T2 installed-app prerequisite gate on macOS and Windows
- Install and launch macOS `.dmg`
- Install and launch Windows `.msi`
- Record PG/Redis available and unavailable paths for each platform
- Do not start T6 closure work until T5 hard gate is marked complete
</verification>

<success_criteria>
- Manual and automated evidence exists for both blocking platforms, or missing evidence is explicitly marked not release-ready.
- User docs are complete, accurate, and installed-app oriented.
- Phase closure artifacts and status updates are evidence-backed and source-audited.
</success_criteria>

## Source Coverage Audit

- GOAL covered: installers, bundled sidecars, installed-app behavior, bounded logs evidence, and first-run docs.
- REQ covered: PKG-01, PKG-02, and PKG-03 explicitly.
- RESEARCH covered: package legitimacy audit, validation architecture, installed-app UAT matrix, and release honesty guidance.
- CONTEXT covered: D-10 through D-13 directly; D-01 through D-09 and D-14 through D-16 are closed through linked evidence from prior plans.
- SPEC covered: Requirements 1 through 6, all Edge Coverage rows, and all three prohibitions with explicit evidence or gap markers.
- Unplanned items in this plan: none; this plan is the explicit closure and source-audit layer.

<output>
Evidence, documentation, and scoped phase closure only. Do not invent release-ready claims where UAT evidence is missing.
</output>
