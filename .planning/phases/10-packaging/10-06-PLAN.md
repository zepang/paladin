---
phase: 10-packaging
plan: "06"
type: execute
wave: 6
depends_on: ["10-09"]
files_modified:
  - scripts/launch-paladin-macos.sh
  - scripts/test-launch-paladin-macos.sh
  - .planning/phases/10-packaging/10-UAT.md
  - .planning/phases/10-packaging/10-UAT.json
  - .planning/phases/10-packaging/evidence/.gitkeep
autonomous: false
requirements: [PKG-01, PKG-02, PKG-03]
must_haves:
  truths:
    - "R1/Edge: DMG 在全新安装位置真实启动且无开发工具/源码依赖。"
    - "R2/Edge: MSI build、Windows UAT、release-ready 分开记录。"
    - "R3/Edge: installed runtime 从 bundle 解析平台 sidecar。"
    - "R4/Edge: wrapper 从当前环境启动；运行中变化不热加载，重启后生效。"
    - "R5/Edge: macOS UAT 覆盖全依赖、缺 PG、缺 Redis、两者缺失；后三者 Go degraded 而 Agent/工作区可用。"
    - "R6/Edge: UAT 观察轮转前后日志 UI 连续，自动化证明 10 MB x 5。"
    - "R7/Edge: Markdown/JSON 逐平台推导 build/UAT/release-ready。"
  prohibitions:
    - "MUST NOT 在缺少完整 macOS matrix 时关闭 Phase；Windows 未 UAT 必须 non-release-ready。"
    - "MUST NOT 在 wrapper、证据、日志摘录或文档保存 secret 值。"
    - "MUST NOT 在 installed-app 诊断要求开发命令或源码编辑。"
    - "MUST NOT 通过端口/进程名控制外部进程；UAT 仅操作 supervisor child handles。"
  artifacts:
    - path: scripts/launch-paladin-macos.sh
      provides: "支持的 env-only macOS 启动 wrapper"
    - path: .planning/phases/10-packaging/10-UAT.json
      provides: "append-only UAT attempts 与 latest-complete status"
    - path: .planning/phases/10-packaging/10-UAT.md
      provides: "人读 UAT summary"
  key_links:
    - from: scripts/launch-paladin-macos.sh
      to: /Applications/Paladin.app/Contents/MacOS/Paladin
      via: "direct exec with inherited env"
      pattern: "Contents/MacOS"
    - from: .planning/phases/10-packaging/10-UAT.json
      to: .planning/phases/10-packaging/evidence
      via: "relative evidence paths"
      pattern: "evidence/"
---
<objective>构建 DMG、安装到真实位置，以统一 wrapper 执行四场景 macOS UAT并保留 append-only Markdown+JSON 证据。（D-04、D-05、D-06、D-07、D-08、D-19、D-20、D-21、D-22、D-23）</objective>
<execution_context>@/Users/kdocs/.codex/gsd-core/workflows/execute-plan.md
@/Users/kdocs/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-packaging/10-SPEC.md
@.planning/phases/10-packaging/10-CONTEXT.md
@.planning/phases/10-packaging/10-VALIDATION.md
@.planning/phases/10-packaging/10-UI-SPEC.md
@docs/packaging.md</context>
## Artifacts This Plan Produces
- `launch-paladin-macos.sh`: `--app` parser, missing-name report, direct executable launch.
- `10-UAT.json`: schema-versioned immutable attempts with required D-21 fields.
- `10-UAT.md`, phase-local redacted evidence, final verification matrix.
<tasks>
<task type="auto" id="T1" tdd="true"><name>Task 1: RED→GREEN supported macOS launch wrapper</name><files>scripts/test-launch-paladin-macos.sh, scripts/launch-paladin-macos.sh</files><read_first>CONTEXT D-04..D-08；docs first-run path。</read_first><behavior>默认仅 `/Applications/Paladin.app`；`--app path` 可覆盖；不搜索别处；拒绝配置值 CLI 参数；只报告空/缺失变量名并仍 direct-launch；继承当前 env、不持久化。</behavior><action>先以 fake app executable 写失败 shell tests，再实现 POSIX wrapper；用户文档/UAT 只能引用此路径。</action><verify><automated>bash scripts/test-launch-paladin-macos.sh</automated></verify><acceptance_criteria>测试覆盖默认/custom/missing/app-not-found/secret-arg rejection；输出无值。</acceptance_criteria><done>wrapper contract green。</done></task>
<task type="auto" id="T2"><name>Task 2: 安装已验证 DMG 并初始化 append-only UAT evidence</name><files>.planning/phases/10-packaging/10-UAT.md, .planning/phases/10-packaging/10-UAT.json, .planning/phases/10-packaging/evidence/.gitkeep</files><read_first>10-09 DMG evidence；CONTEXT D-19..D-23；VALIDATION macOS matrix。</read_first><action>使用 10-09 已完成 staging smoke、secret scan 与 artifact checkpoint 的 DMG，核对 SHA-256 后安装到全新非 repo 路径；初始化 append-only attempts schema及 Windows deferred/non-release-ready 记录，不重新运行长时构建。</action><verify><automated>node -e "const u=require('./.planning/phases/10-packaging/10-UAT.json');const d=require('./.planning/phases/10-packaging/evidence/macos-build.json');if(!Array.isArray(u.attempts)||!d.dmg_sha256||!u.platforms?.windows||u.platforms.windows.release_ready!==false)process.exit(1)"</automated></verify><acceptance_criteria>快速 evidence/schema gate 在数秒内完成；DMG hash 与安装路径可追踪。</acceptance_criteria><done>已安装应用和 UAT 记录结构准备完成。</done></task>
<task type="checkpoint:human-verify" id="T3" gate="blocking"><name>Task 3: 执行并确认 macOS 四场景 installed-app UAT</name><files>.planning/phases/10-packaging/10-UAT.md, .planning/phases/10-packaging/10-UAT.json, .planning/phases/10-packaging/evidence/</files><read_first>10-UAT.md prerequisites；launch wrapper；UI-SPEC Agent/Go semantics。</read_first><action>通过同一 wrapper 逐次启动真实安装应用：PG+Redis 可用、仅 PG 缺失、仅 Redis 缺失、两者缺失。每场记录 Agent /health、Go /healthz、/readyz、主工作区可用性、脱敏截图/短日志；关闭只使用应用/supervisor tracked child lifecycle。自动检查 JSON 完整后请求人工确认观察与证据匹配。</action><verify><automated>node -e "const u=require('./.planning/phases/10-packaging/10-UAT.json');const need=['all_available','postgres_missing','redis_missing','both_missing'];for(const s of need){const a=u.attempts.filter(x=>x.platform==='macos'&&x.scenario===s).at(-1);if(!a||a.result!=='PASS'||!a.evidence?.length)process.exit(1)}"</automated><human-check>确认安装应用并非 repo 内 executable；四场景实际观察与 JSON 一致；Agent 始终可用，后三场 Go degraded 且主工作区不阻塞；证据无 secret。</human-check></verify><acceptance_criteria>四个 latest attempts 均 PASS，必要字段/证据齐全；人工确认真实 installed behavior。</acceptance_criteria><done>输入 approved 后 macOS blocking UAT gate 通过。</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| tester environment→installed app | secrets 仅进当前进程环境 |
| observations→Git evidence | 必须脱敏、不可伪造 release status |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-06-01 | Information Disclosure | wrapper/evidence | high | mitigate | env-only、value-free output、redaction review |
| T-10-06-02 | Repudiation | UAT history | high | mitigate | immutable append attempts + hashes |
| T-10-06-03 | Tampering | release status | high | mitigate | latest complete matrix derived status + human checkpoint |
</threat_model>
<verification>wrapper tests、DMG artifact gate、JSON completeness、manual installed UAT、source audit。</verification>
<success_criteria>macOS 真实安装 UAT 四场景通过；Windows 保持 deferred/non-release-ready 记录。</success_criteria>
<output>Create `.planning/phases/10-packaging/10-06-SUMMARY.md` when done.</output>
