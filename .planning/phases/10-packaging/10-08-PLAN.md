---
phase: 10-packaging
plan: "08"
type: execute
wave: 4
depends_on: ["10-03"]
files_modified:
  - apps/desktop/src/components/StartupMask.tsx
  - apps/desktop/src/components/StatusBar/ProcessLight.tsx
  - apps/desktop/src/components/__tests__/StartupMask.test.tsx
  - apps/desktop/src/components/StatusBar/__tests__/ProcessLight.test.tsx
autonomous: true
requirements: [PKG-01, PKG-03]
must_haves:
  truths: ["R1/Edge: packaged failure 文案适用于 installed app。", "R2/Edge: Windows 状态不由 UI 推断。", "R3/Edge: config/executable/cwd failure 分类明确。", "R4/Edge: UI 不暗示热加载。", "R5/Edge: Go degraded 不显示 StartupMask，且明示 Agent 仍可继续使用。", "R6/Edge: rotation 对 UI 不可见且日志流连续。", "R7/Edge: UI 不声称 release-ready。"]
  prohibitions: ["MUST NOT 错标 release-ready。", "MUST NOT 显示 secret 值。", "MUST NOT 在 packaged 诊断要求开发命令或源码路径操作。", "MUST NOT 为 external owner 启用 stop/restart。"]
  artifacts:
    - path: apps/desktop/src/components/__tests__/StartupMask.test.tsx
      provides: "packaged diagnostics 与 Go non-blocking RTL contract"
    - path: apps/desktop/src/components/StatusBar/__tests__/ProcessLight.test.tsx
      provides: "degraded copy 与 owner actions RTL contract"
  key_links:
    - from: apps/desktop/src/components/StartupMask.tsx
      to: apps/desktop/src/stores/process.ts
      via: Agent-only blocking state
      pattern: "agent"
---
<objective>按 UI-SPEC 修正 packaged startup 与 Go degraded 语义，并用 RTL/Vitest 锁定负向文案和 non-blocking 行为。</objective>
<execution_context>@/Users/kdocs/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/10-packaging/10-UI-SPEC.md
@.planning/phases/10-packaging/10-SPEC.md
@apps/desktop/src/components/StartupMask.tsx
@apps/desktop/src/components/StatusBar/ProcessLight.tsx
@apps/desktop/src/stores/process.ts</context>
## Artifacts This Plan Produces
- Packaged-aware `classifyFailure` copy and Agent-only blocking behavior.
- ProcessLight Go degraded explanatory copy and ownership-safe controls.
<tasks>
<task type="auto" id="T1" tdd="true"><name>Task 1: RED→GREEN StartupMask packaged diagnostics</name><files>apps/desktop/src/components/__tests__/StartupMask.test.tsx, apps/desktop/src/components/StartupMask.tsx</files><read_first>UI-SPEC StartupMask hierarchy/copy；process tuple store。</read_first><behavior>packaged config invalid、executable missing、cwd invalid、port conflict 分别显示锁定可操作文案；渲染输出不含开发工具名或源码配置路径；Go degraded/readiness failure 且 Agent running 时不渲染 StartupMask；Agent failure 仍阻塞且只有一个主按钮。</behavior><action>先写 RTL 失败用例，再复用现有单卡层级和 classifier 增加 runtime-aware copy；不新增设置页或第二主按钮。</action><verify><automated>pnpm --filter @paladin/desktop test -- --run apps/desktop/src/components/__tests__/StartupMask.test.tsx</automated></verify><acceptance_criteria>对实际 rendered text 做正负断言，Go-only degraded case 查询不到 blocking mask。</acceptance_criteria><done>StartupMask 满足 UI-SPEC packaged/non-blocking contract。</done></task>
<task type="auto" id="T2" tdd="true"><name>Task 2: RED→GREEN ProcessLight degraded/owner contract</name><files>apps/desktop/src/components/StatusBar/__tests__/ProcessLight.test.tsx, apps/desktop/src/components/StatusBar/ProcessLight.tsx</files><read_first>UI-SPEC Go copy 与 ProcessLight actions；owner semantics。</read_first><behavior>Go degraded 显示颜色+“降级”文字，popover 明示“Agent 仍可继续使用”；external stop/restart disabled、redetect enabled；supervisor actions 保持；诊断无 secret 值或开发路径指导。</behavior><action>先写 RTL 失败测试，再只调整现有 status maps/popover copy/actions，不新增诊断中心。</action><verify><automated>pnpm --filter @paladin/desktop test -- --run apps/desktop/src/components/StatusBar/__tests__/ProcessLight.test.tsx</automated></verify><acceptance_criteria>copy、a11y 文本与 disabled/enabled 行为均由用户可见 DOM 断言。</acceptance_criteria><done>ProcessLight 明确 non-blocking degraded 语义并保护 external owner。</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Rust diagnostic events→UI | 文案不得泄密或诱导危险操作 |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-08-01 | Information Disclosure | diagnostics | high | mitigate | category/name-only copy + RTL negatives |
| T-10-08-02 | Denial of Service | StartupMask | medium | mitigate | Agent-only blocking state |
</threat_model>
<verification>两个 focused RTL 文件及 desktop Vitest。</verification>
<success_criteria>packaged 诊断无开发指导，Go degraded 非阻塞且清晰可访问。</success_criteria>
<output>Create `.planning/phases/10-packaging/10-08-SUMMARY.md` when done.</output>
