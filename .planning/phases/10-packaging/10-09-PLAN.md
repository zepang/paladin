---
phase: 10-packaging
plan: "09"
type: execute
wave: 5
depends_on: ["10-02", "10-04", "10-05", "10-08"]
files_modified:
  - apps/desktop/scripts/secret-sentinel.mjs
  - apps/desktop/scripts/secret-sentinel.test.mjs
  - .planning/phases/10-packaging/evidence/macos-build.json
autonomous: false
requirements: [PKG-01, PKG-03]
must_haves:
  truths: ["R1/Edge: DMG artifact 在真实安装前有 build/smoke evidence。", "R2/Edge: Windows evidence 仍 buildability-only。", "R3/Edge: staging sidecar/config 可解析。", "R4/Edge: sentinel 环境按启动快照测试。", "R5/Edge: degraded diagnostics 可扫描。", "R6/Edge: persisted/UI logs 均进入 sentinel scan。", "R7/Edge: docs/evidence 扫描目标完整。"]
  prohibitions: ["MUST NOT 错标 release-ready。", "MUST NOT 让 API/DB/Redis/JWT sentinel 出现在任何产物或可见面。", "MUST NOT 在 docs/UI 出现 packaged 开发指导。", "MUST NOT 触发外部进程查杀。"]
  artifacts:
    - path: apps/desktop/scripts/secret-sentinel.mjs
      provides: "fail-closed aggregate leakage scanner"
      exports: [scanRequiredTargets]
    - path: .planning/phases/10-packaging/evidence/macos-build.json
      provides: "staging smoke 与 DMG identity/hash"
  key_links:
    - from: apps/desktop/scripts/secret-sentinel.mjs
      to: apps/desktop/src-tauri/target/release/bundle
      via: recursive required-target scan
      pattern: "scanRequiredTargets"
---
<objective>在真实安装前执行聚合 secret/prohibited-copy 负向门、快速 staging smoke 和独立 DMG artifact checkpoint。</objective>
<execution_context>@/Users/kdocs/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/10-packaging/10-SPEC.md
@.planning/phases/10-packaging/10-VALIDATION.md
@docs/packaging.md</context>
## Artifacts This Plan Produces
- `scanRequiredTargets`：sentinel injection/scan harness。
- `macos-build.json`：smoke、DMG filename/size/SHA-256 与 build commit。
<tasks>
<task type="auto" id="T1" tdd="true"><name>Task 1: RED→GREEN fail-closed 聚合 sentinel 门</name><files>apps/desktop/scripts/secret-sentinel.test.mjs, apps/desktop/scripts/secret-sentinel.mjs</files><read_first>SPEC secret/developer-guidance prohibitions；release staging；Rust log/UI diagnostics；docs/evidence paths。</read_first><behavior>注入四个唯一 sentinel（API key、DB DSN、Redis URL、JWT）；生成 fixture 后递归扫描 bundle staging、persisted logs、captured process-log/UI diagnostics、docs、phase evidence；任何 sentinel 命中失败；任一 required target 不存在/不可读/为空失败；docs/UI 另行扫描 prohibited packaged guidance。</behavior><action>先写命中、缺目录、空目标都会失败的 tests，再实现不跟随越界 symlink 的递归 binary/text scanner；输出只报 target/category，不回显 sentinel。</action><verify><automated>node --test apps/desktop/scripts/secret-sentinel.test.mjs</automated></verify><acceptance_criteria>缺失扫描目标不能被当成零命中通过；四类 sentinel 和 prohibited copy 均有正向泄漏 fixture。</acceptance_criteria><done>aggregate scanner fail-closed tests green。</done></task>
<task type="auto" id="T2"><name>Task 2: 快速 packaged staging smoke 与聚合扫描</name><files>.planning/phases/10-packaging/evidence/macos-build.json</files><read_first>10-01 release verify；10-02 resource layout；10-08 UI tests；sentinel scanner。</read_first><action>先运行 sidecars-only/staging verify，不构建 DMG：确认 packaged config、两 executable、resource resolution、dotenv absence；运行 focused Rust/UI tests并用四 sentinel 驱动捕获 logs/diagnostics，最后对所有 required targets 扫描。记录 smoke commands/results/elapsed，目标 &lt;10 分钟。</action><verify><automated>pnpm release -- --sidecars-only --target current --verify && node apps/desktop/scripts/secret-sentinel.mjs --require-all</automated></verify><acceptance_criteria>快速 staging 在 installer build 前失败反馈；evidence 记录 elapsed 与 target inventory。</acceptance_criteria><done>staging smoke 和聚合负向门通过。</done></task>
<task type="checkpoint:human-verify" id="T3" gate="blocking"><name>Task 3: 构建并确认真实 macOS DMG artifact</name><files>.planning/phases/10-packaging/evidence/macos-build.json</files><read_first>T2 smoke evidence；release installer command；SPEC R1。</read_first><action>仅在 smoke green 后运行真实 DMG build；fail-closed 确认恰有预期 DMG、文件非空、计算 SHA-256、commit 与 target arch，写入 macos-build.json；对最终 bundle 再运行 sentinel scan。人工确认 artifact identity 后才允许 10-06 安装 UAT。</action><verify><automated>find apps/desktop/src-tauri/target/release/bundle/dmg -name '*.dmg' -type f -size +0 -print -quit | grep -q . && node apps/desktop/scripts/secret-sentinel.mjs --require-all && node -e "const e=require('./.planning/phases/10-packaging/evidence/macos-build.json');if(!e.dmg_sha256||!e.commit_sha||e.secret_scan!=='PASS')process.exit(1)"</automated><human-check>确认 DMG filename/hash/commit 与构建输出一致，且这是 artifact checkpoint、尚未冒充 installed UAT。</human-check></verify><acceptance_criteria>长时 installer build 与快速 smoke 分离；最终 artifact 有身份和 clean scan。</acceptance_criteria><done>DMG 可安全交给真实安装 UAT。</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| injected secrets→all release surfaces | 任一泄漏均阻塞 |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-09-01 | Information Disclosure | bundle/log/UI/docs | high | mitigate | required-target recursive sentinel scan |
| T-10-09-02 | Repudiation | DMG evidence | high | mitigate | commit+size+SHA-256 checkpoint |
</threat_model>
<verification>scanner unit tests、staging smoke、final bundle scan、DMG checkpoint。</verification>
<success_criteria>扫描目标完整且零 sentinel，DMG identity 可审计，快速反馈先于长构建。</success_criteria>
<output>Create `.planning/phases/10-packaging/10-09-SUMMARY.md` when done.</output>
