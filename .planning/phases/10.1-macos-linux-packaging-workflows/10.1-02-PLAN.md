---
phase: 10.1-macos-linux-packaging-workflows
plan: 02
type: execute
wave: 2
depends_on:
  - 10.1-01
files_modified:
  - .github/workflows/packaging-macos.yml
autonomous: true
requirements:
  - PKG-01 extension
  - PKG-03 extension
must_haves:
  truths:
    - "macOS buildability artifacts contain exactly one DMG and one build-manifest.json."
    - "Linux buildability artifacts contain exactly one AppImage, exactly one deb, and one build-manifest.json."
    - "Missing required Linux installer type fails manifest validation."
    - "Unsupported target triples fail manifest validation."
    - "Wrong sidecar and installer filenames fail manifest validation."
    - "Missing paths and directories fail manifest validation."
    - "Workflow upload boundaries exclude full build trees, standalone sidecars, caches, logs, dependency directories, and .env files."
    - "Documentation and evidence separate buildability from signing, notarization, installed-app UAT, and release-ready status."
  artifacts:
    - path: ".github/workflows/packaging-macos.yml"
      provides: "macOS arm64 native packaging buildability workflow."
  key_links:
    - from: ".github/workflows/packaging-macos.yml"
      to: "apps/desktop/scripts/release.mjs"
      via: "pnpm release -- --target aarch64-apple-darwin --verify"
      pattern: "pnpm release -- --target aarch64-apple-darwin --verify"
    - from: ".github/workflows/packaging-macos.yml"
      to: "apps/desktop/scripts/build-manifest.mjs"
      via: "one DMG installer path passed through repeatable --installer"
      pattern: "macos-artifact/build-manifest.json"
  prohibitions:
    - statement: "MUST NOT describe macOS/Linux CI artifact success as release-ready, signed, notarized, or installed-app UAT complete."
      status: "resolved"
      verification: "Docs wording audit in Plan 04 and final audit in Plan 05."
    - statement: "MUST NOT upload full build trees, dependency directories, caches, logs, .env files, or standalone sidecar binaries as CI artifacts."
      status: "resolved"
      verification: "This plan uploads only macos-artifact/*.dmg and macos-artifact/build-manifest.json."
    - statement: "MUST NOT copy secret values, API keys, database DSNs, Redis passwords, JWT secrets, tokens, or logs into build-manifest.json."
      status: "resolved"
      verification: "Manifest receives only commit, runner OS, target, sidecar paths, installer path, and output."
    - statement: "MUST NOT bypass pnpm release with a second divergent packaging implementation inside platform workflows."
      status: "resolved"
      verification: "This plan uses only pnpm release for build orchestration."
    - statement: "MUST NOT fold provider configuration, DB/Redis runtime behavior, signing/notarization, automatic release publishing, or installed-app UAT into this phase."
      status: "resolved"
      verification: "This workflow creates CI buildability artifacts only."
---

<objective>
Add the independent macOS packaging workflow locked by D-02, D-04, D-05, D-06, and D-07.

Purpose: macOS native CI should prove buildability at the same standard as the existing Windows workflow without claiming release readiness.
Output: `.github/workflows/packaging-macos.yml`.
</objective>

<execution_context>
@/Users/kdocs/.codex/gsd-core/workflows/execute-plan.md
@/Users/kdocs/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10.1-macos-linux-packaging-workflows/10.1-CONTEXT.md
@.planning/phases/10.1-macos-linux-packaging-workflows/10.1-SPEC.md
@.planning/phases/10.1-macos-linux-packaging-workflows/10.1-RESEARCH.md
@.planning/phases/10.1-macos-linux-packaging-workflows/10.1-VALIDATION.md
@.planning/phases/10.1-macos-linux-packaging-workflows/10.1-PATTERNS.md
@.github/workflows/packaging-windows.yml
@apps/desktop/scripts/release.mjs
@apps/desktop/scripts/build-manifest.mjs
@package.json
@apps/desktop/package.json
</context>

## Artifacts this phase produces

- `.github/workflows/packaging-macos.yml`
- `.github/workflows/packaging-linux.yml`
- `macos-artifact/*.dmg`
- `macos-artifact/build-manifest.json`
- `linux-artifact/*.AppImage`
- `linux-artifact/*.deb`
- `linux-artifact/build-manifest.json`
- `TARGET_RULES` or equivalent target rule constant in `apps/desktop/scripts/build-manifest.mjs`
- supported manifest targets: `x86_64-pc-windows-msvc`, `aarch64-apple-darwin`, `x86_64-unknown-linux-gnu`
- installer roles: `msi`, `dmg`, `appimage`, `deb`
- repeatable CLI flag: `--installer`
- documentation paths: `README.md`, `docs/packaging.md`

<tasks>

<task type="auto">
  <name>Task 1: Create macOS arm64 workflow</name>
  <read_first>
    - .github/workflows/packaging-windows.yml
    - .planning/phases/10.1-macos-linux-packaging-workflows/10.1-CONTEXT.md
    - .planning/phases/10.1-macos-linux-packaging-workflows/10.1-PATTERNS.md
    - apps/desktop/scripts/release.mjs
    - apps/desktop/scripts/build-manifest.mjs
  </read_first>
  <files>.github/workflows/packaging-macos.yml</files>
  <action>Create `.github/workflows/packaging-macos.yml` as an independent workflow per D-02 and D-03. Use name `Packaging macOS arm64`, `workflow_dispatch`, and the same pull_request path scope as Windows with `.github/workflows/packaging-macos.yml` included. Set `permissions: contents: read`. Define job `build-dmg`, `runs-on: macos-14` per D-04, `timeout-minutes: 45`, and env `TARGET_TRIPLE: aarch64-apple-darwin` per D-05. Preserve setup actions from Windows: checkout v4, pnpm/action-setup v4 with version 9, setup-node v4 with Node 22 and pnpm cache, setup-uv v6 with `0.8.3`, setup-python v5 with `3.12`, setup-go v5 with `go-version-file: apps/server/go.mod` and `cache-dependency-path: apps/server/go.sum`, and dtolnay/rust-toolchain stable with target `aarch64-apple-darwin`. Run `pnpm install --frozen-lockfile`, then `pnpm release -- --target aarch64-apple-darwin --verify`. Do not call PyInstaller, Go build, or Tauri directly from workflow steps.</action>
  <verify>
    <automated>rg -n "macos-14|TARGET_TRIPLE: aarch64-apple-darwin|pnpm release -- --target aarch64-apple-darwin --verify|permissions:|contents: read" .github/workflows/packaging-macos.yml</automated>
  </verify>
  <acceptance_criteria>
    - Workflow exists at `.github/workflows/packaging-macos.yml`.
    - Workflow is independent and does not modify or merge `.github/workflows/packaging-windows.yml`.
    - Runner label is exactly `macos-14`.
    - Target triple is exactly `aarch64-apple-darwin`.
    - Build command is exactly `pnpm release -- --target aarch64-apple-darwin --verify`.
  </acceptance_criteria>
  <done>macOS workflow setup and release command match locked Phase 10.1 decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Stage one DMG and upload only DMG plus manifest</name>
  <read_first>
    - .github/workflows/packaging-macos.yml
    - .github/workflows/packaging-windows.yml
    - apps/desktop/scripts/build-manifest.mjs
    - .planning/phases/10.1-macos-linux-packaging-workflows/10.1-SPEC.md
    - .planning/phases/10.1-macos-linux-packaging-workflows/10.1-VALIDATION.md
  </read_first>
  <files>.github/workflows/packaging-macos.yml</files>
  <action>Add the macOS artifact staging and upload steps per D-06 and D-13. After release, count `apps/desktop/src-tauri/target/release/bundle/dmg/*.dmg` and fail unless the count is exactly one. Create `macos-artifact`, copy only that DMG into it, and invoke `node apps/desktop/scripts/build-manifest.mjs` with `--commit "${{ github.sha }}"`, `--runner-os "${{ runner.os }}"`, `--target "$TARGET_TRIPLE"`, `--agent "apps/desktop/src-tauri/binaries/paladin-agent-sidecar-aarch64-apple-darwin"`, `--server "apps/desktop/src-tauri/binaries/paladin-server-sidecar-aarch64-apple-darwin"`, one `--installer "$DMG"`, and `--output "macos-artifact/build-manifest.json"`. Upload artifact with name `paladin-macos-aarch64-dmg-${{ github.sha }}` and paths exactly `macos-artifact/*.dmg` plus `macos-artifact/build-manifest.json`, `if-no-files-found: error`, and `retention-days: 14`.</action>
  <verify>
    <automated>rg -n "macos-artifact/\\*\\.dmg|macos-artifact/build-manifest\\.json|paladin-agent-sidecar-aarch64-apple-darwin|paladin-server-sidecar-aarch64-apple-darwin|if-no-files-found: error|retention-days: 14" .github/workflows/packaging-macos.yml</automated>
    <automated>! rg -n "src-tauri/target/\\*\\*|src-tauri/binaries/\\*\\*|node_modules|\\.env\\*|logs|\\.app" .github/workflows/packaging-macos.yml</automated>
  </verify>
  <acceptance_criteria>
    - Workflow fails before upload unless exactly one `.dmg` exists.
    - Manifest command passes one `--installer` argument for the DMG.
    - Upload paths include only `macos-artifact/*.dmg` and `macos-artifact/build-manifest.json`.
    - Upload paths do not include full target directories, sidecar binaries, dependency directories, caches, logs, `.env*`, or `.app`.
  </acceptance_criteria>
  <done>macOS workflow artifact boundary satisfies SPEC R1/R4 and D-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub runner to release script | Workflow invokes the shared release path with target triple input. |
| Build output to artifact upload | DMG and manifest are copied into a clean staging directory before upload. |
| Workflow context to manifest | GitHub SHA and runner OS are passed as non-secret metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10.1-04 | Information Disclosure | `.github/workflows/packaging-macos.yml` upload paths | high | mitigate | Stage only one DMG and manifest; upload only `macos-artifact/*.dmg` and `macos-artifact/build-manifest.json`. |
| T-10.1-05 | Tampering | macOS manifest command | medium | mitigate | Pass exact target triple and exact sidecar paths to manifest validation. |
| T-10.1-06 | Repudiation | macOS CI status | medium | mitigate | Workflow name and artifact name state build platform and installer format; docs plan preserves buildability-only wording. |
| T-10.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new dependencies are added; setup actions use existing project workflow versions. |
</threat_model>

<verification>
Run static workflow checks:
- `rg -n "macos-14|aarch64-apple-darwin|build-manifest.json|upload-artifact" .github/workflows/packaging-macos.yml`
- `node --test apps/desktop/scripts/build-manifest.test.mjs apps/desktop/scripts/release.test.mjs`
</verification>

<success_criteria>
- macOS workflow uses native `macos-14`.
- macOS workflow runs `pnpm release -- --target aarch64-apple-darwin --verify`.
- macOS workflow stages exactly one DMG and writes one manifest.
- macOS workflow uploads only DMG plus manifest.
</success_criteria>

<output>
Create `.planning/phases/10.1-macos-linux-packaging-workflows/10.1-02-SUMMARY.md` when done.
</output>
