---
phase: 10.1-macos-linux-packaging-workflows
plan: 03
type: execute
wave: 2
depends_on:
  - 10.1-01
files_modified:
  - .github/workflows/packaging-linux.yml
autonomous: true
requirements:
  - PKG-02 extension
  - PKG-03 extension
must_haves:
  truths:
    - "macOS buildability artifacts contain exactly one DMG and one build-manifest.json."
    - "Linux buildability artifacts contain exactly one AppImage, exactly one deb, and one build-manifest.json."
    - "Missing required Linux installer type fails manifest validation."
    - "Unsupported target triples fail manifest validation."
    - "Wrong sidecar and installer filenames fail manifest validation."
    - "Missing paths and directories fail manifest validation."
    - "Workflow upload boundaries exclude full build trees, standalone sidecars, caches, logs, dependency directories, and .env files."
    - "Documentation and evidence separate buildability from signing, notarization, installed-app UAT, and release-ready status."
  artifacts:
    - path: ".github/workflows/packaging-linux.yml"
      provides: "Linux x86_64 native packaging buildability workflow."
  key_links:
    - from: ".github/workflows/packaging-linux.yml"
      to: "apps/desktop/scripts/release.mjs"
      via: "pnpm release -- --target x86_64-unknown-linux-gnu --verify"
      pattern: "pnpm release -- --target x86_64-unknown-linux-gnu --verify"
    - from: ".github/workflows/packaging-linux.yml"
      to: "apps/desktop/scripts/build-manifest.mjs"
      via: "repeatable --installer for AppImage and deb"
      pattern: "linux-artifact/build-manifest.json"
  prohibitions:
    - statement: "MUST NOT describe macOS/Linux CI artifact success as release-ready, signed, notarized, or installed-app UAT complete."
      status: "resolved"
      verification: "Docs wording audit in Plan 04 and final audit in Plan 05."
    - statement: "MUST NOT upload full build trees, dependency directories, caches, logs, .env files, or standalone sidecar binaries as CI artifacts."
      status: "resolved"
      verification: "This plan uploads only linux-artifact/*.AppImage, linux-artifact/*.deb, and linux-artifact/build-manifest.json."
    - statement: "MUST NOT copy secret values, API keys, database DSNs, Redis passwords, JWT secrets, tokens, or logs into build-manifest.json."
      status: "resolved"
      verification: "Manifest receives only commit, runner OS, target, sidecar paths, installer paths, and output."
    - statement: "MUST NOT bypass pnpm release with a second divergent packaging implementation inside platform workflows."
      status: "resolved"
      verification: "This plan uses only pnpm release for build orchestration."
    - statement: "MUST NOT fold provider configuration, DB/Redis runtime behavior, signing/notarization, automatic release publishing, or installed-app UAT into this phase."
      status: "resolved"
      verification: "This workflow creates CI buildability artifacts only."
---

<objective>
Add the independent Linux packaging workflow locked by D-02, D-03, D-08, D-09, D-10, and D-11.

Purpose: Linux native CI should prove AppImage and deb buildability through the shared release path while preserving a tight artifact boundary.
Output: `.github/workflows/packaging-linux.yml`.
</objective>

<execution_context>
@/Users/kdocs/.codex/gsd-core/workflows/execute-plan.md
@/Users/kdocs/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10.1-macos-linux-packaging-workflows/10.1-CONTEXT.md
@.planning/phases/10.1-macos-linux-packaging-workflows/10.1-SPEC.md
@.planning/phases/10.1-macos-linux-packaging-workflows/10.1-RESEARCH.md
@.planning/phases/10.1-macos-linux-packaging-workflows/10.1-VALIDATION.md
@.planning/phases/10.1-macos-linux-packaging-workflows/10.1-PATTERNS.md
@.github/workflows/packaging-windows.yml
@apps/desktop/scripts/release.mjs
@apps/desktop/scripts/build-manifest.mjs
@package.json
@apps/desktop/package.json
</context>

## Artifacts this phase produces

- `.github/workflows/packaging-macos.yml`
- `.github/workflows/packaging-linux.yml`
- `macos-artifact/*.dmg`
- `macos-artifact/build-manifest.json`
- `linux-artifact/*.AppImage`
- `linux-artifact/*.deb`
- `linux-artifact/build-manifest.json`
- `TARGET_RULES` or equivalent target rule constant in `apps/desktop/scripts/build-manifest.mjs`
- supported manifest targets: `x86_64-pc-windows-msvc`, `aarch64-apple-darwin`, `x86_64-unknown-linux-gnu`
- installer roles: `msi`, `dmg`, `appimage`, `deb`
- repeatable CLI flag: `--installer`
- documentation paths: `README.md`, `docs/packaging.md`

<tasks>

<task type="auto">
  <name>Task 1: Create Linux x86_64 workflow with inline apt dependencies</name>
  <read_first>
    - .github/workflows/packaging-windows.yml
    - .planning/phases/10.1-macos-linux-packaging-workflows/10.1-CONTEXT.md
    - .planning/phases/10.1-macos-linux-packaging-workflows/10.1-RESEARCH.md
    - .planning/phases/10.1-macos-linux-packaging-workflows/10.1-PATTERNS.md
    - apps/desktop/scripts/release.mjs
  </read_first>
  <files>.github/workflows/packaging-linux.yml</files>
  <action>Create `.github/workflows/packaging-linux.yml` as an independent workflow per D-02 and D-03. Use name `Packaging Linux x86_64`, `workflow_dispatch`, and the same pull_request path scope as Windows with `.github/workflows/packaging-linux.yml` included. Set `permissions: contents: read`. Define job `build-linux-installers`, `runs-on: ubuntu-22.04`, `timeout-minutes: 45`, and env `TARGET_TRIPLE: x86_64-unknown-linux-gnu`. Preserve setup actions from Windows: checkout v4, pnpm/action-setup v4 with version 9, setup-node v4 with Node 22 and pnpm cache, setup-uv v6 with `0.8.3`, setup-python v5 with `3.12`, setup-go v5 with `go-version-file: apps/server/go.mod` and `cache-dependency-path: apps/server/go.sum`, and dtolnay/rust-toolchain stable. Add an inline apt step per D-08/D-09 before workspace install: `sudo apt-get update` and `sudo apt-get install -y build-essential curl file libayatana-appindicator3-dev libssl-dev libwebkit2gtk-4.1-dev libxdo-dev librsvg2-dev patchelf wget xdg-utils`. Run `pnpm install --frozen-lockfile`, then `pnpm release -- --target x86_64-unknown-linux-gnu --verify`. Do not add `scripts/setup-linux-packaging-deps.sh`.</action>
  <verify>
    <automated>rg -n "ubuntu-22.04|TARGET_TRIPLE: x86_64-unknown-linux-gnu|libwebkit2gtk-4\\.1-dev|libayatana-appindicator3-dev|patchelf|xdg-utils|pnpm release -- --target x86_64-unknown-linux-gnu --verify" .github/workflows/packaging-linux.yml</automated>
    <automated>test ! -f scripts/setup-linux-packaging-deps.sh</automated>
  </verify>
  <acceptance_criteria>
    - Workflow exists at `.github/workflows/packaging-linux.yml`.
    - Runner label is exactly `ubuntu-22.04`.
    - Target triple is exactly `x86_64-unknown-linux-gnu`.
    - Linux dependencies are installed inline in the workflow.
    - Build command is exactly `pnpm release -- --target x86_64-unknown-linux-gnu --verify`.
  </acceptance_criteria>
  <done>Linux workflow setup and release command match locked Phase 10.1 decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Stage one AppImage, one deb, and upload only installers plus manifest</name>
  <read_first>
    - .github/workflows/packaging-linux.yml
    - .github/workflows/packaging-windows.yml
    - apps/desktop/scripts/build-manifest.mjs
    - .planning/phases/10.1-macos-linux-packaging-workflows/10.1-SPEC.md
    - .planning/phases/10.1-macos-linux-packaging-workflows/10.1-VALIDATION.md
  </read_first>
  <files>.github/workflows/packaging-linux.yml</files>
  <action>Add Linux artifact staging and upload steps per D-10 and D-13. After release, count `apps/desktop/src-tauri/target/release/bundle/appimage/*.AppImage` and `apps/desktop/src-tauri/target/release/bundle/deb/*.deb`; fail unless each count is exactly one. Create `linux-artifact`, copy only those two installers into it, and invoke `node apps/desktop/scripts/build-manifest.mjs` with `--commit "${{ github.sha }}"`, `--runner-os "${{ runner.os }}"`, `--target "$TARGET_TRIPLE"`, `--agent "apps/desktop/src-tauri/binaries/paladin-agent-sidecar-x86_64-unknown-linux-gnu"`, `--server "apps/desktop/src-tauri/binaries/paladin-server-sidecar-x86_64-unknown-linux-gnu"`, `--installer "$APPIMAGE"`, `--installer "$DEB"`, and `--output "linux-artifact/build-manifest.json"`. Upload artifact with name `paladin-linux-x86_64-installers-${{ github.sha }}` and paths exactly `linux-artifact/*.AppImage`, `linux-artifact/*.deb`, and `linux-artifact/build-manifest.json`, `if-no-files-found: error`, and `retention-days: 14`.</action>
  <verify>
    <automated>rg -n "linux-artifact/\\*\\.AppImage|linux-artifact/\\*\\.deb|linux-artifact/build-manifest\\.json|paladin-agent-sidecar-x86_64-unknown-linux-gnu|paladin-server-sidecar-x86_64-unknown-linux-gnu|if-no-files-found: error|retention-days: 14" .github/workflows/packaging-linux.yml</automated>
    <automated>! rg -n "src-tauri/target/\\*\\*|src-tauri/binaries/\\*\\*|node_modules|\\.env\\*|logs|\\.AppDir|\\.debuginfo" .github/workflows/packaging-linux.yml</automated>
  </verify>
  <acceptance_criteria>
    - Workflow fails before upload unless exactly one `.AppImage` and exactly one `.deb` exist.
    - Manifest command passes two repeatable `--installer` arguments: AppImage and deb.
    - Upload paths include only `linux-artifact/*.AppImage`, `linux-artifact/*.deb`, and `linux-artifact/build-manifest.json`.
    - Upload paths do not include full target directories, sidecar binaries, dependency directories, caches, logs, `.env*`, or AppDir-style build directories.
  </acceptance_criteria>
  <done>Linux workflow artifact boundary satisfies SPEC R2/R4 and D-10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Ubuntu runner to release script | Workflow installs system packages and invokes the shared release path. |
| Build output to artifact upload | AppImage, deb, and manifest are copied into a clean staging directory before upload. |
| Workflow context to manifest | GitHub SHA and runner OS are passed as non-secret metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10.1-07 | Information Disclosure | `.github/workflows/packaging-linux.yml` upload paths | high | mitigate | Stage only one AppImage, one deb, and manifest; upload only `linux-artifact` installer globs and manifest. |
| T-10.1-08 | Tampering | Linux manifest command | high | mitigate | Pass exact Linux target triple and require manifest validation for both installer roles. |
| T-10.1-09 | Denial of Service | Linux installer completeness | medium | mitigate | Count AppImage and deb separately before manifest generation and upload. |
| T-10.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new package-manager dependency is added; apt packages are runner system prerequisites from Tauri research and remain inline per D-08/D-09. |
</threat_model>

<verification>
Run static workflow checks:
- `rg -n "ubuntu-22.04|x86_64-unknown-linux-gnu|AppImage|\\.deb|build-manifest.json|upload-artifact" .github/workflows/packaging-linux.yml`
- `node --test apps/desktop/scripts/build-manifest.test.mjs apps/desktop/scripts/release.test.mjs`
</verification>

<success_criteria>
- Linux workflow uses native `ubuntu-22.04`.
- Linux workflow installs Tauri Linux packaging dependencies inline.
- Linux workflow runs `pnpm release -- --target x86_64-unknown-linux-gnu --verify`.
- Linux workflow stages exactly one AppImage, exactly one deb, and one manifest.
- Linux workflow uploads only AppImage, deb, and manifest.
</success_criteria>

<output>
Create `.planning/phases/10.1-macos-linux-packaging-workflows/10.1-03-SUMMARY.md` when done.
</output>
