---
phase: 10.1-macos-linux-packaging-workflows
plan: 04
type: execute
wave: 3
depends_on:
  - 10.1-02
  - 10.1-03
files_modified:
  - README.md
  - docs/packaging.md
autonomous: true
requirements:
  - PKG-01 extension
  - PKG-02 extension
  - PKG-03 extension
must_haves:
  truths:
    - "macOS buildability artifacts contain exactly one DMG and one build-manifest.json."
    - "Linux buildability artifacts contain exactly one AppImage, exactly one deb, and one build-manifest.json."
    - "Missing required Linux installer type fails manifest validation."
    - "Unsupported target triples fail manifest validation."
    - "Wrong sidecar and installer filenames fail manifest validation."
    - "Missing paths and directories fail manifest validation."
    - "Workflow upload boundaries exclude full build trees, standalone sidecars, caches, logs, dependency directories, and .env files."
    - "Documentation and evidence separate buildability from signing, notarization, installed-app UAT, and release-ready status."
  artifacts:
    - path: "README.md"
      provides: "Packaging summary that names Windows MSI, macOS DMG, and Linux AppImage/deb CI artifacts as buildability evidence only."
    - path: "docs/packaging.md"
      provides: "Platform status table and workflow audit sections for Windows, macOS, and Linux."
  key_links:
    - from: "README.md"
      to: "docs/packaging.md"
      via: "Packaging / 安装 link"
      pattern: "docs/packaging.md"
    - from: "docs/packaging.md"
      to: ".github/workflows/packaging-macos.yml"
      via: "macOS buildability audit section"
      pattern: "packaging-macos.yml"
    - from: "docs/packaging.md"
      to: ".github/workflows/packaging-linux.yml"
      via: "Linux buildability audit section"
      pattern: "packaging-linux.yml"
  prohibitions:
    - statement: "MUST NOT describe macOS/Linux CI artifact success as release-ready, signed, notarized, or installed-app UAT complete."
      status: "resolved"
      verification: "This plan performs docs wording audits."
    - statement: "MUST NOT upload full build trees, dependency directories, caches, logs, .env files, or standalone sidecar binaries as CI artifacts."
      status: "resolved"
      verification: "Docs state upload boundaries for Windows, macOS, and Linux."
    - statement: "MUST NOT copy secret values, API keys, database DSNs, Redis passwords, JWT secrets, tokens, or logs into build-manifest.json."
      status: "resolved"
      verification: "Docs state manifest content is non-secret file metadata only."
    - statement: "MUST NOT bypass pnpm release with a second divergent packaging implementation inside platform workflows."
      status: "resolved"
      verification: "Docs name `pnpm release` as the shared workflow entrypoint."
    - statement: "MUST NOT fold provider configuration, DB/Redis runtime behavior, signing/notarization, automatic release publishing, or installed-app UAT into this phase."
      status: "resolved"
      verification: "Docs limit Phase 10.1 language to CI buildability artifacts."
---

<objective>
Update README and packaging documentation so macOS/Linux workflow success is described as buildability evidence only.

Purpose: The project must preserve release honesty while adding more platform artifacts.
Output: Updated `README.md` and `docs/packaging.md`.
</objective>

<execution_context>
@/Users/kdocs/.codex/gsd-core/workflows/execute-plan.md
@/Users/kdocs/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10.1-macos-linux-packaging-workflows/10.1-CONTEXT.md
@.planning/phases/10.1-macos-linux-packaging-workflows/10.1-SPEC.md
@.planning/phases/10.1-macos-linux-packaging-workflows/10.1-VALIDATION.md
@.planning/phases/10.1-macos-linux-packaging-workflows/10.1-PATTERNS.md
@README.md
@docs/packaging.md
@.github/workflows/packaging-windows.yml
@.github/workflows/packaging-macos.yml
@.github/workflows/packaging-linux.yml
</context>

## Artifacts this phase produces

- `.github/workflows/packaging-macos.yml`
- `.github/workflows/packaging-linux.yml`
- `macos-artifact/*.dmg`
- `macos-artifact/build-manifest.json`
- `linux-artifact/*.AppImage`
- `linux-artifact/*.deb`
- `linux-artifact/build-manifest.json`
- `TARGET_RULES` or equivalent target rule constant in `apps/desktop/scripts/build-manifest.mjs`
- supported manifest targets: `x86_64-pc-windows-msvc`, `aarch64-apple-darwin`, `x86_64-unknown-linux-gnu`
- installer roles: `msi`, `dmg`, `appimage`, `deb`
- repeatable CLI flag: `--installer`
- documentation paths: `README.md`, `docs/packaging.md`

<tasks>

<task type="auto">
  <name>Task 1: Update packaging platform status and workflow audit docs</name>
  <read_first>
    - docs/packaging.md
    - .planning/phases/10.1-macos-linux-packaging-workflows/10.1-SPEC.md
    - .planning/phases/10.1-macos-linux-packaging-workflows/10.1-CONTEXT.md
    - .planning/phases/10.1-macos-linux-packaging-workflows/10.1-PATTERNS.md
    - .github/workflows/packaging-windows.yml
    - .github/workflows/packaging-macos.yml
    - .github/workflows/packaging-linux.yml
  </read_first>
  <files>docs/packaging.md</files>
  <action>Extend `docs/packaging.md` platform status and buildability audit sections for Windows x86_64, macOS arm64, and Linux x86_64. State that Windows uploads one MSI plus manifest, macOS uploads one DMG plus manifest, and Linux uploads one AppImage, one deb, plus manifest. State that manifest metadata is commit SHA, runner OS, architecture, target, role, filename, size, and SHA-256 only. State that macOS/Linux CI success proves buildability only and does not prove signing, notarization, installed-app UAT, distro compatibility, repository publishing, or release-ready approval. Keep runtime environment docs unchanged except where wording needs to avoid platform-status confusion. Do not add provider configuration, DB/Redis runtime behavior changes, automatic release publishing, or installed-app UAT instructions for this phase.</action>
  <verify>
    <automated>rg -n "packaging-windows.yml|packaging-macos.yml|packaging-linux.yml|AppImage|deb|DMG|buildability|Release-ready|installed-app UAT" docs/packaging.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/packaging.md` includes Linux x86_64 in the platform status table.
    - `docs/packaging.md` has macOS and Linux buildability audit sections naming workflow files and exact installer counts.
    - Docs state macOS/Linux CI artifacts are not signed/notarized release approval and are not installed-app UAT completion.
    - Docs do not claim Linux installed-app UAT, distro compatibility matrix, repository publishing, or automatic release publishing.
  </acceptance_criteria>
  <done>Packaging docs cover Windows/macOS/Linux artifact formats and release-honesty boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Update README packaging summary and audit wording</name>
  <read_first>
    - README.md
    - docs/packaging.md
    - .planning/phases/10.1-macos-linux-packaging-workflows/10.1-SPEC.md
    - .planning/phases/10.1-macos-linux-packaging-workflows/10.1-PATTERNS.md
  </read_first>
  <files>README.md</files>
  <action>Update the `README.md` Packaging / 安装 summary so it names Windows MSI, macOS DMG, and Linux AppImage/deb CI artifacts as buildability evidence only, and points detailed platform status to `docs/packaging.md`. Keep the existing user startup/environment instructions intact. Do not add signing, notarization, installed-app UAT completion, release approval, provider configuration, or runtime dependency behavior changes.</action>
  <verify>
    <automated>rg -n "Windows.*MSI|macOS.*DMG|Linux.*AppImage|deb|buildability|docs/packaging.md" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README names all three CI artifact families: Windows MSI, macOS DMG, Linux AppImage/deb.
    - README says these CI artifacts are buildability evidence only.
    - README links to `docs/packaging.md` for detailed platform status.
  </acceptance_criteria>
  <done>README packaging summary matches Phase 10.1 documentation scope.</done>
</task>

<task type="auto">
  <name>Task 3: Run documentation wording audit</name>
  <read_first>
    - README.md
    - docs/packaging.md
    - .planning/phases/10.1-macos-linux-packaging-workflows/10.1-VALIDATION.md
  </read_first>
  <files>README.md, docs/packaging.md</files>
  <action>Run the documentation wording checks from `10.1-VALIDATION.md` and inspect any hits that could overclaim macOS/Linux release status. Where wording mentions signing/notarization/release-ready, ensure it is a negative or not-yet status for macOS/Linux CI artifacts. Keep any existing Phase 10 macOS installed-app evidence wording historically accurate while preventing Phase 10.1 CI workflow success from being described as release approval.</action>
  <verify>
    <automated>rg -n "buildability|release-ready|notarized|notarization|signed|installed-app UAT|AppImage|deb|DMG" README.md docs/packaging.md</automated>
  </verify>
  <acceptance_criteria>
    - Search output contains buildability wording for Windows/macOS/Linux.
    - Any `release-ready`, signing, notarization, or installed-app UAT wording for macOS/Linux CI artifacts is explicitly negative or separates CI buildability from release approval.
    - No docs section tells users that Phase 10.1 artifacts are installed-app verified.
  </acceptance_criteria>
  <done>Docs wording audit passes and is summarized with exact command output highlights.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI evidence to user documentation | Buildability facts become user-facing platform status claims. |
| Manifest metadata to user documentation | Non-secret artifact fields are documented as audit evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10.1-10 | Repudiation | `docs/packaging.md` platform status | high | mitigate | State buildability, installed-app UAT, signing/notarization, and release-ready as separate statuses. |
| T-10.1-11 | Information Disclosure | README/docs manifest descriptions | medium | mitigate | Document variable names and manifest metadata only; do not add secret values, DSNs, tokens, logs, or screenshots. |
| T-10.1-12 | Spoofing | Documentation references to workflows | low | mitigate | Name exact workflow files and artifact formats so readers can verify source paths. |
| T-10.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task is planned. |
</threat_model>

<verification>
Run docs audit:
- `rg -n "buildability|release-ready|notarized|notarization|signed|installed-app UAT|AppImage|deb|DMG" README.md docs/packaging.md`
</verification>

<success_criteria>
- README and docs describe macOS/Linux workflow success as buildability only.
- Docs list exact installer formats and manifest contents.
- Docs do not claim macOS/Linux CI artifacts are signed, notarized, installed-app verified, or release-ready.
</success_criteria>

<output>
Create `.planning/phases/10.1-macos-linux-packaging-workflows/10.1-04-SUMMARY.md` when done.
</output>
