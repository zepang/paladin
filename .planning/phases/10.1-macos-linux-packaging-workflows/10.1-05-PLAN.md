---
phase: 10.1-macos-linux-packaging-workflows
plan: 05
type: execute
wave: 4
depends_on:
  - 10.1-01
  - 10.1-02
  - 10.1-03
  - 10.1-04
files_modified:
  - .planning/phases/10.1-macos-linux-packaging-workflows/10.1-VERIFICATION.md
autonomous: false
requirements:
  - PKG-01 extension
  - PKG-02 extension
  - PKG-03 extension
must_haves:
  truths:
    - "macOS buildability artifacts contain exactly one DMG and one build-manifest.json."
    - "Linux buildability artifacts contain exactly one AppImage, exactly one deb, and one build-manifest.json."
    - "Missing required Linux installer type fails manifest validation."
    - "Unsupported target triples fail manifest validation."
    - "Wrong sidecar and installer filenames fail manifest validation."
    - "Missing paths and directories fail manifest validation."
    - "Workflow upload boundaries exclude full build trees, standalone sidecars, caches, logs, dependency directories, and .env files."
    - "Documentation and evidence separate buildability from signing, notarization, installed-app UAT, and release-ready status."
  artifacts:
    - path: ".planning/phases/10.1-macos-linux-packaging-workflows/10.1-VERIFICATION.md"
      provides: "Final Phase 10.1 validation record with automated gates, static audits, source coverage, and manual CI artifact-count evidence."
  key_links:
    - from: ".planning/phases/10.1-macos-linux-packaging-workflows/10.1-VERIFICATION.md"
      to: ".github/workflows/packaging-macos.yml"
      via: "manual CI evidence for one DMG plus manifest"
      pattern: "packaging-macos.yml"
    - from: ".planning/phases/10.1-macos-linux-packaging-workflows/10.1-VERIFICATION.md"
      to: ".github/workflows/packaging-linux.yml"
      via: "manual CI evidence for one AppImage, one deb, plus manifest"
      pattern: "packaging-linux.yml"
  prohibitions:
    - statement: "MUST NOT describe macOS/Linux CI artifact success as release-ready, signed, notarized, or installed-app UAT complete."
      status: "resolved"
      verification: "Final docs wording audit recorded here."
    - statement: "MUST NOT upload full build trees, dependency directories, caches, logs, .env files, or standalone sidecar binaries as CI artifacts."
      status: "resolved"
      verification: "Final workflow path audit recorded here."
    - statement: "MUST NOT copy secret values, API keys, database DSNs, Redis passwords, JWT secrets, tokens, or logs into build-manifest.json."
      status: "resolved"
      verification: "Manifest tests and manual artifact inspection recorded here."
    - statement: "MUST NOT bypass pnpm release with a second divergent packaging implementation inside platform workflows."
      status: "resolved"
      verification: "Final workflow command audit recorded here."
    - statement: "MUST NOT fold provider configuration, DB/Redis runtime behavior, signing/notarization, automatic release publishing, or installed-app UAT into this phase."
      status: "resolved"
      verification: "Final source audit recorded here."
---

<objective>
Close Phase 10.1 with automated gates, static workflow/doc audits, and manual CI artifact-count evidence.

Purpose: The phase is complete only when both new workflows and their documentation have evidence matching the SPEC.
Output: `.planning/phases/10.1-macos-linux-packaging-workflows/10.1-VERIFICATION.md`.
</objective>

<execution_context>
@/Users/kdocs/.codex/gsd-core/workflows/execute-plan.md
@/Users/kdocs/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10.1-macos-linux-packaging-workflows/10.1-CONTEXT.md
@.planning/phases/10.1-macos-linux-packaging-workflows/10.1-SPEC.md
@.planning/phases/10.1-macos-linux-packaging-workflows/10.1-RESEARCH.md
@.planning/phases/10.1-macos-linux-packaging-workflows/10.1-VALIDATION.md
@.planning/phases/10.1-macos-linux-packaging-workflows/10.1-PATTERNS.md
@.github/workflows/packaging-windows.yml
@.github/workflows/packaging-macos.yml
@.github/workflows/packaging-linux.yml
@apps/desktop/scripts/build-manifest.mjs
@apps/desktop/scripts/build-manifest.test.mjs
@apps/desktop/scripts/release.test.mjs
@README.md
@docs/packaging.md
</context>

## Artifacts this phase produces

- `.github/workflows/packaging-macos.yml`
- `.github/workflows/packaging-linux.yml`
- `macos-artifact/*.dmg`
- `macos-artifact/build-manifest.json`
- `linux-artifact/*.AppImage`
- `linux-artifact/*.deb`
- `linux-artifact/build-manifest.json`
- `TARGET_RULES` or equivalent target rule constant in `apps/desktop/scripts/build-manifest.mjs`
- supported manifest targets: `x86_64-pc-windows-msvc`, `aarch64-apple-darwin`, `x86_64-unknown-linux-gnu`
- installer roles: `msi`, `dmg`, `appimage`, `deb`
- repeatable CLI flag: `--installer`
- documentation paths: `README.md`, `docs/packaging.md`
- `.planning/phases/10.1-macos-linux-packaging-workflows/10.1-VERIFICATION.md`

<tasks>

<task type="auto">
  <name>Task 1: Run automated and static final gates</name>
  <read_first>
    - .planning/phases/10.1-macos-linux-packaging-workflows/10.1-VALIDATION.md
    - apps/desktop/scripts/build-manifest.test.mjs
    - apps/desktop/scripts/release.test.mjs
    - .github/workflows/packaging-windows.yml
    - .github/workflows/packaging-macos.yml
    - .github/workflows/packaging-linux.yml
    - README.md
    - docs/packaging.md
  </read_first>
  <files>.planning/phases/10.1-macos-linux-packaging-workflows/10.1-VERIFICATION.md</files>
  <action>Create `10.1-VERIFICATION.md` and record final automated evidence. Run the quick script tests, then `pnpm test`. Run static workflow path audits for Windows, macOS, and Linux: confirm Windows still uploads only `windows-artifact/*.msi` plus `windows-artifact/build-manifest.json`; macOS uploads only `macos-artifact/*.dmg` plus `macos-artifact/build-manifest.json`; Linux uploads only `linux-artifact/*.AppImage`, `linux-artifact/*.deb`, plus `linux-artifact/build-manifest.json`. Confirm macOS and Linux workflows use only `pnpm release -- --target ... --verify` for packaging and do not add direct PyInstaller, Go build, Tauri build, release publishing, signing, notarization, or installed-app UAT steps. Run docs wording audit from `10.1-VALIDATION.md` and record results.</action>
  <verify>
    <automated>node --test apps/desktop/scripts/build-manifest.test.mjs apps/desktop/scripts/release.test.mjs</automated>
    <automated>pnpm test</automated>
    <automated>rg -n "windows-artifact/\\*\\.msi|windows-artifact/build-manifest\\.json" .github/workflows/packaging-windows.yml && rg -n "macos-artifact/\\*\\.dmg|macos-artifact/build-manifest\\.json" .github/workflows/packaging-macos.yml && rg -n "linux-artifact/\\*\\.AppImage|linux-artifact/\\*\\.deb|linux-artifact/build-manifest\\.json" .github/workflows/packaging-linux.yml</automated>
    <automated>rg -n "buildability|release-ready|notarized|notarization|signed|installed-app UAT|AppImage|deb|DMG" README.md docs/packaging.md</automated>
  </verify>
  <acceptance_criteria>
    - `10.1-VERIFICATION.md` records pass/fail status for quick script tests and `pnpm test`.
    - Static workflow audit records upload globs for all three platform workflows.
    - Static workflow audit records that macOS and Linux workflows use `pnpm release` and do not contain direct release publishing/signing/notarization/installed-UAT steps.
    - Docs audit records that macOS/Linux CI artifacts are buildability evidence only.
  </acceptance_criteria>
  <done>Automated and static final gates are recorded in `10.1-VERIFICATION.md`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify hosted CI artifact counts</name>
  <read_first>
    - .github/workflows/packaging-macos.yml
    - .github/workflows/packaging-linux.yml
    - .planning/phases/10.1-macos-linux-packaging-workflows/10.1-VALIDATION.md
  </read_first>
  <files>.planning/phases/10.1-macos-linux-packaging-workflows/10.1-VERIFICATION.md</files>
  <action>Pause for hosted CI evidence after local automation has passed. The required evidence is the macOS workflow run URL plus exact artifact contents, and the Linux workflow run URL plus exact artifact contents. Do not mark Phase 10.1 complete from static checks alone.</action>
  <what-built>macOS and Linux GitHub Actions workflows plus manifest/docs support are implemented and static gates have passed.</what-built>
  <how-to-verify>
    1. Trigger `.github/workflows/packaging-macos.yml` manually in GitHub Actions.
    2. Confirm the macOS run succeeds and download the uploaded artifact.
    3. Confirm the macOS artifact contains exactly one `.dmg` and one `build-manifest.json`.
    4. Trigger `.github/workflows/packaging-linux.yml` manually in GitHub Actions.
    5. Confirm the Linux run succeeds and download the uploaded artifact.
    6. Confirm the Linux artifact contains exactly one `.AppImage`, exactly one `.deb`, and one `build-manifest.json`.
    7. Confirm neither artifact contains full build trees, dependency directories, caches, logs, `.env*`, or standalone sidecar binaries outside the installers.
  </how-to-verify>
  <verify>
    <manual>Hosted GitHub macOS/Linux workflow runs and downloadable artifact contents require human CI evidence: provide both run URLs, artifact file counts, and confirmation that uploaded artifacts contain no full build trees, dependency directories, caches, logs, `.env*`, or standalone sidecar binaries outside the installers.</manual>
  </verify>
  <acceptance_criteria>
    - User provides macOS workflow run URL and artifact count: one `.dmg`, one `build-manifest.json`.
    - User provides Linux workflow run URL and artifact count: one `.AppImage`, one `.deb`, one `build-manifest.json`.
    - User confirms uploaded artifacts do not include full build trees, dependency directories, caches, logs, `.env*`, or standalone sidecar binaries outside installers.
  </acceptance_criteria>
  <done>Manual hosted CI artifact-count evidence is available for Task 3 to record in `10.1-VERIFICATION.md`.</done>
  <resume-signal>Type "approved" with macOS/Linux run URLs and artifact counts, or describe the failing run/artifact contents.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record manual CI evidence and source coverage audit</name>
  <read_first>
    - .planning/phases/10.1-macos-linux-packaging-workflows/10.1-VERIFICATION.md
    - .planning/phases/10.1-macos-linux-packaging-workflows/10.1-CONTEXT.md
    - .planning/phases/10.1-macos-linux-packaging-workflows/10.1-SPEC.md
    - .planning/phases/10.1-macos-linux-packaging-workflows/10.1-VALIDATION.md
  </read_first>
  <files>.planning/phases/10.1-macos-linux-packaging-workflows/10.1-VERIFICATION.md</files>
  <action>Append the user-provided manual CI run URLs and artifact counts to `10.1-VERIFICATION.md`. Add a source coverage audit table covering GOAL, REQ `PKG-01 extension`, `PKG-02 extension`, `PKG-03 extension`, RESEARCH recommendations, CONTEXT D-01 through D-16, SPEC Edge Coverage rows, and SPEC Prohibitions. Mark D-16 and deferred ideas as out-of-scope handled, not implemented. State that schema push detection was skipped because Phase 10.1 has workflow/script/doc surfaces and no ORM schema files. Keep release status honest: macOS/Linux workflow success proves buildability artifacts only.</action>
  <verify>
    <automated>rg -n "PKG-01 extension|PKG-02 extension|PKG-03 extension|D-01|D-16|Edge Coverage|Prohibitions|buildability|schema push" .planning/phases/10.1-macos-linux-packaging-workflows/10.1-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Verification artifact records macOS run URL and count: one `.dmg`, one manifest.
    - Verification artifact records Linux run URL and count: one `.AppImage`, one `.deb`, one manifest.
    - Coverage audit includes every locked decision D-01 through D-16.
    - Coverage audit includes all eight SPEC Edge Coverage rows and all five SPEC Prohibitions.
    - Verification artifact states Phase 10.1 did not perform signing/notarization, installed-app UAT, provider configuration, DB/Redis runtime behavior, or automatic release publishing.
  </acceptance_criteria>
  <done>`10.1-VERIFICATION.md` provides the final phase evidence and source coverage audit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local source to verification artifact | Test and static audit outputs become phase evidence. |
| Human CI inspection to verification artifact | Manual artifact counts are recorded after hosted CI runs. |
| Documentation to release status | Final verification prevents status overclaiming. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10.1-13 | Repudiation | `10.1-VERIFICATION.md` | high | mitigate | Record automated commands, workflow static audits, docs audit, CI run URLs, and artifact counts. |
| T-10.1-14 | Information Disclosure | Manual artifact inspection | high | mitigate | Count files and inspect manifest metadata only; do not paste secret values, logs, or installer internals into evidence. |
| T-10.1-15 | Spoofing | CI artifact evidence | medium | mitigate | Require run URLs and exact artifact counts for both macOS and Linux before final evidence completion. |
| T-10.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task is planned; `pnpm test` uses existing dependencies. |
</threat_model>

<verification>
Final phase checks:
- `node --test apps/desktop/scripts/build-manifest.test.mjs apps/desktop/scripts/release.test.mjs`
- `pnpm test`
- Static workflow path audits for Windows/macOS/Linux upload globs
- Docs wording audit
- Manual CI artifact-count evidence for macOS and Linux
</verification>

<success_criteria>
- All automated gates pass.
- Static workflow audits prove minimal artifact boundaries.
- Manual macOS CI evidence records exactly one DMG plus manifest.
- Manual Linux CI evidence records exactly one AppImage, one deb, plus manifest.
- Source coverage audit covers goal, requirements, research, decisions, edges, and prohibitions.
</success_criteria>

<output>
Create `.planning/phases/10.1-macos-linux-packaging-workflows/10.1-05-SUMMARY.md` when done.
</output>
