---
phase: 10.1-macos-linux-packaging-workflows
verified: 2026-07-14T11:16:42Z
status: passed
score: 13/13 must-haves verified
behavior_unverified: 0
overrides_applied: 0
gaps: []
human_verification: []
---

# Phase 10.1: macOS/Linux Packaging Workflows Verification Report

**Phase Goal:** 将 Windows x64 packaging workflow 的 native buildability 能力扩展到 macOS 和 Linux，产出最小可审计安装包 artifact 与 manifest，但不宣称 release-ready。
**Verified:** 2026-07-14T11:16:42Z
**Status:** passed
**Re-verification:** No - initial goal-backward verification with an existing non-frontmatter verification record.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | macOS buildability artifact evidence contains exactly one DMG and one `build-manifest.json`. | VERIFIED | `unzip -l evidence/paladin-macos-aarch64-dmg-2dd7b3a3028d7746e3e87b85df2ee900ed543eee.zip` shows exactly `build-manifest.json` and `paladin_0.1.0_aarch64.dmg`. |
| 2 | Linux buildability artifact evidence contains exactly one AppImage, exactly one deb, and one `build-manifest.json`. | VERIFIED | `unzip -l evidence/paladin-linux-x86_64-installers-faeea50bf44ca819e6f3f836551c766b417b8fbf.zip` shows exactly `paladin_0.1.0_amd64.AppImage`, `paladin_0.1.0_amd64.deb`, and `build-manifest.json`. |
| 3 | Manifest supports Windows, macOS, and Linux target/installer shapes and emits required metadata. | VERIFIED | `apps/desktop/scripts/build-manifest.mjs` defines rules for `x86_64-pc-windows-msvc`, `aarch64-apple-darwin`, and `x86_64-unknown-linux-gnu`; manifests include `schema_version`, `commit_sha`, `runner_os`, `architecture`, `target`, role, filename, size, and sha256. |
| 4 | Missing required Linux installer type fails manifest validation. | VERIFIED | `build-manifest.test.mjs` rejects Linux inputs containing only AppImage or only deb; focused and full test gates pass. |
| 5 | Unsupported target triples fail manifest validation. | VERIFIED | `createBuildManifest` throws `Unsupported target`; test rejects `aarch64-pc-windows-msvc`. |
| 6 | Wrong sidecar and installer filenames fail manifest validation. | VERIFIED | Target-specific sidecar regexes and installer extension rules are implemented; tests reject wrong Windows sidecar, macOS MSI-as-DMG, and duplicate/mismatched installers. |
| 7 | Missing paths and directories fail manifest validation. | VERIFIED | `describeArtifact` uses `lstat`, requires regular files, and tests reject missing files plus directory inputs. |
| 8 | macOS and Linux workflows use native runners and the shared `pnpm release` entrypoint. | VERIFIED | `.github/workflows/packaging-macos.yml` uses `macos-14` and `pnpm release -- --target aarch64-apple-darwin --verify`; `.github/workflows/packaging-linux.yml` uses `ubuntu-22.04`, inline Tauri apt deps, and `pnpm release -- --target x86_64-unknown-linux-gnu --verify`. |
| 9 | Workflow upload boundaries exclude full build trees, standalone sidecars, caches, logs, dependency dirs, and `.env*`. | VERIFIED | Upload paths are limited to `windows-artifact/*.msi`, `macos-artifact/*.dmg`, `linux-artifact/*.AppImage`, `linux-artifact/*.deb`, and `*/build-manifest.json`; evidence zips contain only installers plus manifest. |
| 10 | Documentation and evidence separate buildability from signing, notarization, installed-app UAT, and release-ready status. | VERIFIED | `README.md` and `docs/packaging.md` state CI artifacts prove buildability only and explicitly do not imply signing, notarization, installed-app UAT, repository publishing, or release-ready approval. |
| 11 | Hosted macOS run URL is recorded and corresponds to the macOS packaging job. | VERIFIED | `10.1-VERIFICATION.md` records `https://github.com/zepang/paladin/actions/runs/29297651236/job/86974539784`; opening the GitHub page shows `Packaging macOS arm64`, `build-dmg`, succeeded Jul 14, 2026 in 6m 58s. |
| 12 | Linux run URL is not invented, while local successful-run evidence zip and manifest are present. | VERIFIED | The record states Linux run URL is not provided; the local Linux zip exists and its manifest reports commit `faeea50bf44ca819e6f3f836551c766b417b8fbf`, runner `Linux`, target `x86_64-unknown-linux-gnu`, roles `agent-sidecar`, `server-sidecar`, `appimage`, `deb`. |
| 13 | Schema push was correctly skipped. | VERIFIED | Phase 10.1 touches workflow, script, documentation, and evidence surfaces only; no ORM schema files are in plan `files_modified`, summary key files, or modified code paths. |

**Score:** 13/13 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `.github/workflows/packaging-macos.yml` | Independent macOS arm64 buildability workflow | VERIFIED | Exists, substantive, uses `macos-14`, `aarch64-apple-darwin`, one-DMG count gate, manifest generation, minimal upload path. |
| `.github/workflows/packaging-linux.yml` | Independent Linux x86_64 buildability workflow | VERIFIED | Exists, substantive, uses `ubuntu-22.04`, inline Linux deps, AppImage/deb count gates, manifest generation, minimal upload path. |
| `.github/workflows/packaging-windows.yml` | Existing Windows baseline preserved | VERIFIED | Still uses one MSI plus `build-manifest.json` upload boundary and shared `pnpm release` entrypoint. |
| `apps/desktop/scripts/build-manifest.mjs` | Cross-platform manifest validation | VERIFIED | `TARGET_RULES`, repeatable installers, exact role/count/filename checks, regular-file hashing, deterministic non-secret fields. |
| `apps/desktop/scripts/build-manifest.test.mjs` | Manifest contract tests | VERIFIED | Covers Windows MSI, macOS DMG, Linux AppImage+deb, repeatable `--installer`, unsupported target, missing files, directories, wrong filenames, and missing Linux installer type. |
| `apps/desktop/scripts/release.mjs` | Shared release entrypoint | VERIFIED | Orchestrates Agent, Server, audit, sidecar staging, Tauri build, and macOS DMG recovery without workflow-specific divergent packaging code. |
| `README.md` | User-facing buildability-only packaging summary | VERIFIED | Links `docs/packaging.md`; says CI-generated Windows/macOS/Linux artifacts are not release-ready before signing/notarization requirements, installed-app UAT, and approval. |
| `docs/packaging.md` | Detailed packaging status and upload boundaries | VERIFIED | Platform table and audit sections document exact artifact shapes and non-scope release-readiness limits. |
| `evidence/paladin-macos-aarch64-dmg-2dd7b3a3028d7746e3e87b85df2ee900ed543eee.zip` | Hosted macOS artifact evidence | VERIFIED | Exactly one DMG and one manifest. |
| `evidence/paladin-linux-x86_64-installers-faeea50bf44ca819e6f3f836551c766b417b8fbf.zip` | Local Linux successful-run artifact evidence | VERIFIED | Exactly one AppImage, one deb, and one manifest. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `.github/workflows/packaging-macos.yml` | `apps/desktop/scripts/release.mjs` | `pnpm release -- --target aarch64-apple-darwin --verify` | VERIFIED | Exact workflow command found. |
| `.github/workflows/packaging-linux.yml` | `apps/desktop/scripts/release.mjs` | `pnpm release -- --target x86_64-unknown-linux-gnu --verify` | VERIFIED | Exact workflow command found. |
| `.github/workflows/packaging-macos.yml` | `apps/desktop/scripts/build-manifest.mjs` | `node ... build-manifest.mjs` with one `--installer "$DMG"` and output `macos-artifact/build-manifest.json` | VERIFIED | Manual multiline inspection verifies the link; the generic key-link regex missed the multiline command in plan 01. |
| `.github/workflows/packaging-linux.yml` | `apps/desktop/scripts/build-manifest.mjs` | `node ... build-manifest.mjs` with repeatable `--installer "$APPIMAGE"` and `--installer "$DEB"` | VERIFIED | Manual multiline inspection verifies the link; the generic key-link regex missed the multiline command in plan 01. |
| `README.md` | `docs/packaging.md` | Packaging / 安装 link | VERIFIED | Link text and path found in README. |
| `docs/packaging.md` | `.github/workflows/packaging-macos.yml` / `.github/workflows/packaging-linux.yml` | Workflow audit sections | VERIFIED | Both workflow filenames and artifact shapes are documented. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| macOS evidence zip | DMG + manifest entries | Hosted artifact zip under `evidence/` | Yes | VERIFIED |
| Linux evidence zip | AppImage + deb + manifest entries | Local downloaded successful-run artifact zip under `evidence/` | Yes | VERIFIED |
| `build-manifest.json` in macOS zip | artifact metadata | `build-manifest.mjs` over actual sidecar and DMG file paths | Yes | VERIFIED |
| `build-manifest.json` in Linux zip | artifact metadata | `build-manifest.mjs` over actual sidecar, AppImage, and deb file paths | Yes | VERIFIED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Focused packaging script behavior | `node --test apps/desktop/scripts/build-manifest.test.mjs apps/desktop/scripts/release.test.mjs` | 17 tests passed, 0 failed | PASS |
| Full workspace test command | `pnpm test` | Desktop Vitest: 11 files passed, 66 tests passed. Node script tests: 23 passed, 0 failed. | PASS |
| macOS evidence zip shape | `unzip -l ...paladin-macos...zip` | Exactly 2 files: `build-manifest.json`, one `.dmg` | PASS |
| Linux evidence zip shape | `unzip -l ...paladin-linux...zip` | Exactly 3 files: one `.AppImage`, one `.deb`, `build-manifest.json` | PASS |
| Manifest metadata inspection | `unzip -p ... build-manifest.json` | Required target, architecture, roles, sizes, and sha256 values present | PASS |

### Probe Execution

Step 7c: SKIPPED. No Phase 10.1 probe scripts were declared or discovered; this phase uses workflow/script tests and artifact evidence instead.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| `PKG-01 extension` | Plans 01, 02, 04, 05 | macOS native CI buildability workflow producing one DMG plus manifest without release-ready claims | SATISFIED | macOS workflow exists; hosted job URL recorded; macOS zip contains one DMG and manifest; docs preserve buildability-only wording. |
| `PKG-02 extension` | Plans 01, 03, 04, 05 | Linux native CI buildability workflow producing AppImage and deb plus manifest | SATISFIED | Linux workflow exists; local successful-run evidence zip contains one AppImage, one deb, and manifest; docs preserve buildability-only wording. |
| `PKG-03 extension` | Plans 01, 02, 03, 04, 05 | Manifest and docs preserve non-secret audit metadata and buildability/release-ready separation | SATISFIED | Manifest tests pass; docs describe exact metadata and explicitly separate signing/notarization/UAT/release approval. |

No additional Phase 10.1 requirement IDs were found in `.planning/REQUIREMENTS.md`; it lists PKG-01 through PKG-03 as the packaging requirement family, and Phase 10.1 ROADMAP narrows this to `PKG-01~03 extension`.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| None | - | No `TBD`, `FIXME`, `XXX`, `TODO`, placeholder, empty implementation, or prohibited upload-path blocker found in modified implementation/doc files. | - | - |

Notes:

- `rg` for prohibited upload patterns in workflows only found source bundle lookup paths and GitHub Actions cache settings, not upload-artifact paths.
- README and docs contain example environment variable names and placeholder values in existing runtime setup docs; these are not copied into `build-manifest.json` and do not affect Phase 10.1 artifact evidence.

### Human Verification Required

None. The hosted macOS job URL is recorded and publicly resolves to the expected macOS packaging job. Linux has no URL by instruction; the phase accepts local downloaded artifact evidence and records that absence explicitly.

### Gaps Summary

No blocking gaps found. Phase 10.1 goal is achieved: macOS and Linux native packaging buildability workflows exist, artifact evidence is minimal and auditable, manifest validation is fail-closed for target/installer shapes, upload boundaries are tight, docs avoid release-ready overclaims, the macOS run URL is recorded, the Linux missing URL is not invented, and schema push was correctly skipped.

---

_Verified: 2026-07-14T11:16:42Z_
_Verifier: the agent (gsd-verifier)_
