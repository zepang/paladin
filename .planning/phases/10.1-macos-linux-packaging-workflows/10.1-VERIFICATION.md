# Phase 10.1 Verification

## Status

Task 1 automated and static gates: passed.

Task 2 hosted GitHub Actions artifact-count evidence: passed.

Task 3 manual CI evidence and source coverage audit: passed.

This verification record intentionally treats macOS and Linux CI outputs as buildability evidence only. It does not claim signing, notarization, installed-app UAT, automatic release publishing, or release-ready status.

## Automated Gates

| Gate | Command | Result | Evidence |
| --- | --- | --- | --- |
| Quick script tests | `node --test apps/desktop/scripts/build-manifest.test.mjs apps/desktop/scripts/release.test.mjs` | Passed | 16 tests passed, 0 failed. Covered deterministic non-secret manifests, macOS DMG manifest, Linux AppImage+deb manifest, repeatable `--installer`, invalid targets, missing files/directories, wrong filenames, required Linux installer types, release command ordering, sidecar presence, `.env` rejection, trusted Windows pnpm invocation, and packaged Agent smoke behavior. |
| Full test suite | `pnpm test` | Passed | Desktop Vitest: 7 files passed, 47 tests passed. Script `node:test`: 22 tests passed, 0 failed. |

## Static Workflow Upload Audit

| Workflow | Required upload boundary | Result | Evidence |
| --- | --- | --- | --- |
| `.github/workflows/packaging-windows.yml` | `windows-artifact/*.msi`; `windows-artifact/build-manifest.json` | Passed | Upload path contains only `windows-artifact/*.msi` and `windows-artifact/build-manifest.json`. Manifest output path is `windows-artifact/build-manifest.json`. |
| `.github/workflows/packaging-macos.yml` | `macos-artifact/*.dmg`; `macos-artifact/build-manifest.json` | Passed | Upload path contains only `macos-artifact/*.dmg` and `macos-artifact/build-manifest.json`. Manifest output path is `macos-artifact/build-manifest.json`. |
| `.github/workflows/packaging-linux.yml` | `linux-artifact/*.AppImage`; `linux-artifact/*.deb`; `linux-artifact/build-manifest.json` | Passed | Upload path contains only `linux-artifact/*.AppImage`, `linux-artifact/*.deb`, and `linux-artifact/build-manifest.json`. Manifest output path is `linux-artifact/build-manifest.json`. |

No workflow upload boundary includes full build trees, standalone sidecars, dependency directories, caches, logs, or `.env*` files.

## Static Workflow Command Audit

| Workflow | Required packaging entrypoint | Result | Evidence |
| --- | --- | --- | --- |
| `.github/workflows/packaging-macos.yml` | `pnpm release -- --target aarch64-apple-darwin --verify` | Passed | The workflow builds through the shared release entrypoint. Static search found no direct PyInstaller, direct `go build`, direct Tauri build, release publishing, signing, notarization, or installed-app UAT step. |
| `.github/workflows/packaging-linux.yml` | `pnpm release -- --target x86_64-unknown-linux-gnu --verify` | Passed | The workflow builds through the shared release entrypoint. Static search found no direct PyInstaller, direct `go build`, direct Tauri build, release publishing, signing, notarization, or installed-app UAT step. |

## Documentation Wording Audit

| File | Result | Evidence |
| --- | --- | --- |
| `README.md` | Passed | Packaging text states CI-generated Windows MSI, macOS DMG, and Linux AppImage/deb are buildability evidence only and are not release-ready before signing/notarization requirements, installed-app UAT, and release approval. |
| `docs/packaging.md` | Passed | Platform status table and buildability sections separate CI artifact generation from signing, notarization, installed-app UAT, distro compatibility, repository publishing, automatic GitHub Release publishing, and release-ready approval. |

## Manual Hosted CI Evidence

Hosted CI artifact-count evidence passed from the user-provided macOS run URL and downloaded local evidence artifacts.

### macOS Artifact Evidence

- macOS run URL: https://github.com/zepang/paladin/actions/runs/29297651236/job/86974539784
- Artifact zip: `.planning/phases/10.1-macos-linux-packaging-workflows/evidence/paladin-macos-aarch64-dmg-2dd7b3a3028d7746e3e87b85df2ee900ed543eee.zip`
- Zip entry count: exactly 2 files.

| Zip entry | Size | Role |
| --- | ---: | --- |
| `build-manifest.json` | 816 bytes | manifest |
| `paladin_0.1.0_aarch64.dmg` | 83,232,578 bytes | DMG installer |

Manifest evidence:

| Field | Value |
| --- | --- |
| `commit_sha` | `2dd7b3a3028d7746e3e87b85df2ee900ed543eee` |
| `runner_os` | `macOS` |
| `architecture` | `aarch64` |
| `target` | `aarch64-apple-darwin` |
| artifact roles | `agent-sidecar`, `server-sidecar`, `dmg` |
| DMG filename | `paladin_0.1.0_aarch64.dmg` |
| DMG size | 83,232,578 bytes |
| DMG sha256 | `91075327fee3d59c4c5d3236406f8813a599d7148699e52d02a1034b5b8c15cc` |

Result: passed. The macOS artifact contains exactly one `.dmg` and one `build-manifest.json`.

### Linux Artifact Evidence

- Linux run URL: not provided; artifact evidence downloaded locally from successful run.
- Artifact zip: `.planning/phases/10.1-macos-linux-packaging-workflows/evidence/paladin-linux-x86_64-installers-faeea50bf44ca819e6f3f836551c766b417b8fbf.zip`
- Zip entry count: exactly 3 files.

| Zip entry | Size | Role |
| --- | ---: | --- |
| `paladin_0.1.0_amd64.AppImage` | 180,824,568 bytes | AppImage installer |
| `paladin_0.1.0_amd64.deb` | 105,244,130 bytes | deb installer |
| `build-manifest.json` | 1,023 bytes | manifest |

Manifest evidence:

| Field | Value |
| --- | --- |
| `commit_sha` | `faeea50bf44ca819e6f3f836551c766b417b8fbf` |
| `runner_os` | `Linux` |
| `architecture` | `x86_64` |
| `target` | `x86_64-unknown-linux-gnu` |
| artifact roles | `agent-sidecar`, `server-sidecar`, `appimage`, `deb` |
| AppImage filename | `paladin_0.1.0_amd64.AppImage` |
| AppImage size | 180,824,568 bytes |
| AppImage sha256 | `0c1bc59d5e0e6a4acc6e082a690ebeccb4be543edf736b5f4c343e6eb93ae28a` |
| deb filename | `paladin_0.1.0_amd64.deb` |
| deb size | 105,244,130 bytes |
| deb sha256 | `d8b663755a7fd15a0f08f6299a40e58746297377838ef404d3736289deef0def` |

Result: passed. The Linux artifact contains exactly one `.AppImage`, exactly one `.deb`, and one `build-manifest.json`.

### Negative Artifact Boundary Confirmation

Zip listings confirm both hosted artifacts stay within the required upload boundary:

- Neither zip contains full build trees.
- Neither zip contains dependency directories or caches.
- Neither zip contains logs.
- Neither zip contains `.env*`.
- Neither zip contains standalone sidecar binaries outside installers; sidecars appear only as manifest metadata entries.

## Source Coverage Audit

### Goal and Requirements

| Source | Coverage Result | Evidence |
| --- | --- | --- |
| GOAL | Covered | Phase 10.1 extends Windows native packaging CI to macOS and Linux buildability workflows with audited installer artifacts and non-secret manifests. |
| PKG-01 extension | Covered | macOS workflow evidence records native `aarch64-apple-darwin` buildability, exactly one DMG, and one manifest. |
| PKG-02 extension | Covered | Linux workflow evidence records native `x86_64-unknown-linux-gnu` buildability, exactly one AppImage, exactly one deb, and one manifest. |
| PKG-03 extension | Covered | Manifest tests, README, and `docs/packaging.md` preserve buildability-only wording and non-secret manifest metadata. |

### RESEARCH Recommendations

| Recommendation | Coverage Result | Evidence |
| --- | --- | --- |
| Preserve independent native workflows | Covered | Windows, macOS, and Linux workflows remain separate files; no matrix merge was introduced. |
| Use `pnpm release -- --target ... --verify` as the only packaging entrypoint | Covered | Static workflow command audit passed for macOS and Linux. |
| Stage only platform installer files plus `build-manifest.json` | Covered | Static upload audit and hosted zip listings confirm minimal artifact boundaries. |
| Use explicit target rules and repeatable `--installer` manifest input | Covered | Task 1 automated gates recorded manifest tests for supported targets and repeatable installer handling. |
| Keep Linux dependencies inline rather than adding a setup script | Covered | Linux workflow summary records inline apt dependencies and no separate setup script. |
| Keep CI success as buildability evidence only | Covered | Documentation audit and this verification record avoid release-ready claims. |

### CONTEXT Decisions D-01 through D-16

| Decision | Coverage Result | Evidence |
| --- | --- | --- |
| D-01 | Covered | `.github/workflows/packaging-windows.yml` remains independent and static upload audit passed. |
| D-02 | Covered | `.github/workflows/packaging-macos.yml` and `.github/workflows/packaging-linux.yml` were added as independent workflows. |
| D-03 | Covered | No macOS/Linux matrix workflow or combined all-platform workflow was introduced. |
| D-04 | Covered | macOS workflow uses `macos-14`; Task 1 static workflow audit passed. |
| D-05 | Covered | macOS target and manifest evidence are `aarch64-apple-darwin`. |
| D-06 | Covered | macOS hosted artifact contains one `.dmg` and one `build-manifest.json`. |
| D-07 | Covered | Phase 10.1 does not include Intel macOS/x64 coverage. |
| D-08 | Covered | Linux apt dependencies are inline in `.github/workflows/packaging-linux.yml`. |
| D-09 | Covered | No separate `scripts/setup-linux-packaging-deps.sh` was introduced. |
| D-10 | Covered | Linux hosted artifact contains one `.AppImage`, one `.deb`, and one `build-manifest.json`. |
| D-11 | Covered | Linux remains buildability-only; no installed-app UAT, distro matrix, repository publishing, or desktop-environment QA was added. |
| D-12 | Covered | Manifest CLI accepts repeatable `--installer` arguments; automated tests passed. |
| D-13 | Covered | Windows/macOS/Linux installer counts and roles are represented by manifest tests and hosted evidence. |
| D-14 | Covered | Manifest validation fails closed for unsupported targets, missing Linux installer type, wrong filenames, missing files, and directories. |
| D-15 | Covered | Manifest roles include `msi`, `dmg`, `appimage`, `deb`, `agent-sidecar`, and `server-sidecar`. |
| D-16 | Covered as out of scope | Desktop/provider/UI todos were reviewed but not folded into Phase 10.1. |

### SPEC Edge Coverage

| Edge Coverage Row | Coverage Result | Evidence |
| --- | --- | --- |
| Platform-specific output shape - R1 | Covered | macOS artifact has exactly one `.dmg` and one manifest. |
| Platform-specific output shape - R2 | Covered | Linux artifact has exactly one `.AppImage`, one `.deb`, and one manifest. |
| Multi-artifact completeness - R2, R3 | Covered | Manifest tests reject missing required Linux installer type; hosted Linux evidence has both installers. |
| Unsupported platform/target - R3 | Covered | Manifest tests reject unsupported targets. |
| Filename mismatch - R3 | Covered | Manifest tests reject wrong sidecar and installer filenames. |
| Missing or non-file artifact - R3 | Covered | Manifest tests reject missing files and directories. |
| Artifact over-upload - R4 | Covered | Workflow upload paths and zip listings exclude build trees, sidecars outside installers, caches, logs, dependencies, and `.env*`. |
| Status overclaiming - R5 | Covered | README, `docs/packaging.md`, and this record state buildability evidence only. |

### SPEC Prohibitions

| Prohibition | Coverage Result | Evidence |
| --- | --- | --- |
| Prohibitions: MUST NOT describe macOS/Linux CI artifact success as release-ready, signed, notarized, or installed-app UAT complete. | Covered | Documentation wording audit passed; this record does not claim release-ready status. |
| Prohibitions: MUST NOT upload full build trees, dependency directories, caches, logs, `.env*`, or standalone sidecar binaries as CI artifacts. | Covered | Static upload audit and zip listings confirm only installers plus manifests. |
| Prohibitions: MUST NOT copy secret values, API keys, database DSNs, Redis passwords, JWT secrets, tokens, or logs into `build-manifest.json`. | Covered | Manifest tests and inspected manifests contain deterministic metadata only. |
| Prohibitions: MUST NOT bypass `pnpm release` with a second divergent packaging implementation inside platform workflows. | Covered | Static command audit passed for macOS and Linux. |
| Prohibitions: MUST NOT fold provider configuration, DB/Redis runtime behavior, signing/notarization, automatic release publishing, or installed-app UAT into this phase. | Covered | Source audit confirms these remain out of scope. |

## Schema Push and Explicit Non-Scope

Schema push was skipped because Phase 10.1 touches workflow, script, and documentation surfaces only; no ORM schema files are part of this phase.

Phase 10.1 does not perform signing/notarization, installed-app UAT, provider configuration, DB/Redis runtime behavior, or automatic release publishing.
