---
phase: 11-desktop-ai-provider-configuration
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - apps/agent/tests/test_provider_runtime.py
  - apps/desktop/src-tauri/src/ai_provider/storage_tests.rs
  - apps/desktop/src-tauri/src/ai_provider/bootstrap_tests.rs
  - apps/desktop/src/stores/__tests__/aiProvider.test.ts
  - apps/desktop/src/components/provider/__tests__/AiProviderPanel.test.tsx
  - apps/desktop/src/components/StatusBar/__tests__/AiProviderLight.test.tsx
  - apps/desktop/src/components/__tests__/ChatAreaProviderCta.test.tsx
autonomous: true
requirements:
  - R1
  - R2
  - R3
  - R4
  - R5
  - R6
  - R7
requirements_addressed:
  - R1
  - R2
  - R3
  - R4
  - R5
  - R6
  - R7
must_haves:
  truths:
    - "R1 edge idempotency: repeated no-key startup keeps /health 200 and does not mutate provider config."
    - "R1 edge concurrency: concurrent health/chat probes during unconfigured startup cannot crash Agent."
    - "R2 edge empty: empty or missing provider state maps to a configure-provider prompt."
    - "R2 edge idempotency: repeated missing-provider chat attempts show one stable prompt state."
    - "R2 edge concurrency: concurrent missing-provider chat attempts leave Agent running and surface the same configuration state."
    - "R3 edge adjacency: duplicate display names and duplicate priorities have deterministic handling."
    - "R3 edge empty: empty provider list is valid and represented as unconfigured."
    - "R3 edge encoding: provider IDs, base URLs, and model IDs preserve Unicode text while trimming surrounding whitespace for validation."
    - "R3 edge ordering: priority ties are stable and do not override explicit active provider."
    - "R3 edge idempotency: saving the same provider twice updates in place."
    - "R3 edge concurrency: overlapping edits cannot corrupt persisted provider config."
    - "R4 edge adjacency: deleting or replacing the active provider cannot leave a dangling active selection."
    - "R4 edge empty: persisted empty provider list reloads as unconfigured."
    - "R4 edge ordering: active provider selection wins over priority ordering."
    - "R4 edge idempotency: restart reloads one stable runtime config without duplicate imports."
    - "R4 edge concurrency: in-flight request keeps its original provider and subsequent requests use the newly active provider."
    - "R5 edge empty: empty env vars are ignored and do not create configured providers with blank secrets."
    - "R5 edge encoding: env var names are exact ASCII keys and values preserve exact strings except required trimming."
    - "R5 edge idempotency: repeated bootstrap import does not duplicate default providers."
    - "R5 edge concurrency: env bootstrap and user save cannot race into a mixed active provider."
    - "R6 edge empty: missing key displays as absent/unconfigured, not an empty raw value."
    - "R6 edge encoding: masking handles special characters without leaking suffixes beyond the approved form."
    - "R6 edge idempotency: repeated reads/diagnostics never transform masked values back into raw values."
    - "R6 edge concurrency: concurrent logging/diagnostic emission uses the same redaction path before persistence or UI emission."
    - "R7 edge idempotency: repeated probes report stable separate states until inputs change."
    - "R7 edge concurrency: simultaneous Go readiness and AI provider failures remain independently classified."
  artifacts:
    - path: apps/agent/tests/test_provider_runtime.py
      provides: "RED tests for Agent runtime snapshot, no-key startup, provider-not-configured, DeepSeek base URL, and request-start snapshot semantics"
    - path: apps/desktop/src-tauri/src/ai_provider/storage_tests.rs
      provides: "RED Rust tests for app-data persistence, idempotent saves, overlapping saves, masked readback, and active-provider fallback"
    - path: apps/desktop/src-tauri/src/ai_provider/bootstrap_tests.rs
      provides: "RED Rust tests for PALADIN_AI_* and DEEPSEEK_API_KEY bootstrap precedence"
    - path: apps/desktop/src/stores/__tests__/aiProvider.test.ts
      provides: "RED frontend store tests for readiness and command DTO behavior"
    - path: apps/desktop/src/components/provider/__tests__/AiProviderPanel.test.tsx
      provides: "RED provider panel interaction tests"
    - path: apps/desktop/src/components/StatusBar/__tests__/AiProviderLight.test.tsx
      provides: "RED AI status-bar tests"
    - path: apps/desktop/src/components/__tests__/ChatAreaProviderCta.test.tsx
      provides: "RED chat CTA test"
  key_links:
    - from: "11-SPEC.md edge coverage"
      to: "Wave 0 failing tests"
      via: "every covered edge has at least one explicit failing test assertion before implementation"
      pattern: "provider-not-configured|snapshot|PALADIN_AI|masked|AI provider"
  prohibitions:
    - "MUST NOT block app startup or sidecar liveness solely because an AI provider is absent."
    - "MUST NOT display, log, persist in non-secret metadata, or include raw API key values in evidence."
    - "MUST NOT silently switch the user to a different paid/cloud provider after explicit provider selection."
    - "MUST NOT present missing AI provider as Go DB/Redis readiness failure or Agent process crash."
    - "MUST NOT use fear, blame, or alarmist wording in the missing-provider prompt."
---

<objective>
Create the Phase 11 RED validation scaffold before implementation begins.

Purpose: TDD mode is active, and this phase changes request-time model selection, persisted provider authority, secret masking, and user-visible readiness semantics. The first executable step must make those contracts observable before production code changes.
Output: Focused failing tests for R1-R7 and every resolved edge/prohibition from `11-SPEC.md`.
</objective>

## Artifacts this phase produces

- New test file `apps/agent/tests/test_provider_runtime.py`.
- New Rust test modules `apps/desktop/src-tauri/src/ai_provider/storage_tests.rs` and `apps/desktop/src-tauri/src/ai_provider/bootstrap_tests.rs`.
- New frontend test files `apps/desktop/src/stores/__tests__/aiProvider.test.ts`, `apps/desktop/src/components/provider/__tests__/AiProviderPanel.test.tsx`, `apps/desktop/src/components/StatusBar/__tests__/AiProviderLight.test.tsx`, and `apps/desktop/src/components/__tests__/ChatAreaProviderCta.test.tsx`.
- New test contracts for symbols later implemented as `ProviderRuntime`, `ProviderSnapshot`, `AiProviderConfigManager`, `AiProviderPanel`, `AiProviderLight`, and `useAiProviderStore`.

<execution_context>
@/Users/kdocs/.codex/gsd-core/workflows/execute-plan.md
@/Users/kdocs/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-desktop-ai-provider-configuration/11-CONTEXT.md
@.planning/phases/11-desktop-ai-provider-configuration/11-SPEC.md
@.planning/phases/11-desktop-ai-provider-configuration/11-VALIDATION.md
@.planning/phases/11-desktop-ai-provider-configuration/11-UI-SPEC.md
@.planning/phases/11-desktop-ai-provider-configuration/11-PATTERNS.md
@apps/agent/tests/test_server.py
@apps/agent/tests/test_agent.py
@apps/desktop/src/components/StatusBar/__tests__/ProcessLight.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Agent runtime RED tests for R1, R2, R4, and DeepSeek base URL</name>
  <files>apps/agent/tests/test_provider_runtime.py, apps/agent/tests/test_server.py</files>
  <read_first>
    @apps/agent/tests/test_server.py
    @apps/agent/tests/test_agent.py
    @apps/agent/src/server/main.py
    @apps/agent/src/agent/paladin_agent.py
    @.planning/phases/11-desktop-ai-provider-configuration/11-SPEC.md
  </read_first>
  <behavior>
    - With `DEEPSEEK_API_KEY`, `PALADIN_AI_API_KEY`, `OPENAI_API_KEY`, and LM Studio variables absent, importing `src.server.main` and GET `/health` returns 200 with AI readiness `unconfigured`.
    - POST `/copilotkit` with no usable provider returns a structured provider-not-configured state and leaves the app importable/running.
    - A request captures the provider snapshot at request start; switching snapshots affects the next request only.
    - A DeepSeek runtime snapshot passes its configured `base_url` into the OpenAI-compatible client instead of using the legacy hardcoded value.
  </behavior>
  <action>Create `apps/agent/tests/test_provider_runtime.py` with pytest tests that import the public symbols planned for Wave 1: `ProviderRuntime`, `ProviderSnapshot`, `ProviderReadiness`, and the server app/runtime API. Update `apps/agent/tests/test_server.py` only where needed to add no-key server contract coverage. Tests must be behavior-first and may fail because implementation symbols/routes do not exist yet. Cover D-04, D-05, D-06 and requirements R1, R2, R4.</action>
  <acceptance_criteria>
    - The new tests explicitly unset AI provider env vars with `patch.dict(os.environ, {}, clear=True)` or equivalent and assert `/health` remains 200.
    - The test names mention no-key startup, provider-not-configured, snapshot-at-request-start, and DeepSeek configured base URL.
    - No test requires a real network call or real API key.
    - The test command fails in RED because the new runtime symbols or behavior are not implemented yet, not because of syntax errors.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/agent &amp;&amp; set +e; uv run pytest tests/test_provider_runtime.py tests/test_server.py -x; status=$?; set -e; test "$status" -ne 0</automated>
  </verify>
  <done>Agent RED tests exist, are syntactically valid, and fail for missing Phase 11 behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Rust app-data authority RED tests for R3, R4, R5, and R6</name>
  <files>apps/desktop/src-tauri/src/ai_provider/storage_tests.rs, apps/desktop/src-tauri/src/ai_provider/bootstrap_tests.rs</files>
  <read_first>
    @apps/desktop/src-tauri/src/process/config.rs
    @apps/desktop/src-tauri/src/process/supervisor.rs
    @apps/desktop/src-tauri/src/process/log_redact.rs
    @.planning/phases/11-desktop-ai-provider-configuration/11-SPEC.md
    @.planning/phases/11-desktop-ai-provider-configuration/11-RESEARCH.md
  </read_first>
  <behavior>
    - Saving the same provider twice updates in place and keeps active selection stable.
    - Overlapping saves cannot produce invalid JSON, duplicate IDs, or dangling active provider references.
    - A clean app-data directory can be seeded from `PALADIN_AI_*` or legacy DeepSeek env vars, and saved local config wins after explicit user save.
    - Readback returns `has_api_key` and a fixed short fingerprint, never raw key material or key length.
  </behavior>
  <action>Create Rust RED tests under the planned `ai_provider` module. Use temp directories and direct manager/storage APIs planned for Wave 1; do not touch real app data. Cover D-01, D-02, D-10, D-11, D-12, D-14 and requirements R3, R4, R5, R6.</action>
  <acceptance_criteria>
    - Tests define expected DTO fields: provider `id`, `provider_type`, `display_name`, `base_url`, `model_id`, `priority`, `active`, `readiness`, `has_api_key`, and `api_key_fingerprint`.
    - Tests verify local config provenance so env bootstrap is seed-only after explicit save.
    - Tests assert persisted non-secret metadata does not contain the raw key sample.
    - The test command fails in RED because `ai_provider` implementation is not present yet, not because test syntax is invalid.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/desktop/src-tauri &amp;&amp; set +e; cargo test ai_provider --lib; status=$?; set -e; test "$status" -ne 0</automated>
  </verify>
  <done>Rust RED tests exist for persistence, bootstrap, concurrency, and masked readback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add frontend RED tests for provider settings, CTA, and status separation</name>
  <files>apps/desktop/src/stores/__tests__/aiProvider.test.ts, apps/desktop/src/components/provider/__tests__/AiProviderPanel.test.tsx, apps/desktop/src/components/StatusBar/__tests__/AiProviderLight.test.tsx, apps/desktop/src/components/__tests__/ChatAreaProviderCta.test.tsx</files>
  <read_first>
    @apps/desktop/src/components/ChatArea.tsx
    @apps/desktop/src/components/layout/RightPanel.tsx
    @apps/desktop/src/components/StatusBar.tsx
    @apps/desktop/src/components/StatusBar/ProcessLight.tsx
    @apps/desktop/src/stores/terminal.ts
    @.planning/phases/11-desktop-ai-provider-configuration/11-UI-SPEC.md
  </read_first>
  <behavior>
    - Missing or invalid AI readiness renders one stable chat CTA with copy `尚未配置 AI provider` and button `配置 AI provider`.
    - Clicking the CTA opens the existing RightPanel and selects the `ai-provider` tab.
    - Provider panel supports save/test separation, empty API key input on edit, masked key metadata, and field-level validation.
    - StatusBar shows `AI · 未配置`, `AI · 未测试`, `AI · 可用`, or `AI · 无效` independently from Agent and Go process states.
  </behavior>
  <action>Create frontend RED tests using existing Vitest and Testing Library patterns. Mock Tauri `invoke` at the module boundary. Cover D-07, D-08, D-09, D-10, D-12, D-15 and requirements R2, R3, R6, R7. Do not introduce new UI/form dependencies.</action>
  <acceptance_criteria>
    - Tests assert required UI copy from `11-UI-SPEC.md`.
    - Tests assert the API key input is empty when a provider has an existing key.
    - Tests assert raw key sample text is absent from rendered output while masked metadata is present.
    - The test command fails in RED because store/components do not exist or behavior is missing, not because of syntax errors.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @paladin/desktop test --run AiProvider ChatAreaProviderCta</automated>
  </verify>
  <done>Frontend RED tests exist for the provider store, settings panel, chat CTA, and AI status-bar entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Desktop UI -> Tauri commands | Untrusted form input and secret replacement values cross from React into Rust commands. |
| Tauri app data -> Agent runtime | Secret-bearing runtime snapshots cross from desktop authority into in-memory Agent state. |
| Sidecar logs -> UI/evidence | Process output crosses into persisted logs, status popovers, diagnostics, and test evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Information Disclosure | Wave 0 secret tests | high | mitigate | RED tests must assert raw API keys never appear in UI, metadata, logs, diagnostics, or evidence. |
| T-11-02 | Tampering | Provider persistence tests | high | mitigate | RED tests must cover idempotent saves, concurrent saves, and dangling active-provider prevention. |
| T-11-03 | Spoofing | Readiness classification tests | medium | mitigate | RED tests must prove AI readiness is separate from Agent process health and Go readiness. |
| T-11-04 | Denial of Service | Agent no-key startup tests | high | mitigate | RED tests must prove no-key startup and concurrent unconfigured probes do not crash Agent. |
| T-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs are planned; if executor introduces one, stop for package legitimacy review. |
</threat_model>

<verification>
Run the three RED commands above. The expected outcome for this Wave 0 plan is syntactically valid tests that fail due missing Phase 11 implementation.
</verification>

<success_criteria>
- Every planned Phase 11 behavior has at least one failing test contract before implementation.
- D-01 through D-15 are represented by test assertions or explicit exclusions.
- Deferred layout/sidebar todos from D-15 are not tested or implemented here.
</success_criteria>

<output>
Create `.planning/phases/11-desktop-ai-provider-configuration/11-01-SUMMARY.md` when done.
</output>
