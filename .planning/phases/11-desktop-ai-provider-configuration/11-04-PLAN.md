---
phase: 11-desktop-ai-provider-configuration
plan: 04
type: execute
wave: 1
depends_on:
  - 11-01
files_modified:
  - apps/desktop/src-tauri/src/process/supervisor.rs
  - apps/desktop/src-tauri/src/process/log_redact.rs
  - apps/desktop/src-tauri/src/process/log_redact_tests.rs
  - scripts/launch-paladin-macos.sh
  - scripts/test-launch-paladin-macos.sh
autonomous: true
requirements:
  - R5
  - R6
requirements_addressed:
  - R5
  - R6
must_haves:
  truths:
    - "R5: sidecar env forwarding and macOS launch wrapper accept PALADIN_AI_* bootstrap variables plus legacy DeepSeek compatibility."
    - "R5 edge empty: empty provider env vars are ignored and do not create blank configured providers."
    - "R6: PALADIN_AI_API_KEY, provider-specific aliases, bearer tokens, and provider diagnostic secret fields are centrally redacted."
    - "R6 edge encoding/idempotency/concurrency: redaction handles special characters, repeated reads stay masked, and log/diagnostic paths use one redaction function before persistence or UI emission."
  artifacts:
    - path: apps/desktop/src-tauri/src/process/log_redact.rs
      provides: "Expanded central redaction vocabulary for AI provider secrets"
      exports: ["SECRET_PATTERNS", "redact_log_line"]
    - path: apps/desktop/src-tauri/src/process/supervisor.rs
      provides: "Explicit sidecar env allowlist with PALADIN_AI_* bootstrap keys"
    - path: scripts/launch-paladin-macos.sh
      provides: "No-key launch wrapper semantics and rejected secret CLI arguments for PALADIN_AI_*"
  key_links:
    - from: "sidecar stdout/stderr"
      to: apps/desktop/src-tauri/src/process/log_redact.rs
      via: "redact_log_line before emit/tail/persist"
      pattern: "redact_log_line"
    - from: scripts/test-launch-paladin-macos.sh
      to: scripts/launch-paladin-macos.sh
      via: "shell wrapper tests prove no-key startup and secret CLI rejection"
      pattern: "PALADIN_AI"
  prohibitions:
    - "MUST NOT block app startup or sidecar liveness solely because an AI provider is absent."
    - "MUST NOT display, log, persist in non-secret metadata, or include raw API key values in evidence."
    - "MUST NOT silently switch the user to a different paid/cloud provider after explicit provider selection."
    - "MUST NOT present missing AI provider as Go DB/Redis readiness failure or Agent process crash."
    - "MUST NOT use fear, blame, or alarmist wording in the missing-provider prompt."
---

<objective>
Expand sidecar env bootstrap and secret redaction for the new provider vocabulary.

Purpose: Phase 11 introduces PALADIN_AI_* configuration and runtime provider diagnostics. These values must be allowed only through explicit sidecar boundaries and must be redacted anywhere logs or diagnostics can surface.
Output: Updated env allowlist, launch wrapper checks, central redaction patterns, and passing Rust/shell tests.
</objective>

## Artifacts this phase produces

- Updated `BUSINESS_ENV_ALLOWLIST` entries for `PALADIN_AI_PROVIDER`, `PALADIN_AI_BASE_URL`, `PALADIN_AI_API_KEY`, and `PALADIN_AI_MODEL`.
- Updated `SECRET_PATTERNS` and regex replacements for AI provider key aliases and diagnostic fields.
- Updated macOS launch wrapper behavior and shell tests for no-key launch and PALADIN_AI secret CLI rejection.

<execution_context>
@/Users/kdocs/.codex/gsd-core/workflows/execute-plan.md
@/Users/kdocs/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-desktop-ai-provider-configuration/11-CONTEXT.md
@.planning/phases/11-desktop-ai-provider-configuration/11-SPEC.md
@.planning/phases/11-desktop-ai-provider-configuration/11-RESEARCH.md
@apps/desktop/src-tauri/src/process/supervisor.rs
@apps/desktop/src-tauri/src/process/log_redact.rs
@scripts/launch-paladin-macos.sh
@scripts/test-launch-paladin-macos.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand central redaction for provider secrets</name>
  <files>apps/desktop/src-tauri/src/process/log_redact.rs, apps/desktop/src-tauri/src/process/log_redact_tests.rs</files>
  <read_first>
    @apps/desktop/src-tauri/src/process/log_redact.rs
    @apps/desktop/src-tauri/src/process/log_redact_tests.rs
    @.planning/phases/11-desktop-ai-provider-configuration/11-SPEC.md
  </read_first>
  <behavior>
    - Raw `PALADIN_AI_API_KEY` values, provider key aliases, bearer tokens, and provider diagnostic key fields are replaced with `[REDACTED]`.
    - Redaction preserves key labels and never exposes raw prefix/suffix or length.
    - Existing JWT/password/legacy DeepSeek redaction still passes.
  </behavior>
  <action>Add tests first for provider secret redaction vocabulary, then update `redact_log_line()` and `SECRET_PATTERNS`. Cover D-13 and R6. Keep the central redaction path; do not add ad hoc caller-side replacements. Include examples for env-style, JSON-style, and diagnostic-style key fields, plus special-character values.</action>
  <acceptance_criteria>
    - Existing redaction tests continue to pass.
    - New tests prove raw PALADIN_AI provider key samples are absent from redacted output.
    - The function remains a pure string transform without tokio/process dependencies.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/desktop/src-tauri &amp;&amp; cargo test log_redact --lib</automated>
  </verify>
  <done>Central redaction covers Phase 11 provider secret vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update sidecar env allowlist and macOS launch wrapper for bootstrap/no-key semantics</name>
  <files>apps/desktop/src-tauri/src/process/supervisor.rs, scripts/launch-paladin-macos.sh, scripts/test-launch-paladin-macos.sh</files>
  <read_first>
    @apps/desktop/src-tauri/src/process/supervisor.rs
    @scripts/launch-paladin-macos.sh
    @scripts/test-launch-paladin-macos.sh
    @.planning/phases/11-desktop-ai-provider-configuration/11-CONTEXT.md
  </read_first>
  <behavior>
    - `environment_for_process()` forwards non-empty PALADIN_AI bootstrap variables through the existing explicit allowlist.
    - Empty PALADIN_AI values are ignored.
    - Launch wrapper no longer treats legacy DeepSeek key absence as a recommended missing item required for AI startup; DB/Redis recommendations remain separate.
    - Secret-valued PALADIN_AI CLI arguments are rejected just like existing secret CLI arguments.
  </behavior>
  <action>Add focused tests for environment forwarding and wrapper argument handling, then update the allowlist and shell script. Preserve Phase 10 packaged env-clear behavior and `PALADIN_RUNTIME_MODE` override. The wrapper must still launch without AI provider env vars so packaged diagnostics can show final app state per R1 and R7.</action>
  <acceptance_criteria>
    - `PALADIN_AI_PROVIDER`, `PALADIN_AI_BASE_URL`, `PALADIN_AI_API_KEY`, and `PALADIN_AI_MODEL` are forwarded only when non-empty.
    - `DEEPSEEK_API_KEY` remains accepted for backwards-compatible bootstrap.
    - Missing AI key is not reported as a hard launch prerequisite.
    - Shell tests reject secret CLI arguments without printing the secret value.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/desktop/src-tauri &amp;&amp; cargo test environment_for_process --lib; scripts/test-launch-paladin-macos.sh</automated>
  </verify>
  <done>Sidecar env forwarding and launch wrapper tests match Phase 11 bootstrap and no-key startup semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parent env -> sidecar env | Environment values cross from user launch context into supervised Agent/Go processes. |
| Sidecar output -> Rust log pipeline | Raw process logs cross into persisted logs and frontend status popovers. |
| CLI args -> wrapper stderr | User-provided arguments can accidentally contain secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04-01 | Information Disclosure | log_redact.rs | high | mitigate | Extend central regex redaction before emit/tail/persist; tests cover PALADIN_AI and provider diagnostic fields. |
| T-11-04-02 | Tampering | supervisor.rs env allowlist | medium | mitigate | Keep explicit non-empty allowlist and forced PALADIN_RUNTIME_MODE; no wildcard PALADIN_AI forwarding beyond locked keys. |
| T-11-04-03 | Information Disclosure | launch-paladin-macos.sh | high | mitigate | Reject secret CLI args and assert wrapper output does not include raw secret samples. |
| T-11-04-04 | Denial of Service | launch wrapper no-key path | medium | mitigate | Missing AI key must not block wrapper launch; tests prove app still executes. |
| T-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs are planned. |
</threat_model>

<verification>
`cd apps/desktop/src-tauri && cargo test log_redact environment_for_process --lib`
`scripts/test-launch-paladin-macos.sh`
</verification>

<success_criteria>
- R5 env bootstrap variables are forwarded through explicit allowlists.
- R6 redaction vocabulary covers all Phase 11 provider secret names and diagnostic forms.
- No-key launch behavior remains non-blocking.
</success_criteria>

<output>
Create `.planning/phases/11-desktop-ai-provider-configuration/11-04-SUMMARY.md` when done.
</output>
