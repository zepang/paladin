---
phase: 11-desktop-ai-provider-configuration
plan: 07
type: execute
wave: 4
depends_on:
  - 11-02
  - 11-06
files_modified:
  - apps/desktop/src/components/ChatArea.tsx
  - apps/desktop/src/components/__tests__/ChatAreaProviderCta.test.tsx
  - apps/desktop/src/components/StatusBar/AiProviderLight.tsx
  - apps/desktop/src/components/StatusBar/__tests__/AiProviderLight.test.tsx
  - apps/desktop/src/components/StatusBar.tsx
  - apps/desktop/src/App.tsx
autonomous: true
requirements:
  - R1
  - R2
  - R4
  - R7
requirements_addressed:
  - R1
  - R2
  - R4
  - R7
must_haves:
  truths:
    - "R1: Agent running with AI readiness unconfigured still mounts the desktop app and does not show an Agent crash state."
    - "R2: chat attempts without usable provider show one actionable CTA in ChatArea."
    - "R2 edge empty/idempotency/concurrency: missing-provider responses update one stable CTA and do not stack toasts or duplicate panels."
    - "R4: provider switching copy and state indicate next request uses refreshed provider while in-flight requests may continue."
    - "R7: StatusBar displays AI provider readiness separately from Agent and Go process lights; DB/Redis degraded state is not labeled as AI provider failure."
  artifacts:
    - path: apps/desktop/src/components/ChatArea.tsx
      provides: "Missing-provider chat CTA"
    - path: apps/desktop/src/components/StatusBar/AiProviderLight.tsx
      provides: "AI readiness status-bar entry and popover"
      exports: ["AiProviderLight"]
    - path: apps/desktop/src/components/StatusBar.tsx
      provides: "StatusBar composition with separate AI provider readiness"
    - path: apps/desktop/src/App.tsx
      provides: "Provider-not-configured error handling and readiness loading"
  key_links:
    - from: apps/desktop/src/App.tsx
      to: apps/desktop/src/stores/aiProvider.ts
      via: "CopilotKit provider-not-configured errors update AI provider store/readiness"
      pattern: "provider-not-configured"
    - from: apps/desktop/src/components/ChatArea.tsx
      to: apps/desktop/src/stores/terminal.ts
      via: "CTA opens RightPanel with activePanel `ai-provider`"
      pattern: "setActivePanel\\('ai-provider'\\)"
    - from: apps/desktop/src/components/StatusBar.tsx
      to: apps/desktop/src/components/StatusBar/AiProviderLight.tsx
      via: "separate AI status entry beside Agent and Go lights"
      pattern: "AiProviderLight"
  prohibitions:
    - "MUST NOT block app startup or sidecar liveness solely because an AI provider is absent."
    - "MUST NOT display, log, persist in non-secret metadata, or include raw API key values in evidence."
    - "MUST NOT silently switch the user to a different paid/cloud provider after explicit provider selection."
    - "MUST NOT present missing AI provider as Go DB/Redis readiness failure or Agent process crash."
    - "MUST NOT use fear, blame, or alarmist wording in the missing-provider prompt."
---

<objective>
Integrate provider readiness into ChatArea, StatusBar, and App-level CopilotKit error handling.

Purpose: The configured-provider capability must be visible and actionable where users encounter it: chat cannot proceed without a provider, and status must separate AI readiness from Agent/Go process health.
Output: Chat CTA, AI Provider status light, App error mapping, and passing UI tests.
</objective>

## Artifacts this phase produces

- New component `AiProviderLight`.
- Chat CTA state in `ChatArea`.
- App-level provider-not-configured handling that updates provider readiness instead of showing a generic Agent failure.
- StatusBar AI readiness entry labels `AI · 未配置`, `AI · 未测试`, `AI · 可用`, and `AI · 无效`.

<execution_context>
@/Users/kdocs/.codex/gsd-core/workflows/execute-plan.md
@/Users/kdocs/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-desktop-ai-provider-configuration/11-CONTEXT.md
@.planning/phases/11-desktop-ai-provider-configuration/11-SPEC.md
@.planning/phases/11-desktop-ai-provider-configuration/11-UI-SPEC.md
@apps/desktop/src/App.tsx
@apps/desktop/src/components/ChatArea.tsx
@apps/desktop/src/components/StatusBar.tsx
@apps/desktop/src/components/StatusBar/ProcessLight.tsx
@apps/desktop/src/stores/aiProvider.ts
@apps/desktop/src/components/__tests__/ChatAreaProviderCta.test.tsx
@apps/desktop/src/components/StatusBar/__tests__/AiProviderLight.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ChatArea configure-provider CTA</name>
  <files>apps/desktop/src/components/ChatArea.tsx, apps/desktop/src/components/__tests__/ChatAreaProviderCta.test.tsx</files>
  <read_first>
    @apps/desktop/src/components/ChatArea.tsx
    @apps/desktop/src/components/__tests__/ChatAreaProviderCta.test.tsx
    @apps/desktop/src/stores/aiProvider.ts
    @apps/desktop/src/stores/terminal.ts
    @.planning/phases/11-desktop-ai-provider-configuration/11-UI-SPEC.md
  </read_first>
  <behavior>
    - When AI readiness is unconfigured or invalid due provider-not-configured response, ChatArea shows `尚未配置 AI provider` and `配置 AI provider`.
    - CTA opens RightPanel and selects `ai-provider`.
    - Repeated missing-provider attempts update the same state without duplicate toasts/panels.
  </behavior>
  <action>Add ChatArea branch driven by `useAiProviderStore` readiness/error state while preserving existing welcome state and CopilotChat layout for configured providers. Use one lucide settings-style icon in the primary CTA. Do not describe missing provider as an Agent crash or Go readiness issue. Do not alter conversation-list/sidebar layout per D-15.</action>
  <acceptance_criteria>
    - CTA copy exactly matches UI-SPEC required copy.
    - Clicking CTA calls terminal/right-panel store to open and select `ai-provider`.
    - Existing no-conversation welcome state remains distinct from no-provider state.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @paladin/desktop test --run ChatAreaProviderCta</automated>
  </verify>
  <done>ChatArea missing-provider CTA passes tests and opens provider settings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add AiProviderLight and StatusBar readiness separation</name>
  <files>apps/desktop/src/components/StatusBar/AiProviderLight.tsx, apps/desktop/src/components/StatusBar/__tests__/AiProviderLight.test.tsx, apps/desktop/src/components/StatusBar.tsx</files>
  <read_first>
    @apps/desktop/src/components/StatusBar.tsx
    @apps/desktop/src/components/StatusBar/ProcessLight.tsx
    @apps/desktop/src/components/StatusBar/__tests__/AiProviderLight.test.tsx
    @apps/desktop/src/components/ui/popover.tsx
    @apps/desktop/src/stores/aiProvider.ts
  </read_first>
  <behavior>
    - StatusBar shows separate AI readiness entry beside Agent and Go lights.
    - Popover shows provider name/model/status and actions `打开设置` and `测试连接` when applicable.
    - Status labels are `AI · 未配置`, `AI · 未测试`, `AI · 可用`, and `AI · 无效`.
  </behavior>
  <action>Create `AiProviderLight` mirroring ProcessLight density while using AI readiness vocabulary and status colors from UI-SPEC. Clicking opens a Popover; `打开设置` opens RightPanel `ai-provider`, and `测试连接` calls the provider store action only when a provider exists. Add it to `StatusBar` without changing Agent/Go `ProcessLight` semantics.</action>
  <acceptance_criteria>
    - Tests prove missing AI provider and Go degraded can be displayed simultaneously with distinct labels.
    - Icon/button has `title` and keyboard-reachable actions.
    - Popover never renders raw API key text.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @paladin/desktop test --run AiProviderLight StatusBar</automated>
  </verify>
  <done>StatusBar AI provider readiness is separate from process readiness and passes tests.</done>
</task>

<task type="auto">
  <name>Task 3: Map provider-not-configured CopilotKit errors into provider readiness state</name>
  <files>apps/desktop/src/App.tsx</files>
  <read_first>
    @apps/desktop/src/App.tsx
    @apps/desktop/src/stores/aiProvider.ts
    @apps/agent/src/server/main.py
    @apps/agent/src/server/provider_routes.py
  </read_first>
  <action>Update `handleCopilotError` or the relevant CopilotKit response handling so structured provider-not-configured responses from Plan 02 update `useAiProviderStore` readiness/error state and avoid a generic Agent failure toast. Keep true connection failures mapped to Agent service copy. On app load, initialize provider store/readiness through Tauri command wrappers after runtime config is available. Preserve existing StartupMask behavior: Agent process not running still blocks, but AI unconfigured does not.</action>
  <acceptance_criteria>
    - Provider-not-configured is displayed through ChatArea CTA rather than generic Agent failed toast.
    - Runtime config loading and CopilotKit provider mounting still work when Agent process is running but AI readiness is unconfigured.
    - Missing AI provider does not alter Go readiness display.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @paladin/desktop test --run ChatAreaProviderCta AiProviderLight; pnpm --filter @paladin/desktop build</automated>
  </verify>
  <done>App-level error/readiness wiring drives provider CTA and status without process-health conflation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent response -> React error handling | Structured provider errors cross from local Agent into app UI state. |
| StatusBar actions -> Tauri commands | User clicks can trigger provider test or settings panel open. |
| Readiness store -> ChatArea | Product state controls whether chat UI or CTA is shown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07-01 | Spoofing | App.tsx / ChatArea.tsx | medium | mitigate | Structured provider-not-configured state maps to AI readiness only, not Agent/Go failure. |
| T-11-07-02 | Information Disclosure | AiProviderLight.tsx | high | mitigate | Popover renders provider metadata and masked key only; tests assert raw key absence. |
| T-11-07-03 | Denial of Service | ChatArea fallback | medium | mitigate | Missing provider shows CTA while leaving Agent/Copilot shell mounted when process is running. |
| T-11-07-04 | Repudiation | user-visible status | low | accept | Local desktop status display is not an audit log; command tests and UI labels provide sufficient ASVS L1 evidence. |
| T-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs are planned. |
</threat_model>

<verification>
`pnpm --filter @paladin/desktop test --run ChatAreaProviderCta AiProviderLight StatusBar`
`pnpm --filter @paladin/desktop build`
</verification>

<success_criteria>
- Chat fallback is actionable, neutral, and non-duplicating.
- StatusBar separates AI readiness from Agent/Go process state.
- App error handling routes provider configuration problems to provider UI, not process failure copy.
</success_criteria>

<output>
Create `.planning/phases/11-desktop-ai-provider-configuration/11-07-SUMMARY.md` when done.
</output>
