---
phase: 11-desktop-ai-provider-configuration
plan: 08
type: execute
wave: 5
depends_on:
  - 11-07
files_modified:
  - README.md
  - docs/packaging.md
  - apps/agent/.env.example
  - apps/agent/config/config.json
  - scripts/test-launch-paladin-macos.sh
  - .planning/phases/11-desktop-ai-provider-configuration/11-VERIFICATION.md
autonomous: true
requirements:
  - R1
  - R2
  - R3
  - R4
  - R5
  - R6
  - R7
requirements_addressed:
  - R1
  - R2
  - R3
  - R4
  - R5
  - R6
  - R7
must_haves:
  truths:
    - "R1: no-key app/Agent/Go startup is documented and smoke-verified."
    - "R2: first chat without provider has an actionable configuration entry."
    - "R3: docs describe DeepSeek, OpenAI-compatible, and LM Studio provider setup in the desktop UI."
    - "R4: saved provider survives restart and hot switching affects the next request without sidecar restart."
    - "R5: PALADIN_AI_* and legacy DeepSeek bootstrap are documented as optional seed paths, not hard startup prerequisites."
    - "R6: final secret/evidence scan proves raw test keys are absent from logs, diagnostics, frontend snapshots, docs examples, and verification evidence."
    - "R7: final smoke matrix separates missing AI provider, invalid AI key, missing DB, missing Redis, and all-configured states."
    - "All 27 covered edges from 11-SPEC are represented in source tests or final verification evidence."
  artifacts:
    - path: README.md
      provides: "Updated no-key startup and runtime provider setup guidance"
    - path: docs/packaging.md
      provides: "Packaged runtime provider configuration and readiness separation docs"
    - path: apps/agent/.env.example
      provides: "Optional PALADIN_AI_* bootstrap examples without hard-key prerequisite wording"
    - path: apps/agent/config/config.json
      provides: "Bundled model config kept as non-authoritative compatibility/default reference"
    - path: .planning/phases/11-desktop-ai-provider-configuration/11-VERIFICATION.md
      provides: "Phase 11 source audit, smoke matrix, edge/prohibition evidence, and final commands"
  key_links:
    - from: docs/packaging.md
      to: scripts/launch-paladin-macos.sh
      via: "packaged launch guidance matches wrapper no-key behavior"
      pattern: "PALADIN_AI"
    - from: .planning/phases/11-desktop-ai-provider-configuration/11-VERIFICATION.md
      to: "Phase 11 implementation/tests"
      via: "final source audit maps GOAL, R1-R7, research constraints, D-01..D-15, edges, and prohibitions to evidence"
      pattern: "R1|D-01|Prohibition"
  prohibitions:
    - "MUST NOT block app startup or sidecar liveness solely because an AI provider is absent."
    - "MUST NOT display, log, persist in non-secret metadata, or include raw API key values in evidence."
    - "MUST NOT silently switch the user to a different paid/cloud provider after explicit provider selection."
    - "MUST NOT present missing AI provider as Go DB/Redis readiness failure or Agent process crash."
    - "MUST NOT use fear, blame, or alarmist wording in the missing-provider prompt."
---

<objective>
Close Phase 11 with documentation, smoke checks, secret scans, and coverage evidence.

Purpose: The implementation spans Python Agent, Rust/Tauri, React, scripts, and docs. The final plan proves the full product outcome and updates public setup guidance so AI keys are no longer presented as startup prerequisites.
Output: Updated docs/examples, final verification artifact, and full focused/full-suite evidence.
</objective>

## Artifacts this phase produces

- Updated no-key startup and runtime provider setup docs in `README.md` and `docs/packaging.md`.
- Updated optional bootstrap examples in `apps/agent/.env.example`.
- Updated `apps/agent/config/config.json` comments/semantics only if needed to clarify it is not the runtime authority.
- New `.planning/phases/11-desktop-ai-provider-configuration/11-VERIFICATION.md`.
- Final smoke matrix for no-key startup, env bootstrap, saved provider reload, DeepSeek/OpenAI-compatible hot switch, invalid provider, missing DB, and missing Redis.

<execution_context>
@/Users/kdocs/.codex/gsd-core/workflows/execute-plan.md
@/Users/kdocs/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-desktop-ai-provider-configuration/11-CONTEXT.md
@.planning/phases/11-desktop-ai-provider-configuration/11-SPEC.md
@.planning/phases/11-desktop-ai-provider-configuration/11-VALIDATION.md
@.planning/phases/11-desktop-ai-provider-configuration/11-UI-SPEC.md
@README.md
@docs/packaging.md
@apps/agent/.env.example
@apps/agent/config/config.json
@scripts/test-launch-paladin-macos.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs and bootstrap examples for runtime provider configuration</name>
  <files>README.md, docs/packaging.md, apps/agent/.env.example, apps/agent/config/config.json</files>
  <read_first>
    @README.md
    @docs/packaging.md
    @apps/agent/.env.example
    @apps/agent/config/config.json
    @.planning/phases/11-desktop-ai-provider-configuration/11-CONTEXT.md
  </read_first>
  <action>Rewrite setup and packaging guidance so `DEEPSEEK_API_KEY` is no longer described as a hard startup prerequisite. Document desktop provider settings as the primary product path. Document optional bootstrap keys `PALADIN_AI_PROVIDER`, `PALADIN_AI_BASE_URL`, `PALADIN_AI_API_KEY`, `PALADIN_AI_MODEL`, and legacy `DEEPSEEK_API_KEY` compatibility. Explain that Agent liveness, AI readiness, and Go DB/Redis readiness are separate states. Preserve D-14 folded todo scope and do not document deferred keychain/cloud account/team policy work as available.</action>
  <acceptance_criteria>
    - Docs say Paladin can start without AI credentials.
    - Docs explain save/test separation and hot switching for the next request.
    - Examples do not include real-looking raw secrets beyond placeholder values.
    - Config documentation does not claim bundled `apps/agent/config/config.json` is the runtime persisted authority.
  </acceptance_criteria>
  <verify>
    <automated>! rg -n "DEEPSEEK_API_KEY.*required|缺少必要配置：DEEPSEEK_API_KEY|hard startup prerequisite" README.md docs/packaging.md apps/agent/.env.example apps/agent/config/config.json; rg -n "PALADIN_AI_PROVIDER|配置 AI provider|AI readiness|未配置" README.md docs/packaging.md apps/agent/.env.example</automated>
  </verify>
  <done>Docs and examples describe runtime provider configuration and no-key startup accurately.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add final smoke and evidence scan coverage</name>
  <files>scripts/test-launch-paladin-macos.sh, .planning/phases/11-desktop-ai-provider-configuration/11-VERIFICATION.md</files>
  <read_first>
    @scripts/test-launch-paladin-macos.sh
    @.planning/phases/11-desktop-ai-provider-configuration/11-VALIDATION.md
    @.planning/phases/11-desktop-ai-provider-configuration/11-SPEC.md
  </read_first>
  <behavior>
    - No-key wrapper/app launch remains non-blocking.
    - Env bootstrap smoke distinguishes PALADIN_AI_* and legacy DeepSeek seed paths.
    - Secret scan uses a raw sentinel key and proves it is absent from logs/docs/evidence while masked metadata remains visible.
    - Final verification artifact maps source audit, requirements, edges, prohibitions, and D-01..D-15 to concrete evidence.
  </behavior>
  <action>Extend shell smoke tests where appropriate and create `11-VERIFICATION.md`. The verification artifact must include a multi-source coverage audit for GOAL, R1-R7, research constraints, and D-01 through D-15; list deferred ideas as exclusions; and include evidence for all 27 edge coverage rows plus all five resolved prohibitions. Include commands and results for focused Python/Rust/frontend tests, full tier suites, launch wrapper test, and a secret sentinel scan. Do not archive `.planning/todos/pending/desktop-ai-provider-configuration.md`; D-14 is represented by SPEC/plans and todo cleanup remains workflow management.</action>
  <acceptance_criteria>
    - `11-VERIFICATION.md` has no missing source-audit rows.
    - `11-VERIFICATION.md` explicitly covers D-01 through D-15.
    - Secret sentinel value is absent from evidence and captured output; masked fingerprint remains allowed.
    - Smoke matrix distinguishes missing AI provider from Go DB/Redis readiness failures.
  </acceptance_criteria>
  <verify>
    <automated>scripts/test-launch-paladin-macos.sh; test -f .planning/phases/11-desktop-ai-provider-configuration/11-VERIFICATION.md; rg -n "D-01|D-15|R1|R7|Prohibition|provider-not-configured|PALADIN_AI" .planning/phases/11-desktop-ai-provider-configuration/11-VERIFICATION.md</automated>
  </verify>
  <done>Final verification artifact and smoke coverage prove the phase outcome and source coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Run final focused and full-suite gates</name>
  <files>.planning/phases/11-desktop-ai-provider-configuration/11-VERIFICATION.md</files>
  <read_first>
    @.planning/phases/11-desktop-ai-provider-configuration/11-VALIDATION.md
    @.planning/phases/11-desktop-ai-provider-configuration/11-VERIFICATION.md
  </read_first>
  <action>Run the final gates from `11-VALIDATION.md`: focused Agent provider tests, focused Rust ai_provider/log_redact tests, focused frontend AiProvider/ChatArea/StatusBar tests, then full Agent pytest, full Rust cargo test, and full desktop Vitest. Append exact command outcomes to `11-VERIFICATION.md`. If an unrelated pre-existing failure appears, document the file/test and isolate focused Phase 11 evidence before deciding whether to fix it.</action>
  <acceptance_criteria>
    - Focused gates for Agent, Rust, frontend, wrapper, and secret scan pass.
    - Full suite outcomes are recorded with exact commands.
    - Verification artifact lists any residual risk and does not claim Windows installed UAT or unrelated Phase 10.1 work.
  </acceptance_criteria>
  <verify>
    <automated>(cd apps/agent &amp;&amp; uv run pytest tests/test_provider_runtime.py tests/test_server.py -x); (cd apps/desktop/src-tauri &amp;&amp; cargo test ai_provider --lib &amp;&amp; cargo test log_redact --lib); pnpm --filter @paladin/desktop test --run AiProvider ChatAreaProviderCta AiProviderLight; (cd apps/agent &amp;&amp; uv run pytest); (cd apps/desktop/src-tauri &amp;&amp; cargo test); pnpm --filter @paladin/desktop test --run</automated>
  </verify>
  <done>Final focused/full-suite evidence is recorded and Phase 11 is ready for verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> user setup | Docs influence how users provide secrets and interpret readiness states. |
| Test evidence -> repository | Verification artifacts can accidentally persist raw sentinel secrets. |
| Runtime smoke -> product readiness | Cross-tier smoke evidence determines whether state separation is true. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-08-01 | Information Disclosure | docs/evidence | high | mitigate | Use placeholder/sentinel scanning and record only masked metadata in verification. |
| T-11-08-02 | Spoofing | docs/verification | medium | mitigate | Document AI readiness separately from Agent process and Go DB/Redis readiness; final matrix proves distinctions. |
| T-11-08-03 | Repudiation | 11-VERIFICATION.md | medium | mitigate | Record exact commands and outcomes for source audit, edges, prohibitions, and final gates. |
| T-11-08-04 | Tampering | bootstrap docs | medium | mitigate | Docs state env bootstrap is seed/compatibility path and saved local config wins after explicit selection. |
| T-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs are planned. |
</threat_model>

<verification>
Run Task 3 final commands and append results to `11-VERIFICATION.md`.
</verification>

<success_criteria>
- Public docs no longer present DeepSeek env key as a hard startup prerequisite.
- Final evidence proves no-key startup, provider config UI, env bootstrap, hot switching, secret safety, and readiness separation.
- Multi-source audit covers GOAL, R1-R7, RESEARCH constraints, D-01..D-15, 27 edges, and 5 prohibitions with no missing rows.
</success_criteria>

<output>
Create `.planning/phases/11-desktop-ai-provider-configuration/11-08-SUMMARY.md` when done.
</output>
