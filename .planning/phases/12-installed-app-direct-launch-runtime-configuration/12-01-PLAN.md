---
phase: 12-installed-app-direct-launch-runtime-configuration
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src-tauri/src/lib.rs
  - apps/desktop/src-tauri/src/go_config/storage_tests.rs
  - apps/desktop/src-tauri/src/go_config/bootstrap_tests.rs
  - apps/desktop/src-tauri/src/process/supervisor_tests.rs
  - apps/desktop/src-tauri/src/process/log_redact_tests.rs
  - apps/server/cmd/server/main_test.go
  - apps/desktop/src/stores/__tests__/goService.test.ts
  - apps/desktop/src/components/go-service/__tests__/GoServicePanel.test.tsx
  - apps/desktop/src/components/StatusBar/__tests__/GoServiceLight.test.tsx
  - apps/desktop/scripts/direct-launch-evidence.test.mjs
autonomous: true
requirements: [SDC-01, SDC-02, SDC-03, PKG-01, PKG-02, PKG-03]
requirements_addressed: [SDC-01, SDC-02, SDC-03, PKG-01, PKG-02, PKG-03]
must_haves:
  truths:
    - "D-02: all configuration reads, command results, rendered DOM, diagnostics, logs, and evidence are sentinel-free while write inputs can contain the three Go values."
    - "D-05/D-06/D-08: only a complete valid DB/Redis/JWT set can bootstrap or import; partial input remains diagnosed and clear does not resurrect configuration."
    - "D-07: an ordinary parent environment cannot override saved Go config; only an explicit dev/CI/UAT marker permits a nonpersistent session override."
    - "D-03/SDC-03: saved-but-offline configuration and readiness test results are independently observable; Go degraded state does not block Agent/UI."
    - "D-09--D-12: evidence schema records launcher class and platform status without secret-bearing data."
  artifacts:
    - path: apps/desktop/src-tauri/src/go_config/storage_tests.rs
      provides: "RED contracts for complete write-only persistence, masking, atomic/concurrent saves, clear, and sentinel exclusion."
    - path: apps/desktop/src-tauri/src/go_config/bootstrap_tests.rs
      provides: "RED contracts for one-time complete import and marker-bound session override precedence."
    - path: apps/desktop/src/components/go-service/__tests__/GoServicePanel.test.tsx
      provides: "RED UI contract for form, import, clear, inline result, focus, accessibility, and no-secret DOM output."
    - path: apps/desktop/scripts/direct-launch-evidence.test.mjs
      provides: "RED secret-free structured platform-evidence schema tests."
  key_links:
    - from: GoConfigManager read commands
      to: React masked DTO/store
      via: "sentinel assertions over serialization and rendered output"
      pattern: "configured|fingerprint|field_diagnostics"
    - from: ProcessSupervisor environment construction
      to: packaged Go sidecar
      via: "normal persisted snapshot or marker-bound session override"
      pattern: "PALADIN_GO_SESSION_OVERRIDE"
  prohibitions:
    - "Do not add dependencies or use real PostgreSQL, Redis, or credentials in tests."
    - "Do not test normal installed launch as wrapper/environment injection."
    - "Do not make Go degraded state a StartupMask condition or an Agent failure."
---

<objective>
Create Wave 0 RED contracts for every newly required persistence, runtime, diagnostic, UI, and release-evidence behavior.

Purpose: Phase 12 changes a secret boundary and direct-launch semantics. TDD must make all write-only, precedence, liveness/readiness, and platform-status behavior observable before implementation.
Output: Compiling, intentionally failing focused tests that name the public contracts implementation must satisfy.
</objective>

## Artifacts this phase produces

- RED Rust modules for `GoConfigManager`, `MaskedGoServiceConfig`, `GoRuntimeSnapshot`, bootstrap/import/clear, supervisor selection, and source redaction.
- RED Go readiness tests and frontend store/panel/status tests.
- RED Node tests for a secret-free direct-launch evidence schema and required target scanner.

<context>
@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-CONTEXT.md
@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-RESEARCH.md
@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-VALIDATION.md
@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-UI-SPEC.md
@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-PATTERNS.md
@apps/desktop/src-tauri/src/ai_provider/storage_tests.rs
@apps/desktop/src-tauri/src/ai_provider/bootstrap_tests.rs
@apps/desktop/src-tauri/src/process/supervisor_tests.rs
@apps/desktop/src/components/provider/__tests__/AiProviderPanel.test.tsx
</context>

<tasks>
<task type="auto" tdd="true">
<name>Task 1: Write Rust RED contracts for app-data authority and spawn precedence</name>
<files>apps/desktop/src-tauri/src/lib.rs, apps/desktop/src-tauri/src/go_config/storage_tests.rs, apps/desktop/src-tauri/src/go_config/bootstrap_tests.rs, apps/desktop/src-tauri/src/process/supervisor_tests.rs, apps/desktop/src-tauri/src/process/log_redact_tests.rs</files>
<read_first>@apps/desktop/src-tauri/src/ai_provider/{types,storage,bootstrap,storage_tests,bootstrap_tests}.rs
@apps/desktop/src-tauri/src/process/{supervisor,supervisor_tests,log_redact,log_redact_tests}.rs
@apps/desktop/src-tauri/src/lib.rs
@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-CONTEXT.md</read_first>
<behavior>- Complete valid DB/Redis/JWT saves serialize metadata and secrets separately, return only presence/provenance/fixed fingerprint/field diagnostics, and survive overlapping saves.
- Empty/partial/invalid environment never persists a partial record; clear marks local configuration handled and a later normal bootstrap stays unconfigured.
- Normal packaged environment uses the persisted complete snapshot instead of parent Go values; only a nonsecret explicit marker permits a session-only override and it never changes files.
- Redaction removes DB DSN, Redis URL, JWT, API-key/token sentinels before logs, errors, DTO serialization, and evidence-facing strings.</behavior>
<action>Create `go_config` test modules and wire `mod go_config;` plus its `#[cfg(test)]` modules into the crate so focused tests discover them. Use temporary app-data directories and unique sentinels; tests must assert that metadata, JSON serialization, returned error text, and redacted log output omit those sentinels. Add only test seams for the planned manager/environment selector, not production implementation. Cite D-02, D-04, D-05, D-06, D-07, and D-08 in test names/comments through behavior intent. Commit this task independently immediately after its own RED gate; do not combine it with Go, frontend, or evidence test changes.</action>
<acceptance_criteria>- `cargo test --lib go_config::` and `cargo test --lib process::` each discover the intended new modules.
- Tests cover complete save, concurrent save, masked readback, explicit import, partial rejection, clear-no-reimport, ordinary parent-env rejection, marker override, and redaction.
- RED failure is caused by missing Phase 12 symbols/behavior, never a syntax or fixture failure.</acceptance_criteria>
<verify><automated>cd apps/desktop/src-tauri &amp;&amp; set +e; cargo test --lib go_config::; go_config_status=$?; cargo test --lib process::; process_status=$?; set -e; test "$go_config_status" -ne 0 &amp;&amp; test "$process_status" -ne 0</automated></verify>
<done>Rust test contracts fail only because the Phase 12 manager and selection behavior are absent.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Write Go and frontend RED contracts for structured nonblocking diagnostics</name>
<files>apps/server/cmd/server/main_test.go, apps/desktop/src/stores/__tests__/goService.test.ts, apps/desktop/src/components/go-service/__tests__/GoServicePanel.test.tsx, apps/desktop/src/components/StatusBar/__tests__/GoServiceLight.test.tsx</files>
<read_first>@apps/server/cmd/server/{main.go,main_test.go}
@apps/desktop/src/stores/aiProvider.ts
@apps/desktop/src/components/provider/{AiProviderPanel.tsx,__tests__/AiProviderPanel.test.tsx}
@apps/desktop/src/components/StatusBar/{ProcessLight.tsx,AiProviderLight.tsx,__tests__/ProcessLight.test.tsx}
@apps/desktop/src/components/{StatusBar.tsx,StartupMask.tsx,__tests__/StartupMask.test.tsx}
@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-UI-SPEC.md</read_first>
<behavior>- Packaged Go process does not load repository dotenv and reports field-only missing/invalid/readiness categories without reflecting inputs.
- Store holds masked DTO and operation state only; form secrets stay component-local; save can succeed while test/readiness fails without persisting a test-only draft.
- UI renders all required configuration/readiness/owner categories, Agent-continues wording, separate AI light, inline results, aria/focus behavior, and disabled external restart.
- No rendered text, accessible name, toast, diagnostic copy, or test snapshot contains sentinels or reversible secret detail.</behavior>
<action>Add focused Vitest/Testing Library tests with Tauri invoke mocked at the command module boundary and Go table tests using sentinel environment values. Name expected DTO enums and component/store symbols that Wave 2 will implement. Include 300px layout-safe interaction checks, first-invalid focus after failed submit, AlertDialog clear confirmation, and `StartupMask` absence when only Go is degraded. Cover D-01, D-02, D-03, D-06, D-08, and UI-SPEC accessibility/security acceptance without introducing a form library. Commit this task independently after its own Go/frontend RED gate; do not combine it with Rust or evidence test changes.</action>
<acceptance_criteria>- Tests assert exact required Chinese status/action copy for unconfigured, incomplete, session override, ready, dependency degraded, port conflict, and sidecar failure.
- Tests assert `保存配置` is enabled only for complete locally valid values and test/import/clear remain distinct operations.
- Tests assert Go and AI lights are independently visible and an Agent-running Go degradation has no blocking mask.
- Focused Go and desktop commands fail RED only for unimplemented contracts.</acceptance_criteria>
<verify><automated>cd apps/server &amp;&amp; set +e; go test ./cmd/server -run 'GoConfig|Ready'; go_status=$?; cd ../..; pnpm --filter @paladin/desktop test --run GoService; ui_status=$?; test "$go_status" -ne 0 -o "$ui_status" -ne 0</automated></verify>
<done>Go and frontend RED tests express the complete structured diagnostic and write-only UI contract.</done>
</task>

<task type="auto" tdd="true">
<name>Task 3: Write RED contracts for evidence and platform status</name>
<files>apps/desktop/scripts/direct-launch-evidence.test.mjs</files>
<read_first>@apps/desktop/scripts/{secret-sentinel.mjs,secret-sentinel.test.mjs,build-manifest.mjs}
@scripts/{launch-paladin-macos.sh,test-launch-paladin-macos.sh}
@.planning/phases/10-packaging/evidence/macos-build.json
@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-VALIDATION.md</read_first>
<behavior>- Structured record requires version/commit/artifact identity, launcher entry point, scenario result, diagnostic class, manual-UAT state, and release-ready state.
- The recorder/scanner rejects raw DB/Redis/JWT/API-key/token sentinels and missing/empty required targets without printing the sentinel.
- Windows/Linux automation records manual UAT pending and release-ready false; macOS Finder records are distinguishable from wrapper diagnostic runs.</behavior>
<action>Create Node built-in test cases for the planned evidence helper using temporary fixtures. Define explicit launcher classes for Finder, Start Menu/Explorer, Linux desktop/AppImage, and wrapper diagnostic/UAT. Require each evidence writer to scan serialized JSON and collected diagnostics before output. Cite D-09 through D-12 and do not produce actual evidence in Wave 0. Commit this task independently after its own Node RED gate; do not combine it with Rust, Go, or frontend test changes.</action>
<acceptance_criteria>- Tests include positive records for three macOS scenarios and Windows/Linux automated records, plus failing sentinel/missing-field fixtures.
- Test output reports only category/path/schema failure, not the injected value.
- The command is RED due to absent helper implementation.</acceptance_criteria>
<verify><automated>set +e; node --test apps/desktop/scripts/direct-launch-evidence.test.mjs; status=$?; set -e; test "$status" -ne 0</automated></verify>
<done>Evidence/schema RED tests cover direct-launch truthfulness and secrecy before release tooling changes.</done>
</task>
</tasks>

<threat_model>
| Threat | Severity | Mitigation and verification |
|---|---|---|
| DSN/JWT/API key disclosure across DTO, logs, UI, snapshots, evidence | high | Sentinel RED assertions cover metadata, serialization, redactor, DOM/aria/toast, and evidence scanner. |
| Parent environment tampering | high | Precedence tests require persisted snapshot for normal launch and reject override without marker. |
| Raw secret readback | high | Tests expect masked DTO only; runtime snapshot has no command/UI serialization seam. |
| Sidecar inherited environment | high | Supervisor tests require `env_clear` plus selected explicit snapshot. |
| Go fault blocks Agent/UI | high | Go/frontend tests require health/readiness separation, independent lights, and no StartupMask. |
| ASVS V2 authentication secret misuse | high | Task 1 RED tests require JWT minimum-validity rejection and write-only/masked readback; Task 2 sentinel tests reject JWT reflection; GREEN evidence is the two focused Rust/Go commands. |
| ASVS V3 session override persistence | high | Task 1 RED tests require an explicitly marked override to remain process-only; later bootstrap/supervisor GREEN tests prove no override value reaches app-data. |
| ASVS V4 private snapshot or external-owner bypass | high | Task 1 RED tests require a nonserializable Rust snapshot and owner-gated lifecycle DTO; Task 2/4 later assert external restart is unavailable. |
| ASVS V5 partial input acceptance | high | Task 1 RED tests require all-or-nothing DB/Redis/JWT validation and no partial import; Task 2 UI tests require per-field errors and no partial save. |
</threat_model>

<verification>Run each RED command above; all must be syntactically valid and fail only for missing Phase 12 implementation.</verification>
<success_criteria>Validation Wave 0 covers every row marked missing in `12-VALIDATION.md`, with no three-task gap in automated feedback.</success_criteria>
<output>Create `12-01-SUMMARY.md` after execution, recording RED commands and no secret values.</output>
