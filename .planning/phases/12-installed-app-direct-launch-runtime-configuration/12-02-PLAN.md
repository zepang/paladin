---
phase: 12-installed-app-direct-launch-runtime-configuration
plan: "02"
type: tdd
wave: 2
depends_on: ["12-01"]
files_modified:
  - apps/desktop/src-tauri/src/go_config/types.rs
  - apps/desktop/src-tauri/src/go_config/storage.rs
  - apps/desktop/src-tauri/src/go_config/bootstrap.rs
  - apps/desktop/src-tauri/src/go_config/mod.rs
  - apps/desktop/src-tauri/src/go_config/storage_tests.rs
  - apps/desktop/src-tauri/src/go_config/bootstrap_tests.rs
  - apps/desktop/src-tauri/src/lib.rs
autonomous: true
requirements: [SDC-02, SDC-03]
requirements_addressed: [SDC-02, SDC-03]
must_haves:
  truths:
    - "D-02/D-04: app-data manager owns Go persistence; only an in-Rust runtime snapshot carries DB URL, Redis URL, and JWT."
    - "D-03: save only persists; an already running Go child keeps its spawn-time snapshot until an explicit managed restart, while retry only probes the current child."
    - "D-05--D-08: complete valid bootstrap/import is one-shot; clear prevents silent re-import; session override is nonpersistent."
  artifacts:
    - path: apps/desktop/src-tauri/src/go_config/types.rs
      provides: "Write input, masked DTO, field diagnosis, provenance, readiness result, and nonserializable runtime snapshot types."
      exports: [SaveGoServiceInput, MaskedGoServiceConfig, GoFieldDiagnostic, GoConfigProvenance, GoRuntimeSnapshot]
    - path: apps/desktop/src-tauri/src/go_config/storage.rs
      provides: "Mutex-serialized app-data metadata/secret persistence with atomic writes and masked loading."
      exports: [GoConfigManager]
    - path: apps/desktop/src-tauri/src/go_config/bootstrap.rs
      provides: "Complete-set bootstrap/import and marker-gated session selection."
  key_links:
    - from: GoConfigManager.runtime_snapshot
      to: ProcessSupervisor
      via: "Rust-only complete selected snapshot"
      pattern: "GoRuntimeSnapshot"
  prohibitions:
    - "No Stronghold, keychain, vault, database, or new package."
    - "No metadata field, serde response, or error may expose a raw/derived secret value."
---

<objective>
Implement the Rust Go configuration authority by taking Wave 0 manager tests through RED, GREEN, and REFACTOR.

Purpose: normal installed launch needs a protected local source independent of parent GUI-shell environment.
Output: registered `GoConfigManager` with safe masked read models and Rust-only runtime selection primitives.
</objective>

## Artifacts this phase produces

- `go_config` module with `GoConfigManager`, `MaskedGoServiceConfig`, per-field diagnostics, provenance, and `GoRuntimeSnapshot`.
- Atomic metadata/secrets files in app-data, serialized by manager mutex.
- Bootstrap/import/clear/session-override policy with explicit marker gate.

<context>@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-{CONTEXT,RESEARCH,PATTERNS,VALIDATION}.md
@apps/desktop/src-tauri/src/ai_provider/{types,storage,bootstrap,mod}.rs
@apps/desktop/src-tauri/src/lib.rs</context>

<tasks>
<task type="tdd">
<name>Task 1: RED→GREEN→REFACTOR Go config types and durable write-only storage</name>
<files>apps/desktop/src-tauri/src/go_config/types.rs, apps/desktop/src-tauri/src/go_config/storage.rs, apps/desktop/src-tauri/src/go_config/storage_tests.rs</files>
<read_first>@apps/desktop/src-tauri/src/ai_provider/{types,storage,storage_tests}.rs
@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-RESEARCH.md
@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-UI-SPEC.md</read_first>
<behavior>- Valid save requires nonempty locally valid DB URL, Redis URL, and JWT; it persists all three atomically, writes no secret to metadata, and returns fixed opaque fingerprints/presence only.
- Existing secrets are replace-by-reentry only; masked load never includes secret length/prefix/suffix.
- Concurrent saves leave parseable paired files and a deterministic latest complete record; clear removes secrets and leaves explicit no-auto-import state.</behavior>
<action>Run the existing Wave 0 storage tests RED first. Implement the Phase 11 two-file manager pattern with `tokio::sync::Mutex`, temp-file-plus-rename writes, validation functions, and a single nonserializable `GoRuntimeSnapshot`. Make fingerprints fixed opaque identifiers derived from stored configuration identity rather than secret content. Refactor only after the focused tests are green; retain sentinel assertions in the test suite. Implement D-02, D-04, D-06, and D-08 exactly.</action>
<acceptance_criteria>- All storage tests pass and no public masked type contains DB URL, Redis URL, JWT, size, prefix, suffix, or reversible masking fields.
- Metadata/secrets writes are atomic under a per-manager serialized lock.
- Clear yields `unconfigured` and records enough nonsecret state to block automatic bootstrap resurrection.</acceptance_criteria>
<verify><automated>cd apps/desktop/src-tauri &amp;&amp; cargo test --lib go_config::storage_tests</automated></verify>
<done>Write-only app-data storage passes GREEN tests and has a completed REFACTOR pass.</done>
</task>
<task type="tdd">
<name>Task 2: RED→GREEN→REFACTOR complete import and session selection policy</name>
<files>apps/desktop/src-tauri/src/go_config/bootstrap.rs, apps/desktop/src-tauri/src/go_config/bootstrap_tests.rs, apps/desktop/src-tauri/src/go_config/mod.rs, apps/desktop/src-tauri/src/lib.rs</files>
<read_first>@apps/desktop/src-tauri/src/ai_provider/{bootstrap.rs,bootstrap_tests.rs,mod.rs}
@apps/desktop/src-tauri/src/process/supervisor.rs
@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-CONTEXT.md</read_first>
<behavior>- Empty app-data imports only a complete valid environment group once and persists it with import provenance.
- Partial/invalid group produces per-field diagnostics and no disk mutation.
- Explicit import uses the same complete-set rule; user saved configuration wins thereafter.
- A marker-gated development/CI/UAT override changes only the launch snapshot and is never persisted; normal parent environment cannot supersede local state.</behavior>
<action>Start from failing bootstrap tests. Implement one named, nonsecret marker policy and central selection function; allow only dev/CI/UAT builds/harnesses to set it, fail closed otherwise. Register `GoConfigManager` in Tauri setup once using app-data before commands/supervisor consume it. Define the D-03 apply contract here: save/import/clear only changes durable authority and returns `pending_apply` when a Go child is already running; it never mutates that child environment. `retry_readiness` probes the running child only; a managed restart is the sole action that spawns with the new snapshot, and an external owner cannot be restarted. Do not merge partial env values with app-data, do not auto-import after clear, and do not serialize selected snapshots. Implement D-03, D-05, D-06, D-07, and D-08.</action>
<acceptance_criteria>- Tests prove bootstrap/import/clear provenance and session override have distinct states, plus a saved changed record yields `pending_apply` rather than changing an existing child snapshot.
- Normal mode reads persisted complete config only; an unrelated parent shell cannot alter the chosen DB/Redis/JWT set.
- `lib.rs` manages one Go manager without changing packaged resource resolution.</acceptance_criteria>
<verify><automated>cd apps/desktop/src-tauri &amp;&amp; cargo test --lib go_config::bootstrap_tests</automated></verify>
<done>Configuration provenance and runtime-selection policy pass GREEN tests after RED/GREEN/REFACTOR cycle.</done>
</task>
</tasks>

<threat_model>| Threat | Severity | Mitigation and verification |
|---|---|---|
| Raw secret readback or storage disclosure | high | Split secret/metadata files, opaque fingerprints, nonserializable snapshot, sentinel tests. |
| Parent env tampering | high | Central fail-closed marker selection tests. |
| Partial configuration persistence | high | All-or-nothing validation and atomic paired writes. |
| Sidecar inheritance | high | Expose only selected snapshot for next plan's explicit supervisor injection. |
| Go fault impacts Agent | high | Manager represents configuration/readiness separately; it owns no Agent lifecycle gate. |
| ASVS V2 JWT validity/write-only boundary | high | Task 1 RED→GREEN→REFACTOR validates JWT with the complete trio and proves masked public types; `cargo test --lib go_config::storage_tests` is the evidence gate. |
| ASVS V3 session isolation | high | Task 2 RED→GREEN→REFACTOR requires marker-only process override, no persistence, and saved `pending_apply`; `cargo test --lib go_config::bootstrap_tests` proves it. |
| ASVS V4 Rust-private authority | high | Task 1 keeps `GoRuntimeSnapshot` nonserializable and Task 2 exposes only authority/provenance; their sentinel tests reject command/read-model leakage. |
| ASVS V5 complete-set validation | high | Task 1 rejects partial saves; Task 2 rejects partial bootstrap/import with per-field diagnosis and no disk mutation. |</threat_model>
<verification>`cd apps/desktop/src-tauri && cargo test --lib go_config::`.</verification>
<success_criteria>All Wave 0 Go manager tests turn green without external dependencies or raw secret-bearing APIs.</success_criteria>
<output>Create `12-02-SUMMARY.md` with RED/GREEN/REFACTOR commits and focused output.</output>
