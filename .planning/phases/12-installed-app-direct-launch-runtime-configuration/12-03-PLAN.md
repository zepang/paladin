---
phase: 12-installed-app-direct-launch-runtime-configuration
plan: "03"
type: tdd
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - apps/desktop/src-tauri/src/process/supervisor.rs
  - apps/desktop/src-tauri/src/process/supervisor_tests.rs
  - apps/desktop/src-tauri/src/process/log_redact.rs
  - apps/desktop/src-tauri/src/process/log_redact_tests.rs
  - apps/desktop/src-tauri/src/process/commands.rs
  - apps/server/cmd/server/main.go
  - apps/server/cmd/server/main_test.go
autonomous: true
requirements: [SDC-01, SDC-02, SDC-03]
requirements_addressed: [SDC-01, SDC-02, SDC-03]
must_haves:
  truths:
    - "D-07: supervisor builds Go sidecar env from selected Rust snapshot, retains `env_clear`, and preserves runtime-mode overwrite."
    - "D-03/SDC-03: liveness, readiness, configuration-field failure, port conflict, sidecar failure, owner, and pending-apply are structured separately."
    - "D-02/D-04: every sidecar diagnostic is redacted before event, tail, disk, command result, or UI/evidence consumer."
    - "Go readiness failure remains nonblocking while Agent/UI continue."
  artifacts:
    - path: apps/desktop/src-tauri/src/process/supervisor.rs
      provides: "Selected Go snapshot injection and structured classification over existing owner/state/health tuple."
    - path: apps/desktop/src-tauri/src/process/log_redact.rs
      provides: "Go DSN/Redis/JWT-aware source redaction."
    - path: apps/server/cmd/server/main.go
      provides: "Secret-free missing/invalid/dependency readiness categories while preserving packaged dotenv prohibition."
  key_links:
    - from: GoConfigManager runtime selection
      to: ProcessSupervisor environment_for_process
      via: "explicit allowlisted Go snapshot after env_clear"
      pattern: "DATABASE_URL|REDIS_URL|PALADIN_JWT_SECRET"
    - from: Go readyz categories
      to: status DTO
      via: "structured diagnostics, never error-string parsing"
      pattern: "dependency-unavailable|failed"
  prohibitions:
    - "Do not introduce a second Go runner, parent-env merge, or raw stderr display path."
    - "Do not change external owner semantics: redetect is permitted; restart/stop are not."
---

<objective>Integrate selected Go configuration into the existing secure sidecar path and make its diagnostics structured, redacted, and nonblocking.</objective>

## Artifacts this phase produces

- Supervisor integration that injects only selected complete Go values to the server sidecar after `env_clear`.
- Central redactor coverage for Go configuration values and nonsecret diagnostic categories.
- Go server readiness contract preserving `/healthz` liveness and packaged dotenv prohibition.

<context>@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-{CONTEXT,RESEARCH,PATTERNS,VALIDATION,UI-SPEC}.md
@apps/desktop/src-tauri/src/{go_config/mod.rs,process/supervisor.rs,process/state_machine.rs,process/log_redact.rs}
@apps/server/cmd/server/{main.go,main_test.go}</context>
<tasks>
<task type="tdd"><name>Task 1: RED→GREEN→REFACTOR secure selected-snapshot supervisor integration</name><files>apps/desktop/src-tauri/src/process/supervisor.rs, apps/desktop/src-tauri/src/process/supervisor_tests.rs, apps/desktop/src-tauri/src/process/commands.rs</files><read_first>@apps/desktop/src-tauri/src/process/{supervisor.rs,supervisor_tests.rs,commands.rs,state_machine.rs}
@apps/desktop/src-tauri/src/go_config/{mod.rs,types.rs,storage.rs,bootstrap.rs}</read_first><behavior>- Normal launch inserts only persisted complete Go snapshot values and ignores parent Go values.
- Marker-gated session snapshot wins only for that spawned child and does not alter manager state.
- `PALADIN_RUNTIME_MODE` remains supervisor-owned; system allowlist stays separate; server owner controls restart/redetect permissions.
- Missing/invalid config is a structured nonblocking diagnostic rather than spawn-time Agent failure.
- Save/import/clear never changes an already running child. Retry probes that child unchanged; only an explicit managed restart selects the newly saved snapshot, while external owner reports restart unavailable.</behavior><action>Run Wave 0 supervisor tests RED, then pass selected configuration through the existing `environment_for_process`/spawn path without weakening `env_clear`. Add a narrow manager/supervisor seam instead of reading app-data in React or Go. Preserve existing Agent startup sequencing and existing external owner guard. Emit explicit config/readiness DTO category fields, `pending_apply`, and allowed lifecycle actions for frontend consumption; do not infer them from `last_error`. Test a running child before/after save, retry, managed restart, and an external restart request. Implement D-03, D-07 and SDC-01--03.</action><acceptance_criteria>- Focused tests prove parent Go values cannot override persisted normal selection and marker override is process-only.
- Tests prove server restart does not occur merely because readiness is degraded.
- Tests prove save is persist-only, retry is non-mutating, managed restart applies the new snapshot, and external restart is denied without configuration disclosure.
- Existing runtime-mode and external-owner test cases remain green.</acceptance_criteria><verify><automated>cd apps/desktop/src-tauri &amp;&amp; cargo test --lib process::supervisor_tests</automated></verify><done>Existing supervisor owns selected snapshot injection without inherited secret configuration.</done></task>
<task type="tdd"><name>Task 2: RED→GREEN→REFACTOR secret-free Go readiness and diagnostics</name><files>apps/desktop/src-tauri/src/process/log_redact.rs, apps/desktop/src-tauri/src/process/log_redact_tests.rs, apps/server/cmd/server/main.go, apps/server/cmd/server/main_test.go</files><read_first>@apps/desktop/src-tauri/src/process/{log_redact.rs,log_redact_tests.rs,supervisor.rs}
@apps/server/cmd/server/{main.go,main_test.go}
@apps/server/.env.example</read_first><behavior>- Any raw Go configuration/token value in sidecar output/error is redacted before it reaches memory tail, disk, process event, or command response.
- Packaged mode ignores dotenv; `/healthz` stays live while `/readyz` distinguishes missing/invalid DB/Redis/JWT and unavailable dependencies without reflected values.
- Agent liveness remains independent when Go is stopped, conflict, unhealthy, or readiness-degraded.</behavior><action>Run Go/Rust RED tests before production change. Extend the existing source redactor with runtime values and safe pattern classes, then ensure its invocation remains before event/tail/disk fan-out. Update Go diagnostic structs/routes only as necessary to return fixed categories and field names; retain dev convenience behavior and packaged dotenv guard. Test every sentinel across redactor, server response, and captured diagnostic serialization. Implement D-02, D-04, D-06 and UI-SPEC diagnostic classifications.</action><acceptance_criteria>- No sentinel appears in redactor output, Go HTTP bodies, Rust process DTOs, or test evidence fixtures.
- Packaged dotenv lure test remains green; liveness/readiness outcomes match the stated nonblocking contract.
- Diagnostics identify field/category, not DSN/Redis/JWT content or shell command/path.</acceptance_criteria><verify><automated>cd apps/desktop/src-tauri &amp;&amp; cargo test --lib process::log_redact_tests; cd ../../server &amp;&amp; go test ./cmd/server</automated></verify><done>Go health/readiness and all desktop diagnostic fan-out surfaces are safely classified and redacted.</done></task>
</tasks>
<threat_model>| Threat | Severity | Mitigation and verification |
|---|---|---|
| Sidecar environment inherits malicious parent values | high | `env_clear` retained; selected snapshot precedence tests. |
| DSN/JWT/API key leaks via stderr/readiness | high | source redaction plus sentinel Go/Rust tests. |
| Go failure masks Agent/UI | high | health/readiness tuple tests and no-restart/no-Agent-gate assertions. |
| External process control | high | retain owner-dependent restart/stop disablement and tests. |
| Diagnostic spoofing by error text | high | structured categories supplied from backend rather than frontend string parsing. |
| ASVS V2 JWT handling | high | Task 2 tests minimum-valid/missing JWT categories plus redaction; `go test ./cmd/server` and redactor tests are evidence. |
| ASVS V3 override isolation | high | Task 1 tests marker-bound spawn-only selection and retains save/retry/restart separation. |
| ASVS V4 authorization/private snapshot | high | Task 1 permits snapshot use only in Rust supervisor and emits owner-gated restart actions; external restart denial is tested. |
| ASVS V5 full-input validation | high | Task 2 tests field-specific complete-set categories without partial runtime construction or secret reflection. |</threat_model>
<verification>Run focused Rust supervisor/redaction tests and `go test ./cmd/server`; then run `cd apps/desktop/src-tauri && cargo test --lib process::`.</verification>
<success_criteria>Selected Go configuration reaches only the managed sidecar, all diagnostics are secret-free, and Go degradation never changes Agent availability.</success_criteria>
<output>Create `12-03-SUMMARY.md` with RED/GREEN/REFACTOR record.</output>
