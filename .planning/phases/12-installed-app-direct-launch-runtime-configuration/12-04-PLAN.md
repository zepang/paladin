---
phase: 12-installed-app-direct-launch-runtime-configuration
plan: "04"
type: tdd
wave: 4
depends_on: ["12-02", "12-03"]
files_modified: [apps/desktop/src-tauri/src/go_config/mod.rs, apps/desktop/src-tauri/src/go_config/commands_tests.rs, apps/desktop/src-tauri/src/lib.rs, apps/desktop/src/lib/tauri-commands.ts, apps/desktop/src/stores/goService.ts, apps/desktop/src/stores/__tests__/goService.test.ts]
autonomous: true
requirements: [SDC-01, SDC-02, SDC-03]
requirements_addressed: [SDC-01, SDC-02, SDC-03]
must_haves:
  truths:
    - "D-02: typed read/store surfaces are masked; DB/Redis/JWT are accepted only as write inputs."
    - "D-03: save/import/clear persist authority and report pending apply; retry probes the current process, managed restart applies a new snapshot, external restart is unavailable."
    - "D-05--D-08: import is explicit and complete-set only; clear never reimports automatically."
  artifacts:
    - path: apps/desktop/src/stores/goService.ts
      provides: "Masked DTO state and typed independent save/test/import/clear/retry/restart actions."
  key_links:
    - from: useGoServiceStore
      to: Tauri Go manager commands
      via: "typed invoke wrappers; drafts never enter store"
      pattern: "saveGoServiceConfig|restartGoService"
---
<objective>Expose the secret-safe Go manager through a narrow typed command/store boundary, including the explicit D-03 apply contract.</objective>

## Artifacts this phase produces
- Registered masked commands and typed invoke wrappers.
- A store that holds only masked state, operation results, pending-apply state, and permitted owner actions.

<context>@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-{CONTEXT,RESEARCH,UI-SPEC,VALIDATION}.md
@apps/desktop/src-tauri/src/{go_config/mod.rs,go_config/types.rs,process/commands.rs,lib.rs}
@apps/desktop/src/{lib/tauri-commands.ts,stores/aiProvider.ts}</context>
<tasks>
<task type="tdd"><name>Task 1: RED→GREEN→REFACTOR masked command contract</name><files>apps/desktop/src-tauri/src/go_config/mod.rs, apps/desktop/src-tauri/src/go_config/commands_tests.rs, apps/desktop/src-tauri/src/lib.rs, apps/desktop/src/lib/tauri-commands.ts</files><read_first>@apps/desktop/src-tauri/src/{go_config/mod.rs,go_config/types.rs,go_config/commands_tests.rs,process/commands.rs,lib.rs}
@apps/desktop/src/lib/tauri-commands.ts</read_first><behavior>- Get returns masked configuration, diagnostics, owner, readiness, permitted actions, and pending-apply only.
- Save/import/clear persist but never restart a running child; their result identifies pending apply. Test-current-input and retry do not persist a draft.
- Managed restart consumes the latest snapshot; external owner receives a structured restart-unavailable result.</behavior><action>Run command tests RED, implement only typed commands registered once in `generate_handler!`, then make them GREEN and refactor/re-run. Keep raw values limited to save/test input parameters and Rust-private calls. Define result/DTO fields that explicitly distinguish `saved_pending_restart`, `retry_current_process`, and `restart_unavailable`; do not infer an action from an error string. Implement D-02, D-03, D-05, D-06, and D-08.</action><acceptance_criteria>- Command tests prove read DTO serialization lacks DB/Redis/JWT/length/prefix/suffix, and sentinels do not appear in errors.
- Tests prove save does not restart, retry does not alter persistence, managed restart applies the latest snapshot, and external restart is denied.
- RED, GREEN, and REFACTOR are recorded by rerunning `cargo test --lib go_config::commands_tests`.</acceptance_criteria><verify><automated>cd apps/desktop/src-tauri &amp;&amp; cargo test --lib go_config::commands_tests</automated></verify><done>The command boundary exposes executable, owner-safe D-03 semantics without a raw read path.</done></task>
<task type="tdd"><name>Task 2: RED→GREEN→REFACTOR masked Zustand store</name><files>apps/desktop/src/stores/goService.ts, apps/desktop/src/stores/__tests__/goService.test.ts</files><read_first>@apps/desktop/src/stores/{aiProvider.ts,terminal.ts,__tests__/aiProvider.test.ts}
@apps/desktop/src/lib/tauri-commands.ts</read_first><behavior>- Store refreshes masked backend state and action/result state, never DB/Redis/JWT drafts.
- Save displays pending-restart guidance; retry describes the current process; managed/external owner actions remain distinct.</behavior><action>Run focused store tests RED, add component-local-draft-compatible actions, make GREEN, then refactor/re-run. Map Rust casing at the wrapper boundary only. Include sentinel inspection over store state and mocked invoke arguments/results. Implement D-02 and D-03; leave panel layout to Plan 05.</action><acceptance_criteria>- No read model or store property represents a raw secret.
- Tests prove save/test/import/clear/retry/restart call distinct commands and preserve the pending-apply/owner contract.
- RED→GREEN→REFACTOR is evidenced by the focused Vitest command.</acceptance_criteria><verify><automated>pnpm --filter @paladin/desktop test --run goService</automated></verify><done>The store is a masked orchestration layer, not a secret cache.</done></task>
</tasks>
<threat_model>| Threat | Severity | Mitigation and verification |
|---|---|---|
| ASVS V2 JWT misuse | high | Task 1 requires write-only inputs and JWT-safe command results; Rust sentinel tests are the gate. |
| ASVS V3 session/override confusion | high | Task 1 exposes `pending_apply` and process-only retry/restart semantics; tests prohibit save-triggered restart. |
| ASVS V4 unauthorized lifecycle/raw snapshot access | high | Task 1 keeps snapshot Rust-private and tests external restart denial; Task 2 never holds secrets. |
| ASVS V5 partial input/import | high | Task 1 delegates only complete-set save/import and returns structured field diagnostics; tests cover rejection. |</threat_model>
<verification>Run the Rust command suite and focused store suite after each RED/GREEN/REFACTOR cycle.</verification>
<success_criteria>Every frontend consumer receives masked state plus explicit apply/retry/restart permissions, never an implicit live configuration mutation.</success_criteria>
<output>Create `12-04-SUMMARY.md` with RED/GREEN/REFACTOR evidence.</output>
