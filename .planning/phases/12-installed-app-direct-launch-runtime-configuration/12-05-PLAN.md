---
phase: 12-installed-app-direct-launch-runtime-configuration
plan: "05"
type: execute
wave: 5
depends_on: ["12-04"]
files_modified: [apps/desktop/src/stores/terminal.ts, apps/desktop/src/components/go-service/GoServicePanel.tsx, apps/desktop/src/components/go-service/__tests__/GoServicePanel.test.tsx, apps/desktop/src/components/layout/RightPanel.tsx]
autonomous: true
requirements: [SDC-01, SDC-02, SDC-03]
requirements_addressed: [SDC-01, SDC-02, SDC-03]
must_haves:
  truths:
    - "D-01: Go 服务 is a RightPanel tab after AI Provider."
    - "D-02/D-03: inputs are component-local/write-only and save reports whether a restart is needed without silently applying to a live child."
    - "D-08: explicit complete import and confirmed clear provide safe recovery."
  artifacts:
    - path: apps/desktop/src/components/go-service/GoServicePanel.tsx
      provides: "Accessible configuration, save/test/import/clear, and pending-apply recovery UX."
---
<objective>Implement the narrow, accessible Go 服务 settings panel using the explicit persistence-versus-apply behavior from Plan 04.</objective>

## Artifacts this phase produces
- Dedicated Go 服务 tab after AI Provider with local drafts only.
- Inline result and recovery guidance for managed restart versus external owner re-detection.

<context>@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-{CONTEXT,UI-SPEC,VALIDATION}.md
@apps/desktop/src/{stores/goService.ts,stores/terminal.ts,components/provider/AiProviderPanel.tsx,components/layout/RightPanel.tsx,components/ui/alert-dialog.tsx}</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: RED→GREEN→REFACTOR settings panel and tab</name><files>apps/desktop/src/stores/terminal.ts, apps/desktop/src/components/go-service/GoServicePanel.tsx, apps/desktop/src/components/go-service/__tests__/GoServicePanel.test.tsx, apps/desktop/src/components/layout/RightPanel.tsx</files><read_first>@apps/desktop/src/components/{provider/AiProviderPanel.tsx,layout/RightPanel.tsx,ui/alert-dialog.tsx,ui/button.tsx}
@apps/desktop/src/stores/{goService.ts,terminal.ts}
@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-UI-SPEC.md</read_first><behavior>- Tab order ends AI Provider then Go 服务; 300px form has visible labels, local draft fields, inline results, aria-live feedback, and focus to first invalid field.
- Save only persists and says `配置已保存。运行中的 Go 服务将在重新启动后使用新配置。`; it offers `重启` only for managed owner and says `外部 Go 服务需自行重启后重新检测。` for external owner.
- Test current input never saves it; retry checks the current child. Import requires complete valid fields, clear confirms then never auto-imports.</behavior><action>First run the panel test RED. Implement with existing Button, AlertDialog, ScrollArea, sonner and Lucide; keep DB/Redis/JWT only in component state and clear them after save/clear. Wire tab selection after `ai-provider`, exact UI-SPEC Chinese copy, pending-apply actions, field diagnosis, and focus restoration. Make GREEN, refactor/re-run. Do not add routes/dependencies or put drafts in Zustand, labels, snapshots, toast payloads, URLs, or logs. Implement D-01, D-02, D-03, D-05, D-06, and D-08.</action><acceptance_criteria>- Tests prove save, test-current-input, retry, import, clear, managed restart, and external re-detect have distinct calls/copy.
- Tests prove a save neither restarts nor changes current-child readiness, while explicit managed restart is offered; external owner never gets restart.
- DOM, aria labels, toast mocks, and snapshots exclude sentinels; all UI-SPEC panel accessibility/recovery criteria pass.</acceptance_criteria><verify><automated>pnpm --filter @paladin/desktop test --run GoServicePanel RightPanel &amp;&amp; pnpm --filter @paladin/desktop build</automated></verify><done>The panel makes saved-versus-applied state explicit and keeps all configuration input write-only.</done></task>
</tasks>
<threat_model>| Threat | Severity | Mitigation and verification |
|---|---|---|
| ASVS V2 secret disclosure | high | Task 1 sentinel checks cover DOM, aria, toast, and snapshots; password input never reads back persisted value. |
| ASVS V3 misleading live-session change | high | Task 1 exact pending-restart text and distinct retry/restart tests prohibit a save-triggered live mutation. |
| ASVS V4 unauthorized external restart | high | Task 1 hides/disables restart for external owner and tests only re-detect guidance. |
| ASVS V5 partial import/save | high | Task 1 blocks partial form submit/import and renders field-specific diagnostics. |</threat_model>
<verification>Run focused panel/RightPanel tests and a production desktop build.</verification>
<success_criteria>All UI-SPEC panel behaviors are executable without secret readback or ambiguous apply semantics.</success_criteria>
<output>Create `12-05-SUMMARY.md` with focused evidence.</output>
