---
phase: 12-installed-app-direct-launch-runtime-configuration
plan: "06"
type: execute
wave: 5
depends_on: ["12-04"]
files_modified: [apps/desktop/src/components/StatusBar/GoServiceLight.tsx, apps/desktop/src/components/StatusBar/__tests__/GoServiceLight.test.tsx, apps/desktop/src/components/StatusBar.tsx, apps/desktop/src/components/__tests__/StartupMask.test.tsx]
autonomous: true
requirements: [SDC-01, SDC-02, SDC-03]
requirements_addressed: [SDC-01, SDC-02, SDC-03]
must_haves:
  truths:
    - "D-03: the Go status popover distinguishes current-process retry from saved pending restart and never blocks Agent/UI."
    - "D-02: all status/diagnostic text remains redacted."
  artifacts:
    - path: apps/desktop/src/components/StatusBar/GoServiceLight.tsx
      provides: "Independent Go status light with owner-safe recovery actions."
---
<objective>Deliver the independent, nonblocking Go StatusBar light and its owner-safe D-03 recovery actions.</objective>

## Artifacts this phase produces
- A 24px Go StatusBar light/popover with structured diagnostics, pending-apply, retry, restart/re-detect, and logs.

<context>@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-{CONTEXT,UI-SPEC,VALIDATION}.md
@apps/desktop/src/components/{StatusBar.tsx,StartupMask.tsx,StatusBar/ProcessLight.tsx,StatusBar/AiProviderLight.tsx}
@apps/desktop/src/stores/goService.ts</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: RED→GREEN→REFACTOR Go status light and recovery popover</name><files>apps/desktop/src/components/StatusBar/GoServiceLight.tsx, apps/desktop/src/components/StatusBar/__tests__/GoServiceLight.test.tsx, apps/desktop/src/components/StatusBar.tsx, apps/desktop/src/components/__tests__/StartupMask.test.tsx</files><read_first>@apps/desktop/src/components/StatusBar/{ProcessLight.tsx,AiProviderLight.tsx,__tests__/ProcessLight.test.tsx}
@apps/desktop/src/components/{StatusBar.tsx,StartupMask.tsx,__tests__/StartupMask.test.tsx}
@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-UI-SPEC.md</read_first><behavior>- Structured states show unconfigured, incomplete, session override, checking, ready, dependency degraded, conflict, sidecar failure, and saved-pending-restart without parsing errors.
- Pending apply says saved configuration will apply on restart; managed owner gets restart after retry, external owner gets re-detect only with reason text.
- AI and Go lights remain independent; Go degradation says Agent remains usable and never triggers StartupMask.</behavior><action>Run focused light tests RED, implement using existing Popover/Button semantics at existing density, make GREEN, then refactor/re-run. Preserve locked action order: open settings, test/retry, managed restart where applicable, re-detect, view logs. Use only redacted DTO summary and fixed fingerprint. Cover Chinese aria labels/titles, keyboard close/focus return, aria-live results, and all UI-SPEC state copy. Implement D-02 and D-03.</action><acceptance_criteria>- Tests prove owner-aware pending-apply/retry/restart/re-detect behavior and all prescribed state/action copy.
- External restart/stop cannot be invoked; no raw diagnostic/value appears in popover or copy payload.
- Agent-running Go degradation has no StartupMask and both AI and Go lights remain visible.</acceptance_criteria><verify><automated>pnpm --filter @paladin/desktop test --run GoServiceLight StartupMask StatusBar</automated></verify><done>Users can distinguish save, probe, apply, and external-owner recovery from one safe Go status surface.</done></task>
</tasks>
<threat_model>| Threat | Severity | Mitigation and verification |
|---|---|---|
| ASVS V2 secret exposure | high | Task 1 sentinel tests inspect all displayed/copy/live text. |
| ASVS V3 session ambiguity | high | Task 1 renders session override and pending-apply as separate textual states. |
| ASVS V4 owner authorization | high | Task 1 tests owner-gated restart/stop and permits external re-detect only. |
| ASVS V5 unvalidated state inference | high | Task 1 consumes structured field diagnostics rather than parsing arbitrary error text. |</threat_model>
<verification>Run focused light, StartupMask, and StatusBar Vitest suites.</verification>
<success_criteria>Status recovery is actionable, secret-free, and never conflates Go degradation with Agent or AI-provider availability.</success_criteria>
<output>Create `12-06-SUMMARY.md` with focused evidence.</output>
