---
phase: 12-installed-app-direct-launch-runtime-configuration
plan: "07"
type: execute
wave: 6
depends_on: ["12-03", "12-05", "12-06"]
files_modified: [apps/desktop/scripts/direct-launch-evidence.mjs, apps/desktop/scripts/direct-launch-evidence.test.mjs, scripts/launch-paladin-macos.sh, scripts/test-launch-paladin-macos.sh, scripts/verify-direct-launch-platforms.sh, .planning/phases/12-installed-app-direct-launch-runtime-configuration/evidence/macos-finder-direct-launch.json, .planning/phases/12-installed-app-direct-launch-runtime-configuration/evidence/windows-clean-env.json, .planning/phases/12-installed-app-direct-launch-runtime-configuration/evidence/linux-clean-env.json]
autonomous: false
requirements: [PKG-01, PKG-02, SDC-01, SDC-02, SDC-03]
requirements_addressed: [PKG-01, PKG-02, SDC-01, SDC-02, SDC-03]
must_haves:
  truths:
    - "D-09/D-10: Finder `/Applications` direct launch is the macOS primary evidence for three scenarios."
    - "D-11: Windows/Linux automation is explicitly manual-UAT-pending and release-ready false."
    - "D-12: evidence is schema-valid, secret-free, and distinguishes launcher class."
  artifacts:
    - path: apps/desktop/scripts/direct-launch-evidence.mjs
      provides: "Fail-closed evidence validator/scanner."
    - path: scripts/verify-direct-launch-platforms.sh
      provides: "Clean-environment automation harness."
---
<objective>Build and run secret-free direct-launch evidence automation, then collect the required macOS Finder human observation.</objective>

## Artifacts this phase produces
- Schema scanner, clean-environment harness, Windows/Linux pending records, and a macOS Finder record after human UAT.

<context>@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-{CONTEXT,RESEARCH,VALIDATION}.md
@apps/desktop/scripts/{secret-sentinel.mjs,direct-launch-evidence.test.mjs,build-manifest.mjs}
@scripts/{launch-paladin-macos.sh,test-launch-paladin-macos.sh}</context>
<tasks>
<task type="tdd"><name>Task 1: RED→GREEN→REFACTOR evidence scanner and clean-environment harness</name><files>apps/desktop/scripts/direct-launch-evidence.mjs, apps/desktop/scripts/direct-launch-evidence.test.mjs, scripts/launch-paladin-macos.sh, scripts/test-launch-paladin-macos.sh, scripts/verify-direct-launch-platforms.sh</files><read_first>@apps/desktop/scripts/{direct-launch-evidence.test.mjs,secret-sentinel.mjs,build-manifest.mjs}
@scripts/{launch-paladin-macos.sh,test-launch-paladin-macos.sh}</read_first><behavior>- Schema requires identity, launcher entry, scenario, diagnostic category, manual-UAT status, and release-ready status, then rejects sentinel/partial records without reflecting values.
- Harness uses clean environment, temporary app-data, packaged resources, config resolution, sidecar classification, and records Windows/Linux as pending/false.
- Wrapper remains explicit marker-bound diagnostic/UAT tooling and cannot be used as normal configuration injection.</behavior><action>Run Node tests RED, implement the pure validator/scanner and make GREEN, refactor/re-run. Add portable automation that calls the validator rather than echoing raw diagnostics. Preserve wrapper behavior only for marker-bound UAT and revise its tests to reject secret CLI arguments and label the launcher class diagnostic. Implement D-07, D-09, D-11, D-12.</action><acceptance_criteria>- Required-target/schema/sentinel failure fails closed without printing injected value.
- Windows/Linux records contain `manual_uat_status: pending` and `release_ready: false`; wrapper evidence is distinguishable from GUI launcher evidence.
- Node, wrapper, and automation-only gates pass.</acceptance_criteria><verify><automated>node --test apps/desktop/scripts/direct-launch-evidence.test.mjs &amp;&amp; bash scripts/test-launch-paladin-macos.sh &amp;&amp; bash scripts/verify-direct-launch-platforms.sh --automation-only</automated></verify><done>Automation records truthful, scanner-approved, secret-free platform status.</done></task>
<task type="checkpoint:human-verify" gate="blocking"><name>Task 2: Record macOS Finder direct-launch UAT</name><files>.planning/phases/12-installed-app-direct-launch-runtime-configuration/evidence/macos-finder-direct-launch.json</files><read_first>@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-CONTEXT.md
@.planning/phases/10-packaging/evidence/macos-build.json
@apps/desktop/scripts/direct-launch-evidence.mjs</read_first><action>After automated gates and a current DMG identity check, install the DMG to `/Applications`. For no AI/Go config, AI available/Go missing, and persisted complete Go config, double-click `Paladin.app` in Finder—not wrapper, terminal, or bundle binary. Confirm Agent/UI continues with separated prompts in the first two; in the third confirm app-data configuration and packaged sidecar start. Write only identity, Finder launcher class, outcome, redacted category, and UAT status; validate the record. Implement D-09, D-10, D-12.</action><acceptance_criteria>- Three scanner-valid records use Finder launcher class and no raw value/log/path/command.
- Go degradation never blocks Agent workspace; only macOS completed manual UAT is complete.</acceptance_criteria><verify><automated>node apps/desktop/scripts/direct-launch-evidence.mjs --validate .planning/phases/12-installed-app-direct-launch-runtime-configuration/evidence/macos-finder-direct-launch.json &amp;&amp; node apps/desktop/scripts/secret-sentinel.mjs --require-all</automated><human-check>确认 DMG 已安装到 `/Applications`，三次启动均为 Finder 双击，且证据与观察一致并无机密。</human-check></verify><done>macOS primary evidence is genuine Finder-installed-app UAT, not wrapper evidence.</done></task>
</tasks>
<threat_model>| Threat | Severity | Mitigation and verification |
|---|---|---|
| ASVS V2 secret disclosure in evidence | high | Task 1 scanner sentinel gate and Task 2 validation prohibit values/logs. |
| ASVS V3 marker override abuse | high | Task 1 retains marker-bound diagnostic wrapper and rejects ordinary secret injection. |
| ASVS V4 misleading launcher authority | high | Task 1/2 require launcher class and Finder-only primary evidence. |
| ASVS V5 incomplete/untrustworthy evidence | high | Task 1 rejects partial schema records and Task 2 supplies all three locked scenarios. |</threat_model>
<verification>Run Node scanner, wrapper, automation harness, secret sentinel, then the Finder checkpoint.</verification>
<success_criteria>Evidence makes platform UAT state and launcher provenance auditable without leaking configuration.</success_criteria>
<output>Create `12-07-SUMMARY.md` after automated and human evidence are recorded.</output>
