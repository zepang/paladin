---
phase: 12-installed-app-direct-launch-runtime-configuration
plan: "08"
type: execute
wave: 7
depends_on: ["12-07"]
files_modified: [README.md, docs/packaging.md, docs/supervisor-startup-chain.md, .planning/phases/12-installed-app-direct-launch-runtime-configuration/12-VERIFICATION.md]
autonomous: true
requirements: [PKG-03, SDC-01, SDC-02, SDC-03, PKG-01, PKG-02]
requirements_addressed: [PKG-03, SDC-01, SDC-02, SDC-03, PKG-01, PKG-02]
must_haves:
  truths:
    - "D-09--D-12: documentation and final verification call GUI launch normal, wrapper diagnostic/UAT-only, and retain Windows/Linux pending/non-release-ready."
    - "D-01--D-08: final matrix maps configuration, redaction, apply/retry/restart, UI, and security gates to executable evidence."
  artifacts:
    - path: .planning/phases/12-installed-app-direct-launch-runtime-configuration/12-VERIFICATION.md
      provides: "Final evidence matrix for D-01--D-12, UI-SPEC, ASVS V2--V5, requirements, automation, and manual UAT."
---
<objective>Publish accurate installed-app guidance and complete a strict evidence matrix that reruns every phase gate.</objective>

## Artifacts this phase produces
- README/packaging/startup-chain wording centered on GUI direct launch and product recovery.
- Final `12-VERIFICATION.md` covering all decisions, UI-SPEC acceptance, requirements, ASVS controls, automation, and manual platform status.

<context>@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-{CONTEXT,RESEARCH,UI-SPEC,VALIDATION}.md
@README.md
@docs/{packaging.md,supervisor-startup-chain.md}
@.planning/phases/12-installed-app-direct-launch-runtime-configuration/evidence/{macos-finder-direct-launch.json,windows-clean-env.json,linux-clean-env.json}</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: RED→GREEN→REFACTOR documentation assertions and direct-launch guidance</name><files>README.md, docs/packaging.md, docs/supervisor-startup-chain.md</files><read_first>@README.md
@docs/{packaging.md,supervisor-startup-chain.md}
@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-CONTEXT.md</read_first><behavior>- Each document names system GUI launch as normal installed use and wrapper as diagnostic/UAT-only.
- Documentation separates Agent liveness, AI readiness, Go config/readiness, owner, save-pending-restart, retry, managed restart, and external re-detect without terminal/source-edit remediation.
- Windows/Linux remain manual-UAT-pending/non-release-ready.</behavior><action>First add/run targeted shell assertions. In each of the three documents, add the exact, user-facing statements `GUI 直启是已安装应用的正常使用方式` and `wrapper 仅用于诊断/UAT` plus `Windows/Linux 的人工 UAT 状态为 pending，release_ready=false`; use the verification commands below to assert each statement in each file separately. Also independently reject wrapper-first and release-ready claims. Make the assertions GREEN by editing only these documents, then refactor/re-run. Explain the D-03 contract in product terms: save persists only; retry probes current process; managed restart applies saved configuration; an external owner must be restarted outside Paladin then re-detected. Implement D-03, D-07, D-09, D-11, and D-12.</action><acceptance_criteria>- Each document has required direct-launch/recovery wording and none claims wrapper-first normal use or Windows/Linux release-ready.
- Documentation includes no secret example, shell command, raw diagnostic, or developer-only remediation path.
- Assertion script returns green after a RED→GREEN→REFACTOR cycle.</acceptance_criteria><verify><automated>set -e; rg -Fq 'GUI 直启是已安装应用的正常使用方式' README.md; rg -Fq 'wrapper 仅用于诊断/UAT' README.md; rg -Fq 'Windows/Linux 的人工 UAT 状态为 pending，release_ready=false' README.md; ! rg -qi 'wrapper.*first' README.md; ! rg -qi 'wrapper.*首选' README.md; ! rg -qi 'Windows.*release[-_ ]ready.*true' README.md; ! rg -qi 'Linux.*release[-_ ]ready.*true' README.md; rg -Fq 'GUI 直启是已安装应用的正常使用方式' docs/packaging.md; rg -Fq 'wrapper 仅用于诊断/UAT' docs/packaging.md; rg -Fq 'Windows/Linux 的人工 UAT 状态为 pending，release_ready=false' docs/packaging.md; ! rg -qi 'wrapper.*first' docs/packaging.md; ! rg -qi 'wrapper.*首选' docs/packaging.md; ! rg -qi 'Windows.*release[-_ ]ready.*true' docs/packaging.md; ! rg -qi 'Linux.*release[-_ ]ready.*true' docs/packaging.md; rg -Fq 'GUI 直启是已安装应用的正常使用方式' docs/supervisor-startup-chain.md; rg -Fq 'wrapper 仅用于诊断/UAT' docs/supervisor-startup-chain.md; rg -Fq 'Windows/Linux 的人工 UAT 状态为 pending，release_ready=false' docs/supervisor-startup-chain.md; ! rg -qi 'wrapper.*first' docs/supervisor-startup-chain.md; ! rg -qi 'wrapper.*首选' docs/supervisor-startup-chain.md; ! rg -qi 'Windows.*release[-_ ]ready.*true' docs/supervisor-startup-chain.md; ! rg -qi 'Linux.*release[-_ ]ready.*true' docs/supervisor-startup-chain.md</automated></verify><done>Installed-app documentation is precise about direct launch, recovery, and platform evidence status.</done></task>
<task type="auto"><name>Task 2: Assemble final verification matrix and run all focused evidence gates</name><files>.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-VERIFICATION.md</files><read_first>@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-{CONTEXT,UI-SPEC,VALIDATION}.md
@.planning/phases/12-installed-app-direct-launch-runtime-configuration/evidence/{macos-finder-direct-launch.json,windows-clean-env.json,linux-clean-env.json}
@.planning/phases/12-installed-app-direct-launch-runtime-configuration/12-0{1,2,3,4,5,6,7}-PLAN.md</read_first><action>Create a matrix with one row each for D-01 through D-12, every UI-SPEC acceptance checkbox, SDC-01--03, PKG-01--03, and ASVS V2--V5. For every row identify exact test/evidence path, command, result, and whether it is automated or manual; preserve Windows/Linux as `pending`/`false` and only mark macOS complete after Task 07 human evidence. Execute every focused gate listed in Plans 12-01 through 12-07 plus the full-suite/phase-gate commands in 12-VALIDATION.md: Rust library tests, Go server tests, focused and full desktop Vitest, the Plan 12-05 desktop build, Agent pytest, Node evidence tests, wrapper/platform harnesses, the Task 1 documentation assertions, and the secret sentinel. Record Windows/Linux manual UAT as pending with the unavailable target-OS reason; do not substitute automation or a build for manual UAT. Record any unavailable external dependency or failed command as pending/failed with its actual reason. Do not use an OR-regex as evidence: retain every independent positive and negative documentation assertion in the matrix and record failures honestly.</action><acceptance_criteria>- Matrix has concrete evidence for D-01--D-12, every UI-SPEC acceptance, all phase requirements, and ASVS V2--V5.
- Final automated gate explicitly runs `cargo test --lib go_config::`, `cargo test --lib process::`, `go test ./cmd/server`, focused Go-service Vitest, `pnpm --filter @paladin/desktop build`, Agent pytest, Node evidence tests, wrapper/harness, Task 1 documentation assertions, and secret sentinel.
- It separately proves GUI-direct-launch wording exists, wrapper-normal wording is absent, and Windows/Linux release-ready wording is absent; no result is inferred from a broad OR match.</acceptance_criteria><verify><automated>set -e; cd apps/desktop/src-tauri &amp;&amp; cargo test --lib go_config:: &amp;&amp; cargo test --lib process:: &amp;&amp; cargo test --lib; cd ../../../apps/server &amp;&amp; go test ./cmd/server &amp;&amp; go test ./...; cd ../desktop &amp;&amp; pnpm test --run goService GoServicePanel GoServiceLight StartupMask StatusBar &amp;&amp; pnpm --filter @paladin/desktop test --run &amp;&amp; pnpm --filter @paladin/desktop build; cd ../agent &amp;&amp; uv run pytest; cd ../.. &amp;&amp; node --test apps/desktop/scripts/direct-launch-evidence.test.mjs &amp;&amp; bash scripts/test-launch-paladin-macos.sh &amp;&amp; bash scripts/verify-direct-launch-platforms.sh --automation-only &amp;&amp; node apps/desktop/scripts/secret-sentinel.mjs --require-all &amp;&amp; node apps/desktop/scripts/direct-launch-evidence.mjs --validate .planning/phases/12-installed-app-direct-launch-runtime-configuration/evidence/macos-finder-direct-launch.json --validate .planning/phases/12-installed-app-direct-launch-runtime-configuration/evidence/windows-clean-env.json --validate .planning/phases/12-installed-app-direct-launch-runtime-configuration/evidence/linux-clean-env.json; rg -Fq 'GUI 直启是已安装应用的正常使用方式' README.md; rg -Fq 'wrapper 仅用于诊断/UAT' README.md; rg -Fq 'Windows/Linux 的人工 UAT 状态为 pending，release_ready=false' README.md; ! rg -qi 'wrapper.*first' README.md; ! rg -qi 'wrapper.*首选' README.md; ! rg -qi 'Windows.*release[-_ ]ready.*true' README.md; ! rg -qi 'Linux.*release[-_ ]ready.*true' README.md; rg -Fq 'GUI 直启是已安装应用的正常使用方式' docs/packaging.md; rg -Fq 'wrapper 仅用于诊断/UAT' docs/packaging.md; rg -Fq 'Windows/Linux 的人工 UAT 状态为 pending，release_ready=false' docs/packaging.md; ! rg -qi 'wrapper.*first' docs/packaging.md; ! rg -qi 'wrapper.*首选' docs/packaging.md; ! rg -qi 'Windows.*release[-_ ]ready.*true' docs/packaging.md; ! rg -qi 'Linux.*release[-_ ]ready.*true' docs/packaging.md; rg -Fq 'GUI 直启是已安装应用的正常使用方式' docs/supervisor-startup-chain.md; rg -Fq 'wrapper 仅用于诊断/UAT' docs/supervisor-startup-chain.md; rg -Fq 'Windows/Linux 的人工 UAT 状态为 pending，release_ready=false' docs/supervisor-startup-chain.md; ! rg -qi 'wrapper.*first' docs/supervisor-startup-chain.md; ! rg -qi 'wrapper.*首选' docs/supervisor-startup-chain.md; ! rg -qi 'Windows.*release[-_ ]ready.*true' docs/supervisor-startup-chain.md; ! rg -qi 'Linux.*release[-_ ]ready.*true' docs/supervisor-startup-chain.md</automated></verify><done>The final verification is evidence-backed and cannot mistake keyword presence, wrapper automation, or buildability for installed-app readiness.</done></task>
</tasks>
<threat_model>| Threat | Severity | Mitigation and verification |
|---|---|---|
| ASVS V2 JWT/write-only regression | high | Task 2 reruns Rust/Go/UI sentinel evidence and maps it in the matrix. |
| ASVS V3 override/apply ambiguity | high | Task 1 documents marker/session and save/retry/restart behavior; Task 2 links supervisor tests. |
| ASVS V4 unauthorized snapshot/lifecycle use | high | Task 2 maps Rust-private snapshot and external-owner tests; docs do not promise unavailable restart. |
| ASVS V5 partial-input/import regression | high | Task 2 maps storage/command/UI complete-set tests and rejects unverified matrix claims. |</threat_model>
<verification>Run Task 1 assertions plus every Task 2 command exactly; no watch mode and no broad OR-only claim.</verification>
<success_criteria>Documentation and final evidence honestly prove the phase boundary, all decision/security/UI gates, and platform readiness distinctions.</success_criteria>
<output>Create `12-08-SUMMARY.md` after the matrix and all gates are recorded.</output>
